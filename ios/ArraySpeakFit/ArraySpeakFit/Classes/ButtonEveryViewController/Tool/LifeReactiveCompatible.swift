
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let m_earlyFlowYieldText:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","g","i","f","t","L"]
fileprivate let k_maximumPath:String = "iprofessionalt"

/*: "type" :*/
fileprivate let main_clientName:[UInt8] = [0xf5,0xfa,0xf1,0xe6]

fileprivate func singleBed(uniform num: UInt8) -> UInt8 {
    let value = Int(num) - 129
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/chatRoom/giftList" :*/
fileprivate let appDramaticallyText:String = "mf/chaudience port discourse she"
fileprivate let dream_flatSplitFormat:[Character] = ["a","t","R","o","o","m","/","g","i","f","t","L","i","s","t"]

/*: "live/giftList" :*/
fileprivate let k_agencyUrl:String = "go struggle plot wheellive/"

/*: "party/giftList" :*/
fileprivate let k_hintTitle:String = "party/base seem"
fileprivate let appBusinessFormat:[Character] = ["t"]

/*: "IM/getMessageList" :*/
fileprivate let userScreamName:String = "softly finiteIM/ge"
fileprivate let notiRefuseUrl:[Character] = ["g","e","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let mRidText:String = "predator directionmf/bu"
fileprivate let noti_writeDuringId:String = "justine"
fileprivate let kParticleKey:String = "positive black aside gracendMsg"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let user_stopKey:String = "mf/chchoose after enthusiasm position"
fileprivate let showOkStr:String = "/sendMsgoff scramble plat dynamic finish"

/*: "mf/business/sendGift" :*/
fileprivate let dreamSlightReactPath:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","s","e"]
fileprivate let app_bridgeDuringGuestKey:String = "N"
fileprivate let dreamProgressiveKey:String = "form optimistic roofdGift"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let dream_okFormat:String = "mf/chalibrary present"
fileprivate let user_neverMpFormat:String = "T"
fileprivate let app_soulAddressNumbIdent:[Character] = ["R","o","o","m","/","s","e","n","d","G","i","f","t"]

/*: "live/sendGift" :*/
fileprivate let app_thinCurUrl:[Character] = ["l","i","v","e","/","s","e","n","d","G","i","f","t"]

/*: "party/sendGift" :*/
fileprivate let user_fleetStr:String = "party/senboundary supply sharp verbal visitor"
fileprivate let noti_originalMsg:[Character] = ["d","G","i","f","t"]

/*: "mf/user/getChatInfo" :*/
fileprivate let appReverseName:String = "mf/ushape have zz scenario uniform"
fileprivate let appWholeName:String = "tChatinvite remark target sequence elsewhere"
fileprivate let notiDocName:String = "option port reading signature educationInfo"

/*: "IM/getMessageUserInfoList" :*/
fileprivate let showCounterestMsg:String = "maximum story contactIM/g"
fileprivate let dreamSlightPath:String = "sdoce"
fileprivate let m_superiorUrl:String = "UserInbold script anima sweet"
fileprivate let appAnnouncementKey:String = "foListrecommendation due former custom video"

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let kDesignMsg:[Character] = ["m","f","/","c","r","u","s","h","/","w","h","o","L","i","k","e","M","e","L","i","s","t"]

/*: "mf/crush/ignore" :*/
fileprivate let dream_definitionData:String = "mf/cthreshold message comply welcome recent"
fileprivate let notiShakeData:[Character] = ["o","r","e"]

/*: "mf/crush/like" :*/
fileprivate let dream_liveryWowValue:[Character] = ["m","f","/","c","r","u","s","h","/","l","i","k","e"]

/*: "mf/user/getRelation" :*/
fileprivate let userExistingIdent:[Character] = ["m","f","/","u","s","e","r","/"]
fileprivate let m_towardMessage:String = "kingdom arraygetRe"

/*: "user/attention" :*/
fileprivate let showPiFormat:[UInt8] = [0xe3,0xe5,0xf3,0xe4,0xb9,0xf7,0xe2,0xe2,0xf3,0xf8,0xe2,0xff,0xf9,0xf8]

private func coldContrastAnother(grass num: UInt8) -> UInt8 {
    return num ^ 150
}

/*: "user/removeAttention" :*/
fileprivate let showConstraintIdent:String = "usemargin"
fileprivate let userSatisfyComplyValue:String = "on bubble loo jaw appveAtt"

/*: "Can't earn points if you follow each other~" :*/
fileprivate let dreamPlainKey:[UInt8] = [0x7e,0x72,0x65,0x68,0x74,0x6f,0x20,0x68,0x63,0x61,0x65,0x20,0x77,0x6f,0x6c,0x6c,0x6f,0x66,0x20,0x75,0x6f,0x79,0x20,0x66,0x69,0x20,0x73,0x74,0x6e,0x69,0x6f,0x70,0x20,0x6e,0x72,0x61,0x65,0x20,0x74,0x27,0x6e,0x61,0x43]

/*: "follow Success" :*/
fileprivate let m_pieceAreaMessage:[Character] = ["f","o","l","l","o","w"," ","S","u"]
fileprivate let mDealStr:String = "moreess"

/*: "unfollow Success" :*/
fileprivate let noti_givenKey:[Character] = ["u","n","f","o","l","l","o","w"]
fileprivate let showBarnSortUrl:String = " Sucthere talk compare mp"

/*: "translate/msg" :*/
fileprivate let dreamDealValue:String = "TRANS"
fileprivate let m_mortalityQuantityLabelIdent:String = "license valid disappear musical reserve/msg"

/*: "msgId" :*/
fileprivate let mainWritingData:[UInt8] = [0x2f,0x35,0x29,0xb,0x26]

fileprivate func visionScene(trigger num: UInt8) -> UInt8 {
    let value = Int(num) + 62
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "targetLng" :*/
fileprivate let k_cornerHugeTitle:[UInt8] = [0x9a,0x87,0x98,0x8d,0x8b,0x9a,0x72,0x94,0x8d]

fileprivate func quietlyOutsideSee(personal num: UInt8) -> UInt8 {
    let value = Int(num) - 38
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let mainDuringValue:[Character] = ["m","f","/","g","r","e","e","t","/","g","e","t","A","u","t","o","G","r","e","e","t","C","o","n","t","e","n","t"]

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let noti_addIdent:[Character] = ["m","f","/","g","r","e","e","t","/","g","e"]
fileprivate let m_judgeStr:String = "tIntibold what submit reason disk"
fileprivate let user_yesterdayMessage:[Character] = ["m","a","t","e","P","h","o","t","o"]

/*: "toUid" :*/
fileprivate let notiPurposeValue:[UInt8] = [0x64,0x69,0x55,0x6f,0x74]

/*: "mf/chatRoom/info" :*/
fileprivate let mainHarassmentData:[Character] = ["m","f","/","c","h","a","t","R","o"]
fileprivate let dreamAverWillTitle:[Character] = ["o","m","/","i","n","f","o"]

/*: "mf/chatRoom/list" :*/
fileprivate let showSkinUrl:String = "mf/chdistance cigarette fault activity"
fileprivate let main_trapValue:String = "IST"

/*: "roomId" :*/
fileprivate let k_soonIconStruggleId:[UInt8] = [0xcf,0xd2,0xd2,0xd0,0xf4,0xd9]

private func compareLetter(simultaneous num: UInt8) -> UInt8 {
    return num ^ 189
}

/*: "needExtraInfo" :*/
fileprivate let userAttributeMsg:[UInt8] = [0xc4,0xcf,0xcf,0xce,0xef,0xd2,0xde,0xd8,0xcb,0xe3,0xc4,0xcc,0xc5]

/*: "mf/chatRoom/check" :*/
fileprivate let app_revolutionDepthId:String = "mf/chablind theme"
fileprivate let notiGreetIdent:String = "/checkcoat minute existing storage server"

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let dreamStateUrl:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/"]
fileprivate let userCreateIdent:[Character] = ["j","o","i"]
fileprivate let dream_menuStr:String = "nOrQuitrevenue definition threshold"

/*: "mf/chatRoom/members" :*/
fileprivate let dream_staffMakeupIdent:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","m","e","m","b","e","r","s"]

/*: "translate/text" :*/
fileprivate let mainMindJackIdent:String = "taddansl"
fileprivate let dream_trustMsg:String = "tpossiblext"

/*: "text" :*/
fileprivate let showNagTarText:[UInt8] = [0xe1,0xf0,0xed,0xe1]

/*: "mf/user/queryStatus" :*/
fileprivate let app_terraUrl:String = "female true check fieldmf/us"
fileprivate let userQuickValue:String = "eryStquick whole birth"
fileprivate let appScriptName:[Character] = ["a","t","u","s"]

/*: "toUids" :*/
fileprivate let notiPanTransformMessage:[UInt8] = [0x34,0x2f,0x15,0x29,0x24,0x33]

/*: "call/getCallTab" :*/
fileprivate let userTotaleractionStr:[Character] = ["c","a","l","l","/","g","e","t","C","a","l","l","T","a","b"]

/*: "mf/user/remark" :*/
fileprivate let kLineId:[Character] = ["m","f"]
fileprivate let main_outputIonIdent:String = "/usericon foundation husband"

/*: "content" :*/
fileprivate let dream_balanceSkullName:[UInt8] = [0x4f,0x43,0x42,0x58,0x49,0x42,0x58]

private func freshAgree(plastic num: UInt8) -> UInt8 {
    return num ^ 44
}

/*: "top" :*/
fileprivate let show_domainData:[UInt8] = [0x3f,0x24,0x3b]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LifeReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class LifeReactiveCompatible: NSObject {
public class LifeReactiveCompatible: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [RelatedTransformable]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [RelatedTransformable]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func shadow(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(m_earlyFlowYieldText) + k_maximumPath.replacingOccurrences(of: "professional", with: "s"))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: main_clientName.map{singleBed(uniform: $0)}, encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func highlight(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(appDramaticallyText.prefix(5)) + String(dream_flatSplitFormat))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func procure(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (String(k_agencyUrl.suffix(5)) + "giftList")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_partyGiftList(type: String, completion: @escaping FinishBlock) {
    class func replaceWhen(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "party/giftList"
        reqModel.requestPath = (String(k_hintTitle.prefix(6)) + "giftLis" + String(appBusinessFormat))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func remarkCompletion(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(userScreamName.suffix(5)) + "tMessa" + String(notiRefuseUrl))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func packageCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (String(mRidText.suffix(5)) + noti_writeDuringId.replacingOccurrences(of: "just", with: "s") + "ss/se" + String(kParticleKey.suffix(5)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func representationCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (String(user_stopKey.prefix(5)) + "atRoom" + String(showOkStr.prefix(8)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func sendWithinCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(dreamSlightReactPath) + app_bridgeDuringGuestKey.lowercased() + String(dreamProgressiveKey.suffix(5)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func param(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(dream_okFormat.prefix(6)) + user_neverMpFormat.lowercased() + String(app_soulAddressNumbIdent))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func requireCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (String(app_thinCurUrl))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_sendPartyGift(param: [String: Any], completion: @escaping FinishBlock) {
    class func loadLab(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "party/sendGift"
        reqModel.requestPath = (String(user_fleetStr.prefix(9)) + String(noti_originalMsg))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func hubCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(appReverseName.prefix(4)) + "ser/ge" + String(appWholeName.prefix(5)) + String(notiDocName.suffix(4)))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func aboveRelation(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(showCounterestMsg.suffix(4)) + "etMes" + dreamSlightPath.replacingOccurrences(of: "doc", with: "ag") + String(m_superiorUrl.prefix(6)) + String(appAnnouncementKey.prefix(6)))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, _ in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<RelatedTransformable>.deserialize(from: result as? Array) {
            if let datas = Array<RelatedTransformable>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [RelatedTransformable])
                completion(succeed, datas as! [RelatedTransformable])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func presentCompletion(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        user_topEnvironmentId.noHighlight(giftUrl: giftUrl, path: path) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func tableOfTitles(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (String(kDesignMsg))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func temporary(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(dream_definitionData.prefix(4)) + "rush/ign" + String(notiShakeData))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func nose(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(dream_liveryWowValue))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func agendumClear(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(userExistingIdent) + String(m_towardMessage.suffix(5)) + "lation")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func infoInstall(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: showPiFormat.map{coldContrastAnother(grass: $0)}, encoding: .utf8)! : (showConstraintIdent.replacingOccurrences(of: "margin", with: "r") + "/remo" + String(userSatisfyComplyValue.suffix(5)) + "ention")
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: if AppManager.share.loginUserMode.sex == Gender.female.rawValue {
                    if AppManager.share.loginUserMode.sex == AlongsideDigitalTableConvertible.female.rawValue { // 女性
                        //: object.func__showStatusBarSuccessMsg(showMsg: "Can't earn points if you follow each other~".localized)
                        mainSaveMsg.licenseFee(showMsg: String(bytes: dreamPlainKey.reversed(), encoding: .utf8)!.localized)
                        //: } else {
                    } else {
                        //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                        mainSaveMsg.licenseFee(showMsg: (String(m_pieceAreaMessage) + mDealStr.replacingOccurrences(of: "more", with: "cc")).localized)
                    }
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    mainSaveMsg.licenseFee(showMsg: (String(noti_givenKey) + String(showBarnSortUrl.prefix(4)) + "cess").localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                mainSaveMsg.changeManage(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func pointTranslate(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (dreamDealValue.lowercased() + "late" + String(m_mortalityQuantityLabelIdent.suffix(4)))
        //: reqModel.params = ["msgId": msgId, "targetLng": ImmediatelyThen.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = [String(bytes: mainWritingData.map{visionScene(trigger: $0)}, encoding: .utf8)!: msgId, String(bytes: k_cornerHugeTitle.map{quietlyOutsideSee(personal: $0)}, encoding: .utf8)!: ImmediatelyThen.activate(), String(bytes: main_clientName.map{singleBed(uniform: $0)}, encoding: .utf8)!: "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func canSomeone(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(mainDuringValue))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func engageTabCompletion(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(noti_addIdent) + String(m_judgeStr.prefix(5)) + String(user_yesterdayMessage))
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = [String(bytes: notiPurposeValue.reversed(), encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func anyMulti(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (String(mainHarassmentData) + String(dreamAverWillTitle))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func gameCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(showSkinUrl.prefix(5)) + "atRoom/l" + main_trapValue.lowercased())
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func superior(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: k_soonIconStruggleId.map{compareLetter(simultaneous: $0)}, encoding: .utf8)!: roomId, String(bytes: userAttributeMsg.map{$0^170}, encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(app_revolutionDepthId.prefix(6)) + "tRoom" + String(notiGreetIdent.prefix(6)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func goldJudge(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (String(dreamStateUrl) + String(userCreateIdent) + String(dream_menuStr.prefix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func croixDeGuerre(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (String(dream_staffMakeupIdent))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - text: 文本
    ///   - completion: 回调
    //: class func req_translateText(text: String, completion: @escaping FinishBlock) {
    class func dueRid(text: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "translate/text"
        reqModel.requestPath = (mainMindJackIdent.replacingOccurrences(of: "add", with: "r") + "ate/" + dream_trustMsg.replacingOccurrences(of: "possible", with: "e"))
        //: reqModel.params = ["text": text]
        reqModel.params = [String(bytes: showNagTarText.map{$0^149}, encoding: .utf8)!: text]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func noTwo(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(app_terraUrl.suffix(5)) + "er/qu" + String(userQuickValue.prefix(5)) + String(appScriptName))
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: notiPanTransformMessage.map{$0^64}, encoding: .utf8)!: toUids, String(bytes: main_clientName.map{singleBed(uniform: $0)}, encoding: .utf8)!: 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, _ in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func getClearWeltCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (String(userTotaleractionStr))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func noticePhotoOffce(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (String(kLineId) + String(main_outputIonIdent.prefix(5)) + "/remark")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: dream_balanceSkullName.map{freshAgree(plastic: $0)}, encoding: .utf8)!: content, String(bytes: show_domainData.map{$0^75}, encoding: .utf8)!: isTop, String(bytes: notiPurposeValue.reversed(), encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
