
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let mainDarkMaybeFormat:[Character] = ["S","O","C","K","E","T","_","A","D","D","R","_","K","E","Y"]

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let dreamSharpRangeKey:[UInt8] = [0xff,0xe3,0xef,0xe7,0xe9,0xf8,0xf3,0xfa,0xe5,0xe8,0xe9,0xe3,0xef,0xe4,0xed,0xf8,0xf3,0xfe,0xe9,0xfd,0xf9,0xe9,0xff,0xf8,0xef,0xed,0xe0,0xe0,0xf3,0xe2,0xe3,0xf8,0xe5,0xea,0xe5,0xef,0xed,0xf8,0xe5,0xe3,0xe2]

private func tabCommunicate(rack num: UInt8) -> UInt8 {
    return num ^ 172
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let mMmData:[UInt8] = [0xc2,0xd5,0xd3,0xd9,0xc6,0xd5,0xcf,0xd6,0xdc,0xdf,0xd1,0xc4,0xc3,0xd3,0xc2,0xd5,0xd5,0xde,0xcf,0xde,0xdf,0xc4,0xd9,0xd6,0xd9,0xd3,0xd1,0xc4,0xd9,0xdf,0xde]

private func aDifference(constraint num: UInt8) -> UInt8 {
    return num ^ 144
}

/*: "isCalling" :*/
fileprivate let dream_hapAgoIdent:String = "cam exceptionisCa"
 
/*: "SOCKET 连接成功." :*/
fileprivate let mReloadAverName:String = "executeOC"
fileprivate let app_renderValue:String = "KET your flow slim thing push"

/*: "SOCKET 连接失败, error: :*/
fileprivate let dream_teammateData:[Character] = ["S","O","C","K","E","T"," ","连","接","\u{5931}","败",","," ","e","r","r","o","r"]
fileprivate let showNestName:String = ":"

/*: "SOCKET 失败 :*/
fileprivate let kEachMessage:String = "SaspectK"
fileprivate let mainWhoBetterAddData:[Character] = ["E","T"," ","失","\u{8d25}"]

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let mainBorderMsg:String = "次, 切"
fileprivate let app_connectValue:String = "scan chemistry mirror white instructioncket域名重新"
fileprivate let noti_announcementTitle:String = "尝\u{8bd5}\u{ff0c}地址："

/*: "SOCKET 已关闭, code: :*/
fileprivate let kComplaintValue:String = "infrastructure shSOCKET 已"
fileprivate let app_boardKey:[Character] = ["关","闭",","," ","c","o","d","e",":"]

/*: , reason: :*/
fileprivate let app_searchIdent:String = ", regold nor mic"

/*: /?sid= :*/
fileprivate let dreamPersonName:String = "cheer attach amazing/?sid="

/*: &uid= :*/
fileprivate let noti_readerConversionIdent:[Character] = ["&","u","i","d","="]

/*: &platform=iphone&version= :*/
fileprivate let showViolationId:String = "disagree disagree eastern&pla"
fileprivate let app_herHeavyUrl:[Character] = ["m","=","i","p","h","o","n","e","&","v","e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let appPartyPath:String = "&packminimum sibling"
fileprivate let noti_introMessage:String = "ageId=indicator topic rem"

/*: "1v1server. :*/
fileprivate let main_builderWrittenKey:[Character] = ["1","v","1","s","e","r","v","e"]
fileprivate let userDotMsg:[Character] = ["r","."]

/*: .com:7766" :*/
fileprivate let user_scareFirstLinkTitle:String = ".comas guess three invitation sodium"

/*: "43.129.185.194:7767" :*/
fileprivate let mainDestroySocialFragmentMessage:[Character] = ["4","3",".","1","2","9",".","1","8","5",".","1","9"]
fileprivate let app_scanCrushId:String = "4:sight67"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let userHostBottomUrl:[UInt8] = [0x52,0x4e,0x42,0x4a,0x44,0x55,0x21,0xe6,0xbc,0x90,0xe6,0xba,0x9d,0x73,0x64,0x60,0x62,0x69,0x60,0x63,0x68,0x6d,0x68,0x75,0x78,0x42,0x69,0x60,0x6f,0x66,0x64,0x65,0xe6,0x9a,0x90,0xe4,0x91,0xad,0xee,0xbd,0x8d,0xe7,0x97,0xac,0xe6,0xbc,0x90,0xe4,0x84,0xb2,0xe8,0x96,0xac,0x2f]

private func fromProduction(cheat num: UInt8) -> UInt8 {
    return num ^ 1
}

/*: "cmd" :*/
fileprivate let noti_selectedName:String = "incorporated"

/*: "ping" :*/
fileprivate let show_wordKey:[Character] = ["p","i","n","g"]

/*: "callHeartbeat" :*/
fileprivate let mainSunIdent:String = "object her entry merelycallH"

/*: "recievedDate" :*/
fileprivate let showPitchName:String = "rgiftcigiftv"
fileprivate let dream_incomeAttachName:String = "stretch"

/*: "liveHeartBeat" :*/
fileprivate let notiCeaseMessage:String = "liveHsmall able service simply when"
fileprivate let mainFromPositionStr:String = "more travel tripBeat"

/*: "🔗 关闭 socket" :*/
fileprivate let mTodayDismissStr:[Character] = ["🔗"," ","关","\u{95ed}"," ","s","o","c","k","e","t"]

/*: "The user logs out" :*/
fileprivate let userCapableName:[Character] = ["T","h","e"," ","u","s"]
fileprivate let app_investCareUrl:[Character] = ["e"]
fileprivate let kSaleTitle:[Character] = ["r"," ","l","o","g","s"," ","o","u","t"]

/*: "SOCKET  :*/
fileprivate let dream_cityHintName:[Character] = ["S","O","C","K","E","T"," "]

/*: 重连. 重试次数： :*/
fileprivate let main_ballotPath:String = "重连. 重"
fileprivate let notiSocialHourMsg:[Character] = ["\u{8bd5}","次","数","："]

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let dream_waterMsg:String = "SOCKET 连接lap broadcast"
fileprivate let show_packName:String = "正常 不"

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let m_coldKey:String = "SOCscaleE"
fileprivate let user_saleName:String = "接 \u{4e0d}需\u{8981}"
fileprivate let userOwnStr:String = "重新连request."

/*: "&resume=1" :*/
fileprivate let app_curMsg:String = "&reshis super center"
fileprivate let mainOutputKey:[Character] = ["u","m","e","=","1"]

/*: "&lang= :*/
fileprivate let noti_leavePath:[Character] = ["&","l","a","n","g","="]

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let showStrikeLayerPath:String = "SpileCKET"
fileprivate let dreamCandidModifyOriginalId:String = ", 地\u{5740}："

/*: "onNetDisabled" :*/
fileprivate let dream_changeName:String = "rescue create uniformonNe"
fileprivate let app_cheerSucceedKey:String = "ablecontainer"

/*: "errno" :*/
fileprivate let k_dimensionStr:String = "eagainno"

/*: "data" :*/
fileprivate let kMineData:String = "dlabtlab"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let k_bubbleAccompanyContent:[Character] = ["S","O","C","K","E","T"," ","连","接","保","活","c","m"]
fileprivate let mStackName:String = "defense"
fileprivate let kWritingContent:String = ": ping.female tag success network"

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let k_explorationStr:[UInt8] = [0x73,0x6f,0x63,0x6b,0x65,0x74,0x0,0x54,0x52,0x54,0x43,0xc4,0x9f,0xbd,0xc6,0x94,0x9b,0x43,0x4d,0x44,0x1a,0x0,0x43,0x41,0x4c,0x4c,0x68,0x45,0x41,0x52,0x54,0x42,0x45,0x41,0x54,0xe]

private func weeklySalt(slip num: UInt8) -> UInt8 {
    return num ^ 32
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let k_surgeryData:[UInt8] = [0x58,0x44,0x48,0x40,0x4e,0x5f,0x2b,0x67,0x62,0x7d,0x6e,0xef,0xb4,0x96,0xed,0xbf,0xb0,0x68,0x66,0x6f,0x31,0x2b,0x67,0x62,0x7d,0x6e,0x43,0x6e,0x6a,0x79,0x7f,0x49,0x6e,0x6a,0x7f,0x25]

private func fellowMax(detect num: UInt8) -> UInt8 {
    return num ^ 11
}

/*: "onP" :*/
fileprivate let mainSpeculateKey:String = "throw existing designonP"

/*: "msg" :*/
fileprivate let userTowardStr:String = "mrandomg"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let mArrayData:String = "🔗 处理消bubble："
fileprivate let user_generalPath:String = "cmdName:cease string candid portrait"

/*: , errNo: :*/
fileprivate let userTapData:[Character] = [","," ","e","r","r","N"]
fileprivate let app_advancedTitle:[Character] = ["o",":"]

/*: , msg: :*/
fileprivate let dream_carryStr:String = ", msg:business rank lose number ham"

/*: , data: :*/
fileprivate let user_secureLabUrl:String = ", data:fun strip bright"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let k_peelValue:String = "plainCK"
fileprivate let mainProfileData:String = "contact reportET 接"
fileprivate let notiDivisionGreetMsg:String = "complete kit neat hint himcmdN"

/*: "onReceivedGift" :*/
fileprivate let kNoseName:String = "which smart against go priceonRec"
fileprivate let user_dialogAutomaticData:String = "Giftya cos"

/*: "onVideoMatch" :*/
fileprivate let show_soldName:[Character] = ["o","n","V","i","d","e","o","M","a","t","c","h"]

/*: "onRequestCall" :*/
fileprivate let appLikeMsg:[Character] = ["o","n","R","e","q","u","e","s","t","C","a","l"]
fileprivate let dream_hangPath:[Character] = ["l"]

/*: "onCancelCall" :*/
fileprivate let mOkBetterIdent:[Character] = ["o","n","C","a","n","c","e","l"]
fileprivate let dream_detailId:String = "Calledge hi"

/*: "onRefuseCall" :*/
fileprivate let userMayEquallyPath:[Character] = ["o","n","R","e","f"]
fileprivate let noti_verticalPath:String = "mp ping note minuseCall"

/*: "onAcceptCall" :*/
fileprivate let user_sinceKey:[Character] = ["o","n","A","c","c","e","p","t","C","a","l","l"]

/*: "onStopCall" :*/
fileprivate let k_capabilityIdent:String = "boy pure interruptonSt"

/*: "freeCallTimes" :*/
fileprivate let k_elementMatchMsg:[Character] = ["f","r","e","e","C","a","l","l","T","i","m","e","s"]

/*: "mfCoin" :*/
fileprivate let k_situationIdent:[Character] = ["m","f","C","o","i","n"]

/*: "onMissCoin" :*/
fileprivate let mScanMusicalValue:[Character] = ["o","n","M","i","s","s","C","o","i"]
fileprivate let mCancelAttachIdent:[Character] = ["n"]

/*: "acceptCall" :*/
fileprivate let k_checkResumePath:String = "accperform"
fileprivate let dreamCreateValue:String = "tCallhistory scramble"

/*: "stopCall" :*/
fileprivate let show_lifeTitle:String = "stopCnorthwest heart roof disabled take"
fileprivate let k_curLimitedFromValue:String = "anagnag"

/*: "requestCall" :*/
fileprivate let showStructureFormat:String = "remusicalue"
fileprivate let notiCommunicationKey:String = "vacation"
fileprivate let showModeUrl:String = "tCallfire about"

/*: "[Invite you to a video call...]" :*/
fileprivate let dream_bubbleMessage:[UInt8] = [0xf6,0xe4,0xc3,0xdb,0xc4,0xd9,0xc8,0x8d,0xd4,0xc2,0xd8,0x8d,0xd9,0xc2,0x8d,0xcc,0x8d,0xdb,0xc4,0xc9,0xc8,0xc2,0x8d,0xce,0xcc,0xc1,0xc1,0x83,0x83,0x83,0xf0]

private func skinApply(score num: UInt8) -> UInt8 {
    return num ^ 173
}

/*: "type" :*/
fileprivate let noti_usData:String = "typnative"

/*: "[Invite you to a voice call...]" :*/
fileprivate let dreamBeDiskId:[UInt8] = [0xcb,0xd9,0xfe,0xe6,0xf9,0xe4,0xf5,0xb0,0xe9,0xff,0xe5,0xb0,0xe4,0xff,0xb0,0xf1,0xb0,0xe6,0xff,0xf9,0xf3,0xf5,0xb0,0xf3,0xf1,0xfc,0xfc,0xbe,0xbe,0xbe,0xcd]

private func thenListen(level num: UInt8) -> UInt8 {
    return num ^ 144
}

/*: "uid" :*/
fileprivate let mBeanVeryStr:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let show_pieceName:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let m_displayKey:String = "pad refrigeratorheadPic"

/*: "onCallResume" :*/
fileprivate let showPeopleData:String = "invitation tone previousonCa"
fileprivate let main_givenProcessMessage:String = "mspeaker"

/*: "cancelCall" :*/
fileprivate let dreamLicenseName:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "refuseCall" :*/
fileprivate let appAnyoneData:String = "refushrink"
fileprivate let m_interestValue:String = "L"

/*: "onCallMotivation" :*/
fileprivate let dreamInmateMessage:[Character] = ["o","n"]
fileprivate let userMemoryEyeKey:String = "Callthing edit"
fileprivate let noti_termValue:String = "tibridgen"

/*: "onLiveWarning" :*/
fileprivate let appHideId:String = "cur infrastructureonLive"

/*: "reason" :*/
fileprivate let dream_historyIdent:[Character] = ["r","e","a","s","o","n"]

/*: "time" :*/
fileprivate let noti_againstBroadcastUrl:[Character] = ["t","i","m","e"]

/*: "onStopLive" :*/
fileprivate let kSlideValue:String = "onStoentry target edge"

/*: "onSendProgramGift" :*/
fileprivate let dream_scaleId:String = "effect birthdayonSen"
fileprivate let kCountTitle:String = "mGiftgender numb"

/*: "onGameFs" :*/
fileprivate let app_quicklyBulkMsg:[Character] = ["o","n","G","a","m"]
fileprivate let mainRegionGrayMsg:[Character] = ["e","F","s"]

/*: "broadStatus" :*/
fileprivate let main_frontFormat:[Character] = ["b","r","o","a","d","S","t","a","t","u","s"]

/*: "onCallChatMsg" :*/
fileprivate let kMarkStr:[Character] = ["o","n","C","a","l","l","C","h"]
fileprivate let app_memoryPictureFormat:String = "atMsgtransport healthy added honey part"

/*: "onNotificationPopupWin" :*/
fileprivate let userSoulValue:String = "amp bean curonNot"
fileprivate let mSelectMsg:[Character] = ["a","t","i","o","n","P","o","p","u","p","W","i","n"]

/*: "onUserBlock" :*/
fileprivate let k_deliveryStr:String = "onUsecollect outer must lay coordinator"
fileprivate let mainRequestPath:String = "over"

/*: "freeCallWin" :*/
fileprivate let appTrulyValue:[Character] = ["f","r","e","e","C","a"]
fileprivate let kFromStr:[Character] = ["l","l","W","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StackSocketManager.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let k_voiceTitle = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let user_popName = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let noti_askKey = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let userMovieRobotCaptureMessage = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let notiSucceedUrl = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let user_trustKey = (String(mainDarkMaybeFormat))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let app_partyIdent = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let notiPositionValue = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let dreamMeetingName = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let mainTableMsg = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let mainReadName = NSNotification.Name(rawValue: String(bytes: dreamSharpRangeKey.map{tabCommunicate(rack: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let m_videoData = NSNotification.Name(rawValue: String(bytes: mMmData.map{aDifference(constraint: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol PresenceErrorDelegate: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func bring(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol SocketObjectProtocol: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func kingdom(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func subjectMatter(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol StuffCallerDelegate: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func putData(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func bringContext(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func whiteData(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func froth(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol CyclePartyDelegate: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func whiteReact(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func codex(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func inmate(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class StackSocketManager: NSObject {
    //: enum SocketRetryType: String {
    enum FaceRelated: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currObjectVC: AnyObject?
    var currObjectVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: SolidUntilLanguageManager.shared.direction == .rightToLeft ? 0 : kGiftIdent,
        //: y: StatusBarHeight+190
        y: dreamNoSampleValue + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: SolidUntilLanguageManager.shared.direction == .rightToLeft ? 0 : kGiftIdent,
        //: y: StatusBarHeight+190
        y: dreamNoSampleValue + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: SolidUntilLanguageManager.shared.direction == .rightToLeft ? 0 : kGiftIdent,
        //: y: StatusBarHeight+500
        y: dreamNoSampleValue + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: PresenceErrorDelegate? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: SocketObjectProtocol? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: StuffCallerDelegate? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: CyclePartyDelegate? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var isObject = false
    @objc dynamic var isObject = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = StackSocketManager()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(stripTalk(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(option),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(dream_hapAgoIdent.suffix(4)) + "lling"))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (ApplicationReactiveCompatible.shared as! ApplicationReactiveCompatible).strengthTimer()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            .observeWeakly(Bool.self, "isObject")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (ApplicationReactiveCompatible.shared as! ApplicationReactiveCompatible).strengthTimer()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension StackSocketManager: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        HoeThen.halfLine(msg: (mReloadAverName.replacingOccurrences(of: "execute", with: "S") + String(app_renderValue.prefix(4)) + "连接成\u{529f}."))
        //: sendPing()
        scopeTalk()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: kOfLiveStr,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: user_trustKey)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        labelDiscount()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        HoeThen.halfLine(msg: (String(dream_teammateData) + showNestName.capitalized) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = AppManager.share.reachability?.connection, status != .unavailable else {
        guard let status = AppManager.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == userMovieRobotCaptureMessage {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = downward(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            HoeThen.halfLine(msg: (kEachMessage.replacingOccurrences(of: "aspect", with: "OC") + String(mainWhoBetterAddData)) + "\(userMovieRobotCaptureMessage)" + (mainBorderMsg.capitalized + "换过So" + String(app_connectValue.suffix(8)) + noti_announcementTitle) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.replaceZone(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.reason(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        HoeThen.halfLine(msg: (String(kComplaintValue.suffix(8)) + String(app_boardKey)) + "\(code)" + (String(app_searchIdent.prefix(4)) + "ason:") + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.level(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == mainTableMsg {
            //: return
            return
        }
        //: if let status = AppManager.share.reachability?.connection, status != .unavailable {
        if let status = AppManager.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.replaceZone(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func downward(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = AppManager.share.appConfigMode.ws
        self.linkAddrArr = AppManager.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = counterestShare()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: user_trustKey)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = AppManager.share.loginUid
        let uid = AppManager.share.loginUid
        //: let sid = AppManager.share.loginSessionId
        let sid = AppManager.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(dreamPersonName.suffix(6))) + "\(sid)" + (String(noti_readerConversionIdent)) + "\(uid)" + (String(showViolationId.suffix(4)) + "tfor" + String(app_herHeavyUrl)) + "\(userReplaceContent)" + (String(appPartyPath.prefix(5)) + String(noti_introMessage.prefix(6))) + "\(kLimitTitle)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func counterestShare() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return main_sinceName ? [(String(main_builderWrittenKey) + String(userDotMsg)) + "\(k_filterName)" + (String(user_scareFirstLinkTitle.prefix(4)) + ":7766")] : [(String(mainDestroySocialFragmentMessage) + app_scanCrushId.replacingOccurrences(of: "sight", with: "77"))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension StackSocketManager {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func stripTalk(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            HoeThen.halfLine(msg: String(bytes: userHostBottomUrl.map{fromProduction(cheat: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.level(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.replaceZone(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func option() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.replaceZone(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.grownup()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension StackSocketManager {
    /// socket保活心跳包
    //: private func sendPing() {
    private func scopeTalk() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: k_voiceTitle, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.someoneDisable(info: [(noti_selectedName.replacingOccurrences(of: "incorporate", with: "cm")): (String(show_wordKey))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func electronicImage() {
        //: endTalkingPing()
        nativeDown()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: user_popName, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isObject || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currObjectVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currObjectVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isObject || (self.decideBroad()?.isKind(of: FourthViewController.self) == false && self.decideBroad()?.isKind(of: StackErrorDelegate.self) == false) && (self.currObjectVC?.isKind(of: FourthViewController.self) == false && self.currObjectVC?.isKind(of: StackErrorDelegate.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.someoneDisable(info: [(noti_selectedName.replacingOccurrences(of: "incorporate", with: "cm")): (String(mainSunIdent.suffix(5)) + "eartbeat")])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func nativeDown() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func grownup() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(showPitchName.replacingOccurrences(of: "gift", with: "e") + "edDat" + dream_incomeAttachName.replacingOccurrences(of: "stretch", with: "e"))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < kSkipTitle {
                //: AppManager.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                AppManager.share.anSubstance(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension StackSocketManager {
    /// 开启直播心跳包
    //: func startLivePing() {
    func behindStart() {
        //: endLivePing()
        endWith()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: noti_askKey, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.someoneDisable(info: [(noti_selectedName.replacingOccurrences(of: "incorporate", with: "cm")): (String(notiCeaseMessage.prefix(5)) + "eart" + String(mainFromPositionStr.suffix(4)))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func endWith() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension StackSocketManager {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func labelDiscount() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = CaperReactiveCompatible()
        //: let keyWindow = ImmediatelyThen.getWindow()
        let keyWindow = ImmediatelyThen.wow()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isObject
        self.containerView?.isHidden = !self.isObject
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func dateEffect() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard GuideUnitReactiveCompatible.commonFragment().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard StuffLicenseReactiveCompatible.iconPoint().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension StackSocketManager {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func level(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(mTodayDismissStr)))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: mainTableMsg, reason: (String(userCapableName) + String(app_investCareUrl) + String(kSaleTitle)))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        nativeDown()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            dateEffect()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func sturdiness() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = StackSocketManager.shared.downward(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.replaceZone(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func replaceZone(timeInterval: TimeInterval, type: FaceRelated) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            HoeThen.halfLine(msg: (String(dream_cityHintName)) + "\(type.rawValue)" + (main_ballotPath.capitalized + String(notiSocialHourMsg)) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.beggarMyNeighbourPolicy(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func beggarMyNeighbourPolicy(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                HoeThen.halfLine(msg: (String(dream_waterMsg.prefix(9)) + show_packName.capitalized + "需\u{8981}重新连接."))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                HoeThen.halfLine(msg: (m_coldKey.replacingOccurrences(of: "scale", with: "K") + "T 正在连" + user_saleName + userOwnStr.replacingOccurrences(of: "request", with: "接")))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isObject {
        if self.isObject {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(app_curMsg.prefix(4)) + String(mainOutputKey)))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(noti_leavePath)) + "\(SweepAddrTool.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        level(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = notiSucceedUrl
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        HoeThen.halfLine(msg: (showStrikeLayerPath.replacingOccurrences(of: "pile", with: "O") + " 开始连接" + dreamCandidModifyOriginalId) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func someoneDisable(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if AppManager.share.reachability?.connection == nil || AppManager.share.reachability?.connection == .unavailable {
        if AppManager.share.reachability?.connection == nil || AppManager.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(noti_selectedName.replacingOccurrences(of: "incorporate", with: "cm")): (String(dream_changeName.suffix(4)) + "tDis" + app_cheerSucceedKey.replacingOccurrences(of: "container", with: "d")),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (k_dimensionStr.replacingOccurrences(of: "again", with: "rr")): "\(dreamMeetingName)",
                                       //: "data": [:]]
                                       (kMineData.replacingOccurrences(of: "lab", with: "a")): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            reason(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(noti_selectedName.replacingOccurrences(of: "incorporate", with: "cm"))] as? String == (String(show_wordKey)) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                HoeThen.halfLine(msg: (String(k_bubbleAccompanyContent) + mStackName.replacingOccurrences(of: "defense", with: "d") + String(kWritingContent.prefix(7))))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(noti_selectedName.replacingOccurrences(of: "incorporate", with: "cm"))] as? String == (String(mainSunIdent.suffix(5)) + "eartbeat") {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                HoeThen.halfLine(msg: String(bytes: k_explorationStr.map{weeklySalt(slip: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(noti_selectedName.replacingOccurrences(of: "incorporate", with: "cm"))] as? String == (String(notiCeaseMessage.prefix(5)) + "eart" + String(mainFromPositionStr.suffix(4))) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                HoeThen.halfLine(msg: String(bytes: k_surgeryData.map{fellowMax(detect: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(noti_selectedName.replacingOccurrences(of: "incorporate", with: "cm"))] as? String == (String(show_wordKey)) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(noti_selectedName.replacingOccurrences(of: "incorporate", with: "cm")): (String(mainSpeculateKey.suffix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (k_dimensionStr.replacingOccurrences(of: "again", with: "rr")): "\(app_partyIdent)",
                                           //: "data": [:]]
                                           (kMineData.replacingOccurrences(of: "lab", with: "a")): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                reason(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.replaceZone(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func reason(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(noti_selectedName.replacingOccurrences(of: "incorporate", with: "cm"))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(k_dimensionStr.replacingOccurrences(of: "again", with: "rr"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(userTowardStr.replacingOccurrences(of: "random", with: "s"))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(kMineData.replacingOccurrences(of: "lab", with: "a"))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (mArrayData.replacingOccurrences(of: "bubble", with: "息") + String(user_generalPath.prefix(8))) + "\(cmdName)" + (String(userTapData) + String(app_advancedTitle)) + "\(errorNo)" + (String(dream_carryStr.prefix(6))) + "\(msg)" + (String(user_secureLabUrl.prefix(7))) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        appearExtra(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            HoeThen.halfLine(msg: (k_peelValue.replacingOccurrences(of: "plain", with: "SO") + String(mainProfileData.suffix(4)) + "收异\u{5e38}消息\u{ff1a}" + String(notiDivisionGreetMsg.suffix(4)) + "ame:") + "\(cmdName)" + (String(userTapData) + String(app_advancedTitle)) + "\(errorNo)" + (String(dream_carryStr.prefix(6))) + "\(msg)" + (String(user_secureLabUrl.prefix(7))) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: AppManager.share.loginUserMode.freeCallTimes = 0
                AppManager.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !AppManager.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !AppManager.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                NowThen.shared.chipAt()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.bring(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(kNoseName.suffix(5)) + "eived" + String(user_dialogAutomaticData.prefix(4))) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: appErrAssetId, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(show_soldName)) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.kingdom(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(appLikeMsg) + String(dream_hangPath)) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.subjectMatter(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(mOkBetterIdent) + String(dream_detailId.prefix(4))) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(userMayEquallyPath) + String(noti_verticalPath.suffix(7))) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(user_sinceKey)) { // 接收方发送acceptCall回执
            //: self.isObject = true
            self.isObject = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            electronicImage()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.codex(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(k_capabilityIdent.suffix(4)) + "opCall") { // 接收方发送stopCall回执
            //: self.isObject = false
            self.isObject = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            nativeDown()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(k_elementMatchMsg))] as? Int {
                //: AppManager.share.loginUserMode.freeCallTimes = freeCallTimes
                AppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: AppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            AppManager.share.loginUserMode.mf_coin = json[(kMineData.replacingOccurrences(of: "lab", with: "a"))][(String(k_situationIdent))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: user_collectionStr, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (ApplicationReactiveCompatible.shared as! ApplicationReactiveCompatible).creatureInReport()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(mScanMusicalValue) + String(mCancelAttachIdent)) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.froth(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (k_checkResumePath.replacingOccurrences(of: "perform", with: "ep") + String(dreamCreateValue.prefix(5))) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isObject = true
            self.isObject = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            electronicImage()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.putData(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(show_lifeTitle.prefix(5)) + k_curLimitedFromValue.replacingOccurrences(of: "nag", with: "l")) { // 接收方/拨打方 挂断了通话
            //: self.isObject = false
            self.isObject = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            nativeDown()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.bringContext(data: data)
            // 更新余额
            //: AppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            AppManager.share.loginUserMode.mf_coin = json[(kMineData.replacingOccurrences(of: "lab", with: "a"))][(String(k_situationIdent))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            norProperty(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: user_collectionStr, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (ApplicationReactiveCompatible.shared as! ApplicationReactiveCompatible).creatureInReport()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (showStructureFormat.replacingOccurrences(of: "musical", with: "q") + notiCommunicationKey.replacingOccurrences(of: "vacation", with: "s") + String(showModeUrl.prefix(5))) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: mainReadName, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: AppManager.share.recieve1v1TalkRequest(info: data)
                AppManager.share.anSubstance(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(showPitchName.replacingOccurrences(of: "gift", with: "e") + "edDat" + dream_incomeAttachName.replacingOccurrences(of: "stretch", with: "e"))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: dream_bubbleMessage.map{skinApply(score: $0)}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(kMineData.replacingOccurrences(of: "lab", with: "a"))][(noti_usData.replacingOccurrences(of: "native", with: "e"))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: dreamBeDiskId.map{thenListen(level: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                ComparableBaseDelegate.stagePair(uid: String(json[(kMineData.replacingOccurrences(of: "lab", with: "a"))][(String(mBeanVeryStr))].intValue),
                                            //: title: data["nickname"] as? String,
                                            title: data[(String(show_pieceName))] as? String,
                                            //: body: body,
                                            body: body,
                                            //: imageUrl: data["headPic"] as? String)
                                            imageUrl: data[(String(m_displayKey.suffix(7)))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                ComparableBaseDelegate.shared.chip()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(showPeopleData.suffix(4)) + "llResu" + main_givenProcessMessage.replacingOccurrences(of: "speaker", with: "e")) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isObject = true
            self.isObject = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            electronicImage()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(showPeopleData.suffix(4)) + "llResu" + main_givenProcessMessage.replacingOccurrences(of: "speaker", with: "e"))] = "1"
            //: AppManager.share.recieve1v1TalkResumeRequest(info: resumeData)
            AppManager.share.shadowPet(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (ApplicationReactiveCompatible.shared as! ApplicationReactiveCompatible).strengthTimer()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (String(dreamLicenseName)) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isObject = false
            self.isObject = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            nativeDown()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.whiteData(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                ComparableBaseDelegate.noErase(identifier: String(json[(kMineData.replacingOccurrences(of: "lab", with: "a"))][(String(mBeanVeryStr))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                ComparableBaseDelegate.shared.springBgm()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (appAnyoneData.replacingOccurrences(of: "shrink", with: "s") + "eCal" + m_interestValue.lowercased()) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isObject = false
            self.isObject = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            nativeDown()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.whiteReact(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(dreamInmateMessage) + String(userMemoryEyeKey.prefix(4)) + "Motiva" + noti_termValue.replacingOccurrences(of: "bridge", with: "o")) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if AppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if AppManager.share.loginUserMode.sex == AlongsideDigitalTableConvertible.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.inmate?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(appHideId.suffix(6)) + "Warning") { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            GuideUnitReactiveCompatible.mouthEnable(content: msg, reason: json[(kMineData.replacingOccurrences(of: "lab", with: "a"))][(String(dream_historyIdent))].stringValue, time: json[(kMineData.replacingOccurrences(of: "lab", with: "a"))][(String(noti_againstBroadcastUrl))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(kSlideValue.prefix(5)) + "pLive") { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            GuideUnitReactiveCompatible.commonFragment().theResource()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(dream_scaleId.suffix(5)) + "dProgra" + String(kCountTitle.prefix(5))) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.licenseFee(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(app_quicklyBulkMsg) + String(mainRegionGrayMsg)) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(main_frontFormat))] as? Int ?? -1
            //: if AppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if AppManager.share.appStatus == WrinkleSendable.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: m_videoData, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: appErrAssetId, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: m_videoData, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(kMarkStr) + String(app_memoryPictureFormat.prefix(5))) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            CharacterDanmuManager.metadata().write(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(userSoulValue.suffix(5)) + "ific" + String(mSelectMsg)) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            NowThen.shared.singleLimited(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(k_deliveryStr.prefix(5)) + "rBloc" + mainRequestPath.replacingOccurrences(of: "over", with: "k")) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            GuideUnitReactiveCompatible.commonFragment().theResource()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            StuffLicenseReactiveCompatible.iconPoint().someoneStart()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: notiScaleNetAcceptIdent, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension StackSocketManager {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func appearExtra(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (showStructureFormat.replacingOccurrences(of: "musical", with: "q") + notiCommunicationKey.replacingOccurrences(of: "vacation", with: "s") + String(showModeUrl.prefix(5))) || cmdName == (String(appLikeMsg) + String(dream_hangPath)) {
            //: if TalkingLiveManager.shared().isLive == true {
            if GuideUnitReactiveCompatible.commonFragment().isLive == true {
                //: NEED_RESTORE_LIVE = true
                main_identityMessage = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                GuideUnitReactiveCompatible.writeCompletion(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                GuideUnitReactiveCompatible.commonFragment().theResource()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard main_identityMessage == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (String(dreamLicenseName)) || cmdName == (String(mOkBetterIdent) + String(dream_detailId.prefix(4))) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (appAnyoneData.replacingOccurrences(of: "shrink", with: "s") + "eCal" + m_interestValue.lowercased()) || cmdName == (String(userMayEquallyPath) + String(noti_verticalPath.suffix(7))) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(show_lifeTitle.prefix(5)) + k_curLimitedFromValue.replacingOccurrences(of: "nag", with: "l")) || cmdName == (String(k_capabilityIdent.suffix(4)) + "opCall")
        {
            //: NEED_RESTORE_LIVE = false
            main_identityMessage = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = SeeWorldViewThen(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                GuideUnitReactiveCompatible.commonFragment().threadTo()
            }
            //: alert.show()
            alert.visualLink()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func norProperty(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(String(k_elementMatchMsg))] as? Int {
            //: AppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            AppManager.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(String(appTrulyValue) + String(kFromStr))] as? Bool
        //: if freeCallWin == true && AppManager.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, AppManager.share.loginUserMode.sex == AlongsideDigitalTableConvertible.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = ParentObjectProtocol()
            //: view.show()
            view.capability()
            //: view.setShowData(data: data)
            view.theData(data: data)
        }
    }
}
