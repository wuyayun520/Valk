
//: Declare String Begin

/*: "bg_message_top" :*/
fileprivate let notiWaveKey:String = "miss tee literal extended dogbg_mes"
fileprivate let k_brigId:String = "margin"

/*: "777777" :*/
fileprivate let showSlightStr:String = "777777"

/*: "Messages" :*/
fileprivate let showDenyStr:[Character] = ["M","e","s","s","a","g","e","s"]

/*: "Who like me" :*/
fileprivate let k_coalIdent:String = "little plastic person viewWh"
fileprivate let showObstacleContent:[Character] = ["o"," ","l","i","k","e"," ","m","e"]

/*: "Call" :*/
fileprivate let mainBottomFormat:[Character] = ["C","a","l","l"]

/*: "#FF2348" :*/
fileprivate let dream_detailedWhyKey:String = "#FF2348us nobody fire save numb"

/*: "Current network unavailable" :*/
fileprivate let dreamBucketRefPath:String = "content period customer dogCurren"
fileprivate let m_averageUrl:String = "t neeastern protocol gap"
fileprivate let k_utilizePath:[Character] = ["t","w","o","r","k"," ","u","n","a","v","a","i","l","a","b","l","e"]

/*: "icon_yidu_pre" :*/
fileprivate let kTaskMsg:String = "screenco"
fileprivate let user_birthAMessage:String = "n_yidutag without environment"

/*: "transform.rotation" :*/
fileprivate let main_fileIdent:String = "tpatientn"
fileprivate let app_issueReadFormat:String = ".rotprofess stop case"
fileprivate let main_deviceMsg:[Character] = ["a","t","i","o","n"]

/*: "transform.scale" :*/
fileprivate let kEasternWholeText:[Character] = ["t","r","a","n","s","f","o"]
fileprivate let mRemainText:String = "rm.scalejust player monitor plus"

/*: "zoom&shake" :*/
fileprivate let dream_moveSchemeTitle:String = "zoom&shatill standard judge"
fileprivate let noti_nothingData:String = "lovely"

/*: "yyyy-MM-dd" :*/
fileprivate let user_contentIdent:String = "them search transportyyyy-"
fileprivate let appEnjoyData:[Character] = ["M","M","-","d","d"]

/*: "Allow %@ to send you notifications?" :*/
fileprivate let show_exReaderMessage:[UInt8] = [0x6d,0x40,0x40,0x43,0x5b,0xc,0x9,0x6c,0xc,0x58,0x43,0xc,0x5f,0x49,0x42,0x48,0xc,0x55,0x43,0x59,0xc,0x42,0x43,0x58,0x45,0x4a,0x45,0x4f,0x4d,0x58,0x45,0x43,0x42,0x5f,0x13]

private func localTurn(mm num: UInt8) -> UInt8 {
    return num ^ 44
}

/*: "Cancel" :*/
fileprivate let dream_hiddenName:[Character] = ["C","a","n","c","e","l"]

/*: "Settings" :*/
fileprivate let mainOrangeValue:[Character] = ["S","e","t","t","i","n","g","s"]

/*: "badNumber" :*/
fileprivate let dreamDotValue:String = "badNumbemusical try incorporate"
fileprivate let dreamAvailabilityBenchPath:[Character] = ["r"]

/*: "isConnection" :*/
fileprivate let dreamClientText:[Character] = ["i","s","C","o","n","n","e","c","t","i","o","n"]

/*: "networkStatus" :*/
fileprivate let dreamStartCancelMessage:[Character] = ["n","e","t","w","o","r","k","S"]
fileprivate let app_liberalKey:[Character] = ["t","a","t","u","s"]

/*: "unreadMessageNum" :*/
fileprivate let user_currencyId:String = "unearna"
fileprivate let dream_trustName:String = "dMessaless output export awake"

/*: "Do you want to mark all messages as read?" :*/
fileprivate let showVersionPastValue:[UInt8] = [0xda,0xf1,0xbe,0xe7,0xf1,0xeb,0xbe,0xe9,0xff,0xf0,0xea,0xbe,0xea,0xf1,0xbe,0xf3,0xff,0xec,0xf5,0xbe,0xff,0xf2,0xf2,0xbe,0xf3,0xfb,0xed,0xed,0xff,0xf9,0xfb,0xed,0xbe,0xff,0xed,0xbe,0xec,0xfb,0xff,0xfa,0xa1]

/*: "消息列表一键已读失败：code: :*/
fileprivate let kSubData:String = "消息列\u{8868}"
fileprivate let k_wouldAttachPath:String = "code:cell ready dream actual"

/*: , desc: :*/
fileprivate let m_inviteValue:String = ", "
fileprivate let dreamAdvancedValue:String = "desc:aspect lose shall entry"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ButtonEveryViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/20.
//

//: import UIKit
import UIKit

//: class TalkingMessageViewController: TalkingBaseViewController {
class ButtonEveryViewController: FictionalCharacterRecognizerDelegate {
    //: var isNoChangeTop = false
    var isNoChangeTop = false
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var seleteIndex = 0
    private var seleteIndex = 0

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: setIsShowCleanAmimat()
        spendBefore()
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: createUI()
        pursuing()
        //: bindInteraction()
        bond()
        //: func__turnOnSystemNotification()
        bright()
    }

    // MARK: - Lazy load

    // 背景
    //: private lazy var colorImageView: UIImageView = {
    private lazy var colorImageView: UIImageView = {
        //: let colorV = UIImageView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 100+StatusBarNavigationBarHeight))
        let colorV = UIImageView(frame: CGRect(x: 0, y: 0, width: kGiftIdent, height: 100 + kSoundPath))
        //: colorV.image = UIImage.BundleImageNamed(name: "bg_message_top")
        colorV.image = UIImage.verbalDescription(name: (String(notiWaveKey.suffix(6)) + "sage_to" + k_brigId.replacingOccurrences(of: "margin", with: "p")))
        //: return colorV
        return colorV
        //: }()
    }()

    //: private lazy var noticeView: TalkingNoticeTipView = {
    private lazy var noticeView: CatchOutTipView = {
        //: let V = TalkingNoticeTipView()
        let V = CatchOutTipView()
        //: V.isHidden = true
        V.isHidden = true
        //: return V
        return V
        //: }()
    }()

    //: private lazy var slideVC: QYSlideNavigationViewController = {
    private lazy var slideVC: KindViewController = {
        //: let vc = QYSlideNavigationViewController()
        let vc = KindViewController()
        //: vc.topDistance = 0
        vc.topDistance = 0
        //: vc.controllerItems = controllerItems
        vc.controllerItems = controllerItems
        //: vc.titleItems = titleItems
        vc.titleItems = titleItems
        //: vc.rightSpace = 60
        vc.rightSpace = 60
        //: vc.titleViewBackgroundColor = .clear
        vc.titleViewBackgroundColor = .clear
        //: vc.titleFont = .pingfangFont(type: .Medium, fontSize: 16)
        vc.titleFont = .concern(type: .Medium, fontSize: 16)
        //: vc.selectTitleFont = .pingfangFont(type: .Medium, fontSize: 18)
        vc.selectTitleFont = .concern(type: .Medium, fontSize: 18)
        //: vc.normalColor = UIColor(hex: "777777")!
        vc.normalColor = UIColor(hex: (showSlightStr.capitalized))!
        //: vc.selectedColor = UIColor.appTitleColor()
        vc.selectedColor = UIColor.telecom()
        //: vc.hasBttomTabBar = true
        vc.hasBttomTabBar = true
        //: vc.delegate = self
        vc.delegate = self
        //: vc.scrollEnabled = false
        vc.scrollEnabled = false
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var controllerItems: [TalkingBaseViewController] = {
    private lazy var controllerItems: [FictionalCharacterRecognizerDelegate] = {
        //: var arr = [self.chatListVC, TalkingWhoLikeViewController()]
        var arr = [self.chatListVC, WrongThen()]
        //: if AppManager.share.loginUserMode.callTabSwitch == 1 {
        if AppManager.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert(TalkingVideoCallVC(), at: arr.count-1)
            arr.insert(WrongSweepViewController(), at: arr.count - 1)
        }
        //: return arr
        return arr

        //: }()
    }()

    //: private lazy var chatListVC: TalkingChatListViewController = {
    private lazy var chatListVC: SayManagerDelegate = {
        //: let vc = TalkingChatListViewController()
        let vc = SayManagerDelegate()
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var titleItems: [String] = {
    private lazy var titleItems: [String] = {
        //: var arr = ["Messages".localized, "Who like me".localized]
        var arr = [(String(showDenyStr)).localized, (String(k_coalIdent.suffix(2)) + String(showObstacleContent)).localized]
        //: if AppManager.share.loginUserMode.callTabSwitch == 1 {
        if AppManager.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert("Call".localized, at: arr.count-1)
            arr.insert((String(mainBottomFormat)).localized, at: arr.count - 1)
        }
        //: return arr
        return arr
        //: }()
    }()

    // 断网提醒
    //: private lazy var netTipLabel: UILabel = {
    private lazy var netTipLabel: UILabel = {
        //: let tipView = UILabel.init(frame: CGRect(x: 0, y: StatusBarNavigationBarHeight, width: ScreenWidth, height: 30))
        let tipView = UILabel(frame: CGRect(x: 0, y: kSoundPath, width: kGiftIdent, height: 30))
        //: tipView.font = UIFont.pingfangRugularFont(fontSize: 14)
        tipView.font = UIFont.duringWindowExtreme(fontSize: 14)
        //: tipView.textColor = .white
        tipView.textColor = .white
        //: tipView.backgroundColor = UIColor.init(hex: "#FF2348")?.withAlphaComponent(0.8)
        tipView.backgroundColor = UIColor(hex: (String(dream_detailedWhyKey.prefix(7))))?.withAlphaComponent(0.8)
        //: tipView.textAlignment = .center
        tipView.textAlignment = .center
        //: tipView.text = "Current network unavailable".localized
        tipView.text = (String(dreamBucketRefPath.suffix(6)) + String(m_averageUrl.prefix(4)) + String(k_utilizePath)).localized
        //: tipView.isHidden = true
        tipView.isHidden = true
        //: return tipView
        return tipView
        //: }()
    }()

    // message红点角标
    //: private lazy var msgNumLabel: BadgeLab = {
    private lazy var msgNumLabel: BagComprehensiveBadgeLab = {
        //: let numbLabel = BadgeLab()
        let numbLabel = BagComprehensiveBadgeLab()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // whoLikeMe红点角标
    //: private lazy var badgLB: BadgeLab = {
    private lazy var badgLB: BagComprehensiveBadgeLab = {
        //: let numbLabel = BadgeLab()
        let numbLabel = BagComprehensiveBadgeLab()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // 一键清除
    //: private lazy var cleanBtn: UIButton = {
    private lazy var cleanBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.backgroundColor = .clear
        btn.backgroundColor = .clear
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .normal)
        btn.setImage(UIImage.verbalDescription(name: (kTaskMsg.replacingOccurrences(of: "screen", with: "i") + String(user_birthAMessage.prefix(6)) + "_pre")), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .highlighted)
        btn.setImage(UIImage.verbalDescription(name: (kTaskMsg.replacingOccurrences(of: "screen", with: "i") + String(user_birthAMessage.prefix(6)) + "_pre")), for: .highlighted)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - PeopleTailNavigationDelegate

//: extension TalkingMessageViewController: QYSlideNavigationDelegate {
extension ButtonEveryViewController: PeopleTailNavigationDelegate {
    //: func selectedViewController(_ viewController: UIViewController, index: Int) {
    func succeed(_ viewController: UIViewController, index: Int) {
        //: self.seleteIndex = Int(index)
        self.seleteIndex = Int(index)
        //: if viewController.isKind(of: TalkingChatListViewController.self) {
        if viewController.isKind(of: SayManagerDelegate.self) {
            //: self.cleanBtn.isHidden = false
            self.cleanBtn.isHidden = false
            //: setIsShowCleanAmimat()
            spendBefore()

            //: } else if viewController.isKind(of: TalkingWhoLikeViewController.self) {
        } else if viewController.isKind(of: WrongThen.self) {
            //: self.cleanBtn.isHidden = true
            self.cleanBtn.isHidden = true
            //: uploadRecord.uploadRecordEvent(eventID: ClickWLikeMeTabNoP)
            noti_thirdUrl.toSpeaker(eventID: app_marginPath)
        }
    }
}

// MARK: - Event

//: extension TalkingMessageViewController {
extension ButtonEveryViewController {
    //: func setIsTopAll() {
    func onceAll() {
        /// 默认选中all
        //: if seleteIndex == 0 {
        if seleteIndex == 0 {
            //: let vc = slideVC.controllerItems.first as? TalkingChatListViewController
            let vc = slideVC.controllerItems.first as? SayManagerDelegate
            //: vc?.resetToTopItemView()
            vc?.sight()
            // 刷新在线状态
            //: vc?.refreshOnlineStatus(isFirst: true)
            vc?.doingLine(isFirst: true)
        }
    }

    /// 无网络提示
    //: func func__checkNetTipState() {
    func search() {
        //: if AppManager.share.networkStatus != .Unavailable && AmongObjectManager.shared.isConnection {
        if AppManager.share.networkStatus != .Unavailable, AmongObjectManager.shared.isConnection {
            //: netTipLabel.isHidden = true
            netTipLabel.isHidden = true
            //: } else {
        } else {
            //: netTipLabel.isHidden = false
            netTipLabel.isHidden = false
        }
    }

    /// message红点
    //: func func__messagebadgLBState(num: Int) {
    func recordSmartNum(num: Int) {
        //: if num > 0 {
        if num > 0 {
            //: msgNumLabel.isHidden = false
            msgNumLabel.isHidden = false
            //: if num > 99 {
            if num > 99 {
                //: msgNumLabel.text = "99+"
                msgNumLabel.text = "99+"
                //: } else {
            } else {
                //: msgNumLabel.text = String(num)
                msgNumLabel.text = String(num)
            }
            //: } else {
        } else {
            //: msgNumLabel.isHidden = true
            msgNumLabel.isHidden = true
        }
    }

    /// 一键清除动画
    //: func setIsShowCleanAmimat() {
    func spendBefore() {
        //: let unreadMsgCount = AppManager.share.unreadMessageNum
        let unreadMsgCount = AppManager.share.unreadMessageNum
        //: let isClean: Bool = UserDefaults.standard.object(forKey: isShowCleanAnimatCacheKey) as? Bool ?? false
        let isClean: Bool = UserDefaults.standard.object(forKey: userFailureMessage) as? Bool ?? false
        //: if unreadMsgCount >= 10 && !isClean {
        if unreadMsgCount >= 10, !isClean {
            //: self.cleanBtnBtnAnimat()
            self.faceOf()
        }
    }

    //: func cleanBtnBtnAnimat() {
    func faceOf() {
        //: let degreesToRadians = 180.0 * .pi
        let degreesToRadians = 180.0 * .pi
        //: let angle1 = 0 / degreesToRadians
        let angle1 = 0 / degreesToRadians
        //: let angle2 = 100 / degreesToRadians
        let angle2 = 100 / degreesToRadians
        //: let angle3 = -100 / degreesToRadians
        let angle3 = -100 / degreesToRadians
        //: let angle4 = 100 / degreesToRadians
        let angle4 = 100 / degreesToRadians
        //: let angle5 = -100 / degreesToRadians
        let angle5 = -100 / degreesToRadians
        //: let angle6 = 0 / degreesToRadians
        let angle6 = 0 / degreesToRadians

        //: let du1: NSNumber = 0
        let du1: NSNumber = 0
        //: let du2: NSNumber = 0.13
        let du2: NSNumber = 0.13
        //: let du3: NSNumber = 0.3
        let du3: NSNumber = 0.3
        //: let du4: NSNumber = 0.47
        let du4: NSNumber = 0.47
        //: let du5: NSNumber = 0.8
        let du5: NSNumber = 0.8
        //: let du6: NSNumber = 1.0
        let du6: NSNumber = 1.0

        // 旋转
        //: let roate = CAKeyframeAnimation.init(keyPath: "transform.rotation")
        let roate = CAKeyframeAnimation(keyPath: (main_fileIdent.replacingOccurrences(of: "patient", with: "ra") + "sform" + String(app_issueReadFormat.prefix(4)) + String(main_deviceMsg)))
        //: roate.values = [(angle1), (angle2), (angle3), (angle4), (angle5), (angle6)]
        roate.values = [angle1, angle2, angle3, angle4, angle5, angle6]
        //: roate.keyTimes = [(du1), (du2), (du3), (du4), (du5), (du6)]
        roate.keyTimes = [du1, du2, du3, du4, du5, du6]

        // 缩放
        //: let zoom = CAKeyframeAnimation.init(keyPath: "transform.scale")
        let zoom = CAKeyframeAnimation(keyPath: (String(kEasternWholeText) + String(mRemainText.prefix(8))))
        //: zoom.values = [(1.0), (1.2), (1.0)]
        zoom.values = [1.0, 1.2, 1.0]
        //: zoom.keyTimes = [(0), (0.5), (1.0)]
        zoom.keyTimes = [0, 0.5, 1.0]

        //
        //: let groupAnnimation = CAAnimationGroup.init()
        let groupAnnimation = CAAnimationGroup()
        //: groupAnnimation.autoreverses = false
        groupAnnimation.autoreverses = false
        //: groupAnnimation.animations = [roate, zoom]
        groupAnnimation.animations = [roate, zoom]
        //: groupAnnimation.duration = 1.0
        groupAnnimation.duration = 1.0
        //: groupAnnimation.repeatCount = 2
        groupAnnimation.repeatCount = 2
        //: groupAnnimation.isRemovedOnCompletion = false
        groupAnnimation.isRemovedOnCompletion = false
        //: groupAnnimation.fillMode = .forwards
        groupAnnimation.fillMode = .forwards
        //: cleanBtn.layer.add(groupAnnimation, forKey: "zoom&shake")
        cleanBtn.layer.add(groupAnnimation, forKey: (String(dream_moveSchemeTitle.prefix(8)) + noti_nothingData.replacingOccurrences(of: "lovely", with: "ke")))
    }

    /// 检查系统通知是否开启
    //: private func func__turnOnSystemNotification() {
    private func bright() {
        //: TalkingPermissionTool.checkPushNotification { isEnable in
        SatelliteThen.cruise { isEnable in

            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: if isEnable {
                if isEnable {
                    //: self.noticeView.isHidden = true
                    self.noticeView.isHidden = true
                    //: self.func__hideNotificationTipView(hide: true)
                    self.norHide(hide: true)
                    //: }else{
                } else {
                    /// 页面顶部提示
                    //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                    let today = NSDate.dirndl(date: Date(), dateFormat: (String(user_contentIdent.suffix(5)) + String(appEnjoyData)))
                    //: if let closeDateStr = Defaults.string(forKey: TalkingMessageTabCloseNotificationsKey), today == closeDateStr {
                    if let closeDateStr = show_titleId.string(forKey: mainMagnitudeObjectBlockValue), today == closeDateStr {
                        //: self.func__hideNotificationTipView(hide: true)
                        self.norHide(hide: true)
                        //: }else{
                    } else {
                        //: self.func__hideNotificationTipView(hide: false)
                        self.norHide(hide: false)
                    }
                    /// 弹框逻辑
                    //: let isShow = Defaults.bool(forKey: TalkingMessageTabTurnOnNotificationsKey)
                    let isShow = show_titleId.bool(forKey: noti_maleMeetingContent)
                    //: if !isShow {
                    if !isShow {
                        //: Defaults.set(true, forKey: TalkingMessageTabTurnOnNotificationsKey)
                        show_titleId.set(true, forKey: noti_maleMeetingContent)
                        //: TalkingAlertShow.alert(title: nil,
                        DemonstrateSayAlertReactiveCompatible.rightUsed(title: nil,
                                                   //: message: "Allow %@ to send you notifications?".localizedArguments(AppName),
                                                   message: String(bytes: show_exReaderMessage.map{localTurn(mm: $0)}, encoding: .utf8)!.launchRid(noti_collectionMessage),
                                                   //: leftBtnTitle: "Cancel".localized,
                                                   leftBtnTitle: (String(dream_hiddenName)).localized,
                                                   //: rightBtnTitle: "Settings".localized) {
                                                   rightBtnTitle: (String(mainOrangeValue)).localized)
                        {
                            //: TalkingAlertShow.hideAlert()
                            DemonstrateSayAlertReactiveCompatible.paidVacation()
                            //: return
                            //: } rightBlock: {
                        } rightBlock: {
                            //: if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                            if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                                //: UIApplication.shared.open(settingsUrl)
                                UIApplication.shared.open(settingsUrl)
                            }
                        }
                    }
                }
            }
        }
    }

    //: private func func__hideNotificationTipView(hide: Bool) {
    private func norHide(hide: Bool) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: if hide{
            if hide {
                //: self.noticeView.isHidden = true
                self.noticeView.isHidden = true
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight)
                    make.top.equalToSuperview().offset(dreamNoSampleValue)
                }
                //: self.slideVC.viewHeight = ScreenHeight - StatusBarHeight - TabBarViewHeight
                self.slideVC.viewHeight = user_allMessage - dreamNoSampleValue - dreamOffValue
                //: }else{
            } else {
                //: self.noticeView.isHidden = false
                self.noticeView.isHidden = false
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight + self.noticeView.viewHeight)
                    make.top.equalToSuperview().offset(dreamNoSampleValue + self.noticeView.viewHeight)
                }
                //: self.slideVC.viewHeight = ScreenHeight - self.noticeView.bottom - TabBarViewHeight
                self.slideVC.viewHeight = user_allMessage - self.noticeView.bottom - dreamOffValue
            }
        }
    }
}

// MARK: - 通知

//: extension TalkingMessageViewController {
extension ButtonEveryViewController {
    /// who like me 红点通知
    //: @objc func showbadgNumberNotif(notification: NSNotification) {
    @objc func pastBold(notification: NSNotification) {
        //: let userinfo = notification.userInfo as![String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let badNumber: Int  = userinfo["badNumber"] as! Int
        let badNumber: Int = userinfo[(String(dreamDotValue.prefix(8)) + String(dreamAvailabilityBenchPath))] as! Int
        //: badgLB.isHidden = false
        badgLB.isHidden = false
        //: if badNumber > 99 {
        if badNumber > 99 {
            //: badgLB.text = "99+"
            badgLB.text = "99+"
            //: } else if badNumber<=0 {
        } else if badNumber <= 0 {
            //: badgLB.isHidden = true
            badgLB.isHidden = true
            //: badgLB.text = "0"
            badgLB.text = "0"
            //: } else {
        } else {
            //: badgLB.text = String(badNumber)
            badgLB.text = String(badNumber)
        }
    }
}

// MARK: - Layout

//: extension TalkingMessageViewController {
extension ButtonEveryViewController {
    /// UI
    //: private func createUI() {
    private func pursuing() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white

        //: view.addSubview(colorImageView)
        view.addSubview(colorImageView)
        //: view.addSubview(noticeView)
        view.addSubview(noticeView)
        //: self.addChild(slideVC)
        self.addChild(slideVC)

        //: self.view.addSubview(slideVC.view)
        self.view.addSubview(slideVC.view)
        //: slideVC.view.backgroundColor = UIColor.clear
        slideVC.view.backgroundColor = UIColor.clear
        //: slideVC.view.snp.makeConstraints { make in
        slideVC.view.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(StatusBarHeight)
            make.top.equalToSuperview().offset(dreamNoSampleValue)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.bottom.equalToSuperview()
            make.bottom.equalToSuperview()
        }

        //: for btn in slideVC.allBtn {
        for btn in slideVC.allBtn {
            //: let curenbrn: UIButton = btn
            let curenbrn: UIButton = btn
            //: if curenbrn.titleLabel!.text == "Who like me".localized {
            if curenbrn.titleLabel!.text == (String(k_coalIdent.suffix(2)) + String(showObstacleContent)).localized {
                //: (curenbrn as AnyObject).addSubview(badgLB)
                (curenbrn as AnyObject).addSubview(badgLB)
                //: badgLB.snp.makeConstraints { make in
                badgLB.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
                //: badgLB.isHidden = true
                badgLB.isHidden = true
                //: } else if (curenbrn.titleLabel!.text == "Messages".localized) {
            } else if curenbrn.titleLabel!.text == (String(showDenyStr)).localized {
                //: (curenbrn as AnyObject).addSubview(msgNumLabel)
                (curenbrn as AnyObject).addSubview(msgNumLabel)
                //: msgNumLabel.snp.makeConstraints { make in
                msgNumLabel.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
            }
        }

        //: view.addSubview(cleanBtn)
        view.addSubview(cleanBtn)
        //: cleanBtn.snp.makeConstraints { make in
        cleanBtn.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top)
            make.top.equalTo(slideVC.view.snp.top)
            //: make.trailing.equalTo(-5)
            make.trailing.equalTo(-5)
            //: make.size.equalTo(CGSize(width: 44, height: 44))
            make.size.equalTo(CGSize(width: 44, height: 44))
        }

        //: view.addSubview(netTipLabel)
        view.addSubview(netTipLabel)
        //: netTipLabel.snp.makeConstraints { make in
        netTipLabel.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top).offset(44)
            make.top.equalTo(slideVC.view.snp.top).offset(44)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.height.equalTo(30)
            make.height.equalTo(30)
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func bond() {
        //: AmongObjectManager.shared.rx
        AmongObjectManager.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(dreamClientText)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.search()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: AppManager.share.rx.observeWeakly(Int.self, "networkStatus")
        AppManager.share.rx.observeWeakly(Int.self, (String(dreamStartCancelMessage) + String(app_liberalKey)))
            //: .subscribe(onNext: { [weak self]  (value) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.search()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // 展示全部红点
        //: AppManager.share.rx.observeWeakly(Int.self, "unreadMessageNum")
        AppManager.share.rx.observeWeakly(Int.self, (user_currencyId.replacingOccurrences(of: "earn", with: "re") + String(dream_trustName.prefix(6)) + "geNum"))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let valueInt = value ?? 0
                let valueInt = value ?? 0
                //: self.func__messagebadgLBState(num: valueInt)
                self.recordSmartNum(num: valueInt)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: cleanBtn.rx.controlEvent(.touchUpInside)
        cleanBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: {
            .subscribe(onNext: {
                //: let config = ShowAlertConfig()
                let config = SeeAlertConfig()
                //: config.alignment = .center
                config.alignment = .center

                //: TalkingAlertShow.customAlert(message: "Do you want to mark all messages as read?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, leftBlock: {
                DemonstrateSayAlertReactiveCompatible.alterConfig(message: String(bytes: showVersionPastValue.map{$0^158}, encoding: .utf8)!.localized, leftBtnTitle: (String(dream_hiddenName)).localized, rightBtnTitle: "OK".localized, leftBlock: {
                    //: TalkingAlertShow.hideAlert()
                    DemonstrateSayAlertReactiveCompatible.paidVacation()

                    //: }, rightBlock: {
                }, rightBlock: {
                    //: V2TIMManager.sharedInstance().markAllMessage {
                    V2TIMManager.sharedInstance().markAllMessage {
                        //: UserDefaults.standard.set(true, forKey: isShowCleanAnimatCacheKey)
                        UserDefaults.standard.set(true, forKey: userFailureMessage)
                        //: } fail: { code, desc in
                    } fail: { code, desc in
                        //: UploadLogTool.writeLog(msg: "消息列表一键已读失败：code:\(code), desc:\(String(describing: desc))")
                    }
                    //: }, config: config)
                }, config: config)

                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // who like me 红点通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(showbadgNumberNotif(notification:)),
                                               selector: #selector(pastBold(notification:)),
                                               //: name: WHOLIKEME_BADGENUMBER_NOTIF,
                                               name: appRejectName,
                                               //: object: nil)
                                               object: nil)

        //: noticeView.closeBtnBlock = { [weak self] ()  -> Void in
        noticeView.closeBtnBlock = { [weak self] () in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
            let today = NSDate.dirndl(date: Date(), dateFormat: (String(user_contentIdent.suffix(5)) + String(appEnjoyData)))
            //: Defaults.set(today, forKey: TalkingMessageTabCloseNotificationsKey)
            show_titleId.set(today, forKey: mainMagnitudeObjectBlockValue)
            //: self.func__hideNotificationTipView(hide: true)
            self.norHide(hide: true)
        }

        //: NotificationCenter.default.rx.notification( UIApplication.willEnterForegroundNotification)
        NotificationCenter.default.rx.notification(UIApplication.willEnterForegroundNotification)
            //: .subscribe(onNext: { [weak self] (notification) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__turnOnSystemNotification()
                self.bright()
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
