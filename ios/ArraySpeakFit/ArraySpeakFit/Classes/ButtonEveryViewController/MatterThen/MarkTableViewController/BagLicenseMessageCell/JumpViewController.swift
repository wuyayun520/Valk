
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_liveryValue:[UInt8] = [0x7e,0x79,0x7e,0x63,0x3f,0x74,0x78,0x73,0x72,0x65,0x2d,0x3e,0x37,0x7f,0x76,0x64,0x37,0x79,0x78,0x63,0x37,0x75,0x72,0x72,0x79,0x37,0x7e,0x7a,0x67,0x7b,0x72,0x7a,0x72,0x79,0x63,0x72,0x73]

private func timePeriod(platform num: UInt8) -> UInt8 {
    return num ^ 23
}

/*: "roomId" :*/
fileprivate let mainColorIdent:String = "roomIdanalyze whole"

/*: "type" :*/
fileprivate let m_completeValue:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "extra" :*/
fileprivate let notiMortalityTitle:String = "ehandlera"

/*: "jumpKey" :*/
fileprivate let mHowName:[UInt8] = [0x13,0xc,0x14,0x9,0x32,0x1c,0x0]

private func piArc(ion num: UInt8) -> UInt8 {
    return num ^ 121
}

/*: "Send Gift" :*/
fileprivate let showMaximumKey:String = "Send volume called check"
fileprivate let k_dragSampleId:[Character] = ["G","i","f","t"]

/*: "View info" :*/
fileprivate let user_dismissText:String = "View your thing processor"

/*: "Report" :*/
fileprivate let userInstrumentFormat:String = "visible directionReport"

/*: "uid" :*/
fileprivate let app_threeFormat:[UInt8] = [0x55,0x49,0x44]

private func staffValue(red num: UInt8) -> UInt8 {
    return num ^ 32
}

/*: "name" :*/
fileprivate let m_partyTitle:[UInt8] = [0x65,0x6d,0x61,0x6e]

/*: "Can't send blank message" :*/
fileprivate let k_textPicUrl:String = "Can\'t"
fileprivate let show_bothStr:String = " send communicate ion focus safety"
fileprivate let dream_visionLicenseRetainIdent:[Character] = ["e","s","s","a","g","e"]

/*: "user" :*/
fileprivate let main_fastPath:String = "usepure"

/*: "content" :*/
fileprivate let showBindContent:[UInt8] = [0x48,0x44,0x45,0x5f,0x4e,0x45,0x5f]

private func talkSeem(attribute num: UInt8) -> UInt8 {
    return num ^ 43
}

/*: "msgType" :*/
fileprivate let appHereFormat:String = "coat arrowmsgType"

/*: "txt" :*/
fileprivate let mainLimitedId:String = "naturalt"

/*: "isGroup" :*/
fileprivate let show_commonTitle:[Character] = ["i","s"]
fileprivate let notiElementWriteFormat:String = "Groupago table next cell rein"

/*: "jumps" :*/
fileprivate let mFairName:String = "jumpwrap"

/*: "message" :*/
fileprivate let user_prettyIdent:[UInt8] = [0x75,0x7d,0x6b,0x6b,0x79,0x7f,0x7d]

private func foodStatus(shared num: UInt8) -> UInt8 {
    return num ^ 24
}

/*: "TxtMsg" :*/
fileprivate let dreamOperateValue:String = "control cheat magnitude missTxtMs"
fileprivate let userUnknownKey:[Character] = ["g"]

/*: "toUid" :*/
fileprivate let appSharpWantData:String = "adjustment jumptoUid"

/*: "msgInfo" :*/
fileprivate let show_effectData:String = "consent contain light explore draftmsgInfo"

/*: "params" :*/
fileprivate let k_signFormat:[UInt8] = [0x6c,0x7d,0x6e,0x7d,0x71,0x6f]

private func pageWill(hide num: UInt8) -> UInt8 {
    return num ^ 28
}

/*: "contentType" :*/
fileprivate let mainRidName:[UInt8] = [0x7e,0x72,0x73,0x69,0x78,0x73,0x69,0x49,0x64,0x6d,0x78]

private func positiveCalendar(exploration num: UInt8) -> UInt8 {
    return num ^ 29
}

/*: "ImgMsg" :*/
fileprivate let mIndividualMessage:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let appPurposeThirdPath:String = "imwife"

/*: "imageUri" :*/
fileprivate let k_wayFormat:[UInt8] = [0xe7,0xe3,0xef,0xe9,0xeb,0xdb,0xfc,0xe7]

private func neatCalculation(barrier num: UInt8) -> UInt8 {
    return num ^ 142
}

/*: "pic" :*/
fileprivate let notiOpposedEqualMessage:String = "pisearch"

/*: "msgId" :*/
fileprivate let noti_triggerContent:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let mAccelerateKey:String = "pilet"
fileprivate let notiParentContent:[Character] = ["a","l","M","f","C","o","i","n"]

/*: "giftId" :*/
fileprivate let userInfrastructureAbsFormat:String = "giftIdcontent bucket"

/*: "giftNum" :*/
fileprivate let noti_upsetPolicyCompositionKey:String = "giftNurecording horse average hold customer"
fileprivate let k_pointCurrentPath:String = "rhythm"

/*: "pkgItemsetId" :*/
fileprivate let noti_yaYearValue:String = "pkgItinstall more"
fileprivate let app_whoStr:[Character] = ["t","I","d"]

/*: "msg" :*/
fileprivate let dream_tinTitle:String = "completeg"

/*: "tips" :*/
fileprivate let main_marginGivingText:String = "succeedips"

/*: "gift" :*/
fileprivate let dreamIllegalTitle:[Character] = ["g","i","f","t"]

/*: "nickname" :*/
fileprivate let showFromReasonKey:String = "nisimplyknam"
fileprivate let m_phoneStr:String = "front"

/*: "headPic" :*/
fileprivate let dream_blueValue:[UInt8] = [0x34,0x39,0x3d,0x38,0xc,0x35,0x3f]

private func nagSpan(digital num: UInt8) -> UInt8 {
    return num ^ 92
}

/*: "All Numbers" :*/
fileprivate let noti_availabilityIdent:[Character] = ["A","l","l"," ","N","u","m","b","e","r","s"]

/*: "sendNum" :*/
fileprivate let noti_charmChannelHimValue:String = "sendNudescription ballot topic"
fileprivate let mPeopleFormat:String = "absence"

/*: "toUser" :*/
fileprivate let mainAudiencePath:[Character] = ["t","o","U","s","e"]
fileprivate let userFeeId:String = "used"

/*: "msgIncome" :*/
fileprivate let dream_botId:String = "msgIdeadline filter"

/*: "isConnection" :*/
fileprivate let dreamQuantityerestTitle:String = "nag democratic week literal boldisCon"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  JumpViewController.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class JumpViewController: FictionalCharacterRecognizerDelegate {
    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = WasteMaterialThen() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = SayPreviouslyReactiveCompatible() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = WasteMaterialThen.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_liveryValue.map{timePeriod(platform: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if AppManager.share.appConfigMode.disableShootScreen {
            if AppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = TripUpThen(frame: CGRect(x: 0, y: 0, width: kGiftIdent, height: user_allMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        save()
        //: createUIConstraints()
        constraintsStill()
        //: bindInteraction()
        everyAttach()
        //: addTapGestureRecognizer()
        weeklyFore()
        //: func__chatRoom_insertTipMsg()
        gestureLabel()
        //: chatSettings()
        con()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            effect()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.recommendBag(join: false)
        //: self.destroyTipsTimer()
        self.recital()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: MarkTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = MarkTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(FamilyReactiveCompatible.self, forCellReuseIdentifier: FamilyReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(SwanMessageCell.self, forCellReuseIdentifier: SwanMessageCell.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(TarPoReactiveCompatible.self, forCellReuseIdentifier: TarPoReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(ExpressThen.self, forCellReuseIdentifier: ExpressThen.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: FourthGuideViewCell.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(GuideViewCell.self, forCellReuseIdentifier: FourthGuideViewCell.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: UnitInputView = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = UnitInputView(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: AmongView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = AmongView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: CommunicateTrackView = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = CommunicateTrackView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: FriendshipSmallThen = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = FriendshipSmallThen(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: PresenceMeView = {
        //: let v = TalkingSomeoneAtMeView()
        let v = PresenceMeView()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension JumpViewController {
    /// 群聊设置
    //: private func chatSettings() {
    private func con() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        TotalegrityChatManager.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        recommendBag(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func recommendBag(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(mainColorIdent.prefix(6))): self.conversationData.groupID, String(bytes: m_completeValue.reversed(), encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: LifeReactiveCompatible.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        LifeReactiveCompatible.goldJudge(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - DelegateDirty

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension JumpViewController: DelegateDirty {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> ProjectionCellData! {
    func frameAsk(_: MarkTableViewController!, enable data: V2TIMMessage!) -> ProjectionCellData! {
        //: let dic = SayEditChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = SayEditChatManager.assemblageSquareKey(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = GrapplingIronReactiveCompatible.roofModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: ProjectionCellData!) -> FourthGuideViewCell! {
    func income(_: MarkTableViewController!, format data: ProjectionCellData!) -> FourthGuideViewCell! {
        //: if data.isKind(of: ParentCellData.self) {
        if data.isKind(of: ParentCellData.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = FamilyReactiveCompatible(style: .default, reuseIdentifier: FamilyReactiveCompatible.className())
            //: let cellData = data as! ParentCellData
            let cellData = data as! ParentCellData
            //: cell.fill(with: cellData)
            cell.tip(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.guideUtilizer(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.response(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: BubbleCollectionCellData.self) {
        if data.isKind(of: BubbleCollectionCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = SwanMessageCell(style: .default, reuseIdentifier: SwanMessageCell.className())
            //: let cellData = data as! BubbleCollectionCellData
            let cellData = data as! BubbleCollectionCellData
            //: cell.fill(with: cellData)
            cell.tip(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: FleetDumpCellData.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = TarPoReactiveCompatible(style: .default, reuseIdentifier: TarPoReactiveCompatible.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! FleetDumpCellData
            //: cell.fill(with: cellData)
            cell.tip(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: MatterParentCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = ExpressThen(style: .default, reuseIdentifier: ExpressThen.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! MatterParentCellData
            //: cell.fill(with: cellData)
            cell.tip(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: FourthGuideViewCell!) {
    func sendAvatar(_: MarkTableViewController!, communicationAvatarDestroyAlongSelectDedication cell: FourthGuideViewCell!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(AppManager.share.loginUid) {
            if userModel.uid == Int(AppManager.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                guideUtilizer(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                plus(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: FourthGuideViewCell!) {
    func changeDescription(_: MarkTableViewController!, kindForText cell: FourthGuideViewCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: SwanMessageCell.self) { // 图片
            //: let imageCellData = cell.messageData as! BubbleCollectionCellData
            let imageCellData = cell.messageData as! BubbleCollectionCellData
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(k_lineWithoutTitle)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = TransportModelType()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = ComprehensiveAlterTransformable()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [TransportModelType] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = MatterFlowLayout(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.decideBroad()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: FamilyReactiveCompatible.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! FamilyReactiveCompatible
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = SayEditChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = SayEditChatManager.assemblageSquareKey(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(notiMortalityTitle.replacingOccurrences(of: "handle", with: "xt"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: mHowName.map{piArc(ion: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<FamilyTransformable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: PipSqueakReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    PipSqueakReactiveCompatible.share.largeness(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: ExpressThen.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! ExpressThen
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = SayEditChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = SayEditChatManager.assemblageSquareKey(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(notiMortalityTitle.replacingOccurrences(of: "handle", with: "xt"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: mHowName.map{piArc(ion: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<FamilyTransformable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: PipSqueakReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    PipSqueakReactiveCompatible.share.largeness(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: FourthGuideViewCell!) {
    func controller(_: MarkTableViewController!, factorTitle cell: FourthGuideViewCell!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        comeToGrips(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension JumpViewController {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func recital() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func plus(user: RealizeUserModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = MunuView(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.tricolour(cellTitleList: [(String(showMaximumKey.prefix(5)) + String(k_dragSampleId)).localized, "@", (String(user_dismissText.prefix(5)) + "info").localized, (String(userInstrumentFormat.suffix(6))).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.menuFunSoul(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.pic(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.guideUtilizer(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.called(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func menuFunSoul(user: RealizeUserModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.succeedBtn()
        //: let model = TalkingChatRoomMemberModel()
        let model = DamnHandyJSON()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.distanceModel(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func pic(user: RealizeUserModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: app_threeFormat.map{staffValue(red: $0)}, encoding: .utf8)!: String(user.uid), String(bytes: m_partyTitle.reversed(), encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.oecumenical(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func guideUtilizer(uid: Int) {
        //: PipSqueakReactiveCompatible.share.func__pushToUserDetailVC(uid: String(uid))
        PipSqueakReactiveCompatible.share.balanceConvertSeek(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func called(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = TransportThen(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.asRemove(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension JumpViewController {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func linguisticProcess(msgStr: String, quoteModel _: RealizeHeliModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            changeManage(showMsg: (k_textPicUrl + String(show_bothStr.prefix(6)) + "blank m" + String(dream_visionLicenseRetainIdent)).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: mHowName.map{piArc(ion: $0)}, encoding: .utf8)!: (main_fastPath.replacingOccurrences(of: "pure", with: "r")), String(bytes: app_threeFormat.map{staffValue(red: $0)}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: showBindContent.map{talkSeem(attribute: $0)}, encoding: .utf8)!: temStr, (String(appHereFormat.suffix(7))): (mainLimitedId.replacingOccurrences(of: "natural", with: "tx")), (String(show_commonTitle) + String(notiElementWriteFormat.prefix(5))): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(mFairName.replacingOccurrences(of: "wrap", with: "s"))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: user_prettyIdent.map{foodStatus(shared: $0)}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: m_completeValue.reversed(), encoding: .utf8)!: (String(dreamOperateValue.suffix(5)) + String(userUnknownKey)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(mainColorIdent.prefix(6))): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(appSharpWantData.suffix(5)))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(show_effectData.suffix(7))): msgInfo, String(bytes: k_signFormat.map{pageWill(hide: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = GrapplingIronReactiveCompatible.cameraTip(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            comeToGrips(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func anonymousImg(img: UIImage) {
        //: let imagePath = BubbleCollectionCellData.getChatImagePath(image: img)
        let imagePath = BubbleCollectionCellData.drawnOut(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: mainRidName.map{positiveCalendar(exploration: $0)}, encoding: .utf8)!: (String(mIndividualMessage)), (String(appHereFormat.suffix(7))): (appPurposeThirdPath.replacingOccurrences(of: "wife", with: "g")), String(bytes: k_wayFormat.map{neatCalculation(barrier: $0)}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(notiOpposedEqualMessage.replacingOccurrences(of: "search", with: "c")): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: m_completeValue.reversed(), encoding: .utf8)!: (String(mIndividualMessage)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(mainColorIdent.prefix(6))): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(show_effectData.suffix(7))): msgInfo, String(bytes: k_signFormat.map{pageWill(hide: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = GrapplingIronReactiveCompatible.cameraTip(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            comeToGrips(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: ProjectionCellData, reSend: Bool) {
    private func comeToGrips(msgData: ProjectionCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            changeManage(showMsg: mainSizeFormat)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.first(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((notiOpposedEqualMessage.replacingOccurrences(of: "search", with: "c"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(notiOpposedEqualMessage.replacingOccurrences(of: "search", with: "c"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(notiOpposedEqualMessage.replacingOccurrences(of: "search", with: "c"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: LifeReactiveCompatible.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        LifeReactiveCompatible.representationCompletion(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.transformStatus(msgData, isUser: nil, mode: NSNumber(value: 0), remote: .Msg_Status_Fail)
                
                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.creationMaximum(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(show_effectData.suffix(7)))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.transformStatus(msgData, isUser: msgInfo[(String(noti_triggerContent))] as? String, mode: NSNumber(value: 0), remote: .Msg_Status_Succ)
            
            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((mAccelerateKey.replacingOccurrences(of: "pile", with: "to") + String(notiParentContent))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(mAccelerateKey.replacingOccurrences(of: "pile", with: "to") + String(notiParentContent))] as! NSNumber
                //: AppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                AppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func creationMaximum(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard AppManager.share.loginUserMode.status != 1 else {
            guard AppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    changeManage(showMsg: errorStr)
                }
                //: return
                return
            }

            //: PipSqueakReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            PipSqueakReactiveCompatible.share.halfHardware(clickEvent: user_sendFormat, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.blockPosition()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            darkWith(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = RedactorThen(frame: CGRect(x: 0, y: 0, width: kGiftIdent, height: user_allMessage))
            //: view.show()
            view.skip()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                changeManage(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension JumpViewController: ImaginaryCreatureThen {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func cityStateNum(giftModel _: BoltHandyJSON, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func sumervalLaunch() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func utiliser(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = EveryReactiveCompatible()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.oecumenical(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func teachingMethodDetail(actionType: StuffMultiplierTarget) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            choiceSample()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func curResource(giftModel _: BoltHandyJSON, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func eyeDelay(giftModel: BoltHandyJSON, giftNum: String, model: DamnHandyJSON) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            changeManage(showMsg: mainSizeFormat)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(appSharpWantData.suffix(5)))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(userInfrastructureAbsFormat.prefix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(noti_upsetPolicyCompositionKey.prefix(6)) + k_pointCurrentPath.replacingOccurrences(of: "rhythm", with: "m"))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(mainColorIdent.prefix(6)))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(noti_yaYearValue.prefix(5)) + "emse" + String(app_whoStr))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: LifeReactiveCompatible.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        LifeReactiveCompatible.param(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.creationMaximum(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.nm(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.areaModify(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func packageBottom(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.tillBottom(true)
            self.msgView.tillBottom(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func quitHeight(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.tillBottom(true)
            self.msgView.tillBottom(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func globalCapability(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.globalCapability(tap)
    }

    //: func func__starRecordBtnDown() {}
    func need() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func capableness(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension JumpViewController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func choiceSample() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        SatelliteThen.recognizeSource(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = SmallPickTool.phoneFile(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: PipSqueakReactiveCompatible.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                PipSqueakReactiveCompatible.share.funcProcess()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.anonymousImg(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func totalFormat() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        SatelliteThen.alongEach(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.anonymousImg(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension JumpViewController {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func betweenListenerInfo(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(mainColorIdent.prefix(6)))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension JumpViewController {
    //: func func__TipsNotification() {
    func notificationProgram() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(pushAside(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: k_netStr,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func pushAside(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: MatterParentCellData = userinfo[(dream_tinTitle.replacingOccurrences(of: "complete", with: "ms"))] as! MatterParentCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.tillBottom(true)
        self.msgView.tillBottom(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func darkWith(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: showBindContent.map{talkSeem(attribute: $0)}, encoding: .utf8)!: tipStr, (String(appHereFormat.suffix(7))): (main_marginGivingText.replacingOccurrences(of: "succeed", with: "t"))]
        //: var message: ProjectionCellData?
        var message: ProjectionCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = GrapplingIronReactiveCompatible.cameraTip(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.tillBottom(true)
        self.msgView.tillBottom(true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func gestureLabel() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard AppManager.share.appUserConfigMode.rTips.count > 0 else { return }
            guard AppManager.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: AppManager.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.darkWith(tipStr: AppManager.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension JumpViewController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func areaModify(extral: [String: Any?], model: DamnHandyJSON, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(show_effectData.suffix(7)))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(show_commonTitle) + String(notiElementWriteFormat.prefix(5)))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(appHereFormat.suffix(7)))] = (String(dreamIllegalTitle))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(show_effectData.suffix(7)))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: app_threeFormat.map{staffValue(red: $0)}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (showFromReasonKey.replacingOccurrences(of: "simply", with: "c") + m_phoneStr.replacingOccurrences(of: "front", with: "e")): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: dream_blueValue.map{nagSpan(digital: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(noti_availabilityIdent)).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((String(dreamIllegalTitle))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(String(dreamIllegalTitle))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(showFromReasonKey.replacingOccurrences(of: "simply", with: "c") + m_phoneStr.replacingOccurrences(of: "front", with: "e"))] = "\(giftdic[(String(noti_charmChannelHimValue.prefix(6)) + mPeopleFormat.replacingOccurrences(of: "absence", with: "m"))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(mainAudiencePath) + userFeeId.replacingOccurrences(of: "used", with: "r"))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = GrapplingIronReactiveCompatible.cameraTip(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.first(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(dream_botId.prefix(4)) + "ncome")) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(dream_botId.prefix(4)) + "ncome")] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.transformStatus(cellData, isUser: msgInfoDic[(String(noti_triggerContent))] as? String, mode: msgIncome, remote: .Msg_Status_Succ)
        
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        adjust(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((mAccelerateKey.replacingOccurrences(of: "pile", with: "to") + String(notiParentContent))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(mAccelerateKey.replacingOccurrences(of: "pile", with: "to") + String(notiParentContent))] as! NSNumber
            //: AppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            AppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: AppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.threshold(needReload: false, mf_coin: AppManager.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.below()
    }

    //: func initCachEffectData() {
    func effect() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.locationNight(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func adjust(giftDic: [String: Any], model: DamnHandyJSON) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.bowling(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in FriendshipBoltGiftManager.share.fleet() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [GiftRoofHandyJSON]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.hideReceive(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.bequestDict(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func ridUser(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in FriendshipBoltGiftManager.share.fleet() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [GiftRoofHandyJSON]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.hideReceive(model: giftModel as! ErrTransformable)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.step(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension JumpViewController {
    /// 创建UI
    //: private func createUI() {
    private func save() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func constraintsStill() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(kSoundPath)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(kSoundPath)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - user_sizeValue)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func everyAttach() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = EveryReactiveCompatible()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! ProjectionCellData).isKind(of: ParentCellData.self) {
                    if (cellData as! ProjectionCellData).isKind(of: ParentCellData.self) {
                        //: let groupCellData = cellData as! ParentCellData
                        let groupCellData = cellData as! ParentCellData
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(noti_triggerContent))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: AmongObjectManager.shared.rx
        AmongObjectManager.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(dreamQuantityerestTitle.suffix(5)) + "nection"))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
