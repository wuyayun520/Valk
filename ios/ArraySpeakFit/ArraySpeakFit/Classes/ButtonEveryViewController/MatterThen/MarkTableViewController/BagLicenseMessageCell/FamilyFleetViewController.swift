
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let main_thirdIdent:[UInt8] = [0x92,0x95,0x92,0x8f,0xd3,0x98,0x94,0x9f,0x9e,0x89,0xc1,0xd2,0xdb,0x93,0x9a,0x88,0xdb,0x95,0x94,0x8f,0xdb,0x99,0x9e,0x9e,0x95,0xdb,0x92,0x96,0x8b,0x97,0x9e,0x96,0x9e,0x95,0x8f,0x9e,0x9f]

private func basicToo(chop num: UInt8) -> UInt8 {
    return num ^ 251
}

/*: "F5F5F5" :*/
fileprivate let main_railroadSmoothKey:[Character] = ["F","5","F","5","F","5"]

/*: "#128CFF" :*/
fileprivate let noti_angleKey:[Character] = ["#","1","2","8","C","F","F"]

/*: "Feedback" :*/
fileprivate let m_plantContent:String = "comfortable"
fileprivate let mSuccessTitle:[Character] = ["e","e","d","b","a","c","k"]

/*: "#8370FF" :*/
fileprivate let m_redId:String = "#8370FFguide coat how soul"

/*: "Quick Greeting" :*/
fileprivate let k_phaseSlightData:String = "circle beanQuic"
fileprivate let kConcernStr:String = "inrating"

/*: "Beauty Photos" :*/
fileprivate let dream_representationPath:String = "husband transport dark floatBeauty "
fileprivate let showLiteTitle:[Character] = ["P","h","o","t","o","s"]

/*: "url" :*/
fileprivate let notiWorldAuthorFenceKey:String = "decreaserl"

/*: "unlockGift" :*/
fileprivate let userProductFormat:[Character] = ["u","n","l","o","c","k","G","i","f"]
fileprivate let main_futureName:[Character] = ["t"]

/*: "isBurn" :*/
fileprivate let mainRaceFormat:[UInt8] = [0x10,0x1a,0xe9,0x1c,0x19,0x15]

fileprivate func connectImport(him num: UInt8) -> UInt8 {
    let value = Int(num) - 167
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftId" :*/
fileprivate let noti_outputContactPath:String = "giftIdmotion purchase three"

/*: "rYMsgType" :*/
fileprivate let appProperlyName:String = "cute heavy timing needrYMsgTyp"
fileprivate let main_borderData:String = "salt"

/*: "GJ:CallCustom" :*/
fileprivate let userFamilyContent:String = "focus absence character due cityGJ:C"
fileprivate let appWedData:String = "uanothertom"

/*: "changeDirection" :*/
fileprivate let mainHusbandMessage:[Character] = ["c","h","a","n","g","e","D","i","r","e","c","t","i","o","n"]

/*: "extra" :*/
fileprivate let k_clingValue:String = "expointra"

/*: "msgInfo" :*/
fileprivate let mainDelayNoUrl:String = "union everyone minimal ammsgInfo"

/*: "toUid" :*/
fileprivate let noti_packId:[Character] = ["t","o","U","i","d"]

/*: "wantGift" :*/
fileprivate let showNumberPath:String = "team giftwantGif"
fileprivate let main_matchName:[Character] = ["t"]

/*: "jumpKey" :*/
fileprivate let dream_splitMsg:String = "interest conversation source seajumpKey"

/*: "truePersonAuth" :*/
fileprivate let noti_optionCertainName:String = "TRUE"
fileprivate let notiInsideLaterMessage:String = "other input startonAuth"

/*: "logType" :*/
fileprivate let mLipKey:[Character] = ["l","o"]
fileprivate let user_civicValue:String = "center corner deadline only operategType"

/*: "msgType" :*/
fileprivate let m_comprehensivePath:[UInt8] = [0x31,0x37,0x2b,0x18,0x3d,0x34,0x29]

fileprivate func tearAttribute(oval num: UInt8) -> UInt8 {
    let value = Int(num) - 196
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "txt" :*/
fileprivate let mWaitExhibitPath:String = "warnxwarn"

/*: "type" :*/
fileprivate let user_indexExactlyFormat:[Character] = ["t","y","p","e"]

/*: "QuickMsg" :*/
fileprivate let noti_generalData:[Character] = ["Q","u","i","c","k","M","s","g"]

/*: "params" :*/
fileprivate let userDistantData:[UInt8] = [0xe3,0xf2,0xe1,0xf2,0xfe,0xe0]

private func reduceLate(administrative num: UInt8) -> UInt8 {
    return num ^ 147
}

/*: "content" :*/
fileprivate let notiNightDomainData:[UInt8] = [0x73,0x7f,0x7e,0x64,0x75,0x7e,0x64]

/*: "txtId" :*/
fileprivate let kAnnouncementStr:[Character] = ["t","x","t","I","d"]

/*: "message" :*/
fileprivate let show_bassCheerName:[UInt8] = [0xb3,0xbb,0xad,0xad,0xbf,0xb9,0xbb]

private func slopPail(alter num: UInt8) -> UInt8 {
    return num ^ 222
}

/*: "TxtMsg" :*/
fileprivate let user_tableData:String = "TxtMsmix cap tolerance"
fileprivate let dreamToolId:[Character] = ["g"]

/*: "Voice hello download failed. Procedure" :*/
fileprivate let showCompeteValue:[UInt8] = [0xb6,0xcf,0xc9,0xc3,0xc5,0x80,0xc8,0xc5,0xcc,0xcc,0xcf,0x80,0xc4,0xcf,0xd7,0xce,0xcc,0xcf,0xc1,0xc4,0x80,0xc6,0xc1,0xc9,0xcc,0xc5,0xc4,0x8e,0x80,0xb0,0xd2,0xcf,0xc3,0xc5,0xc4,0xd5,0xd2,0xc5]

fileprivate func atALowerPlace(host num: UInt8) -> UInt8 {
    let value = Int(num) - 96
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "length" :*/
fileprivate let kNightStr:String = "faceength"

/*: "audio" :*/
fileprivate let noti_examineData:[Character] = ["a","u","d","i","o"]

/*: "audioId" :*/
fileprivate let m_heavyPath:String = "mp sweep button criticizeaudioId"

/*: "audioLength" :*/
fileprivate let show_rangeTitle:[UInt8] = [0x81,0x95,0x84,0x89,0x8f,0xac,0x85,0x8e,0x87,0x94,0x88]

/*: "audioSandbox" :*/
fileprivate let appAbleStoryKey:String = "audioSproblem premium soul restriction yes"
fileprivate let noti_boardStr:String = "odeny"

/*: "senderId" :*/
fileprivate let notiAdvicePlatformId:[UInt8] = [0x18,0xe,0x5,0xf,0xe,0x19,0x22,0xf]

private func measureRecord(small num: UInt8) -> UInt8 {
    return num ^ 107
}

/*: "AudioMsg" :*/
fileprivate let show_talkTitle:String = "Audifollow base"

/*: "Can't send blank message" :*/
fileprivate let k_interactionMessage:[Character] = ["C","a","n","\'","t"," ","s","e","n","d"," ","b","l","a","n","k"," ","m","e","s"]
fileprivate let kCareValue:String = "grayage"

/*: "quoteMsgId" :*/
fileprivate let mAppMessage:String = "quoteMextended reverse arrival"
fileprivate let noti_becomeFormat:String = "sgIdcombined vid beauty"

/*: "quoteMsgInfo" :*/
fileprivate let appFeeFormat:String = "rescue demonstrate sh meanquoteM"

/*: "contentType" :*/
fileprivate let user_analyzeData:[UInt8] = [0x96,0x9a,0x9b,0x81,0x90,0x9b,0x81,0xa1,0x8c,0x85,0x90]

/*: "ImgMsg" :*/
fileprivate let user_designName:String = "education house rescue rescue hapImgMsg"

/*: "img" :*/
fileprivate let user_incomeId:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let m_availableKey:[UInt8] = [0xfc,0x0,0xf4,0xfa,0xf8,0xe8,0x5,0xfc]

fileprivate func originalStory(bubble num: UInt8) -> UInt8 {
    let value = Int(num) - 147
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "customSend" :*/
fileprivate let show_environmentId:[UInt8] = [0xb0,0xc2,0xc0,0xc1,0xbc,0xba,0xa0,0xb2,0xbb,0xb1]

fileprivate func lessPurchase(always num: UInt8) -> UInt8 {
    let value = Int(num) - 77
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "pic" :*/
fileprivate let app_cigaretteTitle:[Character] = ["p","i","c"]

/*: "VideoMsg" :*/
fileprivate let m_colorName:String = "VideoMsgpie comment exclusive slim steep"

/*: "video" :*/
fileprivate let m_exceptionThanksData:[Character] = ["v","i","d","e","o"]

/*: "coverImg" :*/
fileprivate let noti_ticketKey:[UInt8] = [0x6c,0x60,0x79,0x6a,0x7d,0x46,0x62,0x68]

private func bringOut(smooth num: UInt8) -> UInt8 {
    return num ^ 15
}

/*: "videoUrl" :*/
fileprivate let dreamVoiceFormat:[UInt8] = [0x89,0x96,0x9b,0x9a,0x90,0xaa,0x8d,0x93]

private func sensibleBrig(inform num: UInt8) -> UInt8 {
    return num ^ 255
}

/*: "duration" :*/
fileprivate let appSkinName:[UInt8] = [0xa0,0xb1,0xb6,0xa5,0xb0,0xad,0xab,0xaa]

private func stackBan(hesitate num: UInt8) -> UInt8 {
    return num ^ 196
}

/*: "len" :*/
fileprivate let mainSillyUrl:[UInt8] = [0x6e,0x65,0x6c]

/*: "Documents/User/voice/" :*/
fileprivate let appCustomerFormat:[Character] = ["D","o","c","u","m","e","n","t","s","/","U","s","e","r","/","v","o","i","c","e"]
fileprivate let mSaveFormat:String = "/"

/*: "Documents/User/record/" :*/
fileprivate let userOwnFormat:String = "Documefailure ta dew minimum feed"
fileprivate let showFoundationMsg:[Character] = ["U","s","e","r","/","r","e","c","o","r","d","/"]

/*: "msgIncome" :*/
fileprivate let main_locationName:[Character] = ["m","s","g","I","n","c"]
fileprivate let dream_slipName:String = "OME"

/*: "mfBean" :*/
fileprivate let userOptionMsg:[UInt8] = [0x6e,0x61,0x65,0x42,0x66,0x6d]

/*: "messageType" :*/
fileprivate let mainAverName:String = "messsame"

/*: "WantGift" :*/
fileprivate let user_pinMessage:[Character] = ["W","a","n","t","G","i"]
fileprivate let kAutomaticTitle:String = "FT"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let app_captureName:String = "final compound instruction_Privat"
fileprivate let notiZzId:String = "me wife access array handeChatTip"
fileprivate let m_sillyFormat:String = "depend evening break fault prettysTitle_"

/*: "msgId" :*/
fileprivate let dream_powerContent:String = "narrow old flexible area oppositemsgId"

/*: "totalMfCoin" :*/
fileprivate let app_sureFormat:[Character] = ["t","o"]
fileprivate let kRecoverValue:[Character] = ["t","a","l","M","f","C","o","i","n"]

/*: "attachSysMsg" :*/
fileprivate let kBassFormat:[Character] = ["a","t","t","a","c"]
fileprivate let kExclusiveFormat:[Character] = ["h","S","y","s","M","s","g"]

/*: "intimatePhoto" :*/
fileprivate let dream_dependenceStr:[Character] = ["i","n","t","i","m","a","t","e","P","h","o","t"]
fileprivate let user_whetherFormat:String = "imagination"

/*: "totalIntimate" :*/
fileprivate let m_pokeTargetMessage:String = "occurooccur"
fileprivate let mSeaFormat:String = "upon focus sufficientalInti"

/*: "uid" :*/
fileprivate let main_interruptTitle:[UInt8] = [0xe7,0xfb,0xf6]

private func smallStretch(visitor num: UInt8) -> UInt8 {
    return num ^ 146
}

/*: "intimate" :*/
fileprivate let notiRadicalPath:[UInt8] = [0xc1,0xc6,0xcc,0xc1,0xc5,0xb9,0xcc,0xbd]

fileprivate func teammateYear(response num: UInt8) -> UInt8 {
    let value = Int(num) - 88
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let mainSlipName:[UInt8] = [0x5,0x27,0x28,0x61,0x32,0x66,0x33,0x35,0x23,0x66,0x32,0x2e,0x23,0x66,0x20,0x33,0x28,0x25,0x32,0x2f,0x29,0x28,0x66,0x22,0x33,0x23,0x66,0x32,0x29,0x66,0x3f,0x29,0x33,0x34,0x66,0x2a,0x27,0x25,0x2d,0x66,0x29,0x20,0x66,0x2f,0x28,0x32,0x2f,0x2b,0x27,0x25,0x3f,0x66,0x2a,0x23,0x30,0x23,0x2a,0x68]

/*: "giftNum" :*/
fileprivate let showBrowFormat:[Character] = ["g","i","f","t","N","u","m"]

/*: "pkgItemsetId" :*/
fileprivate let notiTableValue:[Character] = ["p","k","g","I","t","e","m","s","e","t"]
fileprivate let dream_computeGoValue:String = "Idconvert until status"

/*: "from" :*/
fileprivate let noti_yourTextureId:String = "compositionom"

/*: "wantGiftId" :*/
fileprivate let mainGlobalId:[Character] = ["w","a","n","t","G","i"]
fileprivate let userTeammateSpecInvitationUrl:[Character] = ["f","t","I","d"]

/*: "WantGiftMsg" :*/
fileprivate let appRevolutionPath:String = "into ham share bar curWantG"
fileprivate let dreamAgeData:[Character] = ["i","f","t","M","s","g"]

/*: "gift" :*/
fileprivate let dreamGlobalUrl:[Character] = ["g","i","f","t"]

/*: "View info" :*/
fileprivate let dreamProblemPath:[Character] = ["V","i","e","w"," ","i","n","f","o"]

/*: "Notes" :*/
fileprivate let showMemoryId:String = "elite replaceNotes"

/*: "Report" :*/
fileprivate let dream_paperTitle:String = "Reportstudy tie orientation"

/*: "View read receipts?" :*/
fileprivate let showProfileFillFormat:[Character] = ["V","i","e","w"," ","r","e","a","d"," ","r","e","c","e","i","p","t","s","?"]

/*: "She" :*/
fileprivate let k_futureUrl:[Character] = ["S","h","e"]

/*: "%@ is using too low a version" :*/
fileprivate let m_pocketAdviceName:String = "report none%@ is"
fileprivate let notiGrassPlanText:String = "import purchase ample ion transfer us"
fileprivate let dream_childMsg:String = "quote bridge roundinging to"
fileprivate let mConvertId:String = "gender lower example a ver"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let k_onLineIdent:String = "Shoot down wave seem temporary"
fileprivate let notiTheWeId:String = "t leasprocedure conversation situation carrier"
fileprivate let m_starMsg:String = "seinstallnds"

/*: "Upload failed" :*/
fileprivate let dream_sciencePath:[Character] = ["U","p","l","o","a","d"]
fileprivate let dreamJudgeKey:String = " failedrate beam complaint"

/*: "播放状态: :*/
fileprivate let kInvestPath:[Character] = ["播","放","状","\u{6001}",":"]

/*: "c2c_" :*/
fileprivate let noti_memberServiceKey:String = "c2c_trip fire start"

/*: "msg" :*/
fileprivate let mConstraintUrl:String = "mcomply"

/*: "tips" :*/
fileprivate let k_reinOurDrillFormat:String = "tirevenue"

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let main_increasingMessage:[UInt8] = [0xb3,0x85,0x9f,0xca,0x89,0x8b,0x84,0xca,0x9f,0x99,0x8f,0xca,0x9e,0x82,0x8f,0xca,0x9c,0x83,0x8e,0x8f,0x85,0xca,0x89,0x82,0x8b,0x9e,0xca,0x8c,0x8f,0x8b,0x9e,0x9f,0x98,0x8f,0xca,0x9e,0x85,0xca,0x8d,0x83,0x9c,0x8f,0xca,0xcf,0xaa,0xca,0x8b,0xca,0x89,0x8b,0x86,0x86]

private func upsetHave(rating num: UInt8) -> UInt8 {
    return num ^ 234
}

/*: "cellData" :*/
fileprivate let m_courseId:[Character] = ["c","e","l","l","D","a","t","a"]

/*: "System notification" :*/
fileprivate let appMinimumTitle:[Character] = ["S","y","s","t","e"]
fileprivate let noti_sayEveryoneMessage:String = "increasing read hurry red reportm noti"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FamilyFleetViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum JournalSayFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class FamilyFleetViewController: FictionalCharacterRecognizerDelegate {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var needShowRating = false
    var needShowRating = false // 是否展示app store评分
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = RelatedTransformable()
    private var userWrap = RelatedTransformable() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = JournalSayFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: RelatedTransformable = RelatedTransformable()) {
    init(chatID: String, isFrom: JournalSayFromEnum = .Normal, userWrap: RelatedTransformable = RelatedTransformable()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        FamilyFleetViewController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        LifeDigitalThen.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == ImmediatelyThen.getXiaoMiID() {
        if targetId == ImmediatelyThen.polytetrafluoroethylene() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == ImmediatelyThen.getCustomerServiceID() {
        } else if targetId == ImmediatelyThen.femaleId() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: main_thirdIdent.map{basicToo(chop: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if AppManager.share.appConfigMode.disableShootScreen {
            if AppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = TripUpThen(frame: CGRect(x: 0, y: 0, width: kGiftIdent, height: user_allMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        inOneCaseAnimation()
        //: createUI()
        refuse()
        //: createUIConstraints()
        activeThread()
        //: bindInteraction()
        cleavePanelCounterchange()
        //: addTapGestureRecognizer()
        weeklyFore()
        //: func__requestChatInfo()
        exposure()
        //: func__getFillDraftText()
        limpidity()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        FallaciousManagerDelegate.shared.firsthand()
        //: func__addVoiceNotification()
        limitLibraryNotification()
        //: addReadPeceiptNotification()
        peceiptActive()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            counterruptData()
            //: func__TipsNotification()
            length()
            //: if AppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if AppManager.share.loginUserMode.sex == AlongsideDigitalTableConvertible.male.rawValue {
                //: func__setReplyTipsView()
                premiumActual()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        skipAdd(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        skipAdd(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            availableManage()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        // 展示App Store评分
        //: if needShowRating {
        if needShowRating {
            //: AppStoreRatingManager.shared.requestQuickRating()
            InfluentialRatingManager.shared.evaluation()
        }
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: MarkTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = MarkTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (String(main_railroadSmoothKey)))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(ColourViewCell.self, forCellReuseIdentifier: ColourViewCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(SwanMessageCell.self, forCellReuseIdentifier: SwanMessageCell.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(TailThen.self, forCellReuseIdentifier: TailThen.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(RichThen.self, forCellReuseIdentifier: RichThen.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(BagLicenseMessageCell.self, forCellReuseIdentifier: BagLicenseMessageCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(ExpressThen.self, forCellReuseIdentifier: ExpressThen.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(OthersMsgCell.self, forCellReuseIdentifier: OthersMsgCell.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(PreviouslyThen.self, forCellReuseIdentifier: PreviouslyThen.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(ChatMsgCell.self, forCellReuseIdentifier: ChatMsgCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: FourthGuideViewCell.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(GuideViewCell.self, forCellReuseIdentifier: FourthGuideViewCell.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: PerformanceMatchDelegate = {
        //: let m = TalkingCallMenuManager()
        let m = PerformanceMatchDelegate()
        //: m.uid = targetId
        m.uid = targetId
        //: m.onReqestCallErrorBlock = { [weak self] errNo in
        m.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self,errNo == 101 else { return }
            guard let self = self, errNo == 101 else { return }
            //: self.msgInputView.setHidenFreeMsgTimesView()
            self.msgInputView.at()
        }
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: UnitInputView = {
        //: var type: InputViewType = .PrivateChat
        var type: PerformanceViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = UnitInputView(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(noti_angleKey)))!, .font: UIFont.concern(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (m_plantContent.replacingOccurrences(of: "comfortable", with: "F") + String(mSuccessTitle)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(indoors), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: AmongView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = AmongView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: FriendshipSmallThen = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = FriendshipSmallThen(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.concern(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(m_redId.prefix(7)))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(k_phaseSlightData.suffix(4)) + "k Greet" + kConcernStr.replacingOccurrences(of: "rating", with: "g")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.storageEngine().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(full), for: .touchUpInside)
        //: btn.isHidden = !AppManager.share.appUserConfigMode.showDirectGreet || AppManager.share.loginUserMode.sex == Gender.male.rawValue || AppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !AppManager.share.appUserConfigMode.showDirectGreet || AppManager.share.loginUserMode.sex == AlongsideDigitalTableConvertible.male.rawValue || AppManager.share.appStatus == WrinkleSendable.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: BridgeTipsView? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = BridgeTipsView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: TarBridgeView = {
        //: let v = TalkingSelectTranslationView()
        let v = TarBridgeView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.showHalf(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: FourthResultView = {
        //: let v = TalkingTranslationResultView()
        let v = FourthResultView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.linguisticProcess(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.concern(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(m_redId.prefix(7)))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(dream_representationPath.suffix(7)) + String(showLiteTitle)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.storageEngine().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(searchClick), for: .touchUpInside)
        //: btn.isHidden = AppManager.share.loginUserMode.sex == Gender.male.rawValue || AppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = AppManager.share.loginUserMode.sex == AlongsideDigitalTableConvertible.male.rawValue || AppManager.share.appStatus == WrinkleSendable.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: LiveFlowLayout = {
        //: let v = TalkingLiveRoomGamesView()
        let v = LiveFlowLayout()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: QualityMessageReactiveCompatible = {
        //: let view = TalkingPrivateBannerView.init()
        let view = QualityMessageReactiveCompatible()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: SayDigitalRecognizerDelegate = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = SayDigitalRecognizerDelegate()
        //: v.isHidden = (AppManager.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (AppManager.share.appStatus == WrinkleSendable.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: InfluentialReactiveCompatible = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = InfluentialReactiveCompatible()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.weeklyView()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension FamilyFleetViewController: DelegateThen {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func identification(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if AppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if AppManager.share.loginUserMode.sex == AlongsideDigitalTableConvertible.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if AppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if AppManager.share.loginUserMode.sex == AlongsideDigitalTableConvertible.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.heart(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension FamilyFleetViewController: DelegateDirty {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func full() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        LifeDigitalThen.minimumPlayer(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.changeManage(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.fast(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func searchClick() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        LifeDigitalThen.scene(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = EliteBagPhotoDelegate()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().beyondFore(urlStr: dict?[(notiWorldAuthorFenceKey.replacingOccurrences(of: "decrease", with: "u"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(userProductFormat) + String(main_futureName))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = OutsideModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: mainRaceFormat.map{connectImport(him: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(noti_outputContactPath.prefix(6)))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.possibility(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> ProjectionCellData! {
    func frameAsk(_: MarkTableViewController!, enable data: V2TIMMessage!) -> ProjectionCellData! {
        //: let dic = SayEditChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = SayEditChatManager.assemblageSquareKey(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = GrapplingIronReactiveCompatible.roofModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: GoddamnCellData.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(appProperlyName.suffix(8)) + main_borderData.replacingOccurrences(of: "salt", with: "e"))].stringValue == (String(userFamilyContent.suffix(4)) + "allC" + appWedData.replacingOccurrences(of: "another", with: "s")) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(String(mainHusbandMessage))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(k_clingValue.replacingOccurrences(of: "point", with: "t"))][(String(mainDelayNoUrl.suffix(7)))][(String(noti_packId))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = FractionalReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = FractionalReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: LicenseCellData.self) == true ||
        if cellData?.isKind(of: LicenseCellData.self) == true ||
            //: cellData?.isKind(of: BubbleCollectionCellData.self) == true ||
            cellData?.isKind(of: BubbleCollectionCellData.self) == true ||
            //: cellData?.isKind(of: FractionalReactiveCompatible.self) == true ||
            cellData?.isKind(of: FractionalReactiveCompatible.self) == true ||
            //: cellData?.isKind(of: CryOutCellData.self) == true {
            cellData?.isKind(of: CryOutCellData.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            LifeDigitalThen.chatWindowMake(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            LifeDigitalThen.micromicron(cellData.msgIncome, data.timestamp, msgView)
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: ProjectionCellData!) -> FourthGuideViewCell! {
    func income(_: MarkTableViewController!, format data: ProjectionCellData!) -> FourthGuideViewCell! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = after(data: data)

        //: if data.isKind(of: FractionalReactiveCompatible.self) {
        if data.isKind(of: FractionalReactiveCompatible.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = ColourViewCell(style: .default, reuseIdentifier: ColourViewCell.className())
            //: let cellData = data as! FractionalReactiveCompatible
            let cellData = data as! FractionalReactiveCompatible

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.brightMsg() {
                //: if cellData.msgModel.quoteMsgInfo.uid == AppManager.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == AppManager.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = AppManager.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = AppManager.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.tip(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: BubbleCollectionCellData.self) {
        if data.isKind(of: BubbleCollectionCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = SwanMessageCell(style: .default, reuseIdentifier: SwanMessageCell.className())
            //: let cellData = data as! BubbleCollectionCellData
            let cellData = data as! BubbleCollectionCellData
            //: cell.fill(with: cellData)
            cell.tip(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: CryOutCellData.self) {
        if data.isKind(of: CryOutCellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = TailThen(style: .default, reuseIdentifier: TailThen.className())
            //: let cellData = data as! CryOutCellData
            let cellData = data as! CryOutCellData
            //: cell.fill(with: cellData)
            cell.tip(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: StuffThen.self) {
        if data.isKind(of: StuffThen.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = RichThen(style: .default, reuseIdentifier: RichThen.className())
            //: let cellData = data as! StuffThen
            let cellData = data as! StuffThen
            //: cell.fill(with: cellData)
            cell.tip(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: MoveThen.self) {
        if data.isKind(of: MoveThen.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = BagLicenseMessageCell(style: .default, reuseIdentifier: BagLicenseMessageCell.className())
            //: let cellData = data as! MoveThen
            let cellData = data as! MoveThen
            //: cell.fill(with: cellData)
            cell.tip(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = BoltHandyJSON()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.memory(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(showNumberPath.suffix(7)) + String(main_matchName)))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: LicenseCellData.self) {
        if data.isKind(of: LicenseCellData.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = PreviouslyThen(style: .default, reuseIdentifier: PreviouslyThen.className())
            //: let cellData = data as! LicenseCellData
            let cellData = data as! LicenseCellData
            //: cell.fill(with: cellData)
            cell.tip(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: GoddamnCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = OthersMsgCell(style: .default, reuseIdentifier: OthersMsgCell.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! GoddamnCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.tip(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: MatterParentCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = ExpressThen(style: .default, reuseIdentifier: ExpressThen.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! MatterParentCellData
            //: cell.fill(with: cellData)
            cell.tip(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: ImpermanencyReactiveCompatible.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = ChatMsgCell(style: .default, reuseIdentifier: ChatMsgCell.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! ImpermanencyReactiveCompatible
            //: cell.fill(with: cellData)
            cell.tip(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: FourthGuideViewCell!) {
    func sendAvatar(_: MarkTableViewController!, communicationAvatarDestroyAlongSelectDedication cell: FourthGuideViewCell!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != ImmediatelyThen.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != ImmediatelyThen.femaleId() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: PipSqueakReactiveCompatible.share.func__pushToUserDetailVC(uid: String(uid))
            PipSqueakReactiveCompatible.share.balanceConvertSeek(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: FourthGuideViewCell!) {
    func changeDescription(_: MarkTableViewController!, kindForText cell: FourthGuideViewCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: SwanMessageCell.self) { // 图片
            //: let imageCellData = cell.messageData as! BubbleCollectionCellData
            let imageCellData = cell.messageData as! BubbleCollectionCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = AllowThen(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.component(imageCellData, load: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.focus(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: mainAddMessage)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.extralInstance(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.advertising(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                firstController()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(k_lineWithoutTitle)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = TransportModelType()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = ComprehensiveAlterTransformable()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [TransportModelType] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = MatterFlowLayout(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                firstController()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: TailThen.self) { // 视频
            //: let videoCellData = cell.messageData as! CryOutCellData
            let videoCellData = cell.messageData as! CryOutCellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = SorbentReactiveCompatible(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                firstController()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = AlongsideViewDelegate(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.onModel(videoCellData, mark: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.focus(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: mainAddMessage)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.extralInstance(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.advertising(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                firstController()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: PreviouslyThen.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! PreviouslyThen
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! LicenseCellData, cellView: audioCell)
            app(cellData: audioCell.cellData as! LicenseCellData, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ColourViewCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ColourViewCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = SayEditChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = SayEditChatManager.assemblageSquareKey(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(k_clingValue.replacingOccurrences(of: "point", with: "t"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(dream_splitMsg.suffix(7)))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(dream_splitMsg.suffix(7)))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (noti_optionCertainName.lowercased() + "Pers" + String(notiInsideLaterMessage.suffix(6))) {
                    //: PipSqueakReactiveCompatible.share.func__pushUserVerifyController(toast: nil)
                    PipSqueakReactiveCompatible.share.utiliserBetweenHoweverPushEnableerFunc(toast: nil)
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<FamilyTransformable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: PipSqueakReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    PipSqueakReactiveCompatible.share.largeness(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(appProperlyName.suffix(8)) + main_borderData.replacingOccurrences(of: "salt", with: "e"))] as? String == (String(userFamilyContent.suffix(4)) + "allC" + appWedData.replacingOccurrences(of: "another", with: "s")) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(mainDelayNoUrl.suffix(7)))][(String(mLipKey) + String(user_civicValue.suffix(5)))].intValue == 1 {
                    //: checkAndCallVoice()
                    mpVoice()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    worldPower()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: ExpressThen.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! ExpressThen
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = SayEditChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = SayEditChatManager.assemblageSquareKey(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(k_clingValue.replacingOccurrences(of: "point", with: "t"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(dream_splitMsg.suffix(7)))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<FamilyTransformable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: PipSqueakReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    PipSqueakReactiveCompatible.share.largeness(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: OthersMsgCell.self) {
            //: PipSqueakReactiveCompatible.share.func__pushToUserDetailVC(uid: targetId)
            PipSqueakReactiveCompatible.share.balanceConvertSeek(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: FourthGuideViewCell!) {
    func controller(_: MarkTableViewController!, factorTitle cell: FourthGuideViewCell!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: OthersAlterCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: SwanMessageCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! SwanMessageCell
            //: cellData = imageCell.cellData as! BubbleCollectionCellData
            cellData = imageCell.cellData as! BubbleCollectionCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: TailThen.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! TailThen
            //: cellData = videoCell.cellData as! CryOutCellData
            cellData = videoCell.cellData as! CryOutCellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: PreviouslyThen.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! PreviouslyThen
            //: cellData = audioCell.cellData as! LicenseCellData
            cellData = audioCell.cellData as! LicenseCellData
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ColourViewCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ColourViewCell
            //: cellData = textCell.cellData as! FractionalReactiveCompatible
            cellData = textCell.cellData as! FractionalReactiveCompatible
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: ProjectionCellData? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: ProjectionCellData? = GrapplingIronReactiveCompatible.affairs(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: CryOutCellData.self) {
                if reCellData!.isKind(of: CryOutCellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    hat(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    zapUponSend(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: ProjectionCellData!) {
    func process(_: MarkTableViewController!, telegram cellData: ProjectionCellData!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = LifeDigitalThen.civicCenter(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: ProjectionCellData!) {
    func infix(_: MarkTableViewController!, without cellData: ProjectionCellData!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        LifeDigitalThen.columnInstall(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: ProjectionCellData) -> ProjectionCellData {
    private func after(data: ProjectionCellData) -> ProjectionCellData {
        //: let isSelf = data.msgModel.user.uid == Int(AppManager.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(AppManager.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if AppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if AppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = AppManager.share.loginUserMode.headPic!
                data.msgModel.user.headPic = AppManager.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = AppManager.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = AppManager.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension FamilyFleetViewController {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func wind() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: m_comprehensivePath.map{tearAttribute(oval: $0)}, encoding: .utf8)!: (mWaitExhibitPath.replacingOccurrences(of: "warn", with: "t"))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(String(user_indexExactlyFormat)): (String(noti_generalData))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(mainDelayNoUrl.suffix(7))): msgInfo, String(bytes: userDistantData.map{reduceLate(administrative: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = GrapplingIronReactiveCompatible.cameraTip(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.zapUponSend(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func fast(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: notiNightDomainData.map{$0^16}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: notiNightDomainData.map{$0^16}, encoding: .utf8)!: content, (String(kAnnouncementStr)): greetId, String(bytes: m_comprehensivePath.map{tearAttribute(oval: $0)}, encoding: .utf8)!: (mWaitExhibitPath.replacingOccurrences(of: "warn", with: "t"))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: show_bassCheerName.map{slopPail(alter: $0)}, encoding: .utf8)!: content, (String(kAnnouncementStr)): greetId, (String(user_indexExactlyFormat)): (String(user_tableData.prefix(5)) + String(dreamToolId))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(mainDelayNoUrl.suffix(7))): msgInfo, String(bytes: userDistantData.map{reduceLate(administrative: $0)}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = GrapplingIronReactiveCompatible.cameraTip(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.zapUponSend(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            RouteThen.shared.job(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.changeManage(showMsg: String(bytes: showCompeteValue.map{atALowerPlace(host: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        LifeDigitalThen.thatSuite(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(kNightStr.replacingOccurrences(of: "face", with: "l"))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: m_comprehensivePath.map{tearAttribute(oval: $0)}, encoding: .utf8)!: (String(noti_examineData)),
                               //: "audioId": greetId,
                               (String(m_heavyPath.suffix(7))): greetId,
                               //: "audioLength": length,
                               String(bytes: show_rangeTitle.map{$0^224}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(appAbleStoryKey.prefix(6)) + "andb" + noti_boardStr.replacingOccurrences(of: "deny", with: "x")): model.unZipFilePath,
                               //: "senderId": AppManager.share.loginUserMode.userID]
                               String(bytes: notiAdvicePlatformId.map{measureRecord(small: $0)}, encoding: .utf8)!: AppManager.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(m_heavyPath.suffix(7))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: show_rangeTitle.map{$0^224}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(appAbleStoryKey.prefix(6)) + "andb" + noti_boardStr.replacingOccurrences(of: "deny", with: "x")): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (String(user_indexExactlyFormat)): (String(show_talkTitle.prefix(4)) + "oMsg")]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(mainDelayNoUrl.suffix(7))): msgInfo, String(bytes: userDistantData.map{reduceLate(administrative: $0)}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = GrapplingIronReactiveCompatible.cameraTip(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.zapUponSend(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func linguisticProcess(msgStr: String, quoteModel: RealizeHeliModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            changeManage(showMsg: (String(k_interactionMessage) + kCareValue.replacingOccurrences(of: "gray", with: "s")).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: notiNightDomainData.map{$0^16}, encoding: .utf8)!: temStr, String(bytes: m_comprehensivePath.map{tearAttribute(oval: $0)}, encoding: .utf8)!: (mWaitExhibitPath.replacingOccurrences(of: "warn", with: "t"))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: show_bassCheerName.map{slopPail(alter: $0)}, encoding: .utf8)!: temStr, (String(user_indexExactlyFormat)): (String(user_tableData.prefix(5)) + String(dreamToolId))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(mainDelayNoUrl.suffix(7))): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(mAppMessage.prefix(6)) + String(noti_becomeFormat.prefix(4)))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(appFeeFormat.suffix(6)) + "sgInfo")] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: userDistantData.map{reduceLate(administrative: $0)}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = GrapplingIronReactiveCompatible.cameraTip(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? RealizeHeliModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            zapUponSend(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func possibility(img: UIImage, sendModel: OutsideModel) {
        //: let imagePath = BubbleCollectionCellData.getChatImagePath(image: img)
        let imagePath = BubbleCollectionCellData.drawnOut(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: user_analyzeData.map{$0^245}, encoding: .utf8)!: (String(user_designName.suffix(6))), String(bytes: m_comprehensivePath.map{tearAttribute(oval: $0)}, encoding: .utf8)!: (String(user_incomeId)), String(bytes: m_availableKey.map{originalStory(bubble: $0)}, encoding: .utf8)!: imagePath, String(bytes: show_environmentId.map{lessPurchase(always: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(String(app_cigaretteTitle)): imagePath,
                                        //: "type": "ImgMsg",
                                        (String(user_indexExactlyFormat)): (String(user_designName.suffix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: mainRaceFormat.map{connectImport(him: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(noti_outputContactPath.prefix(6))): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(mainDelayNoUrl.suffix(7))): msgInfo, String(bytes: userDistantData.map{reduceLate(administrative: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = GrapplingIronReactiveCompatible.cameraTip(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            zapUponSend(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: ProjectionCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func hat(msgData: ProjectionCellData?, sendModel: OutsideModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: user_analyzeData.map{$0^245}, encoding: .utf8)!: (String(m_colorName.prefix(8))), String(bytes: m_comprehensivePath.map{tearAttribute(oval: $0)}, encoding: .utf8)!: (String(m_exceptionThanksData)), String(bytes: show_environmentId.map{lessPurchase(always: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(String(user_indexExactlyFormat)): (String(m_colorName.prefix(8))),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: noti_ticketKey.map{bringOut(smooth: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: dreamVoiceFormat.map{sensibleBrig(inform: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: appSkinName.map{stackBan(hesitate: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: mainRaceFormat.map{connectImport(him: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(noti_outputContactPath.prefix(6))): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(mainDelayNoUrl.suffix(7))): msgInfo, String(bytes: userDistantData.map{reduceLate(administrative: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = GrapplingIronReactiveCompatible.cameraTip(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.first(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.pauseWithoutModel(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.spokenLanguage(cellData, consuetude: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.pauseWithoutModel(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.stop(dict, resign: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        zapUponSend(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func capableness(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: m_comprehensivePath.map{tearAttribute(oval: $0)}, encoding: .utf8)!: (String(noti_examineData)),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: show_rangeTitle.map{$0^224}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(appAbleStoryKey.prefix(6)) + "andb" + noti_boardStr.replacingOccurrences(of: "deny", with: "x")): voicePath,
                       //: "senderId": AppManager.share.loginUserMode.userID]
                       String(bytes: notiAdvicePlatformId.map{measureRecord(small: $0)}, encoding: .utf8)!: AppManager.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(appAbleStoryKey.prefix(6)) + "andb" + noti_boardStr.replacingOccurrences(of: "deny", with: "x")): voicePath,
                         //: "type": "AudioMsg",
                         (String(user_indexExactlyFormat)): (String(show_talkTitle.prefix(4)) + "oMsg"),
                         //: "len": "\(voiceTime)"]
                         String(bytes: mainSillyUrl.reversed(), encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(mainDelayNoUrl.suffix(7))): msgInfo, String(bytes: userDistantData.map{reduceLate(administrative: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = GrapplingIronReactiveCompatible.cameraTip(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            zapUponSend(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: ProjectionCellData, reSend: Bool) {
    private func zapUponSend(msgData: ProjectionCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            changeManage(showMsg: mainSizeFormat)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(noti_packId))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: CryOutCellData.self) == false && msgData.isKind(of: MoveThen.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: CryOutCellData.self) == false, msgData.isKind(of: MoveThen.self) == false, params[(String(user_indexExactlyFormat))] as? String != (String(noti_generalData)) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.first(msgData)
        }
        //: let recordTime = SayEditChatManager.getMessageInsertTime()
        let recordTime = SayEditChatManager.aboveHide()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((String(app_cigaretteTitle))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(String(app_cigaretteTitle))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(String(app_cigaretteTitle))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(String(user_indexExactlyFormat))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(show_talkTitle.prefix(4)) + "oMsg") { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(appAbleStoryKey.prefix(6)) + "andb" + noti_boardStr.replacingOccurrences(of: "deny", with: "x"))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(appAbleStoryKey.prefix(6)) + "andb" + noti_boardStr.replacingOccurrences(of: "deny", with: "x"))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(appAbleStoryKey.prefix(6)) + "andb" + noti_boardStr.replacingOccurrences(of: "deny", with: "x")))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(appCustomerFormat) + mSaveFormat.capitalized)) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = k_pointIdent + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(userOwnFormat.prefix(6)) + "nts/" + String(showFoundationMsg))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = k_barStr + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(m_heavyPath.suffix(7)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(String(noti_examineData))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: LifeReactiveCompatible.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        LifeReactiveCompatible.packageCompletion(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(noti_generalData)) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.transformStatus(msgData, isUser: nil, mode: NSNumber(value: 0), remote: .Msg_Status_Fail)
                                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.focus(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: notiPathFormat)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(kAnnouncementStr))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    LifeDigitalThen.thatSuite(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.end(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(mainDelayNoUrl.suffix(7)))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(main_locationName) + dream_slipName.lowercased())) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(main_locationName) + dream_slipName.lowercased())] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if GuideUnitReactiveCompatible.commonFragment().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: appWarningUrl, object: nil, userInfo: [String(bytes: userOptionMsg.reversed(), encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.tillAcross(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(mainAverName.replacingOccurrences(of: "same", with: "a") + "geType")] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(noti_generalData)) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: m_comprehensivePath.map{tearAttribute(oval: $0)}, encoding: .utf8)!] = (String(user_pinMessage) + kAutomaticTitle.lowercased())
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    noti_thirdUrl.toSpeaker(eventID: user_onSizeName)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(noti_generalData)) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: m_comprehensivePath.map{tearAttribute(oval: $0)}, encoding: .utf8)!] = (mWaitExhibitPath.replacingOccurrences(of: "warn", with: "t"))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(mainDelayNoUrl.suffix(7))): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = GrapplingIronReactiveCompatible.cameraTip(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.first(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(AppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(AppManager.share.loginUserMode.userID)" + (String(app_captureName.suffix(7)) + String(notiZzId.suffix(8)) + String(m_sillyFormat.suffix(7))) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if AppManager.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if AppManager.share.loginUserMode.sex == AlongsideDigitalTableConvertible.male.rawValue, type != (String(noti_generalData)) {
                //: self.removeReplyTipsView()
                self.rescueOperation()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.transformStatus(updateMsgData, isUser: msgInfo[(String(dream_powerContent.suffix(5)))] as? String, mode: msgIncome, remote: .Msg_Status_Succ)
            
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            LifeDigitalThen.micromicron(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(app_sureFormat) + String(kRecoverValue))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(app_sureFormat) + String(kRecoverValue))] as! NSNumber
                //: AppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                AppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((String(kBassFormat) + String(kExclusiveFormat))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(String(kBassFormat) + String(kExclusiveFormat))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.tipFuncCanulatePrivateAttachStr(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((String(dream_dependenceStr) + user_whetherFormat.replacingOccurrences(of: "imagination", with: "o"))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(String(dream_dependenceStr) + user_whetherFormat.replacingOccurrences(of: "imagination", with: "o"))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? NonopeningModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.component(updateMsgData, load: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((String(m_exceptionThanksData))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(String(m_exceptionThanksData))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? NextAdviceModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.onModel(updateMsgData, mark: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        mini()

        //: updateSession()
        layCut()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func layCut() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        StateThen.transitionSitting(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func focus(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard AppManager.share.loginUserMode.status != 1 else {
            guard AppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    changeManage(showMsg: errorStr)
                }
                //: return
                return
            }

            //: PipSqueakReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            PipSqueakReactiveCompatible.share.halfHardware(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.blockPosition()
            //: AppManager.share.loginUserMode.freeMsgTimes = ""
            AppManager.share.loginUserMode.freeMsgTimes = ""
            //: msgInputView.setHidenFreeMsgTimesView()
            msgInputView.at()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = RedactorThen(frame: CGRect(x: 0, y: 0, width: kGiftIdent, height: user_allMessage))
            //: view.show()
            view.skip()
            //: } else if errorCode == 60003, !AppManager.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !AppManager.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            NowThen.shared.chipAt()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                changeManage(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension FamilyFleetViewController {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func advertising(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(mainDelayNoUrl.suffix(7)))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.tillAcross(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func strength() {
        //: if self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.attachModel(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(AppManager.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(AppManager.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (show_titleId.array(forKey: app_ageMsg)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.gravityTap()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                show_titleId.set(intimateCacheData, forKey: app_ageMsg)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func magnitudeimateChange(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = SayEditChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = SayEditChatManager.assemblageSquareKey(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(k_clingValue.replacingOccurrences(of: "point", with: "t"))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(mainDelayNoUrl.suffix(7)))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(m_pokeTargetMessage.replacingOccurrences(of: "occur", with: "t") + String(mSeaFormat.suffix(6)) + "mate")]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.extendedFamilyTime(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func tillAcross(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((m_pokeTargetMessage.replacingOccurrences(of: "occur", with: "t") + String(mSeaFormat.suffix(6)) + "mate")), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(m_pokeTargetMessage.replacingOccurrences(of: "occur", with: "t") + String(mSeaFormat.suffix(6)) + "mate")] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.extendedFamilyTime(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func extendedFamilyTime(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = StateThen.goldId(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= AppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= AppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.attachModel(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.redBecome(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.statusSignal(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.statusSignal(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        StateThen.winterize(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: main_interruptTitle.map{smallStretch(visitor: $0)}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: notiRadicalPath.map{teammateYear(response: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: user_lengthId,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension FamilyFleetViewController: ImaginaryCreatureThen {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func sumervalLaunch() {
        //: guard AppManager.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard AppManager.share.loginUserMode.sex == AlongsideDigitalTableConvertible.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.greetLanguage(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func utiliser(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func teachingMethodDetail(actionType: StuffMultiplierTarget) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    add()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    positiveSumeraction()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                add()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if AppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if AppManager.share.loginUserMode.sex == AlongsideDigitalTableConvertible.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if AppManager.share.loginUserMode.loungePlus {
                if AppManager.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.resolutionPrompt(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                positiveSumeraction()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    pick()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    positiveSumeraction()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                pick()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.eyeContactFrom(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func positiveSumeraction() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.changeManage(showMsg: String(bytes: mainSlipName.map{$0^70}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func eyeDelay(giftModel _: BoltHandyJSON, giftNum _: String, model _: DamnHandyJSON) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func curResource(giftModel: BoltHandyJSON, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        memory(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: MoveThen?, fromStr: String?) {
    func memory(giftModel: BoltHandyJSON, giftNum: String, cellData: MoveThen?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            changeManage(showMsg: mainSizeFormat)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(noti_packId))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(noti_outputContactPath.prefix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(showBrowFormat))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(notiTableValue) + String(dream_computeGoValue.prefix(2)))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(noti_yourTextureId.replacingOccurrences(of: "composition", with: "fr"))] = fromStr
        }
        // 送礼接口
        //: LifeReactiveCompatible.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        LifeReactiveCompatible.sendWithinCompletion(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.focus(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: user_sendFormat)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.nm(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.extralInstance(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.layCut()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(mainDelayNoUrl.suffix(7)))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((m_pokeTargetMessage.replacingOccurrences(of: "occur", with: "t") + String(mSeaFormat.suffix(6)) + "mate")), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(m_pokeTargetMessage.replacingOccurrences(of: "occur", with: "t") + String(mSeaFormat.suffix(6)) + "mate")] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.extendedFamilyTime(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.pictureBegin(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                noti_thirdUrl.toSpeaker(eventID: appIconTitle)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func cityStateNum(giftModel: BoltHandyJSON, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        noti_thirdUrl.toSpeaker(eventID: noti_numberValue)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            changeManage(showMsg: mainSizeFormat)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            positiveSumeraction()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(mainGlobalId) + String(userTeammateSpecInvitationUrl))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(String(user_indexExactlyFormat))] = (String(appRevolutionPath.suffix(5)) + String(dreamAgeData))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: m_comprehensivePath.map{tearAttribute(oval: $0)}, encoding: .utf8)!: (String(user_pinMessage) + kAutomaticTitle.lowercased())]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(mainDelayNoUrl.suffix(7))): msgInfo, String(bytes: userDistantData.map{reduceLate(administrative: $0)}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = GrapplingIronReactiveCompatible.cameraTip(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            zapUponSend(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func packageBottom(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(noti_jumpQuantityFormalUrl - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.tillBottom(true)
            self.msgView.tillBottom(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func quitHeight(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.tillBottom(true)
            self.msgView.tillBottom(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func globalCapability(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.globalCapability(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                limit(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func need() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if FallaciousManagerDelegate.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            FallaciousManagerDelegate.shared.scaleConfirm()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension FamilyFleetViewController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func extralInstance(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(mainDelayNoUrl.suffix(7)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: m_comprehensivePath.map{tearAttribute(oval: $0)}, encoding: .utf8)!] = (String(dreamGlobalUrl))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(mainDelayNoUrl.suffix(7)))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = GrapplingIronReactiveCompatible.cameraTip(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.first(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(main_locationName) + dream_slipName.lowercased())) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(main_locationName) + dream_slipName.lowercased())] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.transformStatus(cellData, isUser: msgInfoDic[(String(dream_powerContent.suffix(5)))] as? String, mode: msgIncome, remote: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        verticalDic(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(app_sureFormat) + String(kRecoverValue))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(app_sureFormat) + String(kRecoverValue))] as! NSNumber
            //: AppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            AppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: AppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.threshold(needReload: false, mf_coin: AppManager.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func counterruptData() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.locationNight(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func verticalDic(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.bequestDict(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func giftArr(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.step(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func dollhouse() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension FamilyFleetViewController {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func mpVoice() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.beyondAccount { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.layCut()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func worldPower() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.continuation { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.layCut()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension FamilyFleetViewController {
    //: @objc private func feedbackBtnClick() {
    @objc private func indoors() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = UnitComparableViewController()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        firstController()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func noneMore() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = MunuView(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.tricolour(cellTitleList: [(String(dreamProblemPath)).localized, (String(showMemoryId.suffix(5))).localized, (String(dream_paperTitle.prefix(6))).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .telecom()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: PipSqueakReactiveCompatible.share.func__pushToUserDetailVC(uid: self.targetId)
                PipSqueakReactiveCompatible.share.balanceConvertSeek(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                weeklyView()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = TransportThen(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.asRemove(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func weeklyView() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = DigitalViewDelegate()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.appropriate(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.removeFlush(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func back() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            limit(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            firstController()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func birthday(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = ModelType.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.belowTotal(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension FamilyFleetViewController {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func peceiptActive() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(absence(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: app_accuracyIdent,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(control),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: dreamClickMsg,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func absence(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard AppManager.share.loginUserMode.loungePlus == true else { return }
        guard AppManager.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func control() {
        //: AppManager.share.loginUserMode.loungePlus = true
        AppManager.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func sceneMsg() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? ProjectionCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? ProjectionCellData else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if LifeDigitalThen.prepareBag(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.dealLocal(tipStr: (String(showProfileFillFormat)).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension FamilyFleetViewController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func add() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        SatelliteThen.recognizeSource(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = SmallPickTool.phoneFile(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.domain(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func take() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        SatelliteThen.alongEach(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.domain(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func domain(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: AppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
            AppManager.share.appStatus == WrinkleSendable.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = OutsideModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.possibility(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = MatterFlowLayout(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = OutsideModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.possibility(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        firstController()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension FamilyFleetViewController {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func pick() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard LifeDigitalThen.fill(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == AlongsideDigitalTableConvertible.male.rawValue ? "He".localized : (String(k_futureUrl)).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.changeManage(showMsg: (String(m_pocketAdviceName.suffix(5)) + String(notiGrassPlanText.suffix(3)) + String(dream_childMsg.suffix(6)) + "o low" + String(mConvertId.suffix(6)) + "sion").launchRid(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.put()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func put() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = SmallPickTool.phoneFile(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            SmallPickTool.localRepresentation(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.televisionAnalysis(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.changeManage(showMsg: (String(k_onLineIdent.prefix(6)) + "video a" + String(notiTheWeId.prefix(6)) + "t 5 " + m_starMsg.replacingOccurrences(of: "install", with: "co")).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func televisionAnalysis(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: GraceEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = PerformanceEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = OutsideModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.hat(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        OrdinalProgressHUD.vision()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        firstController()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: ProjectionCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func pauseWithoutModel(msgData: ProjectionCellData, sendModel: OutsideModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        SweepBridgeThen.shared.courseOfAction(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.changeManage(showMsg: (String(dream_sciencePath) + String(dreamJudgeKey.prefix(7))).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.transformStatus(msgData, isUser: nil, mode: NSNumber(value: 0), remote: .Msg_Status_Fail)
                
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.hat(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension FamilyFleetViewController {
    //: func func__addVoiceNotification() {
    func limitLibraryNotification() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(tearDown),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(pitch),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(fallBack(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(child(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: userLocalStr,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(magnitudeimateChange(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: appWithoutId,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func skipAdd(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            FallaciousManagerDelegate.shared.firsthand()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            FallaciousManagerDelegate.shared.innerProfile()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func tearDown() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        skipAdd(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func pitch() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        skipAdd(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func fallBack(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            skipAdd(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            skipAdd(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: LicenseCellData, cellView: TalkingChatAudioMsgCell) {
    func app(cellData: LicenseCellData, cellView: PreviouslyThen) {
        //: guard TalkingSocketManager.shared.isObject == false else {
        guard StackSocketManager.shared.isObject == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.changeManage(showMsg: m_restoreInputKey)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? ProjectionCellData)!.isKind(of: LicenseCellData.self) == false {
            if (indexModel as? ProjectionCellData)!.isKind(of: LicenseCellData.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! LicenseCellData
            let audioModel = indexModel as! LicenseCellData
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = StateThen.net(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        FallaciousManagerDelegate.shared.scaleConfirm()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if FallaciousManagerDelegate.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (String(kInvestPath)) + "\(FallaciousManagerDelegate.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if FallaciousManagerDelegate.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                FallaciousManagerDelegate.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        FallaciousManagerDelegate.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        FallaciousManagerDelegate.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: ProjectionCellData = self.msgView.uiMsgs.object(at: index) as! ProjectionCellData
                let subModel: ProjectionCellData = self.msgView.uiMsgs.object(at: index) as! ProjectionCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: LicenseCellData.self) {
                if subModel.isKind(of: LicenseCellData.self) {
                    //: let audioModel = subModel as! LicenseCellData
                    let audioModel = subModel as! LicenseCellData
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = StateThen.net(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    FullnessThen.networkMsg(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        FallaciousManagerDelegate.shared.throughBy(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension FamilyFleetViewController {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func exposure() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.someoneMenu()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.fast(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? ProjectionCellData
                let indexModel = model as? ProjectionCellData
                //: if indexModel?.isKind(of: LicenseCellData.self) == true || indexModel?.isKind(of: BubbleCollectionCellData.self) == true || indexModel?.isKind(of: FractionalReactiveCompatible.self) == true || indexModel?.isKind(of: CryOutCellData.self) == true {
                if indexModel?.isKind(of: LicenseCellData.self) == true || indexModel?.isKind(of: BubbleCollectionCellData.self) == true || indexModel?.isKind(of: FractionalReactiveCompatible.self) == true || indexModel?.isKind(of: CryOutCellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.mini()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.tiptoe()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.sceneMsg()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func someoneMenu() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.simultaneouslyElsewhere(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.writeToFrom(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.strength()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == AppManager.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == AppManager.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == AppManager.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == AppManager.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.removeFlush(userWrap)
    }

    //: func func__getFillDraftText() {
    func limpidity() {
        //: let userID = "c2c_"+targetId
        let userID = (String(noti_memberServiceKey.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.sumervalLaunch()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func availableManage() {
        //: let userID = "c2c_"+targetId
        let userID = (String(noti_memberServiceKey.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension FamilyFleetViewController {
    //: func func__TipsNotification() {
    func length() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(aboveNotification(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: k_netStr,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func aboveNotification(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: MatterParentCellData = userinfo[(mConstraintUrl.replacingOccurrences(of: "comply", with: "sg"))] as! MatterParentCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.tillBottom(true)
        self.msgView.tillBottom(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func dealLocal(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: notiNightDomainData.map{$0^16}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: m_comprehensivePath.map{tearAttribute(oval: $0)}, encoding: .utf8)!: (k_reinOurDrillFormat.replacingOccurrences(of: "revenue", with: "ps"))]
        //: var message: ProjectionCellData?
        var message: ProjectionCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = GrapplingIronReactiveCompatible.deviceRow(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = GrapplingIronReactiveCompatible.cameraTip(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.tillBottom(true)
        self.msgView.tillBottom(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func tiptoe() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        LifeDigitalThen.jaw { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.dealLocal(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func tipFuncCanulatePrivateAttachStr(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.dealLocal(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func mini() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        LifeDigitalThen.analogDigitalConverter(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.dealLocal(tipStr: String(bytes: main_increasingMessage.map{upsetHave(rating: $0)}, encoding: .utf8)!.launchRid(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func child(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: ProjectionCellData = dict?["cellData"] as! ProjectionCellData
        let cellData: ProjectionCellData = dict?[(String(m_courseId))] as! ProjectionCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.tillBottom(true)
        self.msgView.tillBottom(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension FamilyFleetViewController: DigitalTipsProtocol {
    //: func func__setReplyTipsView() {
    func premiumActual() {
        //: let key =  "\(AppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(AppManager.share.loginUserMode.userID)" + (String(app_captureName.suffix(7)) + String(notiZzId.suffix(8)) + String(m_sillyFormat.suffix(7))) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && AppManager.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && AppManager.share.appStatus == WrinkleSendable.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.groupDiscussion()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            identification(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            rescueOperation()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func spendStr(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        linguisticProcess(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        rescueOperation()
    }

    //: func removeReplyTipsView() {
    func rescueOperation() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            identification(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension FamilyFleetViewController {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func inOneCaseAnimation() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: showReplaceUrl, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: user_allMessage - noti_jumpQuantityFormalUrl, width: kGiftIdent, height: user_allMessage)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: kGiftIdent,
                                     //: height: ScreenHeight)
                                     height: user_allMessage)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func limit(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.decideBroad() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if StuffLicenseReactiveCompatible.iconPoint().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: k_readValue, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: SayManagerDelegate.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: user_allMessage - noti_jumpQuantityFormalUrl,
                                     //: width: ScreenWidth,
                                     width: kGiftIdent,
                                     //: height: ScreenHeight)
                                     height: user_allMessage)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if StuffLicenseReactiveCompatible.iconPoint().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: k_readValue, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension FamilyFleetViewController {
    /// 创建UI
    //: private func createUI() {
    private func refuse() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(appMinimumTitle) + String(noti_sayEveryoneMessage.suffix(6)) + "fication").localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func activeThread() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? noti_jumpQuantityFormalUrl : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.shRadii(width: contentView.width,
                                //: height: contentView.height,
                                height: contentView.height,
                                //: corners: [.topLeft, .topRight],
                                corners: [.topLeft, .topRight],
                                //: cornerRadii: .init(width: 8, height: 8))
                                cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? show_diskTitle : kSoundPath)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + user_sizeValue)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? show_diskTitle : kSoundPath)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + user_sizeValue)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + user_sizeValue)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func cleavePanelCounterchange() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.back()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.noneMore()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.teachingMethodDetail(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                noti_thirdUrl.toSpeaker(eventID: dreamServerNameStr, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
