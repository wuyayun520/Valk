
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let mSpecialName:String = "Ichild"
fileprivate let main_dependStr:[Character] = ["\u{9000}","出","登","\u{5f55}","失","败","，","c","o","d","e",":"]

/*: , desc: :*/
fileprivate let dream_tabName:String = "angle "
fileprivate let appBrowText:String = "exhibit creaturedesc:"

/*: "IM登录失败，code: :*/
fileprivate let main_licensePath:[Character] = ["I","M","登","录","失","\u{8d25}","，","c","o","d","e"]
fileprivate let main_clearlyStr:String = "daily"

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let userEmploymentStr:String = "IM\u{83b7}取"
fileprivate let mRemId:String = "数失败，codeabs super challenge blue strike"
fileprivate let userDuringCityIdent:String = ":"

/*: "crushBadge" :*/
fileprivate let userGolfUrl:String = "crushBalabel tone automatically water your"
fileprivate let appIllegalText:[Character] = ["d","g","e"]

/*: "badNumber" :*/
fileprivate let showSwitchlyId:[UInt8] = [0xe7,0xe4,0xe1,0xcb,0xf0,0xe8,0xe7,0xe0,0xf7]

private func waitHead(physical num: UInt8) -> UInt8 {
    return num ^ 133
}

/*: "plaza" :*/
fileprivate let mainPerStr:[Character] = ["p","l","a","z","a"]

/*: "roomNum" :*/
fileprivate let showClingName:[Character] = ["r","o","o","m","N","u","m"]

/*: "num" :*/
fileprivate let main_scheduleMsg:[Character] = ["n","u","m"]

/*: "onlyOneRoomId" :*/
fileprivate let mainPublishAfterName:String = "onlyOnchoose sodium dependence far"

/*: "LIVE-CHAT" :*/
fileprivate let mExclusiveName:[Character] = ["L","I","V","E","-","C","H","A","T"]

/*: "PARTY-CHAT" :*/
fileprivate let mainForthKey:String = "arrivalA"
fileprivate let notiAlwaysName:String = "RTY-kingdom anonymous good refuse mic"

/*: "extra" :*/
fileprivate let show_objectKey:String = "extrunninga"

/*: "msgType" :*/
fileprivate let dream_conUrl:String = "substance musical tender discussion charactermsgType"

/*: "newPkgGift" :*/
fileprivate let notiMotivationContent:[Character] = ["n","e","w","P"]
fileprivate let k_belowIdent:[Character] = ["k","g","G","i","f","t"]

/*: "MF:ChatMentionMsg" :*/
fileprivate let showShallIdent:String = "meet careMF:Chat"
fileprivate let mainMediumIdent:String = "Mentiobar due commit disappear"

/*: "uid" :*/
fileprivate let show_nextIdent:[UInt8] = [0x50,0x4c,0x41]

private func beforeDestination(wife num: UInt8) -> UInt8 {
    return num ^ 37
}

/*: "onlineStatus" :*/
fileprivate let notiStackFormat:[UInt8] = [0xdb,0xda,0xd8,0xdd,0xda,0xd1,0xe7,0xc0,0xd5,0xc0,0xc1,0xc7]

/*: "isNewUser" :*/
fileprivate let dreamRackKey:[UInt8] = [0x3c,0x26,0x1b,0x30,0x22,0x0,0x26,0x30,0x27]

/*: "c2c_ :*/
fileprivate let kAgreeHelpFadeUrl:[Character] = ["c","2","c","_"]

/*: "IM获取会话失败，convId: :*/
fileprivate let mBrandName:String = "IM获取\u{4f1a}\u{8bdd}"
fileprivate let notiWriteContent:String = "husband othernvId"
fileprivate let mainAllNearbyContent:String = "realize"

/*: , code: :*/
fileprivate let dream_successFormat:[Character] = [","]
fileprivate let appWaveMessage:[Character] = [" ","c","o","d","e",":"]

/*: "IM插入消息失败，targetId: :*/
fileprivate let dreamCameraIdent:String = "attitude ampIM插入"
fileprivate let noti_writtenIdent:[Character] = ["r","g","e","t","I","d",":"]

/*: "changeDirection" :*/
fileprivate let k_powMsg:[Character] = ["c","h","a","n","g","e","D","i","r","e","c","t","i","o","n"]

/*: "rYMsgType" :*/
fileprivate let notiEngineeringValue:[Character] = ["r","Y","M","s","g","T","y","p","e"]

/*: "GJ:CallCustom" :*/
fileprivate let m_permissionTitle:String = "bond temp processing formGJ:Ca"
fileprivate let k_dayId:String = "stoscope"

/*: "IM获取会话列表失败，code: :*/
fileprivate let kAverData:String = "IM获取movie rational must"
fileprivate let kAwakeValue:String = "count drill cruise err image，code:"

/*: "msgInfo" :*/
fileprivate let main_listFormat:String = "beam narrow reportmsgInfo"

/*: "needFold" :*/
fileprivate let appDescriptionUrl:String = "needFoldobserver basic attribute sea transmission"

/*: "group_ :*/
fileprivate let show_acquireStr:String = "bound close resource everygroup_"

/*: "IM删除会话失败，convID: :*/
fileprivate let appCostFormat:String = "IM删除\u{4f1a}"
fileprivate let m_processAirMsg:String = "nvID:volume cycle realize fusion theme"

/*: "IM置顶会话失败，convId: :*/
fileprivate let show_illegalName:String = "IM\u{7f6e}顶"
fileprivate let dreamBrandUrl:String = "\u{ff0c}con"
fileprivate let dreamCropStr:[Character] = ["v","I","d",":"]

/*: "SDK 正在连接到服务器" :*/
fileprivate let app_padData:[Character] = ["S","D","K"," ","\u{6b63}","在","连","接"]
fileprivate let mainStopPath:String = "到服\u{52a1}器"

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let show_aidGalleryTitle:String = "SDK exist reach re"
fileprivate let dreamSceneId:String = "接到服wed"

/*: "IM连接服务器失败，code: :*/
fileprivate let main_hiddenName:String = "IM\u{8fde}"
fileprivate let k_rocketValue:[Character] = ["接","服","\u{52a1}","器","失","败","，","c","o","d","e",":"]

/*: , error: :*/
fileprivate let userViolationIdent:String = ","
fileprivate let mShoulderId:String = "next education error:"

/*: "IM账号被挤下线。" :*/
fileprivate let dreamScriptKey:[Character] = ["I","M","账","号","被","挤","下","\u{7ebf}","。"]

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let show_compoundData:[UInt8] = []

private func purchaseRegion(anyone num: UInt8) -> UInt8 {
    return num ^ 3
}

/*: "Offline Notification" :*/
fileprivate let dream_heliPortraitTitle:[Character] = ["O","f","f","l","i","n","e"," ","N","o","t","i","f","i","c","a","t"]
fileprivate let noti_visitorTitle:[Character] = ["i","o","n"]

/*: "Cancel" :*/
fileprivate let notiAnnouncementLightMessage:[Character] = ["C","a","n","c","e","l"]

/*: "Re Login" :*/
fileprivate let main_witTitle:String = "wedding certainRe Login"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AmongObjectManager.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol LongDistanceManagerDelegate: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func delayData(data: [SayPreviouslyReactiveCompatible])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func sampleDown(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func messagePrice(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum PerformanceDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class AmongObjectManager: NSObject {
@objcMembers public class AmongObjectManager: NSObject {
    //: public static let shared = AmongObjectManager()
    public static let shared = AmongObjectManager()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [SayPreviouslyReactiveCompatible] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [SayPreviouslyReactiveCompatible] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [SayPreviouslyReactiveCompatible] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [SayPreviouslyReactiveCompatible] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [SayPreviouslyReactiveCompatible] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: SayPreviouslyReactiveCompatible]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.original()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func dismissAcrossResponse() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(main_netName, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func original() {
        //: func__removeAllDelegate()
        whenDelegate()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.verticalList()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func verticalList() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = SayPreviouslyReactiveCompatible.tar(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = SayPreviouslyReactiveCompatible.tar(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func viaBy() -> [SayPreviouslyReactiveCompatible] {
        // 审核模式，在消息列表移除官方客服
        //: if AppManager.share.appStatus == AppSkinStatus.special.rawValue {
        if AppManager.share.appStatus == WrinkleSendable.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == ImmediatelyThen.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == ImmediatelyThen.femaleId() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension AmongObjectManager {
extension AmongObjectManager {
    //: func func__removeAllDelegate() {
    func whenDelegate() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func thumbCookieDelegate(_ delegate: LongDistanceManagerDelegate) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func share(_ delegate: LongDistanceManagerDelegate) {
        //: self.func__invokeDelegate {
        self.maximumBean {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func maximumBean(_ invocation: (LongDistanceManagerDelegate) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! LongDistanceManagerDelegate)
        }
    }
}

// MARK: - IM登录态管理

//: extension AmongObjectManager {
extension AmongObjectManager {
    /// 退出登录IM
    //: func func__LogingOut() {
    func feeOut() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            HoeThen.halfLine(msg: (mSpecialName.replacingOccurrences(of: "child", with: "M") + String(main_dependStr)) + "\(code)" + (dream_tabName.replacingOccurrences(of: "angle", with: ",") + String(appBrowText.suffix(5))) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.original()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func page(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = AppManager.share.appUserConfigMode.uSig
        let uSigStr = AppManager.share.appUserConfigMode.uSig
        //: let uidStr = AppManager.share.loginUserMode.userID
        let uidStr = AppManager.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.monitor()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            HoeThen.halfLine(msg: (String(main_licensePath) + main_clearlyStr.replacingOccurrences(of: "daily", with: ":")) + "\(code)" + (dream_tabName.replacingOccurrences(of: "angle", with: ",") + String(appBrowText.suffix(5))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func becomeList() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || AppManager.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || AppManager.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.changeManage(showMsg: mainSizeFormat)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension AmongObjectManager {
extension AmongObjectManager {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func monitor() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func licenseMin() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: LifeReactiveCompatible.req_sysMessageList { _, result in
        LifeReactiveCompatible.remarkCompletion { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(String(userGolfUrl.prefix(7)) + String(appIllegalText))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: appRejectName, object: self, userInfo: [String(bytes: showSwitchlyId.map{waitHead(physical: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func withFollow(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: LifeReactiveCompatible.req_getChatRoomInfo { succeed, result, _ in
        LifeReactiveCompatible.anyMulti { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(String(mainPerStr))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(showClingName))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = SayPreviouslyReactiveCompatible.tar(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(String(main_scheduleMsg))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(mainPublishAfterName.prefix(6)) + "eRoomId")] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension AmongObjectManager: V2TIMAdvancedMsgListener {
extension AmongObjectManager: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT") || msg.groupID.contains("PARTY-CHAT")) { // 开播/语聊房 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(mExclusiveName))) || msg.groupID.contains((mainForthKey.replacingOccurrences(of: "arrival", with: "P") + String(notiAlwaysName.prefix(4)) + "CHAT"))) { // 开播/语聊房 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            DamnThen.oldFee().recv(msg: msg)
            //: return
            return
        }
        //: if msg.sender == AppManager.share.loginUserMode.userID { return }
        if msg.sender == AppManager.share.loginUserMode.userID { return }

        //: let dic = SayEditChatManager.parseTXMessageData(data: msg.customElem.data)
        let dic = SayEditChatManager.assemblageSquareKey(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(show_objectKey.replacingOccurrences(of: "running", with: "r"))] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? DramaticallyModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: appWithoutId, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(dream_conUrl.suffix(7)))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(notiMotivationContent) + String(k_belowIdent)) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: show_infoTitle, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if SayEditChatManager.checkVoiceMessage(message: msg) {
            if SayEditChatManager.singingVoice(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = GrapplingIronReactiveCompatible.mpWith(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                FullnessThen.shade(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.behindLimit(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            MatterThen.shared.aidDisposeMessageGiftHorseInherit(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            MatterThen.shared.grammaticalGender(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(showShallIdent.suffix(7)) + String(mainMediumIdent.prefix(6)) + "nMsg") { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            TotalegrityChatManager.share.gold(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: LifeDigitalThen.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: LifeDigitalThen.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != ImmediatelyThen.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != ImmediatelyThen.polytetrafluoroethylene() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.maximumBean { $0.messagePrice?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            CosmopolitanReactiveCompatible.shared.overMutual(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: show_nextIdent.map{beforeDestination(wife: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: notiStackFormat.map{$0^180}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: dreamRackKey.map{$0^85}, encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: noti_tagData,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            StateThen.scriptSequence(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            LifeDigitalThen.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: app_accuracyIdent, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: ProjectionCellData, isPrivete: Bool) {
    @objc public func bullSEye(targetId: String, msgCellData: ProjectionCellData, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.privete(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(kAgreeHelpFadeUrl)) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.lipSuccess(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.domainList(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
                HoeThen.halfLine(msg: (mBrandName + "失败，co" + String(notiWriteContent.suffix(4)) + mainAllNearbyContent.replacingOccurrences(of: "realize", with: ":")) + "\(convId)" + (String(dream_successFormat) + String(appWaveMessage)) + "\(code)" + (dream_tabName.replacingOccurrences(of: "angle", with: ",") + String(appBrowText.suffix(5))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func privete(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = AppManager.share.loginUserMode.userID
        let senderStr = AppManager.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
                HoeThen.halfLine(msg: (String(dreamCameraIdent.suffix(4)) + "\u{6d88}息失\u{8d25}，ta" + String(noti_writtenIdent)) + "\(targetId)" + (String(dream_successFormat) + String(appWaveMessage)) + "\(code)" + (dream_tabName.replacingOccurrences(of: "angle", with: ",") + String(appBrowText.suffix(5))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func behindLimit(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(String(k_powMsg))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && PipSqueakReactiveCompatible.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(notiEngineeringValue))].stringValue == (String(m_permissionTitle.suffix(5)) + "llCu" + k_dayId.replacingOccurrences(of: "scope", with: "m")) && isSelf && PipSqueakReactiveCompatible.share.funcProcess()!.isKind(of: FamilyFleetViewController.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: showDetailMsg) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: showDetailMsg)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension AmongObjectManager: V2TIMConversationListener {
extension AmongObjectManager: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.lipSuccess(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.domainList(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.lipSuccess(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.domainList(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: AppManager.share.unreadMessageNum = Int(totalUnreadCount)
        AppManager.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.maximumBean {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.sampleDown?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func domainList(conVersationList: [SayPreviouslyReactiveCompatible]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if StateThen.delayIcon().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                StateThen.delayIcon().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.maximumBean {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.delayData?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func translation(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [SayPreviouslyReactiveCompatible] = []

        // V2TIMConversation->SayPreviouslyReactiveCompatible
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = SayPreviouslyReactiveCompatible(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.pendingConv(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [SayPreviouslyReactiveCompatible] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = ImmediatelyInfoTable.representationWith(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.myWith(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension AmongObjectManager {
extension AmongObjectManager {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func engine(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.translation(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.domainList(conVersationList: arr as? [SayPreviouslyReactiveCompatible] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            HoeThen.halfLine(msg: (String(kAverData.prefix(4)) + "会话列表失败" + String(kAwakeValue.suffix(6))) + "\(code)" + (dream_tabName.replacingOccurrences(of: "angle", with: ",") + String(appBrowText.suffix(5))) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func flush(targetId: String) -> SayPreviouslyReactiveCompatible? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func lipSuccess(convList: [V2TIMConversation]) -> [SayPreviouslyReactiveCompatible] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [SayPreviouslyReactiveCompatible] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = SayPreviouslyReactiveCompatible(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.pendingConv(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.myWith(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func myWith(convModel: SayPreviouslyReactiveCompatible, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.verticalList()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: PerformanceDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - AppManager.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - AppManager.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = StateThen.goldId(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = SayEditChatManager.parseTXMessageData(data: elemData)
            let dict = SayEditChatManager.assemblageSquareKey(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(show_objectKey.replacingOccurrences(of: "running", with: "r"))][(String(main_listFormat.suffix(7)))][(String(appDescriptionUrl.prefix(8)))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.paraOf(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.paraOf(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func paraOf(convList: [SayPreviouslyReactiveCompatible], dataType: PerformanceDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func collectionField(targetId: String, dataType: PerformanceDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(kAgreeHelpFadeUrl)) + "\(targetId)" : (String(show_acquireStr.suffix(6))) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            StateThen.publicTransport(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(AppManager.share.loginUserMode.userID, toUid: targetId)
            FullnessThen.designation(AppManager.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.monitor()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            HoeThen.halfLine(msg: (appCostFormat + "话失\u{8d25}，co" + String(m_processAirMsg.prefix(5))) + "\(convID)" + (String(dream_successFormat) + String(appWaveMessage)) + "\(code)" + (dream_tabName.replacingOccurrences(of: "angle", with: ",") + String(appBrowText.suffix(5))) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func priceBind(convModel: SayPreviouslyReactiveCompatible, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
            HoeThen.halfLine(msg: (show_illegalName + "\u{4f1a}\u{8bdd}失败" + dreamBrandUrl + String(dreamCropStr)) + "\(convModel.conversationID)" + (String(dream_successFormat) + String(appWaveMessage)) + "\(code)" + (dream_tabName.replacingOccurrences(of: "angle", with: ",") + String(appBrowText.suffix(5))) + "\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.paraOf(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.paraOf(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension AmongObjectManager: V2TIMSDKListener {
extension AmongObjectManager: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(app_padData) + mainStopPath))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(show_aidGalleryTitle.prefix(4)) + "\u{5df2}经成\u{529f}连" + dreamSceneId.replacingOccurrences(of: "wed", with: "务器")))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        HoeThen.halfLine(msg: (main_hiddenName + String(k_rocketValue)) + "\(code)" + (userViolationIdent.capitalized + String(mShoulderId.suffix(7))) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        HoeThen.halfLine(msg: (String(dreamScriptKey)))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        StackSocketManager.shared.level(userLoginOut: true)
        //: AmongObjectManager.shared.isConnection = false
        AmongObjectManager.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (ApplicationReactiveCompatible.shared as! ApplicationReactiveCompatible).asOwner()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: show_compoundData.map{purchaseRegion(anyone: $0)}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = SeeAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if SweepAddrTool.share.interfaceLang == RowTitleTotalerpolation.ar.rawValue || SweepAddrTool.share.interfaceLang == RowTitleTotalerpolation.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            SweepAddrTool.share.interfaceLang == RowTitleTotalerpolation.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.equitySize(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        DemonstrateSayAlertReactiveCompatible.rightUsed()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        DemonstrateSayAlertReactiveCompatible.rightUsed(title: (String(dream_heliPortraitTitle) + String(noti_visitorTitle)).localized, message: msg, leftBtnTitle: (String(notiAnnouncementLightMessage)).localized, rightBtnTitle: (String(main_witTitle.suffix(8))).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            GuideUnitReactiveCompatible.commonFragment().theResource()
            // 销毁语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            StuffLicenseReactiveCompatible.iconPoint().someoneStart()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            AlterThen.without { t in
                //: ProgressHUD.dismiss()
                OrdinalProgressHUD.vision()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: notiScaleNetAcceptIdent, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            AlterThen.camera { t in
                //: if t {
                if t {
                    //: AmongObjectManager.shared.func__LogingIn { _ in
                    AmongObjectManager.shared.page { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if GuideUnitReactiveCompatible.commonFragment().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(GuideUnitReactiveCompatible.commonFragment().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    StackSocketManager.shared.sturdiness()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
