
//: Declare String Begin

/*: "msgType" :*/
fileprivate let dreamTranslateMsg:String = "to coat forwardmsgType"

/*: "tips" :*/
fileprivate let user_withinInnerKey:[Character] = ["t","i","p","s"]

/*: "extra" :*/
fileprivate let userScreamValue:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let notiDefenseMsg:[Character] = ["m","s","g","I","n","f","o"]

/*: "user" :*/
fileprivate let dream_administratorTitle:[Character] = ["u","s","e","r"]

/*: "uid" :*/
fileprivate let show_towardsFeatureMessage:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let appYourData:[UInt8] = [0x72,0x75,0x7f,0x77,0x72,0x7d,0x71,0x79]

/*: "headPic" :*/
fileprivate let main_brandClearStr:[UInt8] = [0xa7,0xaa,0xae,0xab,0x9f,0xa6,0xac]

private func piImaginationSubmit(agent num: UInt8) -> UInt8 {
    return num ^ 207
}

/*: "age" :*/
fileprivate let mTapPaperPath:[UInt8] = [0x65,0x6b,0x69]

fileprivate func screenProgram(opening num: UInt8) -> UInt8 {
    let value = Int(num) + 252
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "gift" :*/
fileprivate let userEyeSectionWithinName:[Character] = ["g","i","f","t"]

/*: "isGroup" :*/
fileprivate let noti_specialBitMessage:String = "isGroupreverse person"

/*: "WantGift" :*/
fileprivate let mTunOptimisticValue:String = "WantGiftsilly fore"

/*: "txt" :*/
fileprivate let notiLikePath:String = "TXT"

/*: "img" :*/
fileprivate let dreamIonMsg:String = "incorporateg"

/*: "video" :*/
fileprivate let main_uponElementId:String = "viavailableeo"

/*: "audio" :*/
fileprivate let showRunStr:[Character] = ["a","u","d","i","o"]

/*: "messageType" :*/
fileprivate let app_managerAgentData:[Character] = ["m","e","s","s","a","g","e","T","y","p"]
fileprivate let showCoatUrl:String = "find"

/*: "contentType" :*/
fileprivate let userHistoryName:String = "coplayeren"

/*: "ImgMsg" :*/
fileprivate let mSoulIdent:String = "after scanImgMsg"

/*: "imageUri" :*/
fileprivate let appFlushValue:String = "imageUrieh protection down criteria recommendation"

/*: "type" :*/
fileprivate let show_maybeUrl:[Character] = ["t","y","p","e"]

/*: "pic" :*/
fileprivate let dream_conversionFormat:String = "piheel"

/*: "isBurn" :*/
fileprivate let noti_deliveryTitle:String = "prosecutor enough hurry social ownisBurn"

/*: "giftId" :*/
fileprivate let kPieName:[Character] = ["g","i","f","t","I","d"]

/*: "params" :*/
fileprivate let main_baseballKey:String = "PARAMS"

/*: "audioLength" :*/
fileprivate let mainFillStr:String = "AUDI"
fileprivate let kStartRepresentationUrl:String = "salt pad applicationoLength"

/*: "len" :*/
fileprivate let showAmongIdent:String = "lechoice"

/*: "audioSandbox" :*/
fileprivate let m_standardTerraKey:String = "audconfirm"
fileprivate let k_builderFormat:[Character] = ["x"]

/*: "senderId" :*/
fileprivate let noti_informPreviousId:String = "study go personal feesenderI"
fileprivate let k_stageBrightId:[Character] = ["d"]

/*: "AudioMsg" :*/
fileprivate let k_defenseValue:[Character] = ["A","u","d","i","o"]
fileprivate let show_arcEdgeValue:String = "Msgoutput duty resort export product"

/*: "content" :*/
fileprivate let dreamHesitateUrl:[Character] = ["c","o","n","t","e","n","t"]

/*: "TxtMsg" :*/
fileprivate let show_weekContent:[Character] = ["T"]
fileprivate let dreamBeyondTitle:String = "xtMsgmilitary hung route will current"

/*: "message" :*/
fileprivate let showShadowId:String = "pute"
fileprivate let userHatPerformName:String = "complycomplyage"

/*: "quoteMsgId" :*/
fileprivate let m_automaticPath:String = "quothat"

/*: "quoteMsgInfo" :*/
fileprivate let m_looseData:String = "quoteMsimultaneously would"
fileprivate let userRedQuoteSenseTitle:String = "compound"
fileprivate let mPearIdent:[Character] = ["g","I","n","f","o"]

/*: "rYMsgType" :*/
fileprivate let kAlwaysBoldContent:String = "recommend definitionrYMsg"

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let userGraceFormat:String = "roughly remark cycleGJ:S"
fileprivate let noti_secondThanFillKey:String = "toGreemonitor nothing progress production"
fileprivate let showNativeMessage:String = "however trackCust"
fileprivate let app_licenseTitle:String = "total"

/*: "changeDirection" :*/
fileprivate let mFoundIdent:String = "chadjustnge"
fileprivate let dream_mortalityValue:[Character] = ["D","i","r"]
fileprivate let kReplacementFormat:[Character] = ["e","c","t","i","o","n"]

/*: "system" :*/
fileprivate let dreamWindowSampleMessage:String = "systemanager"

/*: "title" :*/
fileprivate let main_towardsShadeVisualValue:String = "perspectiveiperspectivele"

/*: "icon" :*/
fileprivate let main_exceptionId:String = "icosuccess"

/*: "all" :*/
fileprivate let dream_northwestName:String = "avaluevalue"

/*: "icon_chats_pcr" :*/
fileprivate let k_leaveId:[Character] = ["i","c","o","n","_","c","h","a","t","s","_","p","c","r"]

/*: "status" :*/
fileprivate let noti_averageNearTitle:String = "statdramatically"

/*: "msgIncome" :*/
fileprivate let kLiberalPath:String = "msgIncopeople same employment filter busy"
fileprivate let app_inheritPath:String = "mgame"

/*: "msgId" :*/
fileprivate let notiInstructionId:[Character] = ["m","s","g","I","d"]

/*: "MailStrideable" :*/
fileprivate let appWeData:String = "near duty type devote withoutTMsgT"
fileprivate let noti_forthValue:[Character] = ["e","S","t","a","t","u","s"]

/*: "TMsgTranslatedContent" :*/
fileprivate let kReverseLikeId:String = "violation row proposal compound pairTMsgT"
fileprivate let noti_schemeStr:String = "tedConfirm pad anonymous disable"

/*: "formatType" :*/
fileprivate let mMedalPath:String = "formlog"
fileprivate let app_beautyText:[Character] = ["t","T","y","p","e"]

/*: "plain" :*/
fileprivate let noti_mediumKey:[Character] = ["p","l","a","i","n"]

/*: "imageData" :*/
fileprivate let showResourceStr:[Character] = ["i","m","a","g","e","D","a","t","a"]

/*: "VideoMsg" :*/
fileprivate let dreamBrandValue:[Character] = ["V","i","d","e","o","M","s","g"]

/*: "match" :*/
fileprivate let notiExistingText:String = "matcdistribution"

/*: "userIntro" :*/
fileprivate let appTerraText:String = "U"
fileprivate let dreamFactorName:[Character] = ["s","e","r","I","n","t","r","o"]

/*: "targetId" :*/
fileprivate let mSlowId:String = "clearlyarg"

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let mSeekPath:[Character] = ["y","y","y","y","-","M","M","-","d","d"," ","H","H"]
fileprivate let showCertainValue:String = ":"
fileprivate let main_checkPostName:[Character] = ["m","m",":","s","s"]

/*: "sendTime" :*/
fileprivate let dream_homeId:String = "sendTimemust slight"

/*: "isRead" :*/
fileprivate let notiRenderStrikePath:String = "regular process premium database everyisRea"
fileprivate let k_remarkElitePath:String = "more"

/*: "audioData" :*/
fileprivate let dream_maybeKey:[Character] = ["a","u","d","i","o","D","a"]
fileprivate let m_endStr:String = "tpatient"

/*: "audioUri" :*/
fileprivate let mainCookieMessage:[Character] = ["a","u","d"]
fileprivate let show_availabilityData:[Character] = ["i","o","U","r","i"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  GrapplingIronReactiveCompatible.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum BroadcastColumnConvertible: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - GrapplingIronReactiveCompatible

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class GrapplingIronReactiveCompatible: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func databaseTime() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> ProjectionCellData? {
    public class func deviceRow(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> ProjectionCellData? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = cameraTip(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: AmongObjectManager.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            AmongObjectManager.shared.bullSEye(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> ProjectionCellData? {
    public class func cameraTip(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> ProjectionCellData? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(dreamTranslateMsg.suffix(7)))] = (String(user_withinInnerKey))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(String(userScreamValue)): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = medalData(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = MatterParentCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = DramaticallyModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? ConvertModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(notiDefenseMsg))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(String(dream_administratorTitle))] = [
            //: "uid": AppManager.share.loginUserMode.userID,
            (String(show_towardsFeatureMessage)): AppManager.share.loginUserMode.userID,
            //: "nickname": AppManager.share.loginUserMode.nickname,
            String(bytes: appYourData.map{$0^28}, encoding: .utf8)!: AppManager.share.loginUserMode.nickname,
            //: "headPic": AppManager.share.loginUserMode.headPic,
            String(bytes: main_brandClearStr.map{piImaginationSubmit(agent: $0)}, encoding: .utf8)!: AppManager.share.loginUserMode.headPic,
            //: "age": AppManager.share.loginUserMode.age,
            String(bytes: mTapPaperPath.map{screenProgram(opening: $0)}, encoding: .utf8)!: AppManager.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(String(userScreamValue)): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = medalData(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(dreamTranslateMsg.suffix(7)))] as? String == (String(userEyeSectionWithinName)) {
            //: var data: ProjectionCellData
            var data: ProjectionCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(noti_specialBitMessage.prefix(7)))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = FleetDumpCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = StuffThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = StuffThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? DramaticallyModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(dreamTranslateMsg.suffix(7)))] as? String == (String(mTunOptimisticValue.prefix(8))) {
            //: let data = MoveThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = MoveThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? DramaticallyModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(dreamTranslateMsg.suffix(7)))] as? String == (notiLikePath.lowercased()) {
            //: var data: ProjectionCellData
            var data: ProjectionCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(noti_specialBitMessage.prefix(7)))] as? String == "1" { // 群聊
                //: data = ParentCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ParentCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = FractionalReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = FractionalReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? DramaticallyModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(dreamTranslateMsg.suffix(7)))] as? String == (dreamIonMsg.replacingOccurrences(of: "incorporate", with: "im")) {
            //: let data = BubbleCollectionCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = BubbleCollectionCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? DramaticallyModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(dreamTranslateMsg.suffix(7)))] as? String == (main_uponElementId.replacingOccurrences(of: "available", with: "d")) {
            //: let data = CryOutCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = CryOutCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? DramaticallyModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(dreamTranslateMsg.suffix(7)))] as? String == (String(showRunStr)) {
            //: let data = LicenseCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = LicenseCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? DramaticallyModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(String(userEyeSectionWithinName))] != nil && (msgInfo[(String(app_managerAgentData) + showCoatUrl.replacingOccurrences(of: "find", with: "e"))] as? Int) == 4 {
            //: let data = FractionalReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = FractionalReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? DramaticallyModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: ProjectionCellData) -> ProjectionCellData? {
    public class func affairs(withData cellData: ProjectionCellData) -> ProjectionCellData? {
        // 图片
        //: if let reCellData = cellData as? BubbleCollectionCellData {
        if let reCellData = cellData as? BubbleCollectionCellData {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(ImmediatelyThen.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(ImmediatelyThen.pageName())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(userHistoryName.replacingOccurrences(of: "player", with: "nt") + "tType")] = (String(mSoulIdent.suffix(6)))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(dreamTranslateMsg.suffix(7)))] = (dreamIonMsg.replacingOccurrences(of: "incorporate", with: "im"))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(String(appFlushValue.prefix(8)))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(notiDefenseMsg))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(String(show_maybeUrl))] = (String(mSoulIdent.suffix(6)))
            //: params["pic"] = filePath
            params[(dream_conversionFormat.replacingOccurrences(of: "heel", with: "c"))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(noti_deliveryTitle.suffix(6)))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(noti_deliveryTitle.suffix(6)))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(kPieName))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(kPieName))] = "0"
            }

            //: dic["params"] = params
            dic[(main_baseballKey.lowercased())] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = cameraTip(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? CryOutCellData {
        if let reCellData = cellData as? CryOutCellData {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? LicenseCellData {
        if let reCellData = cellData as? LicenseCellData {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = StateThen.net(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(mainFillStr.lowercased() + String(kStartRepresentationUrl.suffix(7)))] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(showAmongIdent.replacingOccurrences(of: "choice", with: "n"))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(m_standardTerraKey.replacingOccurrences(of: "confirm", with: "i") + "oSandbo" + String(k_builderFormat))] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(m_standardTerraKey.replacingOccurrences(of: "confirm", with: "i") + "oSandbo" + String(k_builderFormat))] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(dreamTranslateMsg.suffix(7)))] = (String(showRunStr))
            //: msgInfo["senderId"] = AppManager.share.loginUserMode.userID
            msgInfo[(String(noti_informPreviousId.suffix(7)) + String(k_stageBrightId))] = AppManager.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(notiDefenseMsg))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(String(show_maybeUrl))] = (String(k_defenseValue) + String(show_arcEdgeValue.prefix(3)))
            //: dic["params"] = params
            dic[(main_baseballKey.lowercased())] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = cameraTip(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? FractionalReactiveCompatible {
        if let reCellData = cellData as? FractionalReactiveCompatible {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(String(dreamHesitateUrl))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(dreamTranslateMsg.suffix(7)))] = (notiLikePath.lowercased())
            //: dic["msgInfo"] = msgInfo
            dic[(String(notiDefenseMsg))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(String(show_maybeUrl))] = (String(show_weekContent) + String(dreamBeyondTitle.prefix(5)))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(showShadowId.replacingOccurrences(of: "put", with: "m") + userHatPerformName.replacingOccurrences(of: "comply", with: "s"))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.brightMsg() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(m_automaticPath.replacingOccurrences(of: "that", with: "t") + "eMsgId")] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(m_looseData.prefix(6)) + userRedQuoteSenseTitle.replacingOccurrences(of: "compound", with: "s") + String(mPearIdent))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: dic["params"] = params
            dic[(main_baseballKey.lowercased())] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = cameraTip(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> ProjectionCellData? {
    public class func roofModel(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> ProjectionCellData? {
        //: var cellData: ProjectionCellData?
        var cellData: ProjectionCellData?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(String(userScreamValue))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(notiDefenseMsg))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(String(dream_administratorTitle))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? DramaticallyModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == AppManager.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == AppManager.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == AppManager.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == AppManager.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(kAlwaysBoldContent.suffix(5)) + "Type")] as? String == (String(userGraceFormat.suffix(4)) + "ess:Au" + String(noti_secondThanFillKey.prefix(6)) + "t:Txt" + String(showNativeMessage.suffix(4)) + app_licenseTitle.replacingOccurrences(of: "total", with: "om")) {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(mFoundIdent.replacingOccurrences(of: "adjust", with: "a") + String(dream_mortalityValue) + String(kReplacementFormat))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = reduce(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(dreamTranslateMsg.suffix(7)))] as? String == (dreamWindowSampleMessage.replacingOccurrences(of: "manager", with: "m")) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = DramaticallyModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? ConvertModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = MatterParentCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(String(dreamHesitateUrl))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = BubbleCollectionCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = BubbleCollectionCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = CryOutCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = CryOutCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = FleetDumpCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = StuffThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = StuffThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = notDataBlind(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(String(userScreamValue))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(notiDefenseMsg))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? QualityModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = MoveThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = MoveThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = LicenseCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = LicenseCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? LicenseCellData {
            if let audioCellData = cellData as? LicenseCellData {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = recentCity(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = FullnessThen.resolveWarning(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = FullnessThen.shade(mpWith(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = GoddamnCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(main_towardsShadeVisualValue.replacingOccurrences(of: "perspective", with: "t"))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(String(dreamHesitateUrl))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(dreamIonMsg.replacingOccurrences(of: "incorporate", with: "im"))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ImpermanencyReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = ParentCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ParentCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = FractionalReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = FractionalReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? RealizeUserModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(main_exceptionId.replacingOccurrences(of: "success", with: "n"))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: AppManager.share.loginUserMode.headPic ?? ""
                        AppManager.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = QualityModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(String(dreamHesitateUrl))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(String(userScreamValue))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = GateModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (dream_northwestName.replacingOccurrences(of: "value", with: "l"))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(k_leaveId))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.mouthManage(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(noti_averageNearTitle.replacingOccurrences(of: "dramatically", with: "us"))] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(kLiberalPath.prefix(7)) + app_inheritPath.replacingOccurrences(of: "game", with: "e"))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(String(app_managerAgentData) + showCoatUrl.replacingOccurrences(of: "find", with: "e"))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(notiInstructionId))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["MailStrideable"] as? Int {
        if let translateStatus = customDic[(String(appWeData.suffix(5)) + "ranslat" + String(noti_forthValue))] as? Int {
            //: cellData?.translateStatus = MailStrideable(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = MailStrideable(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(kReverseLikeId.suffix(5)) + "ransla" + String(noti_schemeStr.prefix(6)) + "tent")] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = NonopeningModel.page()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = NonopeningModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = NextAdviceModel.suite()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = NextAdviceModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = BugModel.observerGravity()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func reduce(_ extraDic: [String: Any]) -> BroadcastColumnConvertible {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(String(userScreamValue))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(notiDefenseMsg))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? DramaticallyModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(mMedalPath.replacingOccurrences(of: "log", with: "a") + String(app_beautyText))] as? String == (String(noti_mediumKey)) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(String(appFlushValue.prefix(8)))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(String(showResourceStr))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(dreamBrandValue)) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = msginfo(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(notiExistingText.replacingOccurrences(of: "distribution", with: "h"))] as? Int) == 1 || (msgInfo?[(notiExistingText.replacingOccurrences(of: "distribution", with: "h"))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(dreamTranslateMsg.suffix(7)))] as? String == (String(user_withinInnerKey)) || (dic?[(String(app_managerAgentData) + showCoatUrl.replacingOccurrences(of: "find", with: "e"))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(mMedalPath.replacingOccurrences(of: "log", with: "a") + String(app_beautyText))] as? String == "mf") && ((dic?[(String(app_managerAgentData) + showCoatUrl.replacingOccurrences(of: "find", with: "e"))] as? Int) == 15 || (dic?[(String(app_managerAgentData) + showCoatUrl.replacingOccurrences(of: "find", with: "e"))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(dreamTranslateMsg.suffix(7)))] as? String == (appTerraText.lowercased() + String(dreamFactorName)) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(dreamTranslateMsg.suffix(7)))] as? String == (String(mTunOptimisticValue.prefix(8))) || (msgInfo?[(String(app_managerAgentData) + showCoatUrl.replacingOccurrences(of: "find", with: "e"))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func mpWith(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.mouthManage(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(String(userScreamValue))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(notiDefenseMsg))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(String(dream_administratorTitle))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(String(noti_informPreviousId.suffix(7)) + String(k_stageBrightId))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == AppManager.share.loginUserMode.userID {
        if senderId == AppManager.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(String(noti_informPreviousId.suffix(7)) + String(k_stageBrightId))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(mSlowId.replacingOccurrences(of: "clearly", with: "t") + "etId")] = message.userID ?? (user?[(mSlowId.replacingOccurrences(of: "clearly", with: "t") + "etId")] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(noti_informPreviousId.suffix(7)) + String(k_stageBrightId))] = "\(msgInfo?[(String(show_towardsFeatureMessage))] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(mSlowId.replacingOccurrences(of: "clearly", with: "t") + "etId")] = "\(msgInfo?[(String(show_towardsFeatureMessage))] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(notiInstructionId))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(notiInstructionId))] = "\(msgInfo?[(String(notiInstructionId))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.mouthManage(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(notiInstructionId))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(notiInstructionId))] = "\(customDic[(String(notiInstructionId))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(notiInstructionId))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((m_standardTerraKey.replacingOccurrences(of: "confirm", with: "i") + "oSandbo" + String(k_builderFormat))) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(m_standardTerraKey.replacingOccurrences(of: "confirm", with: "i") + "oSandbo" + String(k_builderFormat))] = "\(msgInfo?[(m_standardTerraKey.replacingOccurrences(of: "confirm", with: "i") + "oSandbo" + String(k_builderFormat))] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((mainFillStr.lowercased() + String(kStartRepresentationUrl.suffix(7)))) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(mainFillStr.lowercased() + String(kStartRepresentationUrl.suffix(7)))] = "\(msgInfo?[(mainFillStr.lowercased() + String(kStartRepresentationUrl.suffix(7)))] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (String(mSeekPath) + showCertainValue.capitalized + String(main_checkPostName))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(dream_homeId.prefix(8)))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == AppManager.share.loginUserMode.userID ? "1" : "0"
        info?[(String(notiRenderStrikePath.suffix(5)) + k_remarkElitePath.replacingOccurrences(of: "more", with: "d"))] = senderId == AppManager.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func recentCity(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = mpWith(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(notiInstructionId))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func notDataBlind(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func medalData(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func msginfo(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(dreamTranslateMsg.suffix(7)))] as? String, msgType == (String(showRunStr)) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(userHistoryName.replacingOccurrences(of: "player", with: "nt") + "tType")] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(k_defenseValue) + String(show_arcEdgeValue.prefix(3)))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(String(dream_maybeKey) + m_endStr.replacingOccurrences(of: "patient", with: "a"))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(String(mainCookieMessage) + String(show_availabilityData))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
