
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let noti_atThroughPrimaryMessage:[UInt8] = [0x6,0x1,0x6,0x1b,0x47,0xc,0x0,0xb,0xa,0x1d,0x55,0x46,0x4f,0x7,0xe,0x1c,0x4f,0x1,0x0,0x1b,0x4f,0xd,0xa,0xa,0x1,0x4f,0x6,0x2,0x1f,0x3,0xa,0x2,0xa,0x1,0x1b,0xa,0xb]

private func givingHap(numb num: UInt8) -> UInt8 {
    return num ^ 111
}

/*: "uid" :*/
fileprivate let showBeanValue:[UInt8] = [0x60,0x7c,0x71]

private func bondStyle(standard num: UInt8) -> UInt8 {
    return num ^ 21
}

/*: "headPic" :*/
fileprivate let mAnonymousAgoStr:[Character] = ["h","e","a","d","P","i"]
fileprivate let userInnerName:String = "curve"

/*: "nickname" :*/
fileprivate let showMayId:[Character] = ["n","i","c","k","n"]
fileprivate let app_actualContent:String = "AME"

/*: "birthday" :*/
fileprivate let kStructureTitle:[Character] = ["b","i","r"]
fileprivate let dream_rowMonitorKey:[Character] = ["t","h","d","a","y"]

/*: "videoUrl" :*/
fileprivate let app_runText:String = "videoUrfence pan hour"
fileprivate let m_liveMsg:String = "index"

/*: "cover" :*/
fileprivate let dreamNoMessage:[Character] = ["c","o","v","e","r"]

/*: "location" :*/
fileprivate let userIllegalMsg:[Character] = ["l","o","c","a","t","i","o","n"]

/*: "cmd" :*/
fileprivate let show_missFormat:[UInt8] = [0xda,0xd4,0xdd]

/*: "refuseCall" :*/
fileprivate let k_differenceText:String = "refuseCdeny plastic find"
fileprivate let userRawLayExitIdent:[Character] = ["a","l","l"]

/*: "data" :*/
fileprivate let k_waterId:[UInt8] = [0xe6,0xe3,0xf6,0xe3]

private func readingDrown(literal num: UInt8) -> UInt8 {
    return num ^ 130
}

/*: "Call interruption" :*/
fileprivate let noti_enhanceUrl:String = "Call icompound translation very solar"
fileprivate let app_supposedFormat:[Character] = ["t","i","o","n"]

/*: "stopCall" :*/
fileprivate let appVoiceButterflyStr:String = "every support future former boystopCall"

/*: "logId" :*/
fileprivate let user_subtleStr:[UInt8] = [0x17,0x1a,0x12,0xf4,0xf]

fileprivate func roundOfGolf(local num: UInt8) -> UInt8 {
    let value = Int(num) - 171
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let m_hardwareIdent:[Character] = ["c"]
fileprivate let k_jumpUponItId:[Character] = ["a","n","c","e","l","C","a","l","l"]

/*: "No answer" :*/
fileprivate let mEqualIdent:[Character] = ["N","o"," ","a","n","s","w","e","r"]

/*: "duration" :*/
fileprivate let app_busyText:[Character] = ["d","u","r","a","t","i","o"]
fileprivate let notiPlasticEntrySeatContent:[Character] = ["n"]

/*: "She has hung up" :*/
fileprivate let noti_angleIdent:[Character] = ["S","h","e"," ","h","a","s"]
fileprivate let show_maxAnotherUnknownStr:[Character] = [" ","h","u","n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let mainGolfStr:String = "CAN"
fileprivate let showReStr:String = "cute night process pet scramblecelSex"

/*: "He has hung up" :*/
fileprivate let mainRewardPath:String = "He hasteammate supportive"
fileprivate let k_netQuickId:[Character] = ["g"," ","u","p"]

/*: "endType" :*/
fileprivate let main_comfortableName:String = "endTyaccuracy pie"
fileprivate let main_belowKey:[Character] = ["p","e"]

/*: "stopSex" :*/
fileprivate let show_materialData:String = "state exposure mediastopS"
fileprivate let show_sourceIdent:String = "color"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let main_partnerBedPath:[UInt8] = [0xde,0xf9,0xe4,0xe2,0xf1,0xf1,0xfe,0xf4,0xfe,0xf2,0xf9,0xe3,0xb7,0xf0,0xf8,0xfb,0xf3,0xb7,0xf4,0xf8,0xfe,0xf9,0xe4,0xbb,0xb7,0xf4,0xf6,0xfb,0xfb,0xb7,0xff,0xf6,0xe4,0xb7,0xf2,0xf9,0xf3,0xf2,0xf3]

private func upperStripName(scientific num: UInt8) -> UInt8 {
    return num ^ 151
}

/*: "The other hung up the phone" :*/
fileprivate let k_guidanceData:[Character] = ["T","h","e"," ","o","t","h","e"]
fileprivate let dreamJawPath:[Character] = ["r"," ","h","u","n","g"," "]
fileprivate let show_innerName:[Character] = ["u","p"," ","t","h","e"," ","p","h","o","n","e"]

/*: "type" :*/
fileprivate let dreamMeetingStr:String = "typstrength"

/*: "jumpUrl" :*/
fileprivate let show_verticalValue:[Character] = ["j","u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let m_exhibitData:String = "hurry gesture absenceCancel"

/*: "Get gold coins" :*/
fileprivate let mainSuperiorData:String = "Get goldseek across flesh action turn"
fileprivate let mPitchMsg:String = " coinsexport statement"

/*: "addBean" :*/
fileprivate let user_comprehensiveId:[Character] = ["a","d","d","B","e","a","n"]

/*: "refuseSex" :*/
fileprivate let user_greetMeasureName:String = "narrowefu"

/*: "onAcceptCall" :*/
fileprivate let dream_curveHikeCarrierIdent:String = "abs demonstrate team elseonAccep"
fileprivate let dream_observeUrl:String = "plan as solid citytCall"

/*: "acceptCall" :*/
fileprivate let main_designTitle:String = "firm evaluation resort one masteracceptC"
fileprivate let user_representMsg:[Character] = ["a","l","l"]

/*: "Calls are cancelled" :*/
fileprivate let m_selectContent:String = "Calls profile given"
fileprivate let m_scanDarkPath:[Character] = ["n","c","e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let main_planText:String = "The cabar error extended holiday"
fileprivate let noti_partnerFormat:[Character] = ["s"," "]
fileprivate let notiColumnMessage:String = "ENDED"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FourthViewController.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let mWarningUrl = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class FourthViewController: FictionalCharacterRecognizerDelegate {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: OthersChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: SmallPlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: GuideMiniView? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: SayThen? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        StackSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        StackSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        StackSocketManager.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: noti_atThroughPrimaryMessage.map{givingHap(numb: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! BagHasDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.curDot()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            throughCharm()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            askKind(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currObjectVC = nil
            StackSocketManager.shared.currObjectVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.upgrade()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        birthdayExit()
        //: setupSubViewsConstraint()
        swaddlingClothesPicture()
        //: initNotificationObserver()
        warningDoing()
        //: req_callGetUserInfo()
        wheelLike()
        //: timeoutRules()
        face()
        //: setUpBGMPlayer()
        straightforwardResume()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.naturalResourcesBubble()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if AppManager.share.appConfigMode.disableShootScreen {
            if AppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = TripUpThen(frame: CGRect(x: 0, y: 0, width: kGiftIdent, height: user_allMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isObject = false
        StackSocketManager.shared.isObject = false
        StackSocketManager.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.overCompare()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: FaceAlterVideoView = {
        //: let v = TalkingBGVideoView()
        let v = FaceAlterVideoView()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.disableBy(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: AttemptViewThen = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = AttemptViewThen(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: ValueComprehensiveThen = {
        //: let v = TalkingVideoWindowView()
        let v = ValueComprehensiveThen()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = kGiftIdent - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if SolidUntilLanguageManager.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: dreamNoSampleValue + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.supposed()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: BagChatManager = {
        //: let m = TalkingVideoChatManager()
        let m = BagChatManager()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension FourthViewController {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func wheelLike() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        BagChatManager.duty(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: showBeanValue.map{bondStyle(standard: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(mAnonymousAgoStr) + userInnerName.replacingOccurrences(of: "curve", with: "c"))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(showMayId) + app_actualContent.lowercased())].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(kStructureTitle) + String(dream_rowMonitorKey))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(app_runText.prefix(7)) + m_liveMsg.replacingOccurrences(of: "index", with: "l"))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(String(dreamNoMessage))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(userIllegalMsg))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.wit()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.viewFinder()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func viewFinder() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = ParentCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = ParentCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.ridCamera(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.connectPost(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension FourthViewController {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func throughCharm() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        SatelliteThen.keepDependMicrophone { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.changeManage(showMsg: kCaptureStr)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                StackSocketManager.shared.someoneDisable(info: [String(bytes: show_missFormat.map{$0^185}, encoding: .utf8)!: (String(k_differenceText.prefix(7)) + String(userRawLayExitIdent)), String(bytes: k_waterId.map{readingDrown(literal: $0)}, encoding: .utf8)!: [String(bytes: showBeanValue.map{bondStyle(standard: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.bilge()
                //: return
                return
            }

            //: self.startPreview()
            self.naturalResourcesBubble()
            //: self.stopBGM()
            self.databaseSection()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = SmallPlayer.birthEdit(type: .Response)
            //: self.startBGM()
            self.curDot()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func naturalResourcesBubble() {
        //: self.videoManager.startPreview()
        self.videoManager.readyAndWaitingGold()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func overCompare() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.databaseSection()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        CharacterDanmuManager.support()
        //: TalkingSocketManager.shared.callerDelegate = nil
        StackSocketManager.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        StackSocketManager.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        StackSocketManager.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func bilge(animated: Bool = true) {
        //: self.stopBGM()
        self.databaseSection()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func supposed() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.conclusionQuit()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.viewFinder()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func pureFirst() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.constraintControl()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.viewFinder()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.doingScheduleControl()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func doingScheduleControl() {
        //: guard AppManager.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard AppManager.share.loginUserMode.sex == AlongsideDigitalTableConvertible.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = SayThen()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.per()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension FourthViewController {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func warningDoing() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(glisteningNotif(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(connect(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: noti_beginIdent,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(heading(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: kOfLiveStr,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        mWarningUrl.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func glisteningNotif(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            bilge()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func heading(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard StackSocketManager.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        StackSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        StackSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        StackSocketManager.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.restartAvailable()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func connect(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.viewFinder()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension FourthViewController: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.changeManage(showMsg: (String(noti_enhanceUrl.prefix(6)) + "nterrup" + String(app_supposedFormat)))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                StackSocketManager.shared.someoneDisable(info: [String(bytes: show_missFormat.map{$0^185}, encoding: .utf8)!: (String(appVoiceButterflyStr.suffix(8))), String(bytes: k_waterId.map{readingDrown(literal: $0)}, encoding: .utf8)!: [String(bytes: user_subtleStr.map{roundOfGolf(local: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                StackSocketManager.shared.someoneDisable(info: [String(bytes: show_missFormat.map{$0^185}, encoding: .utf8)!: (String(m_hardwareIdent) + String(k_jumpUponItId)), String(bytes: k_waterId.map{readingDrown(literal: $0)}, encoding: .utf8)!: [String(bytes: user_subtleStr.map{roundOfGolf(local: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                StackSocketManager.shared.someoneDisable(info: [String(bytes: show_missFormat.map{$0^185}, encoding: .utf8)!: (String(k_differenceText.prefix(7)) + String(userRawLayExitIdent)), String(bytes: k_waterId.map{readingDrown(literal: $0)}, encoding: .utf8)!: [String(bytes: showBeanValue.map{bondStyle(standard: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.dismissInEnd()
                //: TalkingSocketManager.shared.currObjectVC = nil
                StackSocketManager.shared.currObjectVC = nil
            }
            //: popCurrentViewController()
            bilge()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension FourthViewController {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func straightforwardResume() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? QualityStrideable.Call : QualityStrideable.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = SmallPlayer.birthEdit(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func curDot() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.listLibrary()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func databaseSection() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.actualStop()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension FourthViewController {
    /// 超时定时器
    //: private func timeoutRules() {
    private func face() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? show_contentPath : self.chatModel?.outsideSimultaneouslyConstraint()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            island()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.island()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func island() {
        //: guard !TalkingSocketManager.shared.isObject else { return }
        guard !StackSocketManager.shared.isObject else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.changeManage(showMsg: (String(mEqualIdent)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: show_missFormat.map{$0^185}, encoding: .utf8)!: (String(m_hardwareIdent) + String(k_jumpUponItId)), String(bytes: k_waterId.map{readingDrown(literal: $0)}, encoding: .utf8)!: [String(bytes: user_subtleStr.map{roundOfGolf(local: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: show_missFormat.map{$0^185}, encoding: .utf8)!: (String(k_differenceText.prefix(7)) + String(userRawLayExitIdent)), String(bytes: k_waterId.map{readingDrown(literal: $0)}, encoding: .utf8)!: [String(bytes: showBeanValue.map{bondStyle(standard: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        StackSocketManager.shared.someoneDisable(info: socketDict)
        //: popCurrentViewController()
        bilge()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension FourthViewController {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func statement(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: user_subtleStr.map{roundOfGolf(local: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(app_busyText) + String(notiPlasticEntrySeatContent))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.recoverClear(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.twoCheck()
            //: createTalkRoom()
            pureFirst()
            //: self.stopBGM()
            self.databaseSection()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func askKind(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.conclusionQuit()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.disableBy(show: false)
        //: self.stopBGM()
        self.databaseSection()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.twoCheck()
        //: self.createTalkRoom()
        self.pureFirst()
    }
}

// MARK: - StuffCallerDelegate【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension FourthViewController: StuffCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func whiteData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(noti_angleIdent) + String(show_maxAnotherUnknownStr)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(mainGolfStr.lowercased() + String(showReStr.suffix(6)))].stringValue == AlongsideDigitalTableConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(mainRewardPath.prefix(6)) + " hun" + String(k_netQuickId)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.changeManage(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.dismissInEnd()
            //: TalkingSocketManager.shared.currObjectVC = nil
            StackSocketManager.shared.currObjectVC = nil
        }

        //: popCurrentViewController()
        bilge()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func putData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        askKind(logId: json[String(bytes: user_subtleStr.map{roundOfGolf(local: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func bringContext(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(main_comfortableName.prefix(5)) + String(main_belowKey))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(noti_angleIdent) + String(show_maxAnotherUnknownStr)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(show_materialData.suffix(5)) + show_sourceIdent.replacingOccurrences(of: "color", with: "ex"))].stringValue == AlongsideDigitalTableConvertible.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(mainRewardPath.prefix(6)) + " hun" + String(k_netQuickId)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if AppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if AppManager.share.appStatus == WrinkleSendable.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: main_partnerBedPath.map{upperStripName(scientific: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(k_guidanceData) + String(dreamJawPath) + String(show_innerName)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.changeManage(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.dismissInEnd()
            //: TalkingSocketManager.shared.currObjectVC = nil
            StackSocketManager.shared.currObjectVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.simply()
        //: popCurrentViewController()
        bilge()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func froth(msg: String, data: [String: Any]) {
        //: guard AppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard AppManager.share.appStatus == WrinkleSendable.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(dreamMeetingStr.replacingOccurrences(of: "strength", with: "e"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(show_verticalValue))] as? String else { return }
            //: let httpsUrl = AppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = AppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = OthersConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: PipSqueakReactiveCompatible.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            PipSqueakReactiveCompatible.share.pokeAt(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            DemonstrateSayAlertReactiveCompatible.rightUsed(title: nil,
                                       //: message: msg,
                                       message: msg,
                                       //: leftBtnTitle: "Cancel".localized,
                                       leftBtnTitle: (String(m_exhibitData.suffix(6))).localized,
                                       //: rightBtnTitle: "Get gold coins".localized) {
                                       rightBtnTitle: (String(mainSuperiorData.prefix(8)) + String(mPitchMsg.prefix(6))).localized)
            {
                //: TalkingAlertShow.hideAlert()
                DemonstrateSayAlertReactiveCompatible.paidVacation()
                //: } rightBlock: {
            } rightBlock: {
                //: PipSqueakReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                PipSqueakReactiveCompatible.share.halfHardware(clickEvent: userSaltName, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func voiceCommunication(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.notTransition(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func inmate(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(user_comprehensiveId))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.squareSituation(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.usTick(point: addBean)
        }
    }
}

// MARK: - CyclePartyDelegate【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension FourthViewController: CyclePartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func whiteReact(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(noti_angleIdent) + String(show_maxAnotherUnknownStr)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(user_greetMeasureName.replacingOccurrences(of: "narrow", with: "r") + "seSex")].stringValue == AlongsideDigitalTableConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(mainRewardPath.prefix(6)) + " hun" + String(k_netQuickId)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.changeManage(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.dismissInEnd()
            //: TalkingSocketManager.shared.currObjectVC = nil
            StackSocketManager.shared.currObjectVC = nil
        }

        //: popCurrentViewController()
        bilge()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func codex(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        askKind(logId: json[String(bytes: user_subtleStr.map{roundOfGolf(local: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - PresenceErrorDelegate【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension FourthViewController: PresenceErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func bring(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(k_differenceText.prefix(7)) + String(userRawLayExitIdent)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.changeManage(showMsg: (String(mEqualIdent)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.dismissInEnd()
                //: TalkingSocketManager.shared.currObjectVC = nil
                StackSocketManager.shared.currObjectVC = nil
            }
            //: popCurrentViewController()
            bilge()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(dream_curveHikeCarrierIdent.suffix(7)) + String(dream_observeUrl.suffix(5))) && errorNo == 101 { // 接听通话时，余额不足
            //: guard AppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard AppManager.share.appStatus == WrinkleSendable.normal.rawValue else { return }
            //: PipSqueakReactiveCompatible.share.func__jumpToWebRecharge(sufficient: false)
            PipSqueakReactiveCompatible.share.halfHardware(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(dream_curveHikeCarrierIdent.suffix(7)) + String(dream_observeUrl.suffix(5))) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.changeManage(showMsg: msg)
            //: popCurrentViewController()
            bilge()
        }
    }
}

// MARK: - BucketViewDelegate【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension FourthViewController: BucketViewDelegate {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func sink() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        StackSocketManager.shared.someoneDisable(info: [String(bytes: show_missFormat.map{$0^185}, encoding: .utf8)!: (String(main_designTitle.suffix(7)) + String(user_representMsg)), String(bytes: k_waterId.map{readingDrown(literal: $0)}, encoding: .utf8)!: [String(bytes: showBeanValue.map{bondStyle(standard: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func atTheSameTime() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.changeManage(showMsg: (String(m_selectContent.prefix(6)) + "are ca" + String(m_scanDarkPath)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            StackSocketManager.shared.someoneDisable(info: [String(bytes: show_missFormat.map{$0^185}, encoding: .utf8)!: (String(m_hardwareIdent) + String(k_jumpUponItId)), String(bytes: k_waterId.map{readingDrown(literal: $0)}, encoding: .utf8)!: [String(bytes: user_subtleStr.map{roundOfGolf(local: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.changeManage(showMsg: (String(m_selectContent.prefix(6)) + "are ca" + String(m_scanDarkPath)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            StackSocketManager.shared.someoneDisable(info: [String(bytes: show_missFormat.map{$0^185}, encoding: .utf8)!: (String(k_differenceText.prefix(7)) + String(userRawLayExitIdent)), String(bytes: k_waterId.map{readingDrown(literal: $0)}, encoding: .utf8)!: [String(bytes: showBeanValue.map{bondStyle(standard: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.changeManage(showMsg: (String(main_planText.prefix(6)) + "ll ha" + String(noti_partnerFormat) + notiColumnMessage.lowercased()).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            StackSocketManager.shared.someoneDisable(info: [String(bytes: show_missFormat.map{$0^185}, encoding: .utf8)!: (String(appVoiceButterflyStr.suffix(8))), String(bytes: k_waterId.map{readingDrown(literal: $0)}, encoding: .utf8)!: [String(bytes: user_subtleStr.map{roundOfGolf(local: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        bilge()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func lightFront(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.track(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func serviceEye(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.disableBy(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.generalPicture(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.viewFinder()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func shRevenue() {
        //: TalkingSocketManager.shared.currObjectVC = self
        StackSocketManager.shared.currObjectVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = GuideMiniView.skirt()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.straight()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.reload()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.decideBroad()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        bilge()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func until() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension FourthViewController {
    /// 添加视图
    //: private func setupSubviews() {
    private func birthdayExit() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.duringHome(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func swaddlingClothesPicture() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
