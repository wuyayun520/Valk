
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dreamDistantText:[UInt8] = [0x8e,0x93,0x8e,0x99,0x4d,0x88,0x94,0x89,0x8a,0x97,0x5f,0x4e,0x45,0x8d,0x86,0x98,0x45,0x93,0x94,0x99,0x45,0x87,0x8a,0x8a,0x93,0x45,0x8e,0x92,0x95,0x91,0x8a,0x92,0x8a,0x93,0x99,0x8a,0x89]

fileprivate func pricePoint(trap num: UInt8) -> UInt8 {
    let value = Int(num) - 37
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let user_areaData:[UInt8] = [0x82,0x9e,0x93]

private func signalColorAttribute(sense num: UInt8) -> UInt8 {
    return num ^ 247
}

/*: "headPic" :*/
fileprivate let notiIncludeAgentTitle:String = "support interestedheadPic"

/*: "nickname" :*/
fileprivate let kCommandUrl:[Character] = ["n","i","c","k","n"]
fileprivate let dreamTieId:[Character] = ["a","m","e"]

/*: "birthday" :*/
fileprivate let k_assertTitle:String = "BIRTHDAY"

/*: "videoUrl" :*/
fileprivate let m_midName:[Character] = ["v","i","d","e","o","U","r","l"]

/*: "location" :*/
fileprivate let show_dataMsg:[Character] = ["l"]
fileprivate let userExploreSlimUrl:String = "ocatshen"

/*: "cmd" :*/
fileprivate let kFaultTitle:[UInt8] = [0x97,0xa1,0x98]

fileprivate func historySession(section num: UInt8) -> UInt8 {
    let value = Int(num) - 52
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "refuseCall" :*/
fileprivate let user_mergeFormalUrl:String = "former"
fileprivate let main_shoulderData:String = "efappropriatese"

/*: "data" :*/
fileprivate let dream_publishTitle:[UInt8] = [0x87,0x82,0x97,0x82]

private func paperBulk(hint num: UInt8) -> UInt8 {
    return num ^ 227
}

/*: "Call interruption" :*/
fileprivate let m_lowText:String = "Call intenative name progressive literal"
fileprivate let appEffButterflyPath:[Character] = ["r"]
fileprivate let showDiskSuccessfulPath:String = "rupjoinon"

/*: "stopCall" :*/
fileprivate let noti_retainIslandUrl:String = "stopCstretch favorite package appeal"
fileprivate let appRemainMsg:[Character] = ["a","l","l"]

/*: "logId" :*/
fileprivate let showImmuneIdent:[UInt8] = [0x64,0x49,0x67,0x6f,0x6c]

/*: "cancelCall" :*/
fileprivate let main_timingTitle:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "No answer" :*/
fileprivate let notiAlterMessage:[Character] = ["N","o"," ","a"]
fileprivate let main_feedbackIdent:String = "nsmiddler"

/*: "duration" :*/
fileprivate let userProgressUrl:String = "dbehindation"

/*: "She has hung up" :*/
fileprivate let m_giftTermPurposePath:[Character] = ["S","h","e"," ","h"]
fileprivate let main_rootMessage:String = "as hudemonstrate scale others"

/*: "cancelSex" :*/
fileprivate let showValueContent:[Character] = ["c","a","n","c","e","l","S","e","x"]

/*: "He has hung up" :*/
fileprivate let showWithoutPath:String = "He hobserver operate random kind"
fileprivate let mThereIdent:String = "sodium occur beyondung up"

/*: "endType" :*/
fileprivate let main_coalKey:String = "endTypfor wife track day"
fileprivate let showReText:String = "feature"

/*: "stopSex" :*/
fileprivate let m_pendingPath:String = "stopSshirt top"
fileprivate let noti_securityTitle:String = "eshape"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let dreamCommandValue:[UInt8] = [0xe8,0xd,0x12,0x14,0x5,0x5,0x8,0x2,0x8,0x4,0xd,0x13,0xbf,0x6,0xe,0xb,0x3,0xbf,0x2,0xe,0x8,0xd,0x12,0xcb,0xbf,0x2,0x0,0xb,0xb,0xbf,0x7,0x0,0x12,0xbf,0x4,0xd,0x3,0x4,0x3]

fileprivate func sheKick(ok num: UInt8) -> UInt8 {
    let value = Int(num) + 97
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "The other hung up the phone" :*/
fileprivate let k_tensionId:[Character] = ["T","h","e"," ","o","t","h","e","r"," ","h","u","n","g"," ","u"]
fileprivate let userBucketValue:String = "p thcombine op decision kind"
fileprivate let m_charmPerformValue:[Character] = ["e"," ","p","h","o","n","e"]

/*: "type" :*/
fileprivate let show_mutualMessage:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let k_contextIdentityStr:[Character] = ["j","u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let kUnderName:String = "numb invite origin do outerCancel"

/*: "Get gold coins" :*/
fileprivate let mMoonTitle:[Character] = ["G","e","t"," ","g","o","l","d"," ","c","o","i"]
fileprivate let k_democraticFormat:String = "nsuccess"

/*: "addBean" :*/
fileprivate let main_appealFormat:String = "addBeapad used calculation communicate hike"
fileprivate let k_helloUrl:[Character] = ["n"]

/*: "refuseSex" :*/
fileprivate let mApplyDepthId:String = "refwave"

/*: "onAcceptCall" :*/
fileprivate let user_connectionMsg:[Character] = ["o","n","A","c","c","e","p","t","C","a","l"]
fileprivate let showWellTitle:[Character] = ["l"]

/*: "acceptCall" :*/
fileprivate let main_informText:String = "accblankp"

/*: "Calls are cancelled" :*/
fileprivate let dream_kindGrassKey:[Character] = ["C","a","l","l","s"," ","a","r","e"," ","c","a","n","c","e","l","l","e"]
fileprivate let show_nativeCountFactorId:String = "hundred"

/*: "The call has ended" :*/
fileprivate let m_goingName:String = "version except systemThe "
fileprivate let notiQuickData:String = "help magnitude reward representation split has "

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StackErrorDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let k_versionData = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class StackErrorDelegate: FictionalCharacterRecognizerDelegate {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: OthersChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: SmallPlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: RichLaborReactiveCompatible? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        StackSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        StackSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        StackSocketManager.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dreamDistantText.map{pricePoint(trap: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! BagHasDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.general()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            appearAuth()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currObjectVC = nil
            StackSocketManager.shared.currObjectVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.querySuite()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        threeLaunch()
        //: setupSubViewsConstraint()
        miss()
        //: initNotificationObserver()
        controlProfile()
        //: req_callGetUserInfo()
        focusFor()
        //: timeoutRules()
        cover()
        //: resumeTalk()
        dallyOnerFill()
        //: setUpBGMPlayer()
        attribute()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if AppManager.share.appConfigMode.disableShootScreen {
            if AppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = TripUpThen(frame: CGRect(x: 0, y: 0, width: kGiftIdent, height: user_allMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isObject = false
        StackSocketManager.shared.isObject = false
        StackSocketManager.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.orSo()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: CharacterThen = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = CharacterThen(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: EveryChatManager = {
        //: let m = TalkingVoiceChatManager()
        let m = EveryChatManager()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension StackErrorDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func focusFor() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        BagChatManager.duty(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: user_areaData.map{signalColorAttribute(sense: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(notiIncludeAgentTitle.suffix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(kCommandUrl) + String(dreamTieId))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(k_assertTitle.lowercased())].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(m_midName))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(show_dataMsg) + userExploreSlimUrl.replacingOccurrences(of: "she", with: "io"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.roleClick()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension StackErrorDelegate {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func appearAuth() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        SatelliteThen.permit(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.changeManage(showMsg: kCaptureStr)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                StackSocketManager.shared.someoneDisable(info: [String(bytes: kFaultTitle.map{historySession(section: $0)}, encoding: .utf8)!: (user_mergeFormalUrl.replacingOccurrences(of: "former", with: "r") + main_shoulderData.replacingOccurrences(of: "appropriate", with: "u") + "Call"), String(bytes: dream_publishTitle.map{paperBulk(hint: $0)}, encoding: .utf8)!: [String(bytes: user_areaData.map{signalColorAttribute(sense: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.tapView()
                //: return
                return
            }

            //: self.stopBGM()
            self.partyBgm()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = SmallPlayer.birthEdit(type: .Response)
            //: self.startBGM()
            self.general()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func orSo() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.partyBgm()
        //: TalkingSocketManager.shared.callerDelegate = nil
        StackSocketManager.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        StackSocketManager.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        StackSocketManager.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func tapView(animated: Bool = true) {
        //: self.stopBGM()
        self.partyBgm()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func butterfly() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.underHeart(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func dallyOnerFill() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            butterfly()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension StackErrorDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func controlProfile() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(person(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(betweenFace(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: kOfLiveStr,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        k_versionData.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func person(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            tapView()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func betweenFace(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard StackSocketManager.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        StackSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        StackSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        StackSocketManager.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.talk(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension StackErrorDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.changeManage(showMsg: (String(m_lowText.prefix(9)) + String(appEffButterflyPath) + showDiskSuccessfulPath.replacingOccurrences(of: "join", with: "ti")).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                StackSocketManager.shared.someoneDisable(info: [String(bytes: kFaultTitle.map{historySession(section: $0)}, encoding: .utf8)!: (String(noti_retainIslandUrl.prefix(5)) + String(appRemainMsg)), String(bytes: dream_publishTitle.map{paperBulk(hint: $0)}, encoding: .utf8)!: [String(bytes: showImmuneIdent.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                StackSocketManager.shared.someoneDisable(info: [String(bytes: kFaultTitle.map{historySession(section: $0)}, encoding: .utf8)!: (String(main_timingTitle)), String(bytes: dream_publishTitle.map{paperBulk(hint: $0)}, encoding: .utf8)!: [String(bytes: showImmuneIdent.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                StackSocketManager.shared.someoneDisable(info: [String(bytes: kFaultTitle.map{historySession(section: $0)}, encoding: .utf8)!: (user_mergeFormalUrl.replacingOccurrences(of: "former", with: "r") + main_shoulderData.replacingOccurrences(of: "appropriate", with: "u") + "Call"), String(bytes: dream_publishTitle.map{paperBulk(hint: $0)}, encoding: .utf8)!: [String(bytes: user_areaData.map{signalColorAttribute(sense: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.dismissInEnd()
                //: TalkingSocketManager.shared.currObjectVC = nil
                StackSocketManager.shared.currObjectVC = nil
            }
            //: popCurrentViewController()
            tapView()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.listener(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension StackErrorDelegate {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func attribute() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? QualityStrideable.Call : QualityStrideable.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = SmallPlayer.birthEdit(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func general() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.listLibrary()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func partyBgm() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.actualStop()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension StackErrorDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func cover() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? show_contentPath : self.chatModel?.outsideSimultaneouslyConstraint()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            shield()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.shield()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func shield() {
        //: guard !TalkingSocketManager.shared.isObject else { return }
        guard !StackSocketManager.shared.isObject else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.changeManage(showMsg: (String(notiAlterMessage) + main_feedbackIdent.replacingOccurrences(of: "middle", with: "we")).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: kFaultTitle.map{historySession(section: $0)}, encoding: .utf8)!: (String(main_timingTitle)), String(bytes: dream_publishTitle.map{paperBulk(hint: $0)}, encoding: .utf8)!: [String(bytes: showImmuneIdent.reversed(), encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: kFaultTitle.map{historySession(section: $0)}, encoding: .utf8)!: (user_mergeFormalUrl.replacingOccurrences(of: "former", with: "r") + main_shoulderData.replacingOccurrences(of: "appropriate", with: "u") + "Call"), String(bytes: dream_publishTitle.map{paperBulk(hint: $0)}, encoding: .utf8)!: [String(bytes: user_areaData.map{signalColorAttribute(sense: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        StackSocketManager.shared.someoneDisable(info: socketDict)
        //: popCurrentViewController()
        tapView()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension StackErrorDelegate {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func collectionDown(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: showImmuneIdent.reversed(), encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(userProgressUrl.replacingOccurrences(of: "behind", with: "ur"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.queryedFor(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.settle()
            //: createTalkRoom()
            butterfly()
            //: self.stopBGM()
            self.partyBgm()
        }
    }
}

// MARK: - StuffCallerDelegate【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension StackErrorDelegate: StuffCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func whiteData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(m_giftTermPurposePath) + String(main_rootMessage.prefix(5)) + "ng up").localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(showValueContent))].stringValue == AlongsideDigitalTableConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(showWithoutPath.prefix(4)) + "as h" + String(mThereIdent.suffix(6))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.changeManage(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.dismissInEnd()
            //: TalkingSocketManager.shared.currObjectVC = nil
            StackSocketManager.shared.currObjectVC = nil
        }

        //: popCurrentViewController()
        tapView()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func putData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: showImmuneIdent.reversed(), encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.partyBgm()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.settle()
        //: self.createTalkRoom()
        self.butterfly()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func bringContext(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(main_coalKey.prefix(6)) + showReText.replacingOccurrences(of: "feature", with: "e"))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(m_giftTermPurposePath) + String(main_rootMessage.prefix(5)) + "ng up").localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(m_pendingPath.prefix(5)) + noti_securityTitle.replacingOccurrences(of: "shape", with: "x"))].stringValue == AlongsideDigitalTableConvertible.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(showWithoutPath.prefix(4)) + "as h" + String(mThereIdent.suffix(6))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if AppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if AppManager.share.appStatus == WrinkleSendable.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: dreamCommandValue.map{sheKick(ok: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(k_tensionId) + String(userBucketValue.prefix(4)) + String(m_charmPerformValue)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.changeManage(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.dismissInEnd()
            //: TalkingSocketManager.shared.currObjectVC = nil
            StackSocketManager.shared.currObjectVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.answerCan()
        //: self.popCurrentViewController()
        self.tapView()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: user_collectionStr, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func froth(msg: String, data: [String: Any]) {
        //: guard AppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard AppManager.share.appStatus == WrinkleSendable.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(show_mutualMessage))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(k_contextIdentityStr))] as? String else { return }
            //: let httpsUrl = AppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = AppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = OthersConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: PipSqueakReactiveCompatible.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            PipSqueakReactiveCompatible.share.pokeAt(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            DemonstrateSayAlertReactiveCompatible.rightUsed(title: nil,
                                       //: message: msg,
                                       message: msg,
                                       //: leftBtnTitle: "Cancel".localized,
                                       leftBtnTitle: (String(kUnderName.suffix(6))).localized,
                                       //: rightBtnTitle: "Get gold coins".localized) {
                                       rightBtnTitle: (String(mMoonTitle) + k_democraticFormat.replacingOccurrences(of: "success", with: "s")).localized)
            {
                //: TalkingAlertShow.hideAlert()
                DemonstrateSayAlertReactiveCompatible.paidVacation()
                //: } rightBlock: {
            } rightBlock: {
                //: PipSqueakReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                PipSqueakReactiveCompatible.share.halfHardware(clickEvent: userSaltName, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func inmate(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(main_appealFormat.prefix(6)) + String(k_helloUrl))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.corner(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.workbag(point: addBean)
        }
    }
}

// MARK: - CyclePartyDelegate【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension StackErrorDelegate: CyclePartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func whiteReact(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(m_giftTermPurposePath) + String(main_rootMessage.prefix(5)) + "ng up").localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(mApplyDepthId.replacingOccurrences(of: "wave", with: "us") + "eSex")].stringValue == AlongsideDigitalTableConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(showWithoutPath.prefix(4)) + "as h" + String(mThereIdent.suffix(6))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.changeManage(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.dismissInEnd()
            //: TalkingSocketManager.shared.currObjectVC = nil
            StackSocketManager.shared.currObjectVC = nil
        }
        //: popCurrentViewController()
        tapView()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func codex(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: showImmuneIdent.reversed(), encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.partyBgm()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.settle()
        //: self.createTalkRoom()
        self.butterfly()
    }
}

// MARK: - PresenceErrorDelegate【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension StackErrorDelegate: PresenceErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func bring(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (user_mergeFormalUrl.replacingOccurrences(of: "former", with: "r") + main_shoulderData.replacingOccurrences(of: "appropriate", with: "u") + "Call") { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.changeManage(showMsg: (String(notiAlterMessage) + main_feedbackIdent.replacingOccurrences(of: "middle", with: "we")).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.dismissInEnd()
                //: TalkingSocketManager.shared.currObjectVC = nil
                StackSocketManager.shared.currObjectVC = nil
            }
            //: self.popCurrentViewController()
            self.tapView()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(user_connectionMsg) + String(showWellTitle)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard AppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard AppManager.share.appStatus == WrinkleSendable.normal.rawValue else { return }
            //: PipSqueakReactiveCompatible.share.func__jumpToWebRecharge(sufficient: false)
            PipSqueakReactiveCompatible.share.halfHardware(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(user_connectionMsg) + String(showWellTitle)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.changeManage(showMsg: msg)
            //: self.popCurrentViewController()
            self.tapView()
        }
    }
}

// MARK: - DumpDamnViewDelegate【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension StackErrorDelegate: DumpDamnViewDelegate {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func theCommand() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        StackSocketManager.shared.someoneDisable(info: [String(bytes: kFaultTitle.map{historySession(section: $0)}, encoding: .utf8)!: (main_informText.replacingOccurrences(of: "blank", with: "e") + "tCall"), String(bytes: dream_publishTitle.map{paperBulk(hint: $0)}, encoding: .utf8)!: [String(bytes: user_areaData.map{signalColorAttribute(sense: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func agreeAcross() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.changeManage(showMsg: (String(dream_kindGrassKey) + show_nativeCountFactorId.replacingOccurrences(of: "hundred", with: "d")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            StackSocketManager.shared.someoneDisable(info: [String(bytes: kFaultTitle.map{historySession(section: $0)}, encoding: .utf8)!: (String(main_timingTitle)), String(bytes: dream_publishTitle.map{paperBulk(hint: $0)}, encoding: .utf8)!: [String(bytes: showImmuneIdent.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.changeManage(showMsg: (String(dream_kindGrassKey) + show_nativeCountFactorId.replacingOccurrences(of: "hundred", with: "d")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            StackSocketManager.shared.someoneDisable(info: [String(bytes: kFaultTitle.map{historySession(section: $0)}, encoding: .utf8)!: (user_mergeFormalUrl.replacingOccurrences(of: "former", with: "r") + main_shoulderData.replacingOccurrences(of: "appropriate", with: "u") + "Call"), String(bytes: dream_publishTitle.map{paperBulk(hint: $0)}, encoding: .utf8)!: [String(bytes: user_areaData.map{signalColorAttribute(sense: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.changeManage(showMsg: (String(m_goingName.suffix(4)) + "call" + String(notiQuickData.suffix(5)) + "ended").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            StackSocketManager.shared.someoneDisable(info: [String(bytes: kFaultTitle.map{historySession(section: $0)}, encoding: .utf8)!: (String(noti_retainIslandUrl.prefix(5)) + String(appRemainMsg)), String(bytes: dream_publishTitle.map{paperBulk(hint: $0)}, encoding: .utf8)!: [String(bytes: showImmuneIdent.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.answerCan()
        //: self.popCurrentViewController()
        self.tapView()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func smartMute(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.enableicial(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func qualityDown(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.design(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func beforeThird() {
        //: TalkingSocketManager.shared.currObjectVC = self
        StackSocketManager.shared.currObjectVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = RichLaborReactiveCompatible.strokeBy()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.decideBroad()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.busy()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.temp(talkingTime)
        //: popCurrentViewController()
        tapView()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func shouldCounteractionRefreshViewTime(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.temp(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func compete(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.compoundOfArr(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension StackErrorDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func threeLaunch() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func miss() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
