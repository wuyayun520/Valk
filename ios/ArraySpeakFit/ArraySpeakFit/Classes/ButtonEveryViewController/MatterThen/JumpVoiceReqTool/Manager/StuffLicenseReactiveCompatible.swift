
//: Declare String Begin

/*: "PARTY_STATUS_CHANGE_NOTIFICATION" :*/
fileprivate let dream_guestData:[UInt8] = [0x82,0x93,0x80,0x86,0x8b,0x8d,0x81,0x86,0x93,0x86,0x87,0x81,0x8d,0x91,0x9a,0x93,0x9c,0x95,0x97,0x8d,0x9c,0x9d,0x86,0x9b,0x94,0x9b,0x91,0x93,0x86,0x9b,0x9d,0x9c]

/*: "redirectUrl" :*/
fileprivate let showScriptPath:[Character] = ["r","e","d","i","r","e","c","t","U"]
fileprivate let mainArrayMsg:String = "rcontent"

/*: "VOICEROOM: 进房模型解析失败。" :*/
fileprivate let mainVoiceInvestThenFormat:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","进","房","模"]
fileprivate let kCheckIdent:String = "\u{578b}解析失\u{8d25}。"

/*: "Party room exception" :*/
fileprivate let notiGiveMsg:String = "max fatalParty"
fileprivate let k_customIdent:[Character] = [" ","r","o","o","m"," ","e","x","c","e","p","t","i","o","n"]

/*: "male" :*/
fileprivate let main_outsideFallName:[UInt8] = [0x65,0x6c,0x61,0x6d]

/*: "female" :*/
fileprivate let dream_onMsg:String = "fballotmalballot"

/*: "VOICEROOM: 群聊进房失败。groupId:  :*/
fileprivate let dream_exclusiveIdent:[Character] = ["V","O","I","C","E","R"]
fileprivate let appTooCalculateUrl:String = "progress voiceOOM: "
fileprivate let m_tubeInsertValue:[Character] = ["。","g","r","o","u","p","I","d",":"," "]

/*: "VOICEROOM: 切房模型解析失败。" :*/
fileprivate let k_pendingTitle:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","\u{5207}","房","模","型","\u{89e3}","\u{6790}","失","败"]
fileprivate let dreamGreetKey:[Character] = ["。"]

/*: "VOICEROOM: 群聊切房失败。groupId:  :*/
fileprivate let k_softenBalanceTitle:String = "VsquareICE"
fileprivate let showPleaseIdent:String = "room"
fileprivate let show_howData:String = "切房失\u{8d25}。g"

/*: "errCode" :*/
fileprivate let mainYourShakeTitle:[UInt8] = [0xd3,0xc4,0xc4,0xf5,0xd9,0xd2,0xd3]

/*: "custom_kickout_message" :*/
fileprivate let mainDisableSegmentId:[Character] = ["c","u","s","t","o","m","_","k","i","c","k","o","u","t","_","m","e"]
fileprivate let dream_marginMsg:String = "skullskullage"

/*: "closeRoom" :*/
fileprivate let k_waitValue:String = "crecoverse"

/*: "adminCloseRoom" :*/
fileprivate let mStudySharpMessage:String = "admmerge"
fileprivate let kCoordinatorUrl:String = "equity average cityeRoom"

/*: "The room is closed, please go to another room" :*/
fileprivate let user_peelBarrierId:[UInt8] = [0x78,0x44,0x49,0xc,0x5e,0x43,0x43,0x41,0xc,0x45,0x5f,0xc,0x4f,0x40,0x43,0x5f,0x49,0x48,0x0,0xc,0x5c,0x40,0x49,0x4d,0x5f,0x49,0xc,0x4b,0x43,0xc,0x58,0x43,0xc,0x4d,0x42,0x43,0x58,0x44,0x49,0x5e,0xc,0x5e,0x43,0x43,0x41]

private func femaleParent(population num: UInt8) -> UInt8 {
    return num ^ 44
}

/*: "You were kicked out of the room" :*/
fileprivate let mainApplyKey:[UInt8] = [0x9d,0xab,0xb1,0xe4,0xb3,0xa1,0xb6,0xa1,0xe4,0xaf,0xad,0xa7,0xaf,0xa1,0xa0,0xe4,0xab,0xb1,0xb0,0xe4,0xab,0xa2,0xe4,0xb0,0xac,0xa1,0xe4,0xb6,0xab,0xab,0xa9]

private func tearUniversal(drag num: UInt8) -> UInt8 {
    return num ^ 196
}

/*: "VOICEROOM：进房失败，roomID:  :*/
fileprivate let app_detailSlimId:[Character] = ["V","O","I","C","E","R","O","O","M","：","进","\u{623f}","失","败","，","r","o","o","m","I","D"]
fileprivate let show_alwaysValue:[Character] = [":"," "]

/*: , error:  :*/
fileprivate let user_photoUrl:[Character] = [","," ","e","r","r","o","r"]
fileprivate let main_automaticRemarkPatterPath:String = ": "

/*: , reason:  :*/
fileprivate let noti_mixTitle:[Character] = [","," ","r","e","a","s"]
fileprivate let mSecureUrl:[Character] = ["o","n",":"," "]

/*: , extendedData:  :*/
fileprivate let dream_colorPath:[Character] = [","," ","e","x","t","e","n","d","e","d","D","a","t","a"]
fileprivate let m_enthusiasmMessage:String = "letter "

/*: "VOICEROOM: 当前用户推流失败，roomID:  :*/
fileprivate let userOperateStr:String = "VOImin"
fileprivate let app_pastData:String = "M: 当\u{524d}用"
fileprivate let notiPriorityKey:String = "dream shall mic audience capability败，roo"
fileprivate let show_tripUrl:String = "mID: absence multiple anyone"

/*: , errorCode:  :*/
fileprivate let m_speculateMessage:String = ", ersibling movie appeal note"
fileprivate let show_simultaneouslyRunMsg:String = "ode:fusion black number"
fileprivate let dreamCancelUrl:String = "attribute"

/*: , streamID:  :*/
fileprivate let k_plotLightStr:String = "when owner supportive path, strea"
fileprivate let user_fatalText:[Character] = ["m","I","D",":"," "]

/*: "An error occurred, please try again" :*/
fileprivate let m_announcementKey:[UInt8] = [0x10,0x3d,0xef,0x34,0x41,0x41,0x3e,0x41,0xef,0x3e,0x32,0x32,0x44,0x41,0x41,0x34,0x33,0xfb,0xef,0x3f,0x3b,0x34,0x30,0x42,0x34,0xef,0x43,0x41,0x48,0xef,0x30,0x36,0x30,0x38,0x3d]

fileprivate func sendSub(conversion num: UInt8) -> UInt8 {
    let value = Int(num) + 49
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "VOICEROOM: 拉流失败，roomID:  :*/
fileprivate let mMenuSupportDigitalUrl:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","拉"]
fileprivate let m_assetName:String = "流失败，rowood remind last to"

/*: "VOICEROOM: 信令解析失败，command:  :*/
fileprivate let kHusbandCreationContent:String = "VOexecuteCE"
fileprivate let main_checkSharpIdent:String = " \u{4fe1}令解"
fileprivate let kLatSlimName:String = "oresultan"
fileprivate let noti_simplyMsg:[Character] = ["d",":"," "]

/*: , roomID:  :*/
fileprivate let userQuoteEngageFormat:String = ", rooam financial basic"
fileprivate let mainPerKey:String = " "

/*: "data" :*/
fileprivate let userWorldFormat:String = "dbelowtbelow"

/*: "position" :*/
fileprivate let mainPhoneFloorName:[Character] = ["p","o","s"]
fileprivate let dreamIndigenousKey:String = "cornertcorneron"

/*: "xxxx--  :*/
fileprivate let dream_perfectFormat:String = "xxxx-- help play statement"

/*: "opType" :*/
fileprivate let showAngleContent:[Character] = ["o","p","T","y","p"]
fileprivate let mainResortText:String = "E"

/*: "party:changeMike" :*/
fileprivate let main_agentContent:String = "engage darkparty:"
fileprivate let mScreamUrl:[Character] = ["e","M","i","k","e"]

/*: "type" :*/
fileprivate let userStripMsg:String = "tyshirt"

/*: "toPosition" :*/
fileprivate let noti_devicePath:[Character] = ["t","o","P","o","s","i","t"]
fileprivate let app_relationKey:[Character] = ["i","o","n"]

/*: "party:adminChangeMike" :*/
fileprivate let k_graceKey:String = "added customerparty:"
fileprivate let kDailyStr:String = "denydmin"
fileprivate let m_dreamSearchValue:[Character] = ["C","h","a","n","g","e","M","i","k","e"]

/*: "You were kicked off the seat" :*/
fileprivate let dream_administrativePath:String = "ambitious givingYou "
fileprivate let mainStandardValue:String = "icked ohurry remind edge fence"
fileprivate let appStrokeGivenContent:String = "please play thanksff t"

/*: "party:changeCoin" :*/
fileprivate let show_universalName:String = "PART"
fileprivate let mMixPath:[Character] = ["n","g","e","C","o","i","n"]

/*: "party:closeStream" :*/
fileprivate let user_roughlyJawValue:[Character] = ["p","a","r","t","y"]
fileprivate let app_expectedSecretPath:String = ":closesweep access interval"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StuffLicenseReactiveCompatible.swift
//  Adjust
//
//  Created by DouXiu on 2024/9/12.
//

//: import ZegoExpressEngine
import ZegoExpressEngine

/// 语聊房状态变更通知
//: public let PARTY_STATUS_CHANGE_NOTIFICATION = NSNotification.Name(rawValue: "PARTY_STATUS_CHANGE_NOTIFICATION")
public let noti_endMessage = NSNotification.Name(rawValue: String(bytes: dream_guestData.map{$0^210}, encoding: .utf8)!)
/// 麦位数
//: public let MIKE_SEAT_COUNT = 8
public let main_numberKey = 8

/// 语聊房角色
//: enum VoiceRoomRole: Int {
enum GlobuleSum: Int {
    //: case host = 1
    case host = 1 // 房主
    //: case audience = 2
    case audience = 2 // 观众
    //: case manager = 4
    case manager = 4 // 管理员
}

//: class TalkingVoiceRoomManager: NSObject {
class StuffLicenseReactiveCompatible: NSObject {
    /// 语聊房模型
    //: private(set) var partyModel = TalkingVoiceRoomModel()
    private(set) var partyModel = MoveModel()
    /// 是否正在语聊房中（状态变更时发送通知）
    //: private(set) var isParty: Bool = false {
    private(set) var isParty: Bool = false {
        //: didSet {
        didSet {
            //: NotificationCenter.default.post(name: PARTY_STATUS_CHANGE_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: noti_endMessage, object: self)
        }
    }

    /// 是否小窗口模式
    //: private(set) var isSmallMode: Bool = false
    private(set) var isSmallMode: Bool = false
    /// singleton
    //: private static var _instance: TalkingVoiceRoomManager?
    private static var _instance: StuffLicenseReactiveCompatible?
    // 语聊房控制器
    //: private var voiceRoomVC: TalkingVoiceRoomViewController?
    private var voiceRoomVC: CreamThen?
    /// 麦位信息字典 [麦位 0~7：麦位模型]
    //: private var mikePositionDict = [Int: TalkingMikeListItemModel]()
    private var mikePositionDict = [Int: PerformanceMeasurable]()
    //: private let mplock = NSLock()
    private let mplock = NSLock()
    /// 正在进房请求中（防止多次调用）
    //: private var isEnterRoomReq = false
    private var isEnterRoomReq = false
    /// 麦克风操作请求中（防止多次调用）
    //: private var isMicReq = false
    private var isMicReq = false

    //: override init() {
    override init() {
        //: super.init()
        super.init()
        //: self.mikePositionDict = initMikePosition()
        self.mikePositionDict = withoutPosition()
    }

    //: class func shared() -> TalkingVoiceRoomManager {
    class func iconPoint() -> StuffLicenseReactiveCompatible {
        //: guard _instance != nil else {
        guard _instance != nil else {
            //: _instance = TalkingVoiceRoomManager()
            _instance = StuffLicenseReactiveCompatible()
            //: return _instance!
            return _instance!
        }
        //: return _instance!
        return _instance!
    }

    //: class func destroy() {
    class func padlock() {
        //: if TalkingVoiceRoomManager._instance != nil {
        if StuffLicenseReactiveCompatible._instance != nil {
            //: TalkingVoiceRoomManager._instance = nil
            StuffLicenseReactiveCompatible._instance = nil
        }
    }

    // MARK: - Lazy Load

    // 语聊房小窗口悬浮窗
    //: private lazy var miniView: TalkingVoiceRoomMiniView = {
    private lazy var miniView: ExecMiniView = {
        //: let mini = TalkingVoiceRoomMiniView.buildLiveMiniView()
        let mini = ExecMiniView.beanAboveView()
        //: mini.isHidden = true
        mini.isHidden = true
        //: mini.tapGestureBlock = { [weak self] in
        mini.tapGestureBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.voiceRoom_goback()
            self.vocalisation()
        }
        //: return mini
        return mini
        //: }()
    }()
}

// MARK: - 操作麦位请求

//: extension TalkingVoiceRoomManager {
extension StuffLicenseReactiveCompatible {
    /// 房主/观众 麦位操作请求
    /// - Parameters:
    ///   - type: 1 上麦，2 下麦，3 切麦，4 开麦，5 闭麦
    ///   - position: 麦位
    ///   - toPosition: 目标麦位（切麦传值）
    //: func voiceRoom_reqChangeMike(type: Int, position: Int, toPosition: Int? = nil) {
    func connectPosition(type: Int, position: Int, toPosition: Int? = nil) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_changeMike(roomId: partyModel.roomId,
        JumpVoiceReqTool.prevalenceSeek(roomId: partyModel.roomId,
                                               //: uid: Int(AppManager.share.loginUid) ?? 0,
                                               uid: Int(AppManager.share.loginUid) ?? 0,
                                               //: type: type,
                                               type: type,
                                               //: position: position,
                                               position: position,
                                               //: toPos: toPosition)
                                               toPos: toPosition)
        //: { succeed, result, errorModel in
        { succeed, result, errorModel in
            //: self.isMicReq = false
            self.isMicReq = false
            //: guard succeed else {
            guard succeed else { // 失败提示
                //: if errorModel?.errorCode == 20001 {
                if errorModel?.errorCode == 20001 { // 已在麦位，开始推流，刷新UI
                    //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    //: let streamID = self.voiceRoom_getStreamID()
                    let streamID = self.holding()
                    //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                    ZegoExpressEngine.shared().startPublishingStream(streamID)
                    //: guard let dict = result as? [String: Any] else { return }
                    guard let dict = result as? [String: Any] else { return }
                    //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: dict) else { return }
                    guard let mikeModel = PerformanceMeasurable.deserialize(from: dict) else { return }
                    //: self.updateMikePosition(position: position, model: mikeModel)
                    self.alongSkip(position: position, model: mikeModel)
                }
                //: return
                return
            }

            // 成功后只做逻辑处理，在接收信令处统一刷新UI
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                //: let streamID = self.voiceRoom_getStreamID()
                let streamID = self.holding()
                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                ZegoExpressEngine.shared().startPublishingStream(streamID)

            //: case 2:
            case 2: // 下麦成功
                //: ZegoExpressEngine.shared().stopPublishingStream()
                ZegoExpressEngine.shared().stopPublishingStream()

            //: case 3:
            case 3: // 切麦成功
                //: break
                break

            //: case 4:
            case 4: // 开麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }
        }
    }

    /// 房主对他人操作请求
    /// - Parameters:
    ///   - uid: 用户Id
    ///   - type: 1 锁麦，2 解锁麦，3 踢下麦，4 开麦，5 闭麦
    ///   - position: 麦位
    //: func voiceRoom_reqAdminChangeMike(uid: Int, type: Int, position: Int) {
    func adminPointLevelTypeVoiceRotundaRevolution(uid: Int, type: Int, position: Int) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_adminChangeMike(roomId: partyModel.roomId,
        JumpVoiceReqTool.allover(roomId: partyModel.roomId,
                                        //: uid: uid,
                                        uid: uid,
                                        //: type: type,
                                        type: type,
                                        //: position: position)
                                        position: position)
        //: { _, _, _ in
        { _, _, _ in
            //: self.isMicReq = false
            self.isMicReq = false
        }
    }

    /// 获取推流Id
    //: private func voiceRoom_getStreamID() -> String {
    private func holding() -> String {
        //: return "p-\(AppManager.share.loginUid)-\(partyModel.roomId)"
        return "p-\(AppManager.share.loginUid)-\(partyModel.roomId)"
    }
}

// MARK: - 权限检测，开启/进入/切换 语聊房

//: extension TalkingVoiceRoomManager {
extension StuffLicenseReactiveCompatible {
    /// 检测状态并进入语聊房
    /// - Parameters:
    ///   - roomId: 房间Id（nil：创建房间；有值：进入房间）
    //: func voiceRoom_checkAndTurnOn(roomId: String? = nil) {
    func versionTo(roomId: String? = nil) {
        //: guard isParty == false else { return }
        guard isParty == false else { return }
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true

        // 1. 权限检测
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        SatelliteThen.permit(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.changeManage(showMsg: kCaptureStr)
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: return
                return
            }

            // 2. 接口是否禁播检测
            //: TalkingVoiceRoomReqTool.req_voiceRoomCheck(roomId: roomId) { succeed, result, errorModel in
            JumpVoiceReqTool.paraYearReady(roomId: roomId) { succeed, result, errorModel in
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: guard succeed else {
                guard succeed else {
                    //: if errorModel?.errorCode == 302 {
                    if errorModel?.errorCode == 302 { // 失败，跳转H5
                        //: if let result = result as? [String: Any] {
                        if let result = result as? [String: Any] {
                            //: PipSqueakReactiveCompatible.share.func__pushToWebVC(urlStr: result["redirectUrl"] as? String)
                            PipSqueakReactiveCompatible.share.pokeAt(urlStr: result[(String(showScriptPath) + mainArrayMsg.replacingOccurrences(of: "content", with: "l"))] as? String)
                        }
                        //: } else if errorModel?.errorMsg.count ?? 0 > 0 {
                    } else if errorModel?.errorMsg.count ?? 0 > 0 {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                        self.changeManage(showMsg: errorModel!.errorMsg)
                    }
                    //: return
                    return
                }

                // 3. 是否正在音视频通话中
                //: guard TalkingSocketManager.shared.isObject == false else {
                guard StackSocketManager.shared.isObject == false else {
                    //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
                    self.changeManage(showMsg: m_restoreInputKey)
                    //: return
                    return
                }

                // 4. 校验成功，解析模型
                //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
                guard let model = MoveModel.deserialize(from: result as? Dictionary) else {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 进房模型解析失败。")
                    HoeThen.halfLine(msg: (String(mainVoiceInvestThenFormat) + kCheckIdent))
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.changeManage(showMsg: (String(notiGiveMsg.suffix(5)) + String(k_customIdent)).localized)
                    //: return
                    return
                }
                //: self.partyModel = model
                self.partyModel = model

                // 5. 创建语聊房UI，同时调用sdk创建房间
                //: self.voiceRoomVC = TalkingVoiceRoomViewController()
                self.voiceRoomVC = CreamThen()
                //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
                self.firstController()?.pushViewController(self.voiceRoomVC!, animated: true)
                //: self.miniView.refreshMiniView()
                self.miniView.viewLimitedMini()
                //: self.miniView.isHidden = true
                self.miniView.isHidden = true
                //: self.voiceRoom_create()
                self.party()
                //: UIApplication.shared.isIdleTimerDisabled = true
                UIApplication.shared.isIdleTimerDisabled = true // 防止息屏
                //: self.isParty = true
                self.isParty = true

                // 6. 埋点
                //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(AppManager.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
                noti_thirdUrl.toSpeaker(eventID: "\(userTurnName)_\(AppManager.share.loginUserMode.sex == AlongsideDigitalTableConvertible.male.rawValue ? String(bytes: main_outsideFallName.reversed(), encoding: .utf8)! : (dream_onMsg.replacingOccurrences(of: "ballot", with: "e")))")

                // 7. 初始化弹幕群聊
                //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                DamnThen.federalize(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                    //: if succeed == false {
                    if succeed == false {
                        //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊进房失败。groupId: \(self.partyModel.chatGroupId).")
                        HoeThen.halfLine(msg: (String(dream_exclusiveIdent) + String(appTooCalculateUrl.suffix(5)) + "\u{7fa4}\u{804a}进房失败" + String(m_tubeInsertValue)) + "\(self.partyModel.chatGroupId).")
                        //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                        self.changeManage(showMsg: (String(notiGiveMsg.suffix(5)) + String(k_customIdent)).localized)
                        //: self.voiceRoom_releaseAllResource()
                        self.someoneStart()
                    }
                }
            }
        }
    }

    /// 切换语聊房
    /// - Parameters:
    ///   - roomId: 当前进入房间Id
    ///   - beforeRoomId: 上一房间Id
    //: func voiceRoom_switch(roomId: String, beforeRoomId: String) {
    func libraryConclusionLip(roomId: String, beforeRoomId: String) {
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true
        // 1. 调用切房接口
        //: TalkingVoiceRoomReqTool.req_voiceRoomSwitch(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
        JumpVoiceReqTool.form(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
            //: self.isEnterRoomReq = false
            self.isEnterRoomReq = false
            //: guard succeed else { return }
            guard succeed else { return }
            // 2. 释放上个房间资源 (使用partyModel旧值)
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.ne()

            // 3. 主播切房，需要关闭底部tabbar动效 (使用partyModel旧值)
            //: if String(self.partyModel.streamerInfo.uid) == AppManager.share.loginUid {
            if String(self.partyModel.streamerInfo.uid) == AppManager.share.loginUid {
                //: self.isParty = false
                self.isParty = false
            }

            // 4. 校验成功，解析模型（给partyModel重新赋值）
            //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
            guard let model = MoveModel.deserialize(from: result as? Dictionary) else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM: 切房模型解析失败。")
                HoeThen.halfLine(msg: (String(k_pendingTitle) + String(dreamGreetKey)))
                //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                self.changeManage(showMsg: (String(notiGiveMsg.suffix(5)) + String(k_customIdent)).localized)
                //: return
                return
            }
            //: self.partyModel = model
            self.partyModel = model

            // 5. 创建语聊房UI，同时调用sdk切房
            //: self.voiceRoomVC = TalkingVoiceRoomViewController()
            self.voiceRoomVC = CreamThen()
            //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
            self.firstController()?.pushViewController(self.voiceRoomVC!, animated: true)
            //: self.miniView.refreshMiniView()
            self.miniView.viewLimitedMini()
            //: self.miniView.isHidden = true
            self.miniView.isHidden = true
            //: self.voiceRoom_switch(beforeRoomId: beforeRoomId)
            self.monitorId(beforeRoomId: beforeRoomId)
            //: self.isParty = true
            self.isParty = true
            //: self.isSmallMode = false
            self.isSmallMode = false

            // 6. 埋点
            //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(AppManager.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
            noti_thirdUrl.toSpeaker(eventID: "\(userTurnName)_\(AppManager.share.loginUserMode.sex == AlongsideDigitalTableConvertible.male.rawValue ? String(bytes: main_outsideFallName.reversed(), encoding: .utf8)! : (dream_onMsg.replacingOccurrences(of: "ballot", with: "e")))")

            // 7. 初始化弹幕群聊
            //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
            DamnThen.federalize(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                //: if succeed == false {
                if succeed == false {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊切房失败。groupId: \(self.partyModel.chatGroupId).")
                    HoeThen.halfLine(msg: (k_softenBalanceTitle.replacingOccurrences(of: "square", with: "O") + showPleaseIdent.uppercased() + ": 群\u{804a}" + show_howData + "roupId: ") + "\(self.partyModel.chatGroupId).")
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.changeManage(showMsg: (String(notiGiveMsg.suffix(5)) + String(k_customIdent)).localized)
                    //: self.voiceRoom_releaseAllResource()
                    self.someoneStart()
                }
            }
        }
    }
}

// MARK: - Public Event

//: extension TalkingVoiceRoomManager {
extension StuffLicenseReactiveCompatible {
    /// 最小化语聊房
    //: func voiceRoom_mini() {
    func attentionMini() {
        //: isSmallMode = true
        isSmallMode = true
        //: miniView.isHidden = false
        miniView.isHidden = false
        //: voiceRoomVC?.popCurrentViewController()
        voiceRoomVC?.am()
    }

    /// 返回语聊房
    //: func voiceRoom_goback() {
    func vocalisation() {
        //: isSmallMode = false
        isSmallMode = false
        //: miniView.isHidden = true
        miniView.isHidden = true
        //: guard voiceRoomVC != nil else { return }
        guard voiceRoomVC != nil else { return }
        //: guard let currentVC = currentViewController() else { return }
        guard let currentVC = decideBroad() else { return }
        // 如果顶部视图是私信页，先返回列表，避免多个私信页消息同步问题
        //: if currentVC.isKind(of: TalkingPrivateChatController.self) {
        if currentVC.isKind(of: FamilyFleetViewController.self) {
            //: if var vcArr = currentVC.navigationController?.viewControllers {
            if var vcArr = currentVC.navigationController?.viewControllers {
                //: vcArr.removeLast()
                vcArr.removeLast()
                //: vcArr.append(voiceRoomVC!)
                vcArr.append(voiceRoomVC!)
                //: currentVC.navigationController?.setViewControllers(vcArr, animated: true)
                currentVC.navigationController?.setViewControllers(vcArr, animated: true)
            }
            //: } else {
        } else {
            //: currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
            currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
        }
    }

    /// 销毁语聊房所有资源
    //: func voiceRoom_releaseAllResource() {
    func someoneStart() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.isParty = false
            self.isParty = false // 有通知监听，需置为false
            //: UIApplication.shared.isIdleTimerDisabled = false
            UIApplication.shared.isIdleTimerDisabled = false // 恢复息屏
            //: self.miniView.removeFromSuperview()
            self.miniView.removeFromSuperview() // 移除小窗口
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.ne() // 将语聊房视图从栈中移除并释放资源
            //: if self.partyModel.position >= 0 {
            if self.partyModel.position >= 0 { // 退房下麦
                //: self.voiceRoom_reqChangeMike(type: 2, position: self.partyModel.position)
                self.connectPosition(type: 2, position: self.partyModel.position)
            }
            //: ZegoExpressEngine.shared().logoutRoom()
            ZegoExpressEngine.shared().logoutRoom() // 退出房间
            //: ZegoExpressEngine.destroy(nil)
            ZegoExpressEngine.destroy(nil) // 销毁引擎
            //: TalkingVoiceRoomManager.destroy()
            StuffLicenseReactiveCompatible.padlock() // 销毁当前单例
        }
    }
}

// MARK: - Private Event

//: extension TalkingVoiceRoomManager {
extension StuffLicenseReactiveCompatible {
    /// 创建语聊房
    //: private func voiceRoom_create() {
    private func party() {
        // 创建引擎
        //: let profile = ZegoEngineProfile()
        let profile = ZegoEngineProfile()
        //: profile.appID = ImmediatelyThen.getVoiceRoomAppId()
        profile.appID = ImmediatelyThen.towardDigital()
        //: profile.scenario = .standardChatroom
        profile.scenario = .standardChatroom
        //: ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        // 进阶配置，进入房间
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: let user = ZegoUser(userID: AppManager.share.loginUid)
        let user = ZegoUser(userID: AppManager.share.loginUid)
        //: ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
        ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
    }

    /// 切换语聊房
    /// - Parameter beforeRoomId: 上一房间Id
    //: private func voiceRoom_switch(beforeRoomId: String) {
    private func monitorId(beforeRoomId: String) {
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
        ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
    }
}

// MARK: - ZegoEventHandler

//: extension TalkingVoiceRoomManager: ZegoEventHandler {
extension StuffLicenseReactiveCompatible: ZegoEventHandler {
    // MARK: - 进房/切房 监听回调

    /// 进房/切房 回调
    //: func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
    func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: if errorCode == 0, reason == .logined {
        if errorCode == 0, reason == .logined { // 进房成功
            // 启动声浪等级监控
            //: ZegoExpressEngine.shared().startSoundLevelMonitor()
            ZegoExpressEngine.shared().startSoundLevelMonitor()
            // 开启音量稳定控制
            //: ZegoExpressEngine.shared().enableAEC(true)
            ZegoExpressEngine.shared().enableAEC(true)
            // 获取嘉宾列表接口
            //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
            JumpVoiceReqTool.beginCompletion(roomId: partyModel.roomId) { list in
                // 保存麦位列表信息
                //: for (key, value) in list.enumerated() {
                for (key, value) in list.enumerated() {
                    //: self.modifyMikePosition(key: key, model: value)
                    self.sourceModel(key: key, model: value)
                }
                // 刷新麦位列表UI
                //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                self.voiceRoomVC?.mikePositionView.userContact()
                // 房主首次创建房间，主动上麦
                //: if self.partyModel.userRole == VoiceRoomRole.host.rawValue, self.partyModel.isCreate == true {
                if self.partyModel.userRole == GlobuleSum.host.rawValue, self.partyModel.isCreate == true {
                    //: self.voiceRoom_reqChangeMike(type: 1, position: 0)
                    self.connectPosition(type: 1, position: 0)
                }
            }

            //: } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
        } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
            //: if errorCode == 0, reason == .reconnected {
            if errorCode == 0, reason == .reconnected { // 重连成功
                //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
                JumpVoiceReqTool.beginCompletion(roomId: partyModel.roomId) { list in
                    // 更新麦位列表信息
                    //: for (key, value) in list.enumerated() {
                    for (key, value) in list.enumerated() {
                        //: self.modifyMikePosition(key: key, model: value)
                        self.sourceModel(key: key, model: value)
                        //: if String(value.uid) == AppManager.share.loginUid {
                        if String(value.uid) == AppManager.share.loginUid {
                            //: if value.mikeStatus == 1 {
                            if value.mikeStatus == 1 { // 房主闭麦
                                //: ZegoExpressEngine.shared().stopPublishingStream()
                                ZegoExpressEngine.shared().stopPublishingStream()
                                //: } else {
                            } else {
                                //: let streamID = self.voiceRoom_getStreamID()
                                let streamID = self.holding()
                                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                                ZegoExpressEngine.shared().startPublishingStream(streamID)
                                //: let isMute = value.mikeStatus == 0 ? true : false
                                let isMute = value.mikeStatus == 0 ? true : false
                                //: ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                                ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                            }
                        }
                    }
                    // 刷新麦位列表UI
                    //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                    self.voiceRoomVC?.mikePositionView.userContact()
                }
                //: } else if errorCode != 0 {
            } else if errorCode != 0 {
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                noti_thirdUrl.libraryDataWeight(eventID: dream_fileKey, parameterStr: [String(bytes: mainYourShakeTitle.map{$0^182}, encoding: .utf8)!: String(errorCode)])
            }

            //: } else {
        } else { // 进房失败
            //: if errorCode == 1002055 || errorCode == 1002050 {
            if errorCode == 1_002_055 || errorCode == 1_002_050 { // 用户退出 || 被踢出房间
                //: let json = JSON(extendedData)
                let json = JSON(extendedData)
                //: let value = json["custom_kickout_message"].stringValue
                let value = json[(String(mainDisableSegmentId) + dream_marginMsg.replacingOccurrences(of: "skull", with: "s"))].stringValue
                //: if value == "closeRoom" || value == "adminCloseRoom" { // 关闭房间
                if value == (k_waitValue.replacingOccurrences(of: "recover", with: "lo") + "Room") || value == (mStudySharpMessage.replacingOccurrences(of: "merge", with: "i") + "nClos" + String(kCoordinatorUrl.suffix(5))) { // 关闭房间
                    //: voiceRoom_releaseAllResource()
                    someoneStart()
                    //: let config = ShowAlertConfig()
                    let config = SeeAlertConfig()
                    //: config.alignment = .center
                    config.alignment = .center
                    //: TalkingAlertShow.customAlert(message: "The room is closed, please go to another room".localized, rightBtnTitle: "OK".localized, rightBlock: {
                    DemonstrateSayAlertReactiveCompatible.alterConfig(message: String(bytes: user_peelBarrierId.map{femaleParent(population: $0)}, encoding: .utf8)!.localized, rightBtnTitle: "OK".localized, rightBlock: {
                        // 跳转到首页tab-party
                        //: NotificationCenter.default.post(name: SWITCH_TABBAR_HOME_PARTY_NOTIFICATION, object: nil, userInfo: nil)
                        NotificationCenter.default.post(name: m_warnStr, object: nil, userInfo: nil)
                        //: }, config: config)
                    }, config: config)

                    //: } else {
                } else { // 被踢出房间
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked out of the room".localized)
                    changeManage(showMsg: String(bytes: mainApplyKey.map{tearUniversal(drag: $0)}, encoding: .utf8)!.localized)
                }

                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM：进房失败，roomID: \(roomID), error: \(errorCode), reason: \(reason), extendedData: \(extendedData).")
                HoeThen.halfLine(msg: (String(app_detailSlimId) + String(show_alwaysValue)) + "\(roomID)" + (String(user_photoUrl) + main_automaticRemarkPatterPath.capitalized) + "\(errorCode)" + (String(noti_mixTitle) + String(mSecureUrl)) + "\(reason)" + (String(dream_colorPath) + m_enthusiasmMessage.replacingOccurrences(of: "letter", with: ":")) + "\(extendedData).")
                //: func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                changeManage(showMsg: (String(notiGiveMsg.suffix(5)) + String(k_customIdent)).localized)
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                noti_thirdUrl.libraryDataWeight(eventID: dream_fileKey, parameterStr: [String(bytes: mainYourShakeTitle.map{$0^182}, encoding: .utf8)!: String(errorCode)])
            }
            //: voiceRoom_releaseAllResource()
            someoneStart()
        }
    }

    // MARK: - 推流回调

    /// 当前用户推流状态回调
    //: func onPublisherStateUpdate(_ state: ZegoPublisherState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPublisherStateUpdate(_: ZegoPublisherState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 || errorCode == 1003020 || errorCode == 1002055 else {
        guard errorCode == 0 || errorCode == 1_003_020 || errorCode == 1_002_055 else { // 推流失败，用户下麦
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 当前用户推流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            HoeThen.halfLine(msg: (userOperateStr.replacingOccurrences(of: "min", with: "C") + "EROO" + app_pastData + "户推流失" + String(notiPriorityKey.suffix(5)) + String(show_tripUrl.prefix(5))) + "\(partyModel.roomId)" + (String(m_speculateMessage.prefix(4)) + "rorC" + String(show_simultaneouslyRunMsg.prefix(4)) + dreamCancelUrl.replacingOccurrences(of: "attribute", with: " ")) + "\(errorCode)" + (String(k_plotLightStr.suffix(7)) + String(user_fatalText)) + "\(streamID).")
            //: func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
            changeManage(showMsg: String(bytes: m_announcementKey.map{sendSub(conversion: $0)}, encoding: .utf8)!.localized)
            //: let dict = getAllMikePositions()
            let dict = capturePositions()
            //: for (position, model) in dict where String(model.uid) == AppManager.share.loginUid {
            for (position, model) in dict where String(model.uid) == AppManager.share.loginUid {
                //: voiceRoom_reqChangeMike(type: 2, position: position)
                connectPosition(type: 2, position: position)
                //: resetMikePosition(position: position, model: model)
                resetWithFramework(position: position, model: model)
            }
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                someoneStart()
            }
            //: return
            return
        }
    }

    // MARK: - 拉流回调

    /// 当前用户拉流状态回调
    //: func onPlayerStateUpdate(_ state: ZegoPlayerState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPlayerStateUpdate(_: ZegoPlayerState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 else {
        guard errorCode == 0 else { // 拉流失败
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 拉流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            HoeThen.halfLine(msg: (String(mMenuSupportDigitalUrl) + String(m_assetName.prefix(6)) + "omID: ") + "\(partyModel.roomId)" + (String(m_speculateMessage.prefix(4)) + "rorC" + String(show_simultaneouslyRunMsg.prefix(4)) + dreamCancelUrl.replacingOccurrences(of: "attribute", with: " ")) + "\(errorCode)" + (String(k_plotLightStr.suffix(7)) + String(user_fatalText)) + "\(streamID).")
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                someoneStart()
            }

            //: return
            return
        }
    }

    // MARK: - 流更新回调

    /// 流更新回调
    //: func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData: [AnyHashable: Any]?, roomID: String) {
    func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData _: [AnyHashable: Any]?, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: switch updateType {
        switch updateType {
        //: case .add:
        case .add:
            //: streamList.forEach { zego in
            streamList.forEach { zego in
                // 开始拉流
                //: ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
            }

        //: case .delete:
        case .delete:
            //: streamList.forEach { zego in
            streamList.forEach { zego in
                // 停止拉流
                //: ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
            }

        //: @unknown default:
        @unknown default:
            //: fatalError()
            fatalError()
        }
    }

    // MARK: - 声浪等级监听回调

    /// 本地声浪等级
    //: func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
    func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
        //: guard Int(truncating: soundLevel) > 0 else { return }
        guard Int(truncating: soundLevel) > 0 else { return }
        //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: partyModel.position, level: soundLevel)
        voiceRoomVC?.mikePositionView.drag(position: partyModel.position, level: soundLevel)
    }

    /// 远端声浪等级
    //: func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
    func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
        //: let dict = getAllMikePositions()
        let dict = capturePositions()
        //: for (streamID, level) in soundLevels {
        for (streamID, level) in soundLevels {
            //: let components = streamID.split(separator: "-").map(String.init)
            let components = streamID.split(separator: "-").map(String.init)
            //: guard components.count > 2 else { continue }
            guard components.count > 2 else { continue }
            //: if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
            if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
                //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: position, level: level)
                voiceRoomVC?.mikePositionView.drag(position: position, level: level)
            }
        }
    }

    // MARK: - 信令

    /// 上麦、下麦、锁麦、解锁麦、切麦操作会收到后端下发信令
    //: func onIMRecvCustomCommand(_ command: String, from fromUser: ZegoUser, roomID: String) {
    func onIMRecvCustomCommand(_ command: String, from _: ZegoUser, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: guard let decodedStr = command.removingPercentEncoding else {
        guard let decodedStr = command.removingPercentEncoding else {
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 信令解析失败，command: \(command), roomID: \(roomID).")
            HoeThen.halfLine(msg: (kHusbandCreationContent.replacingOccurrences(of: "execute", with: "I") + "ROOM:" + main_checkSharpIdent + "析失败，c" + kLatSlimName.replacingOccurrences(of: "result", with: "mm") + String(noti_simplyMsg)) + "\(command)" + (String(userQuoteEngageFormat.prefix(5)) + "mID:" + mainPerKey.capitalized) + "\(roomID).")
            //: return
            return
        }
        // 更新麦位信息，刷新列表UI
        //: let json = JSON(parseJSON: decodedStr)
        let json = JSON(parseJSON: decodedStr)
        //: let position = json["data"]["position"].intValue
        let position = json[(userWorldFormat.replacingOccurrences(of: "below", with: "a"))][(String(mainPhoneFloorName) + dreamIndigenousKey.replacingOccurrences(of: "corner", with: "i"))].intValue
        //: guard position < MIKE_SEAT_COUNT else { return }
        guard position < main_numberKey else { return }
        //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: json["data"].dictionaryObject) else { return }
        guard let mikeModel = PerformanceMeasurable.deserialize(from: json[(userWorldFormat.replacingOccurrences(of: "below", with: "a"))].dictionaryObject) else { return }
        //: printLog(message: "xxxx-- \(json.rawValue)")
        printLog(message: (String(dream_perfectFormat.prefix(7))) + "\(json.rawValue)")
        //: if json["opType"].stringValue == "party:changeMike" { // 房主/观众 麦位操作信令
        if json[(String(showAngleContent) + mainResortText.lowercased())].stringValue == (String(main_agentContent.suffix(6)) + "chang" + String(mScreamUrl)) { // 房主/观众 麦位操作信令
            //: let type = json["data"]["type"].intValue
            let type = json[(userWorldFormat.replacingOccurrences(of: "below", with: "a"))][(userStripMsg.replacingOccurrences(of: "shirt", with: "pe"))].intValue
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: position, model: mikeModel)
                alongSkip(position: position, model: mikeModel)

            //: case 2:
            case 2: // 下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = absurdStatement(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    resetWithFramework(position: position, model: oldModel)
                }

            //: case 3:
            case 3: // 切麦
                //: resetMikePosition(position: position, model: mikeModel)
                resetWithFramework(position: position, model: mikeModel)
                //: let toPosition = json["data"]["toPosition"].intValue
                let toPosition = json[(userWorldFormat.replacingOccurrences(of: "below", with: "a"))][(String(noti_devicePath) + String(app_relationKey))].intValue
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: toPosition, model: mikeModel)
                alongSkip(position: toPosition, model: mikeModel)

            //: case 4, 5:
            case 4, 5: // 开麦、闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                alongSkip(position: position, model: mikeModel)

            //: default:
            default:
                //: break
                break
            }
            // 刷新礼物面板头像
            //: if type == 1 || type == 2 || type == 3 {
            if type == 1 || type == 2 || type == 3 {
                //: voiceRoomVC?.bottomView.refreshGiftIcon()
                voiceRoomVC?.bottomView.timePeriod()
            }

            //: } else if json["opType"].stringValue == "party:adminChangeMike" { // 房主对他人操作信令
        } else if json[(String(showAngleContent) + mainResortText.lowercased())].stringValue == (String(k_graceKey.suffix(6)) + kDailyStr.replacingOccurrences(of: "deny", with: "a") + String(m_dreamSearchValue)) { // 房主对他人操作信令
            //: switch json["data"]["type"].intValue {
            switch json[(userWorldFormat.replacingOccurrences(of: "below", with: "a"))][(userStripMsg.replacingOccurrences(of: "shirt", with: "pe"))].intValue {
            //: case 1, 2:
            case 1, 2: // 锁麦、解锁麦
                //: updateMikePosition(position: position, model: mikeModel)
                alongSkip(position: position, model: mikeModel)

            //: case 3:
            case 3: // 踢下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = absurdStatement(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    resetWithFramework(position: position, model: oldModel)
                    // 刷新礼物面板头像
                    //: voiceRoomVC?.bottomView.refreshGiftIcon()
                    voiceRoomVC?.bottomView.timePeriod()
                    //: guard String(oldModel.uid) == AppManager.share.loginUid else { return }
                    guard String(oldModel.uid) == AppManager.share.loginUid else { return }
                    //: ZegoExpressEngine.shared().stopPublishingStream()
                    ZegoExpressEngine.shared().stopPublishingStream()
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked off the seat".localized)
                    changeManage(showMsg: (String(dream_administrativePath.suffix(4)) + "were k" + String(mainStandardValue.prefix(7)) + String(appStrokeGivenContent.suffix(4)) + "he seat").localized)
                }

            //: case 4:
            case 4: // 开麦
                //: updateMikePosition(position: position, model: mikeModel)
                alongSkip(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == AppManager.share.loginUid else { return }
                guard String(mikeModel.uid) == AppManager.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                alongSkip(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == AppManager.share.loginUid else { return }
                guard String(mikeModel.uid) == AppManager.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }

            //: } else if json["opType"].stringValue == "party:changeCoin" { // 更新用户麦位金币
        } else if json[(String(showAngleContent) + mainResortText.lowercased())].stringValue == (show_universalName.lowercased() + "y:cha" + String(mMixPath)) { // 更新用户麦位金币
            //: TalkingVoiceRoomManager.shared().getAllMikePositions().forEach { position, model in
            StuffLicenseReactiveCompatible.iconPoint().capturePositions().forEach { position, model in
                //: if model.uid == mikeModel.uid {
                if model.uid == mikeModel.uid {
                    //: model.coin = mikeModel.coin
                    model.coin = mikeModel.coin
                    //: updateMikePosition(position: position, model: model)
                    alongSkip(position: position, model: model)
                    //: return
                    return
                }
            }

            //: } else if json["opType"].stringValue == "party:closeStream" { // 推流失败
        } else if json[(String(showAngleContent) + mainResortText.lowercased())].stringValue == (String(user_roughlyJawValue) + String(app_expectedSecretPath.prefix(6)) + "Stream") { // 推流失败
            //: ZegoExpressEngine.shared().stopPublishingStream()
            ZegoExpressEngine.shared().stopPublishingStream()
        }
    }
}

// MARK: - 信令处理

//: extension TalkingVoiceRoomManager {
extension StuffLicenseReactiveCompatible {
    /// 更新麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func updateMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func alongSkip(position: Int, model: PerformanceMeasurable) {
        //: modifyMikePosition(key: position, model: model)
        sourceModel(key: position, model: model)
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.toALowerPlace(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        guess(uid: model.uid)
    }

    /// 重置麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func resetMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func resetWithFramework(position: Int, model: PerformanceMeasurable) {
        //: modifyMikePosition(key: position, model: TalkingMikeListItemModel())
        sourceModel(key: position, model: PerformanceMeasurable())
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.toALowerPlace(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        guess(uid: model.uid)
    }

    /// 更新本人麦位、底部麦克风状态
    /// - Parameter uid: 用户Id
    //: private func updateBottomMicStatusIfCurrentUser(uid: Int) {
    private func guess(uid: Int) {
        //: guard String(uid) == AppManager.share.loginUid else { return }
        guard String(uid) == AppManager.share.loginUid else { return }
        //: let dict = getAllMikePositions()
        let dict = capturePositions()
        //: if let position = dict.first(where: { $0.value.uid == uid })?.key {
        if let position = dict.first(where: { $0.value.uid == uid })?.key {
            //: partyModel.position = position
            partyModel.position = position
            //: } else {
        } else {
            //: partyModel.position = -1
            partyModel.position = -1
        }
        //: voiceRoomVC?.bottomView.refreshBottomMicStatus()
        voiceRoomVC?.bottomView.status()
    }
}

// MARK: - 麦位字典安全操作

//: extension TalkingVoiceRoomManager {
extension StuffLicenseReactiveCompatible {
    /// 获取初始化模型
    //: private func initMikePosition() -> [Int: TalkingMikeListItemModel] {
    private func withoutPosition() -> [Int: PerformanceMeasurable] {
        //: for index in 0 ..< MIKE_SEAT_COUNT {
        for index in 0 ..< main_numberKey {
            //: modifyMikePosition(key: index, model: TalkingMikeListItemModel())
            sourceModel(key: index, model: PerformanceMeasurable())
        }
        //: return mikePositionDict
        return mikePositionDict
    }

    /// 添加/修改 麦位
    //: func modifyMikePosition(key: Int, model: TalkingMikeListItemModel) {
    func sourceModel(key: Int, model: PerformanceMeasurable) {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: mikePositionDict[key] = model
        mikePositionDict[key] = model
    }

    /// 获取麦位
    //: func getMikePosition(key: Int) -> TalkingMikeListItemModel? {
    func absurdStatement(key: Int) -> PerformanceMeasurable? {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict[key]
        return mikePositionDict[key]
    }

    /// 获取所有麦位
    //: func getAllMikePositions() -> [Int: TalkingMikeListItemModel] {
    func capturePositions() -> [Int: PerformanceMeasurable] {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict
        return mikePositionDict
    }
}
