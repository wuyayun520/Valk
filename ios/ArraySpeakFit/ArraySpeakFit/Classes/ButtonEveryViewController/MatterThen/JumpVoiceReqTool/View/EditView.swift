
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kVerbalData:[UInt8] = [0xb6,0xb1,0xb6,0xab,0xf7,0xbc,0xb0,0xbb,0xba,0xad,0xe5,0xf6,0xff,0xb7,0xbe,0xac,0xff,0xb1,0xb0,0xab,0xff,0xbd,0xba,0xba,0xb1,0xff,0xb6,0xb2,0xaf,0xb3,0xba,0xb2,0xba,0xb1,0xab,0xba,0xbb]

private func everyWhyArrival(indicator num: UInt8) -> UInt8 {
    return num ^ 223
}

/*: "Followers" :*/
fileprivate let m_chemistryUrl:String = "outer background suiteFoll"
fileprivate let user_popularMsg:String = "author"

/*: "Following" :*/
fileprivate let noti_forceId:[Character] = ["F","o","l","l","o","w","i","n","g"]

/*: "#E6E6E6" :*/
fileprivate let user_screenOperateIdent:String = "#E6E6slip ample"
fileprivate let mainHourUrl:[Character] = ["E","6"]

/*: "btn_message_more" :*/
fileprivate let dreamPublisherPath:String = "gift allow main last closedbtn_messa"
fileprivate let m_vacationUrl:String = "busy"
fileprivate let mThresholdMatterName:[Character] = ["e","_","m","o","r","e"]

/*: "UID:  :*/
fileprivate let k_calculateKey:String = "UID: guess index red"

/*: "level_ :*/
fileprivate let show_housePath:[Character] = ["l"]
fileprivate let userFatalData:[Character] = ["e","v","e","l","_"]

/*: "Gift" :*/
fileprivate let main_buildMsg:[UInt8] = [0x32,0x54,0x51,0x5f]

fileprivate func pageSameAdjust(scramble num: UInt8) -> UInt8 {
    let value = Int(num) - 235
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Mike" :*/
fileprivate let userArcJudgeMessage:[UInt8] = [0xcc,0xe8,0xea,0xe4]

private func buttonInfluential(anonymous num: UInt8) -> UInt8 {
    return num ^ 129
}

/*: "Follow" :*/
fileprivate let showRhythmUrl:[UInt8] = [0xd4,0xfd,0xfa,0xfa,0xfd,0x5]

fileprivate func ableLicense(come num: UInt8) -> UInt8 {
    let value = Int(num) - 142
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "gift_voice_card" :*/
fileprivate let dreamMotionMsg:[Character] = ["g","i","f","t","_","v","o","i","c","e","_","c","a","r","d"]

/*: "btn_party_voice_nor" :*/
fileprivate let dream_privacyId:String = "without his jump readerbtn_pa"
fileprivate let show_promptStr:String = "role libraryvoice_n"
fileprivate let kOkKey:String = "odiscourse"

/*: "btn_party_voice_pre" :*/
fileprivate let main_taskStripData:[Character] = ["b","t","n","_","p","a","r","t","y","_","v"]
fileprivate let userRewardGoalIdent:String = "plastic draft administrativeoice_pre"

/*: "attentionUid" :*/
fileprivate let m_cuteCaptureKey:String = "atteorigint"

/*: "source" :*/
fileprivate let app_slowId:[Character] = ["s","o","u","r","c","e"]

/*: "Kick out" :*/
fileprivate let mainTapPath:String = "civic informKick out"

/*: "Unmute" :*/
fileprivate let userSpanData:[Character] = ["U","n","m","u","t","e"]

/*: "Mute" :*/
fileprivate let k_globalMsg:String = "Muteprogress light photo combined score"

/*: "Kick off" :*/
fileprivate let notiBusinessUrl:String = "zone back brig before pressKick of"
fileprivate let appFrameworkData:[Character] = ["f"]

/*: "Block" :*/
fileprivate let dreamVerbalKey:String = "Blockdried listen stack yourself"

/*: "Report" :*/
fileprivate let app_normallyStr:String = "superior exampleReport"

/*: "#F6F6F6" :*/
fileprivate let showLowerData:String = "server again zz license protection#F6F6F"
fileprivate let notiMomentName:[Character] = ["6"]

/*: "Confirm to kick out the room?" :*/
fileprivate let main_sameUrl:[Character] = ["C","o","n","f","i","r","m"," ","t","o"," ","k","i","c","k"," "]
fileprivate let userLadderStr:String = "beyond discount informout "
fileprivate let noti_discUniformStr:String = "the room?mic yet field choice union"

/*: "Cancel" :*/
fileprivate let kPerspectiveMessage:String = "muse headlineCancel"

/*: "Sure" :*/
fileprivate let noti_pictureName:[Character] = ["S","u","r","e"]

/*: "Are you sure you want to cancel the mute?" :*/
fileprivate let notiTrapKey:[UInt8] = [0xde,0xed,0xfa,0xbf,0xe6,0xf0,0xea,0xbf,0xec,0xea,0xed,0xfa,0xbf,0xe6,0xf0,0xea,0xbf,0xe8,0xfe,0xf1,0xeb,0xbf,0xeb,0xf0,0xbf,0xfc,0xfe,0xf1,0xfc,0xfa,0xf3,0xbf,0xeb,0xf7,0xfa,0xbf,0xf2,0xea,0xeb,0xfa,0xa0]

/*: "Confirm" :*/
fileprivate let mVisualMsg:[Character] = ["C","o","n","f","i","r","m"]

/*: "The mute has been lifted~" :*/
fileprivate let show_additionalPutFormat:String = "procedure accompany sign measure lessThe "
fileprivate let show_likePath:String = "forehead step editor focus girlas b"
fileprivate let m_matterAbsMsg:String = "lifburnd"
fileprivate let userSupportiveId:[Character] = ["~"]

/*: "Confirm to kick off the seat?" :*/
fileprivate let kNorName:String = "white title fire remarkConf"
fileprivate let kEnableicialTitle:String = "spectrum consentto k"
fileprivate let dreamFindUniteMessage:String = "off mini line"

/*: "Shielding Success" :*/
fileprivate let appSharedOriginalIdent:String = "Shiebehavior distribution"
fileprivate let appLaunchData:String = "lthirding"
fileprivate let notiCryMinIdent:String = "concentrationss"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EditView.swift
//  AbroadTalking
//

//: import UIKit
import UIKit

//: @objc protocol TalkingVoiceRoomCardViewDelegate: NSObjectProtocol {
@objc protocol CycleViewDelegate: NSObjectProtocol {
    // @
    //: func func__atUserClick(uid: String, nickname: String)
    func conserve(uid: String, nickname: String)

    //: func func__giftUserClick(uid: String)
    func livery(uid: String)

    //: func func__muteUserMickClick(uid: String, mikeStatus: Int, position: Int)
    func beautyChild(uid: String, mikeStatus: Int, position: Int)

    //: func func__followUserSucess(uid: String)
    func publisherWith(uid: String)
}

//: class TalkingVoiceRoomCardView: UIView {
class EditView: UIView {
    //: var popView: TalkingPopView?
    var popView: PanoramicViewBucketRecognizerDelegate?
    //: var data =  [String: Any]()
    var data = [String: Any]()
    //: var type: LiveUserCardType?
    var type: SolidScalar?
    //: var uid = ""
    var uid = ""
    //: var liveUserId = String(TalkingVoiceRoomManager.shared().partyModel.streamerInfo.uid)
    var liveUserId = String(StuffLicenseReactiveCompatible.iconPoint().partyModel.streamerInfo.uid) // 主播ID

    //: var userModel = TalkingUserInfoModel.init()
    var userModel = BagHandyJSON()

    //: open weak var delegate: TalkingVoiceRoomCardViewDelegate?
    open weak var delegate: CycleViewDelegate?

    //: init(frame: CGRect, uid: String) {
    init(frame: CGRect, uid: String) {
        //: super.init(frame: frame)
        super.init(frame: frame)

        //: if uid == AppManager.share.loginUserMode.userID {
        if uid == AppManager.share.loginUserMode.userID {
            //: self.type = .user_me
            self.type = .user_me
            //: } else {
        } else {
            //: self.type = .user_other
            self.type = .user_other
        }
        //: self.uid = uid
        self.uid = uid
        //: setupSubviews()
        publicTransportShare()
        //: setupSubViewsConstraint()
        decisionMaking()
        //: reqUserCardInfo()
        amInfo()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kVerbalData.map{everyWhyArrival(indicator: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {}

    // 懒加载初始化

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = UIColor.white
        view.backgroundColor = UIColor.white
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: return view
        return view
        //: }()
    }()

    //: lazy var uidLb: UILabel = {
    lazy var uidLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 12)
        label.font = .concern(type: .Regular, fontSize: 12)
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.appLayer()
        //: return label
        return label
        //: }()
    }()

    //: lazy var topIcon: UIButton = {
    lazy var topIcon: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(iconBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(social), for: .touchUpInside)
        //: btn.layer.cornerRadius = 40
        btn.layer.cornerRadius = 40
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.layer.borderWidth = 2
        btn.layer.borderWidth = 2
        //: btn.layer.borderColor = UIColor.white.cgColor
        btn.layer.borderColor = UIColor.white.cgColor
        //: return btn
        return btn

        //: }()
    }()

    //: lazy var nameLb: UILabel = {
    lazy var nameLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 15)
        label.font = .concern(type: .Semibold, fontSize: 15)
        //: label.textColor = UIColor.appTitleColor()
        label.textColor = UIColor.telecom()
        //: return label
        return label
        //: }()
    }()

    //: lazy var levelIcon: UIImageView = {
    lazy var levelIcon: UIImageView = {
        //: let imag = UIImageView.init()
        let imag = UIImageView()
        //: imag.contentMode = .scaleAspectFill
        imag.contentMode = .scaleAspectFill
        //: return imag
        return imag
        //: }()
    }()

    //: lazy var signLb: UILabel = {
    lazy var signLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 13)
        label.font = .concern(type: .Regular, fontSize: 13)
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.appLayer()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 2
        label.numberOfLines = 2
        //: return label
        return label
        //: }()
    }()

    //: private lazy var followersLab: UILabel = {
    private lazy var followersLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appValueDetailColor()
        lab.textColor = .appLayer()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Followers".localized
        lab.text = (String(m_chemistryUrl.suffix(4)) + "ower" + user_popularMsg.replacingOccurrences(of: "author", with: "s")).localized
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 12)
        lab.font = UIFont.concern(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followersNum: UILabel = {
    private lazy var followersNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .telecom()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 15)
        lab.font = UIFont.concern(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingLab: UILabel = {
    private lazy var followingLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appValueDetailColor()
        lab.textColor = .appLayer()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Following".localized
        lab.text = (String(noti_forceId)).localized
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 12)
        lab.font = UIFont.concern(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingNum: UILabel = {
    private lazy var followingNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .telecom()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 15)
        lab.font = UIFont.concern(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var lineView: UIView = {
    private lazy var lineView: UIView = {
        //: let view = UIView()
        let view = UIView()
        //: view.backgroundColor = UIColor.init(hex: "#E6E6E6")
        view.backgroundColor = UIColor(hex: (String(user_screenOperateIdent.prefix(5)) + String(mainHourUrl)))
        //: return view
        return view
        //: }()
    }()

    //: private lazy var stackView: UIStackView = {
    private lazy var stackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .fill
        v.alignment = .fill
        //: v.distribution = .fillEqually
        v.distribution = .fillEqually
        //: return v
        return v
        //: }()
    }()

    //: private lazy var moreBtn: UIButton = {
    private lazy var moreBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_message_more"), for: .normal)
        btn.setImage(UIImage.verbalDescription(name: (String(dreamPublisherPath.suffix(9)) + m_vacationUrl.replacingOccurrences(of: "busy", with: "g") + String(mThresholdMatterName))), for: .normal)
        //: btn.addTarget(self, action: #selector(moreBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(roundBe), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var muteView: TalkingLiveRoomMuteView = {
    private lazy var muteView: LicenseStraddleMuteView = {
        //: let v = TalkingLiveRoomMuteView()
        let v = LicenseStraddleMuteView()
        //: v.viewType  = .voiceRoom
        v.viewType = .voiceRoom
        //: return v
        return v
        //: }()
    }()
}

//: extension TalkingVoiceRoomCardView {
extension EditView {
    //: func reqUserCardInfo() {
    func amInfo() {
        //: TalkingLiveManager.req__userCardInfo(uid: self.uid, streamerUid: liveUserId, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
        GuideUnitReactiveCompatible.restateCompletion(uid: self.uid, streamerUid: liveUserId, roomId: StuffLicenseReactiveCompatible.iconPoint().partyModel.roomId) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }

            //: if let userModel = JSONDeserializer<TalkingUserInfoModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<BagHandyJSON>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: self.userModel = userModel
                self.userModel = userModel
                //: self.setUserCardData()
                self.animaData()
            }
        }
    }

    //: func setUserCardData() {
    func animaData() {
        //: uidLb.text = "UID: \(self.userModel.uid)"
        uidLb.text = (String(k_calculateKey.prefix(5))) + "\(self.userModel.uid)"
        //: topIcon.setUrlImage(urlStr: self.userModel.headPic)
        topIcon.attentionStateSoil(urlStr: self.userModel.headPic)
        //: nameLb.text = self.userModel.nickname
        nameLb.text = self.userModel.nickname
        //: levelIcon.image = UIImage.BundleImageNamed(name: "level_\(self.userModel.level)")
        levelIcon.image = UIImage.verbalDescription(name: (String(show_housePath) + String(userFatalData)) + "\(self.userModel.level)")
        //: signLb.text = self.userModel.signature
        signLb.text = self.userModel.signature
        //: followersNum.text = self.userModel.attentionNum
        followersNum.text = self.userModel.attentionNum
        //: followingNum.text = self.userModel.fansNum
        followingNum.text = self.userModel.fansNum

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: setBottomView(sex: self.userModel.sex)
            generalFrameFail(sex: self.userModel.sex)
        }
    }

    //: func setBottomView(sex: String) {
    func generalFrameFail(sex _: String) {
        //: var dataSource = Array<String>()
        var dataSource = [String]()
        //: if self.userModel.isOnMike {
        if self.userModel.isOnMike {
            //: dataSource.append("Gift")
            dataSource.append(String(bytes: main_buildMsg.map{pageSameAdjust(scramble: $0)}, encoding: .utf8)!)
            //: if liveUserId == AppManager.share.loginUserMode.userID && self.userModel.mikeStatus != 0  {
            if liveUserId == AppManager.share.loginUserMode.userID, self.userModel.mikeStatus != 0 { // 当前用户是主播非自己闭麦  闭麦/开麦 按钮
                //: dataSource.append("Mike")
                dataSource.append(String(bytes: userArcJudgeMessage.map{buttonInfluential(anonymous: $0)}, encoding: .utf8)!)
            }
        }
        //: dataSource.append("@")
        dataSource.append("@")

        //: if self.userModel.isAttention == false {
        if self.userModel.isAttention == false {
            //: dataSource.append("Follow")
            dataSource.append(String(bytes: showRhythmUrl.map{ableLicense(come: $0)}, encoding: .utf8)!)
        }

        //: for i in 0 ..< dataSource.count {
        for i in 0 ..< dataSource.count {
            //: let btnStr = dataSource[i]
            let btnStr = dataSource[i]
            //: let btn = UIButton()
            let btn = UIButton()
            //: btn.accessibilityLabel = btnStr
            btn.accessibilityLabel = btnStr
            //: btn.setTitleColor(UIColor.appThemeColor(), for: UIControl.State.normal)
            btn.setTitleColor(UIColor.storageEngine(), for: UIControl.State.normal)
            //: if btnStr == "Gift" {
            if btnStr == String(bytes: main_buildMsg.map{pageSameAdjust(scramble: $0)}, encoding: .utf8)! {
                //: btn.setImage(UIImage.BundleImageNamed(name: "gift_voice_card"), for: .normal)
                btn.setImage(UIImage.verbalDescription(name: (String(dreamMotionMsg))), for: .normal)
                //: }else if btnStr == "Mike"{
            } else if btnStr == String(bytes: userArcJudgeMessage.map{buttonInfluential(anonymous: $0)}, encoding: .utf8)! {
                //: btn.setImage(UIImage.BundleImageNamed(name: "btn_party_voice_nor"), for: .normal)
                btn.setImage(UIImage.verbalDescription(name: (String(dream_privacyId.suffix(6)) + "rty_" + String(show_promptStr.suffix(7)) + kOkKey.replacingOccurrences(of: "discourse", with: "r"))), for: .normal)
                //: btn.setImage(UIImage.BundleImageNamed(name: "btn_party_voice_pre"), for: .selected)
                btn.setImage(UIImage.verbalDescription(name: (String(main_taskStripData) + String(userRewardGoalIdent.suffix(8)))), for: .selected)
                //: if self.userModel.mikeStatus != 2 {
                if self.userModel.mikeStatus != 2 {
                    //: btn.isSelected = true
                    btn.isSelected = true
                }
                //: }else{
            } else {
                //: btn.titleLabel?.font = UIFont.pingfangMediumFont(fontSize: 15)
                btn.titleLabel?.font = UIFont.equitySize(fontSize: 15)
                //: btn.setTitle(btnStr.localized, for: UIControl.State.normal)
                btn.setTitle(btnStr.localized, for: UIControl.State.normal)
            }
            //: btn.addTarget(self, action: #selector(itemTouch(sender:)), for: UIControl.Event.touchUpInside)
            btn.addTarget(self, action: #selector(rankEmpty(sender:)), for: UIControl.Event.touchUpInside)
            //: self.stackView.addArrangedSubview(btn)
            self.stackView.addArrangedSubview(btn)

            //: if i != 0 {
            if i != 0 {
                //: let lineView = UIView.init()
                let lineView = UIView()
                //: lineView.backgroundColor = UIColor.init(hex: "#E6E6E6")
                lineView.backgroundColor = UIColor(hex: (String(user_screenOperateIdent.prefix(5)) + String(mainHourUrl)))
                //: btn.addSubview(lineView)
                btn.addSubview(lineView)
                //: lineView.snp.makeConstraints { make in
                lineView.snp.makeConstraints { make in
                    //: make.top.equalTo(10)
                    make.top.equalTo(10)
                    //: make.leading.equalToSuperview()
                    make.leading.equalToSuperview()
                    //: make.width.equalTo(1)
                    make.width.equalTo(1)
                    //: make.height.equalTo(24)
                    make.height.equalTo(24)
                }
            }
        }
    }

    //: @objc func itemTouch(sender: UIButton) {
    @objc func rankEmpty(sender: UIButton) {
        //: let accessLabel = sender.accessibilityLabel ?? ""
        let accessLabel = sender.accessibilityLabel ?? ""
        //: switch accessLabel {
        switch accessLabel {
        //: case "Gift":
        case String(bytes: main_buildMsg.map{pageSameAdjust(scramble: $0)}, encoding: .utf8)!:
            //: self.delegate?.func__giftUserClick(uid: self.userModel.uid)
            self.delegate?.livery(uid: self.userModel.uid)
            //: self.dismiss()
            self.honor()
        //: case "Mike":
        case String(bytes: userArcJudgeMessage.map{buttonInfluential(anonymous: $0)}, encoding: .utf8)!:
            //: self.delegate?.func__muteUserMickClick(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            self.delegate?.beautyChild(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            //: self.dismiss()
            self.honor()
        //: case "@":
        case "@":
            //: self.delegate?.func__atUserClick(uid: self.userModel.uid, nickname: self.userModel.nickname)
            self.delegate?.conserve(uid: self.userModel.uid, nickname: self.userModel.nickname)
            //: self.dismiss()
            self.honor()
        //: case "Follow":
        case String(bytes: showRhythmUrl.map{ableLicense(come: $0)}, encoding: .utf8)!:
            //: freshfollowUser(sender: sender)
            commentSender(sender: sender)
        //: default: break
        default: break
        }
    }

    //: func freshfollowUser(sender: UIButton) {
    func commentSender(sender: UIButton) {
        //: req_followUser(uid: self.userModel.uid) { succeed in
        ringlikeCity(uid: self.userModel.uid) { succeed in
            //: if succeed {
            if succeed {
                //: self.followingNum.text = "\((Int(self.userModel.fansNum ) ?? 0) + 1)"
                self.followingNum.text = "\((Int(self.userModel.fansNum) ?? 0) + 1)"
                //: self.stackView.removeArrangedSubview(sender)
                self.stackView.removeArrangedSubview(sender)
                //: sender.isHidden = true
                sender.isHidden = true
                //: self.delegate?.func__followUserSucess(uid: self.userModel.uid)
                self.delegate?.publisherWith(uid: self.userModel.uid)
            }
        }
    }

    //: func req_followUser(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
    func ringlikeCity(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: var dict = [String: Any]()
        var dict = [String: Any]()
        //: dict["attentionUid"] = uid
        dict[(m_cuteCaptureKey.replacingOccurrences(of: "origin", with: "n") + "ionUid")] = uid
        //: dict["source"] = "2"
        dict[(String(app_slowId))] = "2"
        //: LifeReactiveCompatible.req_atationTool(isAttention: true, params: dict) { succeed, result, errorModel in
        LifeReactiveCompatible.infoInstall(isAttention: true, params: dict) { succeed, _, _ in
            //: completion?(succeed)
            completion?(succeed)
        }
    }

    //: @objc private func iconBtnClick() {
    @objc private func social() {
        //: PipSqueakReactiveCompatible.share.func__pushToUserDetailVC(uid: self.userModel.uid)
        PipSqueakReactiveCompatible.share.balanceConvertSeek(uid: self.userModel.uid)
        //: self.dismiss()
        self.honor()
    }
}

//: extension TalkingVoiceRoomCardView {
extension EditView {
    //: @objc func moreBtnClick() {
    @objc func roundBe() {
        //: var titleArray: [String] = []
        var titleArray: [String] = []
        /// 主播 & 用户在房间内
        //: if liveUserId == AppManager.share.loginUserMode.userID && self.userModel.isInRoom {
        if liveUserId == AppManager.share.loginUserMode.userID, self.userModel.isInRoom {
            //: titleArray.append("Kick out".localized)
            titleArray.append((String(mainTapPath.suffix(8))).localized)
            //: if self.userModel.isPartyMute {
            if self.userModel.isPartyMute {
                //: titleArray.append("Unmute".localized)
                titleArray.append((String(userSpanData)).localized)
                //: }else {
            } else {
                //: titleArray.append("Mute".localized)
                titleArray.append((String(k_globalMsg.prefix(4))).localized)
            }
            //: if self.userModel.isOnMike {
            if self.userModel.isOnMike {
                //: titleArray.append("Kick off".localized)
                titleArray.append((String(notiBusinessUrl.suffix(7)) + String(appFrameworkData)).localized)
            }
            //: if !self.userModel.inMyBlackList {
            if !self.userModel.inMyBlackList {
                //: titleArray.append("Block".localized)
                titleArray.append((String(dreamVerbalKey.prefix(5))).localized)
            }
        }
        //: titleArray.append("Report".localized)
        titleArray.append((String(app_normallyStr.suffix(6))).localized)

        //: Config.MenuCellConfig.menuCellHeight = 26
        Config.CompartmentMatter.menuCellHeight = 26
        //: let dropMenu = DropDownMenuView.pullDropDrownMenu(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        let dropMenu = RoofDataSource.untilVisible(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        //: dropMenu.menuBgColor = UIColor.init(hex: "#F6F6F6")!
        dropMenu.menuBgColor = UIColor(hex: (String(showLowerData.suffix(6)) + String(notiMomentName)))!
        //: dropMenu.menuDelegate = self
        dropMenu.menuDelegate = self
        //: dropMenu.menuStyle = .MenuCustStyle
        dropMenu.menuStyle = .MenuCustStyle
    }

    //: func show() {
    func birthIn() {
        //: popView = TalkingPopView.init(frame: UIScreen.main.bounds)
        popView = PanoramicViewBucketRecognizerDelegate(frame: UIScreen.main.bounds)
        //: self.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        self.frame = CGRect(x: 0, y: 0, width: kGiftIdent, height: user_allMessage)
        //: popView?.initWithView(view: self)
        popView?.innerView(view: self)
        //: popView?.showInView(view: self.currentViewController()?.view ?? ImmediatelyThen.getWindow())
        popView?.viewEffect(view: self.decideBroad()?.view ?? ImmediatelyThen.wow())
        //: popView?.isRemoveTapGes = true
        popView?.isRemoveTapGes = true
    }

    //: @objc func dismiss() {
    @objc func honor() {
        //: popView?.dismissView()
        popView?.err()
        //: popView = nil
        popView = nil
    }
}

//: extension TalkingVoiceRoomCardView: DropDownMenuViewDelegate {
extension EditView: LineViewDelegate {
    //: func didClickSelectedRow(index: Int, title: String) {
    func speakLikeHeader(index _: Int, title: String) {
        //: switch title {
        switch title {
        //: case "Kick out".localized:  // 踢出房间
        case (String(mainTapPath.suffix(8))).localized: // 踢出房间
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick out the room?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            DemonstrateSayAlertReactiveCompatible.rightUsed(title: nil, message: (String(main_sameUrl) + String(userLadderStr.suffix(4)) + String(noti_discUniformStr.prefix(9))).localized, leftBtnTitle: (String(kPerspectiveMessage.suffix(6))).localized, rightBtnTitle: (String(noti_pictureName)).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                OrdinalProgressHUD.loo()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomKickout(targetUid: self.userModel.uid, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
                JumpVoiceReqTool.thenHome(targetUid: self.userModel.uid, roomId: StuffLicenseReactiveCompatible.iconPoint().partyModel.roomId) { _, _, _ in
                    //: ProgressHUD.dismiss()
                    OrdinalProgressHUD.vision()
                    //: self.dismiss()
                    self.honor()
                }
            }
        //: case "Unmute".localized: // 解除禁言
        case (String(userSpanData)).localized: // 解除禁言
            //: TalkingAlertShow.alert(title: nil, message: "Are you sure you want to cancel the mute?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Confirm".localized) {
            DemonstrateSayAlertReactiveCompatible.rightUsed(title: nil, message: String(bytes: notiTrapKey.map{$0^159}, encoding: .utf8)!.localized, leftBtnTitle: (String(kPerspectiveMessage.suffix(6))).localized, rightBtnTitle: (String(mVisualMsg)).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                OrdinalProgressHUD.loo()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomUnMute(targetUid: self.userModel.uid, roomID:TalkingVoiceRoomManager.shared().partyModel.roomId, completion: { succeed, result, errorModel in
                JumpVoiceReqTool.ridAcross(targetUid: self.userModel.uid, roomID: StuffLicenseReactiveCompatible.iconPoint().partyModel.roomId, completion: { succeed, _, _ in
                    //: ProgressHUD.dismiss()
                    OrdinalProgressHUD.vision()
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "The mute has been lifted~".localized)
                        self.licenseFee(showMsg: (String(show_additionalPutFormat.suffix(4)) + "mute h" + String(show_likePath.suffix(4)) + "een " + m_matterAbsMsg.replacingOccurrences(of: "burn", with: "te") + String(userSupportiveId)).localized)
                    }
                    //: self.dismiss()
                    self.honor()
                    //: })
                })
            }
        //: case "Mute".localized: // 设置禁言
        case (String(k_globalMsg.prefix(4))).localized: // 设置禁言
            //: muteView.showView()
            muteView.colorView()
            //: muteView.targetUid = self.userModel.uid
            muteView.targetUid = self.userModel.uid
            //: self.dismiss()
            self.honor()

        //: case "Kick off".localized: /// 踢下麦
        case (String(notiBusinessUrl.suffix(7)) + String(appFrameworkData)).localized: /// 踢下麦
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick off the seat?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            DemonstrateSayAlertReactiveCompatible.rightUsed(title: nil, message: (String(kNorName.suffix(4)) + "irm " + String(kEnableicialTitle.suffix(4)) + "ick " + String(dreamFindUniteMessage.prefix(4)) + "the seat?").localized, leftBtnTitle: (String(kPerspectiveMessage.suffix(6))).localized, rightBtnTitle: (String(noti_pictureName)).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingVoiceRoomManager.shared().voiceRoom_reqAdminChangeMike(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                StuffLicenseReactiveCompatible.iconPoint().adminPointLevelTypeVoiceRotundaRevolution(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                //: self.dismiss()
                self.honor()
            }

        //: case "Report".localized:
        case (String(app_normallyStr.suffix(6))).localized:
            //: let reportView = TalkingReportAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            let reportView = TransportThen(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            //: reportView.showReportViewIn(view: nil)
            reportView.asRemove(view: nil)
        //: case "Block".localized:
        case (String(dreamVerbalKey.prefix(5))).localized:
            //: TalkingAlertShow.alert(title: nil, message: kMessage_blocking, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized) {
            DemonstrateSayAlertReactiveCompatible.rightUsed(title: nil, message: k_featureEndBusyPath, leftBtnTitle: (String(kPerspectiveMessage.suffix(6))).localized, rightBtnTitle: "OK".localized) {
                //: TalkingAlertShow.hideAlert()
                DemonstrateSayAlertReactiveCompatible.paidVacation()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAlertShow.hideAlert()
                DemonstrateSayAlertReactiveCompatible.paidVacation()
                //: TalkingUserRequestManager.func__pullBlackWithUserId(uid: self.userModel.uid, isBlack: true) { succeed, result, errorModel in
                FurtheranceReactiveCompatible.banGeneral(uid: self.userModel.uid, isBlack: true) { succeed, _, _ in
                    //: if succeed == true {
                    if succeed == true {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Shielding Success".localized)
                        self.licenseFee(showMsg: (String(appSharedOriginalIdent.prefix(4)) + appLaunchData.replacingOccurrences(of: "third", with: "d") + " Suc" + notiCryMinIdent.replacingOccurrences(of: "concentration", with: "ce")).localized)
                        //: self.dismiss()
                        self.honor()
                    }
                }
            }
        //: default: break
        default: break
        }
    }
}

//: extension TalkingVoiceRoomCardView {
extension EditView {
    // 添加视图
    //: private func setupSubviews() {
    private func publicTransportShare() {
        //: self.addSubview(contentView)
        self.addSubview(contentView)
        //: self.addSubview(topIcon)
        self.addSubview(topIcon)
        //: contentView.addSubview(uidLb)
        contentView.addSubview(uidLb)
        //: contentView.addSubview(nameLb)
        contentView.addSubview(nameLb)
        //: contentView.addSubview(levelIcon)
        contentView.addSubview(levelIcon)
        //: contentView.addSubview(signLb)
        contentView.addSubview(signLb)
        //: contentView.addSubview(followersLab)
        contentView.addSubview(followersLab)
        //: contentView.addSubview(followersNum)
        contentView.addSubview(followersNum)
        //: contentView.addSubview(followingLab)
        contentView.addSubview(followingLab)
        //: contentView.addSubview(followingNum)
        contentView.addSubview(followingNum)

        //: contentView.layoutIfNeeded()
        contentView.layoutIfNeeded()

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: contentView.addSubview(lineView)
            contentView.addSubview(lineView)
            //: contentView.addSubview(stackView)
            contentView.addSubview(stackView)
            //: contentView.addSubview(moreBtn)
            contentView.addSubview(moreBtn)
        }
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func decisionMaking() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-44)
            make.bottom.equalToSuperview().offset(-44)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: if self.type == .user_me {
            if self.type == .user_me {
                //: make.height.equalTo(192)
                make.height.equalTo(192)
                //: }else {
            } else {
                //: make.height.equalTo(233)
                make.height.equalTo(233)
            }
        }
        //: topIcon.snp.makeConstraints { make in
        topIcon.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(contentView).offset(-33)
            make.top.equalTo(contentView).offset(-33)
            //: make.width.height.equalTo(80)
            make.width.height.equalTo(80)
        }
        //: uidLb.snp.makeConstraints { make in
        uidLb.snp.makeConstraints { make in
            //: make.leading.top.equalTo(12)
            make.leading.top.equalTo(12)
            //: make.trailing.equalTo(topIcon.snp.leading).offset(-10)
            make.trailing.equalTo(topIcon.snp.leading).offset(-10)
        }
        //: nameLb.snp.makeConstraints { make in
        nameLb.snp.makeConstraints { make in
            //: make.top.equalTo(topIcon.snp.bottom).offset(12)
            make.top.equalTo(topIcon.snp.bottom).offset(12)
            //: make.centerX.equalToSuperview().offset(-12)
            make.centerX.equalToSuperview().offset(-12)
            //: make.width.lessThanOrEqualTo(ScreenWidth/2)
            make.width.lessThanOrEqualTo(kGiftIdent / 2)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: levelIcon.snp.makeConstraints { make in
        levelIcon.snp.makeConstraints { make in
            //: make.leading.equalTo(nameLb.snp.trailing).offset(5)
            make.leading.equalTo(nameLb.snp.trailing).offset(5)
            //: make.centerY.equalTo(nameLb)
            make.centerY.equalTo(nameLb)
            //: make.size.equalTo(CGSize(width: 32, height: 14))
            make.size.equalTo(CGSize(width: 32, height: 14))
        }
        //: signLb.snp.makeConstraints { make in
        signLb.snp.makeConstraints { make in
            //: make.top.equalTo(nameLb.snp.bottom).offset(12)
            make.top.equalTo(nameLb.snp.bottom).offset(12)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(38)
            make.height.equalTo(38)
        }
        //: followersNum.snp.makeConstraints { make in
        followersNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(-ScreenWidth/4+10)
            make.centerX.equalTo(contentView).offset(-kGiftIdent / 4 + 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followingNum.snp.makeConstraints { make in
        followingNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(ScreenWidth/4-10)
            make.centerX.equalTo(contentView).offset(kGiftIdent / 4 - 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followersLab.snp.makeConstraints { make in
        followersLab.snp.makeConstraints { make in
            //: make.top.equalTo(followersNum.snp.bottom).offset(5)
            make.top.equalTo(followersNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followersNum)
            make.centerX.equalTo(followersNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }
        //: followingLab.snp.makeConstraints { make in
        followingLab.snp.makeConstraints { make in
            //: make.top.equalTo(followingNum.snp.bottom).offset(5)
            make.top.equalTo(followingNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followingNum)
            make.centerX.equalTo(followingNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: lineView.snp.makeConstraints { make in
            lineView.snp.makeConstraints { make in
                //: make.top.equalTo(followersLab.snp.bottom).offset(12)
                make.top.equalTo(followersLab.snp.bottom).offset(12)
                //: make.leading.equalTo(7)
                make.leading.equalTo(7)
                //: make.trailing.equalTo(-7)
                make.trailing.equalTo(-7)
                //: make.height.equalTo(1)
                make.height.equalTo(1)
            }
            //: stackView.snp.makeConstraints { make in
            stackView.snp.makeConstraints { make in
                //: make.top.equalTo(lineView)
                make.top.equalTo(lineView)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(44)
                make.height.equalTo(44)
            }
            //: moreBtn.snp.makeConstraints { make in
            moreBtn.snp.makeConstraints { make in
                //: make.trailing.equalToSuperview()
                make.trailing.equalToSuperview()
                //: make.top.equalTo(12)
                make.top.equalTo(12)
                //: make.size.equalTo(CGSize(width: 44, height: 20))
                make.size.equalTo(CGSize(width: 44, height: 20))
            }
        }
    }

    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with _: UIEvent?) {
        //: let touch = touches.first!
        let touch = touches.first!
        //: var point = touch.location(in: self.currentViewController()?.view)
        var point = touch.location(in: self.decideBroad()?.view)
        //: point = contentView.layer.convert(point, from: self.currentViewController()?.view.layer)
        point = contentView.layer.convert(point, from: self.decideBroad()?.view.layer)
        //: if !contentView.layer.contains(point) {
        if !contentView.layer.contains(point) {
            //: self.dismiss()
            self.honor()
        }
    }
}
