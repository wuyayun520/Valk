
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let notiBadValue:[UInt8] = [0x27,0x20,0x27,0x3a,0x66,0x2d,0x21,0x2a,0x2b,0x3c,0x74,0x67,0x6e,0x26,0x2f,0x3d,0x6e,0x20,0x21,0x3a,0x6e,0x2c,0x2b,0x2b,0x20,0x6e,0x27,0x23,0x3e,0x22,0x2b,0x23,0x2b,0x20,0x3a,0x2b,0x2a]

/*: "Load..." :*/
fileprivate let appHandUrl:[Character] = ["L","o","a","d","."]
fileprivate let dream_gutStr:String = ".."

/*: "Skip" :*/
fileprivate let app_mediumUpValue:String = "tun distance location orSkip"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let show_dragTitle:[UInt8] = [0xdf,0xe3,0xe3,0xe7,0xc3,0xf8,0xf8,0xfb,0xb9,0xd9,0xf6,0xe3,0xfe,0xe1,0xf2,0xc3,0xf8,0xdd,0xe4,0xbf,0xb0,0xe5,0xf2,0xf4,0xff,0xf6,0xe5,0xf0,0xf2,0xb0,0xbe]

private func blueBusiness(slow num: UInt8) -> UInt8 {
    return num ^ 151
}

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let mFragmentMsg:[UInt8] = [0x9c,0x82,0x85,0x8f,0x84,0x9c,0xc5,0x84,0x85,0xbb,0x8a,0x8c,0x8e,0xb8,0x83,0x84,0x9c,0xcd,0xcd,0x84,0x85,0xbb,0x8a,0x8c,0x8e,0xb8,0x83,0x84,0x9c,0xc3,0xc2,0xd0]

private func inmateTail(lap num: UInt8) -> UInt8 {
    return num ^ 235
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let k_renderIdent:String = "jsEvforce private salt bond"
fileprivate let dreamPossibleIdent:String = "patient expressiongeSh"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let showOldData:[UInt8] = [0x40,0x32,0x37,0x2d,0x38,0x40,0xf7,0x38,0x37,0x19,0x2a,0x30,0x2e,0x11,0x32,0x2d,0x2e,0xef,0xef,0x38,0x37,0x19,0x2a,0x30,0x2e,0x11,0x32,0x2d,0x2e,0xf1,0xf2,0x4]

fileprivate func searchSave(north num: UInt8) -> UInt8 {
    let value = Int(num) - 201
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "jsEvent(onPageHide):  :*/
fileprivate let user_barrierFormat:String = "location depend exjsEve"
fileprivate let kClingAspectValue:String = "PageHremove healthy age"

/*: "title" :*/
fileprivate let dream_sampleMmGuestPath:[Character] = ["t","i","t","l","e"]

/*: "canGoBack" :*/
fileprivate let show_boldId:[Character] = ["c"]
fileprivate let noti_mirrorLittleValue:String = "pass ream smoothanGoBack"

/*: "PHPSESSID= :*/
fileprivate let mainStorageFormat:String = "peerPSES"

/*: ";uid= :*/
fileprivate let kExBridgeUrl:String = "speaker follow;uid="

/*: "Cookie" :*/
fileprivate let main_mustValue:String = "soul snap ehCookie"

/*: "nav_back_black_nor" :*/
fileprivate let appGuestKey:String = "price limited edit skinnav_ba"
fileprivate let userAvailableName:[Character] = ["c","k","_","b"]
fileprivate let app_passMessage:String = "lacbecome"

/*: "ico_intimate_list_sm" :*/
fileprivate let dreamTensionId:String = "ico_intisingle going"
fileprivate let m_scriptPath:String = "effective fill transmission plastic salemate_"
fileprivate let main_constraintStr:String = "list_smzone zz golf dit rocket"

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let notiFatalTitle:[UInt8] = [0xdd,0xd6,0xda,0xcc,0xd4,0xdc,0xd7,0xcd,0x97,0xde,0xdc,0xcd,0xfc,0xd5,0xdc,0xd4,0xdc,0xd7,0xcd,0xca,0xfb,0xc0,0xed,0xd8,0xde,0xf7,0xd8,0xd4,0xdc,0x91,0x9e,0xdb,0xd6,0xdd,0xc0,0x9e,0x90,0xe2,0x89,0xe4,0x97,0xca,0xcd,0xc0,0xd5,0xdc,0x97,0xdb,0xd8,0xda,0xd2,0xde,0xcb,0xd6,0xcc,0xd7,0xdd,0x84,0x9e,0xcb,0xde,0xdb,0xd8,0x91,0x89,0x95,0x89,0x95,0x89,0x95,0x89,0x90,0x9e]

private func easternOthers(of num: UInt8) -> UInt8 {
    return num ^ 185
}

/*: "Alert" :*/
fileprivate let dreamMaterialMessage:[Character] = ["A","l","e","r","t"]

/*: "Cancle" :*/
fileprivate let kStatusFormat:[Character] = ["C"]
fileprivate let m_movieMsg:[Character] = ["a","n","c","l","e"]

/*: "Done" :*/
fileprivate let notiModelViaData:String = "err confirm greet hello detectDone"

/*: "js call method name =  :*/
fileprivate let userBoldValue:[Character] = ["j","s"," ","c","a","l","l"," ","m","e"]
fileprivate let notiWorkPlainSecurityTitle:String = "tproductiono"
fileprivate let showIncludeId:String = "sequence mind nam"

/*: , message =  :*/
fileprivate let main_challengeMsg:[Character] = [","," ","m","e","s","s"]
fileprivate let userGoIdent:String = "vision museage = "

/*: "http://" :*/
fileprivate let showManagerData:String = "deny list pleasehttp:/"
fileprivate let showAdvertisingStr:String = "/"

/*: "https://" :*/
fileprivate let main_literalData:[Character] = ["h","t","t","p","s",":"]
fileprivate let m_deadlineMsg:[Character] = ["/","/"]

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let kLeastUrl:[UInt8] = [0x2a,0x3d,0x2e,0x7c,0x3f,0x33,0x33,0x37,0x35,0x39,0x12,0x3d,0x31,0x39,0x2f,0x7c,0x61,0x7c,0x38,0x33,0x3f,0x29,0x31,0x39,0x32,0x28,0x72,0x3f,0x33,0x33,0x37,0x35,0x39,0x72,0x2f,0x2c,0x30,0x35,0x28,0x74,0x7b,0x67,0x7c,0x7b,0x75,0x72,0x31,0x3d,0x2c,0x74,0x3a,0x29,0x32,0x3f,0x28,0x35,0x33,0x32,0x74,0x3f,0x33,0x33,0x37,0x35,0x39,0x75,0x7c,0x27,0x7c,0x2e,0x39,0x28,0x29,0x2e,0x32,0x7c,0x3f,0x33,0x33,0x37,0x35,0x39,0x72,0x2f,0x2c,0x30,0x35,0x28,0x74,0x7b,0x61,0x7b,0x75,0x7,0x6c,0x1,0x7c,0x21,0x7c,0x75,0x67,0x56]

private func buttonResort(frame num: UInt8) -> UInt8 {
    return num ^ 92
}

/*: "uid= :*/
fileprivate let dreamFamilyQuoteTitle:[Character] = ["u","i","d","="]

/*: ;domain= :*/
fileprivate let k_slightMessage:String = ";dorepresentation send shirt sharp"
fileprivate let kRemoteMessage:String = "huge placemain="

/*: ;path=/" :*/
fileprivate let mainWowStr:[Character] = [";","p","a","t","h","=","/"]

/*: "document.cookie=' :*/
fileprivate let userMakerName:String = "dconditionu"
fileprivate let appTipData:String = "ookie=\'"

/*: ;domin= :*/
fileprivate let kSightTitle:[Character] = [";"]
fileprivate let showSunData:[Character] = ["d","o","m","i","n","="]

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let showChooseValue:[UInt8] = [0xc4,0xc1,0x7b,0x83,0xbe,0xca,0xca,0xc6,0xc4,0xc0,0xa9,0xbc,0xc8,0xc0,0xce,0x89,0xc4,0xc9,0xbf,0xc0,0xd3,0xaa,0xc1,0x83,0x82,0xab,0xa3,0xab,0xae,0xa0,0xae,0xae,0xa4,0x9f,0x82,0x84,0x7b,0x98,0x98,0x7b,0x88,0x8c,0x84,0x7b,0xd6,0x7b,0xbf,0xca,0xbe,0xd0,0xc8,0xc0,0xc9,0xcf,0x89,0xbe,0xca,0xca,0xc6,0xc4,0xc0,0x98,0x82]

fileprivate func lastCore(gold num: UInt8) -> UInt8 {
    let value = Int(num) + 165
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "h5Domain =  :*/
fileprivate let show_haveRankUrl:String = "disabled input properly functionh5Dom"
fileprivate let m_tagUrl:[Character] = ["a","i","n"," ","="," "]

/*: "getAllCookies =  :*/
fileprivate let userTradeLimitName:String = "stream mouth female return partnergetA"
fileprivate let dream_accommodateData:[Character] = [" ","="," "]

/*: "PHPSESSID" :*/
fileprivate let user_particleValue:String = "PHPevery"

/*: "uid" :*/
fileprivate let noti_choiceData:[Character] = ["u","i","d"]

/*: "appCookies =  :*/
fileprivate let mainSubtleName:String = "appComanage spend"
fileprivate let user_foodFormalName:String = "s = circle play substance base schedule"

/*: "UID" :*/
fileprivate let main_normalIdent:String = "Uinvest"

/*: "navigator.userAgent" :*/
fileprivate let m_secondId:[Character] = ["n","a","v","i","g","a","t","o","r",".","u"]
fileprivate let app_fireSoonMessage:String = "popular"
fileprivate let mYellowCloseKey:[Character] = ["e","r","A","g","e","n","t"]

/*: "xunwu_iphone" :*/
fileprivate let k_tradePath:[Character] = ["x","u","n","w","u","_","i","p","h","o","n","e"]

/*: " xunwu_device_id/ :*/
fileprivate let main_documentData:String = " xunwuthird opening manager"
fileprivate let noti_rootIssueTitle:String = "upper woman file detecte_id/"

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let mGiveIdent:String = "trapn"
fileprivate let dream_indigenousTitle:String = "medal discourse mutualwu_i"
fileprivate let dreamWarnName:String = "story middle drage xun"
fileprivate let appArrayKey:String = "hour calculation cam mobilersion/"

/*:  xunwu_package/ :*/
fileprivate let showLabText:String = " xunwu_ticket screen refuse"
fileprivate let showHapMessage:String = "pdate"
fileprivate let mainCheckPath:[Character] = ["k","a","g","e","/"]

/*:  xunwu_bundleId/ :*/
fileprivate let main_themSegmentMsg:String = " xunwugiven meeting mp cost appear"
fileprivate let showAgoMsg:String = "post teammate supposedleId/"

/*:   webkit/1" :*/
fileprivate let mRackWillFormat:[Character] = [" "," ","w","e","b","k"]
fileprivate let dreamChopFormat:String = "it/1channel process compete"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SmallMessageHandler.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class SmallMessageHandler: OthersRecognizerDelegate {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: OthersConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: CharacterSubqueryConvertible?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: CharacterSubqueryConvertible?, webConfig: OthersConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: notiBadValue.map{$0^78}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        eventShowAlongFullPage()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        birthAlong()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(appHandUrl) + dream_gutStr.capitalized)
        //: addNotification()
        premiumIndividual()
        //: func__checkConfig()
        musicalConfig()
        //: func__setUpWebView()
        managerUpwardly()
        //: func__registerObserver()
        existing()
        //: func__beginStartRequest()
        commenceRequest()
        //: func_checkAppleIAP()
        omitIap()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: main_searchNetworkTagName, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            earlierPackage(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.storageEngine(), .font: UIFont.concern(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(app_mediumUpValue.suffix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension SmallMessageHandler {
    /// 添加通知
    //: private func addNotification() {
    private func premiumIndividual() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(range),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: m_pingTitle,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(eventShowAlongFullPage),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func range() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: show_dragTitle.map{blueBusiness(slow: $0)}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func eventShowAlongFullPage() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: mFragmentMsg.map{inmateTail(lap: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(k_renderIdent.prefix(4)) + "ent(onPa" + String(dreamPossibleIdent.suffix(4)) + "ow): ") + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func birthAlong() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: showOldData.map{searchSave(north: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(user_barrierFormat.suffix(5)) + "nt(on" + String(kClingAspectValue.prefix(5)) + "ide): ") + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension SmallMessageHandler {
    //: func func__registerObserver() {
    func existing() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (String(dream_sampleMmGuestPath)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(show_boldId) + String(noti_mirrorLittleValue.suffix(8))))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(however), name: user_collectionStr, object: nil)
    }

    //: func func__setUpWebView() {
    func managerUpwardly() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        funcAgent()
        //: func__addBridgeMethod()
        imageMethod()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.colouringDramatically()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            OrdinalProgressHUD.upInclude(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(dreamNoSampleValue)
            }
        }
    }

    //: func func__beginStartRequest() {
    func commenceRequest() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.funcProfile()
            }
            //: } else {
        } else {
            //: func__startRequest()
            funcProfile()
        }
    }

    //: func func__startRequest() {
    func funcProfile() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = AppManager.share.loginUid
        let uid = AppManager.share.loginUid
        //: let ssid = AppManager.share.loginSessionId
        let ssid = AppManager.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (mainStorageFormat.replacingOccurrences(of: "peer", with: "PH") + "SID=") + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(kExBridgeUrl.suffix(5))) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(main_mustValue.suffix(6))))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        miniProgram()
        //: func__initRightButton()
        thumbBlack()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func omitIap() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            TarRequestDelegate.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.supportive(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            TarRequestDelegate.shared.goOverForAcross()
        }
    }

    //: func func__initLeftButton() {
    func agreeTee() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.verbalDescription(name: (String(appGuestKey.suffix(6)) + String(userAvailableName) + app_passMessage.replacingOccurrences(of: "become", with: "k") + "_nor")), style: .plain, target: self, action: #selector(shSender(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func thumbBlack() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(CharacterSubqueryConvertible.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.verbalDescription(name: (String(dreamTensionId.prefix(8)) + String(m_scriptPath.suffix(5)) + String(main_constraintStr.prefix(7)))), style: .plain, target: self, action: #selector(charmAction))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func shSender(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        activityMethod()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.decideBroad() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: SmallMessageHandler.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! SmallMessageHandler).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! SmallMessageHandler).eventShowAlongFullPage()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func musicalConfig() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func consentWarm(sucess: Bool) {
        //: ProgressHUD.dismiss()
        OrdinalProgressHUD.vision()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func miniProgram() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: notiFatalTitle.map{easternOthers(of: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func synchronic(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: PipSqueakReactiveCompatible.share.func__pushToWebVC(urlStr: url)
            PipSqueakReactiveCompatible.share.pokeAt(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func bitIn(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func however() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: user_collectionStr, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func charmAction() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = UntilView(frame: CGRect(x: 0, y: 0, width: kGiftIdent, height: user_allMessage))
        //: view.show()
        view.reduceBottomRecording()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension SmallMessageHandler: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            OrdinalProgressHUD.upInclude(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            consentWarm(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        agreeTee()
        //: func__clearJSBgColor()
        miniProgram()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            consentWarm(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            consentWarm(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(dreamMaterialMessage)), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(kStatusFormat) + String(m_movieMsg)), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(notiModelViaData.suffix(4))).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension SmallMessageHandler: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(userBoldValue) + notiWorkPlainSecurityTitle.replacingOccurrences(of: "production", with: "h") + String(showIncludeId.suffix(5)) + "e = ") + "\(message.name)" + (String(main_challengeMsg) + String(userGoIdent.suffix(6))) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.messageBan(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func imageMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in TransitCaseIterable.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func activityMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in TransitCaseIterable.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension SmallMessageHandler {
    //: func func__addUserScript() {
    func outsideMemory() {
        //: var htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(showManagerData.suffix(6)) + showAdvertisingStr.capitalized)) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(showManagerData.suffix(6)) + showAdvertisingStr.capitalized), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(main_literalData) + String(m_deadlineMsg))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(main_literalData) + String(m_deadlineMsg)), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = AppManager.share.loginUserMode.userID
        let tempUid: String = AppManager.share.loginUserMode.userID
        //: let tempSessionID: String = AppManager.share.loginSessionId
        let tempSessionID: String = AppManager.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: kLeastUrl.map{buttonResort(frame: $0)}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(dreamFamilyQuoteTitle)) + "\(tempUid)" + (String(k_slightMessage.prefix(3)) + String(kRemoteMessage.suffix(5))) + "\(htmlDomain)" + (String(mainWowStr))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((userMakerName.replacingOccurrences(of: "condition", with: "oc") + "ment.c" + appTipData) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (mainStorageFormat.replacingOccurrences(of: "peer", with: "PH") + "SID=") + "\(tempSessionID)" + (String(kSightTitle) + String(showSunData)) + "\(htmlDomain)" + (String(mainWowStr))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((userMakerName.replacingOccurrences(of: "condition", with: "oc") + "ment.c" + appTipData) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: showChooseValue.map{lastCore(gold: $0)}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(dreamFamilyQuoteTitle)) + "\(tempUid)" + (String(k_slightMessage.prefix(3)) + String(kRemoteMessage.suffix(5))) + "\(htmlDomain)" + (String(mainWowStr))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((userMakerName.replacingOccurrences(of: "condition", with: "oc") + "ment.c" + appTipData) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(dreamFamilyQuoteTitle)) + "\(tempSessionID)" + (String(k_slightMessage.prefix(3)) + String(kRemoteMessage.suffix(5))) + "\(htmlDomain)" + (String(mainWowStr))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((userMakerName.replacingOccurrences(of: "condition", with: "oc") + "ment.c" + appTipData) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func earlierPackage(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain
            let htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(show_haveRankUrl.suffix(5)) + String(m_tagUrl)) + "\(htmlDomain)")
            //: let userID: String = AppManager.share.loginUserMode.userID
            let userID: String = AppManager.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(userTradeLimitName.suffix(4)) + "llCookies" + String(dream_accommodateData)) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (user_particleValue.replacingOccurrences(of: "every", with: "S") + "ESSID") {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (String(noti_choiceData)), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.commenceRequest()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.amp(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func amp(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain
                let htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(show_haveRankUrl.suffix(5)) + String(m_tagUrl)) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: SweepAddrTool.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(mainSubtleName.prefix(5)) + "okie" + String(user_foodFormalName.prefix(4))) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (main_normalIdent.replacingOccurrences(of: "invest", with: "ID")), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (String(noti_choiceData))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (user_particleValue.replacingOccurrences(of: "every", with: "S") + "ESSID"), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (user_particleValue.replacingOccurrences(of: "every", with: "S") + "ESSID")
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.commenceRequest()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func funcAgent() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((String(m_secondId) + app_fireSoonMessage.replacingOccurrences(of: "popular", with: "s") + String(mYellowCloseKey))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (String(m_secondId) + app_fireSoonMessage.replacingOccurrences(of: "popular", with: "s") + String(mYellowCloseKey)))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(k_tradePath))) {
                //: let str = getUserAgentString()
                let str = utilizer()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.important { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(main_documentData.prefix(6)) + "_devic" + String(noti_rootIssueTitle.suffix(5))) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func utilizer() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (mGiveIdent.replacingOccurrences(of: "trap", with: "xu") + String(dream_indigenousTitle.suffix(4)) + "phon" + String(dreamWarnName.suffix(5)) + "wu_ve" + String(appArrayKey.suffix(6))) + "\(userReplaceContent)" + (String(showLabText.prefix(7)) + showHapMessage.replacingOccurrences(of: "date", with: "ac") + String(mainCheckPath)) + "\(kLimitTitle)" + (String(main_themSegmentMsg.prefix(6)) + "_bund" + String(showAgoMsg.suffix(5))) + "\(k_clickMessage)" + (String(mRackWillFormat) + String(dreamChopFormat.prefix(4)))
    }
}
