
//: Declare String Begin

/*: "formatType" :*/
fileprivate let app_sureName:[Character] = ["f","o","r","m","a","t","T","y","p","e"]

/*: "plain" :*/
fileprivate let appSweepTitle:[Character] = ["p","l","a","i","n"]

/*: "init(coder:) has not been implemented" :*/
fileprivate let app_closeFormat:[UInt8] = [0x51,0x56,0x51,0x4c,0x10,0x5b,0x57,0x5c,0x5d,0x4a,0x2,0x11,0x18,0x50,0x59,0x4b,0x18,0x56,0x57,0x4c,0x18,0x5a,0x5d,0x5d,0x56,0x18,0x51,0x55,0x48,0x54,0x5d,0x55,0x5d,0x56,0x4c,0x5d,0x5c]

private func sureQuantity(increase num: UInt8) -> UInt8 {
    return num ^ 56
}

/*: "#FF2348" :*/
fileprivate let showTarTitle:[Character] = ["#","F","F","2","3","4","8"]

/*: "icon_lounge" :*/
fileprivate let m_fenceCurrentId:String = "icon_count sequence wed mean"

/*: "icon_home_v" :*/
fileprivate let dreamCreatePath:String = "quality greenicon_"
fileprivate let dreamRoundingName:String = "exactly"

/*: "icon_chat_list_new" :*/
fileprivate let dreamAdvancedName:String = "icon_creward drawing"
fileprivate let mObserveFarGuardMessage:[Character] = ["h"]
fileprivate let dreamAllName:String = "tender headlineat_li"

/*: "Official" :*/
fileprivate let main_whiteOthersMsg:String = "ok select ignoreOfficial"

/*: "#30D00B" :*/
fileprivate let dream_monitorBallKey:[Character] = ["#","3","0","D","0","0","B"]

/*: "iv_message_honey" :*/
fileprivate let kFailureMsg:String = "attach missing primaryiv_mes"
fileprivate let dreamChaseOkUrl:[Character] = ["s","a","g","e","_","h","o","n","e","y"]

/*: "#FF60B9" :*/
fileprivate let user_communicateName:String = "#FF60B9balance bond"

/*: "#EEEEEE" :*/
fileprivate let main_underContent:[Character] = ["#","E","E","E","E","E"]
fileprivate let main_sheData:String = "well"

/*: "opacity" :*/
fileprivate let dream_scriptMessage:String = "OPACITY"

/*: "path" :*/
fileprivate let dreamMultipleIdent:String = "padailyh"

/*: "extra" :*/
fileprivate let noti_imageStr:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let notiThreadId:String = "msgInwhole open lap vision"
fileprivate let kHintKey:[Character] = ["f","o"]

/*: "content" :*/
fileprivate let mFunActName:String = "contthumbt"

/*: "messageType" :*/
fileprivate let mainKitData:String = "zoness"
fileprivate let mEnhanceData:String = "E"

/*: "[Reply messages to get points]" :*/
fileprivate let show_absenceUrl:String = "[Repparticipant wow later processor"
fileprivate let notiGrassListenTitle:String = "darkdarkag"
fileprivate let dream_roleName:String = "read can second hangget "
fileprivate let userCookieTitle:[Character] = ["p","o","i","n","t","s","]"]

/*: "title" :*/
fileprivate let user_stretchDateKey:String = "titsimultaneouse"

/*: "img" :*/
fileprivate let dreamChemistryExtremeName:[Character] = ["i","m","g"]

/*: "[Photo]" :*/
fileprivate let dream_speakPowData:String = "[Photmoment detect"
fileprivate let k_environmentBeText:String = "root roundingo]"

/*: "[Audio]" :*/
fileprivate let dream_hintStr:String = "letter influential[Audi"
fileprivate let m_movieValue:String = "o]tone tone plastic import"

/*: "rYMsgType" :*/
fileprivate let appConditionKey:String = "rYMsgTypetrack female dog alter strategy"

/*: "GJ:CallCustom" :*/
fileprivate let dreamRedPanData:String = "be serviceGJ:Cal"
fileprivate let mStatWithId:String = "lCustomsign end channel"

/*: "callCmd" :*/
fileprivate let mainHerUrl:String = "callCmdsun outside sound message"

/*: "missCall" :*/
fileprivate let m_evaluationFormat:[Character] = ["m","i","s"]
fileprivate let main_acrossFormat:[Character] = ["s","C","a","l","l"]

/*: "[Missed Call]" :*/
fileprivate let dream_thenFormat:String = "[Misseuser end makeup ambitious house"

/*: "logType" :*/
fileprivate let show_exposureAmpStr:String = "larrow"
fileprivate let userDevoteFormat:String = "info extended plant publiclygType"

/*: "[Voice Call]" :*/
fileprivate let m_yourName:[Character] = ["[","V","o","i","c","e"]
fileprivate let notiFarSoilId:[Character] = [" "]
fileprivate let dreamIconStr:[Character] = ["C","a","l","l","]"]

/*: "[Video Call]" :*/
fileprivate let mViaInmateContent:String = "mystery alongside[Vide"
fileprivate let mPendingSlightMessage:[Character] = ["l","]"]

/*: "video" :*/
fileprivate let k_reactData:String = "vdestroyeo"

/*: "contentType" :*/
fileprivate let showSayId:String = "coadditionale"

/*: "VideoMsg" :*/
fileprivate let noti_proRemId:String = "VideoMsgshot open"

/*: "[Short Video]" :*/
fileprivate let k_mindMsg:String = "[Sbill abs self politic help"
fileprivate let notiCompoundValue:String = "automatically creation participanthort "

/*: "match" :*/
fileprivate let notiInjuryIdent:String = "MATCH"

/*: "[Cupid messaging]" :*/
fileprivate let notiAccelerateText:String = "[Cupiprogress ease"
fileprivate let dream_normalMessage:String = "warm device no panel bridgeging]"

/*: "GJ:Sess:TxtCustom" :*/
fileprivate let showEveningPath:String = "GJ:Sesgood between progress advanced me"
fileprivate let noti_rangeKey:String = "surgery publics:Txt"
fileprivate let m_stackIdent:String = "dramatically admin where slow modelCustom"

/*: "isSend" :*/
fileprivate let mainScrambleText:String = "isSennothing sharp tab item"
fileprivate let noti_directorMsg:String = "time"

/*: "Sent Gift" :*/
fileprivate let m_productionStr:[Character] = ["S","e"]
fileprivate let dream_borderEveryId:String = "invite hike span child quotent Gift"

/*: "Receive Gift" :*/
fileprivate let main_slideStr:String = "Receiveadmin channel select"
fileprivate let appCutValue:[Character] = [" ","G","i","f","t"]

/*: "uid" :*/
fileprivate let noti_reverseIdent:String = "UID"

/*: "gift" :*/
fileprivate let k_behaviorTitle:[Character] = ["g","i","f","t"]

/*: "name" :*/
fileprivate let main_networkRidPath:String = "nowne"

/*: "num" :*/
fileprivate let mainDefenseFormat:String = "npose"

/*: "imageUri" :*/
fileprivate let user_operationText:[Character] = ["i","m","a","g","e","U","r","i"]

/*: "imageData" :*/
fileprivate let k_heValue:[Character] = ["i","m","a","g","e","D","a","t","a"]

/*: "<img>" :*/
fileprivate let dream_teammateName:String = "year difference<img>"

/*: "</img>" :*/
fileprivate let k_resolutionPath:[Character] = ["<","/","i","m","g",">"]

/*: "wantGift" :*/
fileprivate let show_deadlineName:[Character] = ["w","a","n","t","G","i"]
fileprivate let noti_dropBodyTitle:[Character] = ["f","t"]

/*: "intimatePhoto" :*/
fileprivate let mNoseStr:String = "intron"
fileprivate let main_physicalText:String = "tihourate"

/*: "audioData" :*/
fileprivate let mainPopularPath:String = "aumaximumio"
fileprivate let mRailroadTitle:String = "Datalater for visual colon pow"

/*: "audioUri" :*/
fileprivate let m_userId:[Character] = ["a","u","d","i","o","U","r","i"]

/*: "draftContent" :*/
fileprivate let notiRecommendationWellRealizeStr:[Character] = ["d","r","a","f","t","C","o","n","t","e","n","t"]

/*: "[draft]%@" :*/
fileprivate let main_analysisValue:[Character] = ["[","d","r","a","f","t","]","%","@"]

/*: "99999+" :*/
fileprivate let dream_underData:[UInt8] = [0x42,0x42,0x42,0x42,0x42,0x50]

private func minimalFlow(grace num: UInt8) -> UInt8 {
    return num ^ 123
}

/*: "New" :*/
fileprivate let dream_concentrationMessage:String = "subtle function increasingNew"

/*: "Account is restricted！" :*/
fileprivate let noti_sceneTitle:[Character] = ["A","c","c","o","u","n","t"," ","i","s"," ","r","e","s","t","r","i","c","t","e","d","！"]

/*: "%@ Online" :*/
fileprivate let main_womanTitle:String = "%@ Onlinegap disc wrap poke"

/*: "Messages not been replied here" :*/
fileprivate let kAuthorUrl:[Character] = ["M","e","s","s","a","g","e","s"," ","n","o","t"," ","b","e","e"]
fileprivate let dreamSupplyKey:String = "n rewedding trap"
fileprivate let user_edgeFormat:[Character] = ["p","l","i","e","d"," ","h","e","r","e"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SceneryView.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/12.
//

//: import UIKit
import UIKit

//: let RCSystemFormatType                 = "formatType"
let appRobotNameStr = (String(app_sureName))
//: let RCSystemFormatTypePlain            = "plain"
let showStorageManagerData = (String(appSweepTitle))

//: class TalkingChatListTableCell: UITableViewCell {
class SceneryView: UITableViewCell {
    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: super.init(coder: coder)
        super.init(coder: coder)
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: app_closeFormat.map{sureQuantity(increase: $0)}, encoding: .utf8)!)
    }

    //: override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        //: super.init(style: style, reuseIdentifier: reuseIdentifier)
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        //: createCellUI()
        astatineFlag()
        //: createCellUIConstraints()
        estimated()
    }

    // MARK: - Lazy load

    //: private lazy var headImgView: UIImageView = {
    private lazy var headImgView: UIImageView = {
        //: let imageView = UIImageView.init()
        let imageView = UIImageView()
        //: imageView.layer.cornerRadius = 30
        imageView.layer.cornerRadius = 30
        //: imageView.clipsToBounds = true
        imageView.clipsToBounds = true
        //: imageView.backgroundColor = UIColor.white
        imageView.backgroundColor = UIColor.white
        //: imageView.contentMode = .scaleAspectFill
        imageView.contentMode = .scaleAspectFill
        //: return imageView
        return imageView
        //: }()
    }()

    //: private lazy var iconBorder: UIButton = {
    private lazy var iconBorder: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.isUserInteractionEnabled = false
        btn.isUserInteractionEnabled = false
        //: self.addSubview(btn)
        self.addSubview(btn)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var badgeLab: UILabel = {
    private lazy var badgeLab: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 13)
        label.font = .concern(type: .Medium, fontSize: 13)
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.textColor = .white
        label.textColor = .white
        //: label.backgroundColor = UIColor(hex: "#FF2348")
        label.backgroundColor = UIColor(hex: (String(showTarTitle)))
        //: label.layer.cornerRadius = 10
        label.layer.cornerRadius = 10
        //: label.layer.masksToBounds = true
        label.layer.masksToBounds = true
        //: label.layer.borderColor = UIColor.white.cgColor
        label.layer.borderColor = UIColor.white.cgColor
        //: label.layer.borderWidth = 1
        label.layer.borderWidth = 1
        //: label.isHidden = true
        label.isHidden = true
        //: return label
        return label
        //: }()
    }()

    //: private lazy var nickNameLabel: UILabel = {
    private lazy var nickNameLabel: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 18)
        label.font = .concern(type: .Medium, fontSize: 18)
        //: label.textColor = .appTitleColor()
        label.textColor = .telecom()
        //: return label
        return label
        //: }()
    }()

    //: private lazy var msgShowLabel: UILabel = {
    private lazy var msgShowLabel: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 15)
        label.font = .concern(type: .Regular, fontSize: 15)
        //: label.textColor = .appValueDetailColor()
        label.textColor = .appLayer()
        //: return label
        return label
        //: }()
    }()

    //: private lazy var timeLab: UILabel = {
    private lazy var timeLab: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 12)
        label.font = .concern(type: .Medium, fontSize: 12)
        //: label.textColor = .appValueDetailColor()
        label.textColor = .appLayer()
        //: return label
        return label
        //: }()
    }()

    //: lazy var loungeImgV: UIImageView = {
    lazy var loungeImgV: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.verbalDescription(name: (String(m_fenceCurrentId.prefix(5)) + "lounge"))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var authImgV: UIImageView = {
    private lazy var authImgV: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image = UIImage.BundleImageNamed(name: "icon_home_v")
        imgV.image = UIImage.verbalDescription(name: (String(dreamCreatePath.suffix(5)) + "home_" + dreamRoundingName.replacingOccurrences(of: "exactly", with: "v")))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var newImgV: TalkingButton = {
    private lazy var newImgV: GoddamnThen = {
        //: let imgV = TalkingButton()
        let imgV = GoddamnThen()
        //: imgV.setBackgroundImage(UIImage.BundleImageNamed(name: "icon_chat_list_new"), for: .normal)
        imgV.setBackgroundImage(UIImage.verbalDescription(name: (String(dreamAdvancedName.prefix(6)) + String(mObserveFarGuardMessage) + String(dreamAllName.suffix(5)) + "st_new")), for: .normal)
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var officialImgV: UIButton = {
    private lazy var officialImgV: UIButton = {
        //: let img = UIButton()
        let img = UIButton()
        //: img.setBackgroundColor(color: UIColor.msgTipsColor(), forState: .normal)
        img.inviteLicense(color: UIColor.animalOf(), forState: .normal)
        //: img.setTitle("Official".localized, for: .normal)
        img.setTitle((String(main_whiteOthersMsg.suffix(8))).localized, for: .normal)
        //: img.setTitleColor(.white, for: .normal)
        img.setTitleColor(.white, for: .normal)
        //: img.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 11)
        img.titleLabel?.font = UIFont.duringWindowExtreme(fontSize: 11)
        //: img.layer.cornerRadius = 8
        img.layer.cornerRadius = 8
        //: img.layer.masksToBounds = true
        img.layer.masksToBounds = true
        //: return img
        return img
        //: }()
    }()

    //: private lazy var onlineStatus: UIView = {
    private lazy var onlineStatus: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = UIColor(hex: "#30D00B")
        v.backgroundColor = UIColor(hex: (String(dream_monitorBallKey)))
        //: v.layer.cornerRadius = 5
        v.layer.cornerRadius = 5
        //: v.layer.borderColor = UIColor.white.cgColor
        v.layer.borderColor = UIColor.white.cgColor
        //: v.layer.borderWidth = 1
        v.layer.borderWidth = 1
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var imtimateBtn: TalkingButton = {
    private lazy var imtimateBtn: GoddamnThen = {
        //: let btn = TalkingButton.init()
        let btn = GoddamnThen()
        //: btn.setImage(UIImage.BundleImageNamed(name: "iv_message_honey"), for: .normal)
        btn.setImage(UIImage.verbalDescription(name: (String(kFailureMsg.suffix(6)) + String(dreamChaseOkUrl))), for: .normal)
        //: btn.spaceBetweenTitleAndImage = 2
        btn.spaceBetweenTitleAndImage = 2
        //: btn.titleLabel?.font = .pingfangFont(type: .Medium, fontSize: 12)
        btn.titleLabel?.font = .concern(type: .Medium, fontSize: 12)
        //: btn.setTitleColor(UIColor.init(hex: "#FF60B9"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(user_communicateName.prefix(7)))), for: .normal)
        //: btn.contentHorizontalAlignment = .right
        btn.contentHorizontalAlignment = .right
        //: if LanguageManager.shared.direction == .rightToLeft {
        if SolidUntilLanguageManager.shared.direction == .rightToLeft {
            //: btn.contentHorizontalAlignment = .left
            btn.contentHorizontalAlignment = .left
        }
        //: return btn
        return btn
        //: }()
    }()
}

//: extension TalkingChatListTableCell {
extension SceneryView {
    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
        //: let animationColor = UIColor.init(hex: "#EEEEEE")
        let animationColor = UIColor(hex: (String(main_underContent) + main_sheData.replacingOccurrences(of: "well", with: "E")))
        //: let animationDuration: Double = 0.4
        let animationDuration = 0.4

        //: super.touchesBegan(touches, with: event)
        super.touchesBegan(touches, with: event)
        //: let coverView = UIView(frame: bounds)
        let coverView = UIView(frame: bounds)
        //: coverView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        coverView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        //: coverView.backgroundColor = UIColor.clear
        coverView.backgroundColor = UIColor.clear
        //: self.addSubview(coverView)
        self.addSubview(coverView)

        //: let touch = touches.first!
        let touch = touches.first!
        //: let point = touch.location(in: self)
        let point = touch.location(in: self)

        //: let ourTouchView = UIView(frame: CGRect(x: point.x - 5, y: point.y - 5, width: 10, height: 10))
        let ourTouchView = UIView(frame: CGRect(x: point.x - 5, y: point.y - 5, width: 10, height: 10))

        //: let circleMaskPathInitial = UIBezierPath(ovalIn: ourTouchView.frame)
        let circleMaskPathInitial = UIBezierPath(ovalIn: ourTouchView.frame)
        //: let pathRect = self.bounds.inset(by: UIEdgeInsets(top: 1, left: 1, bottom: 1, right: 1))
        let pathRect = self.bounds.inset(by: UIEdgeInsets(top: 1, left: 1, bottom: 1, right: 1))
        //: let circleMaskPathFinal = UIBezierPath(roundedRect: pathRect, cornerRadius: 5)
        let circleMaskPathFinal = UIBezierPath(roundedRect: pathRect, cornerRadius: 5)

        //: let rippleLayer = CAShapeLayer()
        let rippleLayer = CAShapeLayer()
        //: rippleLayer.opacity = 1.0
        rippleLayer.opacity = 1.0
        //: rippleLayer.fillColor = animationColor?.cgColor
        rippleLayer.fillColor = animationColor?.cgColor
        //: rippleLayer.path = circleMaskPathFinal.cgPath
        rippleLayer.path = circleMaskPathFinal.cgPath
        //: coverView.layer.addSublayer(rippleLayer)
        coverView.layer.addSublayer(rippleLayer)

        // fade up
        //: let fadeUp = CABasicAnimation(keyPath: "opacity")
        let fadeUp = CABasicAnimation(keyPath: (dream_scriptMessage.lowercased()))
        //: fadeUp.beginTime = CACurrentMediaTime()
        fadeUp.beginTime = CACurrentMediaTime()
        //: fadeUp.duration = 0.1
        fadeUp.duration = 0.1
        //: fadeUp.toValue = 0.8
        fadeUp.toValue = 0.8
        //: fadeUp.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        fadeUp.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: fadeUp.fillMode = CAMediaTimingFillMode.forwards
        fadeUp.fillMode = CAMediaTimingFillMode.forwards
        //: fadeUp.isRemovedOnCompletion = false
        fadeUp.isRemovedOnCompletion = false
        //: rippleLayer.add(fadeUp, forKey: nil)
        rippleLayer.add(fadeUp, forKey: nil)

        // fade down
        //: let fade = CABasicAnimation(keyPath: "opacity")
        let fade = CABasicAnimation(keyPath: (dream_scriptMessage.lowercased()))
        //: fade.beginTime = CACurrentMediaTime() + animationDuration * 0.60
        fade.beginTime = CACurrentMediaTime() + animationDuration * 0.60
        //: fade.duration = 0.1
        fade.duration = 0.1
        //: fade.toValue = 0
        fade.toValue = 0
        //: fade.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        fade.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: fade.fillMode = CAMediaTimingFillMode.forwards
        fade.fillMode = CAMediaTimingFillMode.forwards
        //: fade.isRemovedOnCompletion = false
        fade.isRemovedOnCompletion = false
        //: rippleLayer.add(fade, forKey: nil)
        rippleLayer.add(fade, forKey: nil)

        // change path
        //: CATransaction.begin()
        CATransaction.begin()
        //: let maskLayerAnimation = CABasicAnimation(keyPath: "path")
        let maskLayerAnimation = CABasicAnimation(keyPath: (dreamMultipleIdent.replacingOccurrences(of: "daily", with: "t")))
        //: maskLayerAnimation.fromValue = circleMaskPathInitial.cgPath
        maskLayerAnimation.fromValue = circleMaskPathInitial.cgPath
        //: maskLayerAnimation.toValue = circleMaskPathFinal.cgPath
        maskLayerAnimation.toValue = circleMaskPathFinal.cgPath
        //: maskLayerAnimation.beginTime = CACurrentMediaTime()
        maskLayerAnimation.beginTime = CACurrentMediaTime()
        //: maskLayerAnimation.duration = 0.1
        maskLayerAnimation.duration = 0.1
        //: maskLayerAnimation.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        maskLayerAnimation.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: CATransaction.setCompletionBlock({
        CATransaction.setCompletionBlock {
            //: coverView.removeFromSuperview()
            coverView.removeFromSuperview()
            //: })
        }
        //: rippleLayer.add(maskLayerAnimation, forKey: "path")
        rippleLayer.add(maskLayerAnimation, forKey: (dreamMultipleIdent.replacingOccurrences(of: "daily", with: "t")))
        //: CATransaction.commit()
        CATransaction.commit()
    }
}

// MARK: - Public Event

//: extension TalkingChatListTableCell {
extension SceneryView {
    /// 刷新cell
    /// - Parameter model: 模型
    //: func refreshCell(model: TalkingConversationModel?) {
    func chemistryLaboratory(model: SayPreviouslyReactiveCompatible?) {
        //: guard let model = model else { return }
        guard let model = model else { return }
        //: let isOther = (model.chatType == .system || model.chatType == .service ||
        let isOther = (model.chatType == .system || model.chatType == .service ||
            //: model.chatType == .group || model.chatType == .moreMsg)
            model.chatType == .group || model.chatType == .moreMsg)
        // 更新昵称、头像、vip标识
        //: updateUserInfo(userInfo: model.gj_userInfo, model: model, isOther: isOther)
        distinctness(userInfo: model.gj_userInfo, model: model, isOther: isOther)
        // 未读消息数
        //: updateUnreadNumber(unreadCount: Int(model.unreadCount))
        nearGuide(unreadCount: Int(model.unreadCount))
        // 详情消息
        //: handleC2CMessage(model: model)
        toTillModel(model: model)

        //: if model.chatType == .group || model.chatType == .moreMsg {
        if model.chatType == .group || model.chatType == .moreMsg {
            //: self.accessoryType = .disclosureIndicator
            self.accessoryType = .disclosureIndicator // 添加箭头
            //: } else {
        } else {
            //: self.accessoryType = .none
            self.accessoryType = .none
        }
        // 时间
        //: timeLab.text = nil
        timeLab.text = nil
        //: if isOther == false && model.hasTimestamp {
        if isOther == false, model.hasTimestamp {
            //: let timestamp: Date? = model.timestamp as Date?
            let timestamp: Date? = model.timestamp as Date?
            //: if timestamp != nil && timestamp!.timeIntervalSince1970 > NSTimeIntervalSince1970 {
            if timestamp != nil, timestamp!.timeIntervalSince1970 > NSTimeIntervalSince1970 {
                //: timeLab.text = NSDate.tx_messageTimeString(showDetail: false, date: timestamp!)
                timeLab.text = NSDate.roundPermission(showDetail: false, date: timestamp!)
            }
        }
        // 置顶背景色
        //: if model.isPinned == true {
        if model.isPinned == true {
            //: contentView.backgroundColor = UIColor(hex: "#EEEEEE")
            contentView.backgroundColor = UIColor(hex: (String(main_underContent) + main_sheData.replacingOccurrences(of: "well", with: "E")))
            //: } else {
        } else {
            //: contentView.backgroundColor = (AppManager.share.appStatus == AppSkinStatus.special.rawValue) ? .clear:.white
            contentView.backgroundColor = (AppManager.share.appStatus == WrinkleSendable.special.rawValue) ? .clear : .white
        }
        //: self.backgroundColor = contentView.backgroundColor
        self.backgroundColor = contentView.backgroundColor
    }

    /// 处理最新消息
    //: static func handleShowC2CDetailTextOfMessage(model: TalkingConversationModel) -> NSMutableAttributedString? {
    static func fieldAlongside(model: SayPreviouslyReactiveCompatible) -> NSMutableAttributedString? {
//        messageType 消息类型：1系统消息 2官方小秘 3自动打招呼 4主动打招呼 5付费消息 6普通消息 7主动打招呼未回复 8.主打招呼回复 9，自动打招呼未回复 10, 自动打招呼回复11.发送礼物消息 12互相关注消息 13 取消关注 22家族邀请跳转消息 25索要礼物
        //: var detailAttrString: NSMutableAttributedString?
        var detailAttrString: NSMutableAttributedString?
        //: let change = false
        let change = false
        //: var speColor = [NSValue: UIColor]()
        var speColor = [NSValue: UIColor]()
        //: let showUnread = model.isShowMsgUnread && model.unreadCount > 0
        let showUnread = model.isShowMsgUnread && model.unreadCount > 0
        //: let direction: TMsgDirection = (model.listShowMessage?.isSelf ?? false) ? .MsgDirectionOutgoing:.MsgDirectionIncoming
        let direction: AbsenceWidthTotal = (model.listShowMessage?.isSelf ?? false) ? .MsgDirectionOutgoing : .MsgDirectionIncoming
        //: var voice_msgInfo: JSON?
        var voice_msgInfo: JSON?

        //: guard model.draftText.isEmptyString else {
        guard model.draftText.isEmptyString else { // 草稿
            //: detailAttrString = self.getDraftString(draft: &model.draftText)
            detailAttrString = self.errorAnnouncement(draft: &model.draftText)
            //: detailAttrString?.addAttribute(.font, value: UIFont.pingfangFont(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            detailAttrString?.addAttribute(.font, value: UIFont.concern(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            //: return detailAttrString
            return detailAttrString
        }

        //: let extra = String(data: (model.listShowMessage?.customElem.data)!, encoding: .utf8)
        let extra = String(data: (model.listShowMessage?.customElem.data)!, encoding: .utf8)
        //: let json = JSON(parseJSON: extra!)
        let json = JSON(parseJSON: extra!)
        //: let extraDic = json["extra"]
        let extraDic = json[(String(noti_imageStr))]
        //: let msgInfo = extraDic["msgInfo"]
        let msgInfo = extraDic[(String(notiThreadId.prefix(5)) + String(kHintKey))]
        //: var content = msgInfo["content"].stringValue.deleteHtmlTips()
        var content = msgInfo[(mFunActName.replacingOccurrences(of: "thumb", with: "en"))].stringValue.north()
        //: let messageType = msgInfo["messageType"].intValue
        let messageType = msgInfo[(mainKitData.replacingOccurrences(of: "zone", with: "me") + "ageTyp" + mEnhanceData.lowercased())].intValue
        //: let formatTypeStr = extraDic["formatType"].stringValue
        let formatTypeStr = extraDic[(String(app_sureName))].stringValue

        //: if TalkingPrivateChatManager.isReplyToGetPointsMsg(direction, messageType, msgTime: model.listShowMessage?.timestamp) {
        if LifeDigitalThen.reward(direction, messageType, msgTime: model.listShowMessage?.timestamp) { // 回复获取积分消息
            //: let content = "[Reply messages to get points]".localized
            let content = (String(show_absenceUrl.prefix(4)) + "ly me" + notiGrassListenTitle.replacingOccurrences(of: "dark", with: "s") + "es to " + String(dream_roleName.suffix(4)) + String(userCookieTitle)).localized
            //: let color  = UIColor.appValueColor()
            let color = UIColor.componentHighlight()
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)
            //: speColor = [NSValue(range: NSRange(location: 0, length: detailAttrString?.length ?? 0)): color]
            speColor = [NSValue(range: NSRange(location: 0, length: detailAttrString?.length ?? 0)): color]

            //: } else if model.chatType == .system {
        } else if model.chatType == .system { // 官方消息，只展示文本
            //: var content = extraDic["title"].stringValue
            var content = extraDic[(user_stretchDateKey.replacingOccurrences(of: "simultaneous", with: "l"))].stringValue
            //: let sysContent = extraDic["content"].stringValue
            let sysContent = extraDic[(mFunActName.replacingOccurrences(of: "thumb", with: "en"))].stringValue
            //: let sysImg = extraDic["img"].stringValue
            let sysImg = extraDic[(String(dreamChemistryExtremeName))].stringValue
            //: if content.count > 0 || sysContent.count > 0 || sysImg.count > 0 {
            if content.count > 0 || sysContent.count > 0 || sysImg.count > 0 { // 跳转系统消息
                //: if content.count == 0 {
                if content.count == 0 {
                    //: content.append(sysContent)
                    content.append(sysContent)
                }
                //: if sysImg.count > 0 {
                if sysImg.count > 0 {
                    //: content.append("[Photo]".localized)
                    content.append((String(dream_speakPowData.prefix(5)) + String(k_environmentBeText.suffix(2))).localized)
                }

                //: } else {
            } else { // 默认系统消息
                //: content = msgInfo["content"].stringValue
                content = msgInfo[(mFunActName.replacingOccurrences(of: "thumb", with: "en"))].stringValue
                //: if content.isEmptyString {
                if content.isEmptyString {
                    //: content = json["content"].stringValue
                    content = json[(mFunActName.replacingOccurrences(of: "thumb", with: "en"))].stringValue
                }
                //: content = content.deleteHtmlTips()
                content = content.north()
            }
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if SayEditChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
        } else if SayEditChatManager.shrink(msginfo: msgInfo.dictionaryValue) { // 语音
            //: voice_msgInfo = msgInfo
            voice_msgInfo = msgInfo
            //: content = "[Audio]".localized
            content = (String(dream_hintStr.suffix(5)) + String(m_movieValue.prefix(2))).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if json["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话
        } else if json[(String(appConditionKey.prefix(9)))].stringValue == (String(dreamRedPanData.suffix(6)) + String(mStatWithId.prefix(7))) { // 音视频通话
            //: if msgInfo["callCmd"].stringValue == "missCall" { // 未接来电
            if msgInfo[(String(mainHerUrl.prefix(7)))].stringValue == (String(m_evaluationFormat) + String(main_acrossFormat)) { // 未接来电
                //: content = "[Missed Call]".localized
                content = (String(dream_thenFormat.prefix(6)) + "d Call]").localized
                //: } else {
            } else {
                //: if msgInfo["logType"].intValue == 1 { // 音频
                if msgInfo[(show_exposureAmpStr.replacingOccurrences(of: "arrow", with: "o") + String(userDevoteFormat.suffix(5)))].intValue == 1 { // 音频
                    //: content = "[Voice Call]".localized
                    content = (String(m_yourName) + String(notiFarSoilId) + String(dreamIconStr)).localized
                    //: } else {
                } else {
                    //: content = "[Video Call]".localized
                    content = (String(mViaInmateContent.suffix(5)) + "o Cal" + String(mPendingSlightMessage)).localized
                }
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if msgInfo["video"].exists() || msgInfo["contentType"].stringValue == "VideoMsg" { // 短视频
        } else if msgInfo[(k_reactData.replacingOccurrences(of: "destroy", with: "id"))].exists() || msgInfo[(showSayId.replacingOccurrences(of: "additional", with: "nt") + "ntType")].stringValue == (String(noti_proRemId.prefix(8))) { // 短视频
            //: content = "[Short Video]".localized
            content = (String(k_mindMsg.prefix(2)) + String(notiCompoundValue.suffix(5)) + "Video]").localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if (extraDic["match"].stringValue == "1" || msgInfo["match"].stringValue == "1") { // 匹配消息
        } else if extraDic[(notiInjuryIdent.lowercased())].stringValue == "1" || msgInfo[(notiInjuryIdent.lowercased())].stringValue == "1" { // 匹配消息
            //: content = "[Cupid messaging]".localized
            content = (String(notiAccelerateText.prefix(5)) + "d messa" + String(dream_normalMessage.suffix(5))).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if json["rYMsgType"].stringValue == "GJ:Sess:TxtCustom" { // 提示消息
        } else if json[(String(appConditionKey.prefix(9)))].stringValue == (String(showEveningPath.prefix(6)) + String(noti_rangeKey.suffix(5)) + String(m_stackIdent.suffix(6))) { // 提示消息
            //: content = json["content"].stringValue
            content = json[(mFunActName.replacingOccurrences(of: "thumb", with: "en"))].stringValue
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 11 {
        } else if messageType == 11 { // 送礼
            //: let isSend = msgInfo["isSend"].string
            let isSend = msgInfo[(String(mainScrambleText.prefix(5)) + noti_directorMsg.replacingOccurrences(of: "time", with: "d"))].string
            //: var string1: String?
            var string1: String?
            //: if isSend != nil {
            if isSend != nil {
                //: string1 = isSend!.boolValue ? "Sent Gift".localized:"Receive Gift".localized
                string1 = isSend!.boolValue ? (String(m_productionStr) + String(dream_borderEveryId.suffix(7))).localized : (String(main_slideStr.prefix(7)) + String(appCutValue)).localized
                //: } else {
            } else {
                //: let uid = msgInfo["uid"].stringValue
                let uid = msgInfo[(noti_reverseIdent.lowercased())].stringValue
                //: string1 = (uid == AppManager.share.loginUserMode.userID) ? "Sent Gift".localized:"Receive Gift".localized
                string1 = (uid == AppManager.share.loginUserMode.userID) ? (String(m_productionStr) + String(dream_borderEveryId.suffix(7))).localized : (String(main_slideStr.prefix(7)) + String(appCutValue)).localized
            }
            //: let name = extraDic["gift"]["name"].stringValue
            let name = extraDic[(String(k_behaviorTitle))][(main_networkRidPath.replacingOccurrences(of: "own", with: "am"))].stringValue
            //: let num = extraDic["gift"]["num"].stringValue
            let num = extraDic[(String(k_behaviorTitle))][(mainDefenseFormat.replacingOccurrences(of: "pose", with: "um"))].stringValue

            //: content = "\(string1!) \(name) x\(num)"
            content = "\(string1!) \(name) x\(num)"
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 4 {
        } else if messageType == 4 { // 主动打招呼
//            let uid = msgInfo["uid"].stringValue
//            let toUid = msgInfo["toUid"].stringValue
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 3 {
        } else if messageType == 3 { // 自动打招呼
            //: let imageUri = msgInfo["imageUri"].stringValue
            let imageUri = msgInfo[(String(user_operationText))].stringValue
            //: let imageData = msgInfo["imageData"].stringValue
            let imageData = msgInfo[(String(k_heValue))].stringValue

            //: if SayEditChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
            if SayEditChatManager.shrink(msginfo: msgInfo.dictionaryValue) {
                //: content = "[Audio]".localized
                content = (String(dream_hintStr.suffix(5)) + String(m_movieValue.prefix(2))).localized
                //: } else if (imageUri.count > 0 || imageData.count > 0) {
            } else if imageUri.count > 0 || imageData.count > 0 {
                //: content = "[Photo]".localized
                content = (String(dream_speakPowData.prefix(5)) + String(k_environmentBeText.suffix(2))).localized
            }
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if formatTypeStr == RCSystemFormatTypePlain {
        } else if formatTypeStr == showStorageManagerData {
            //: var content  = msgInfo["content"].stringValue
            var content = msgInfo[(mFunActName.replacingOccurrences(of: "thumb", with: "en"))].stringValue
            //: var imageUrl = extraDic["imageUri"].stringValue
            var imageUrl = extraDic[(String(user_operationText))].stringValue
            //: let imageData = extraDic["imageData"].stringValue
            let imageData = extraDic[(String(k_heValue))].stringValue
            //: if imageUrl.count == 0 || imageData.count > 0 {
            if imageUrl.count == 0 || imageData.count > 0 {
                //: if content.hasPrefix("<img>") && content.hasSuffix("</img>") {
                if content.hasPrefix((String(dream_teammateName.suffix(5)))), content.hasSuffix((String(k_resolutionPath))) {
                    //: var str = NSString(string: content)
                    var str = NSString(string: content)
                    //: str = str.replacingOccurrences(of: "<img>", with: "") as NSString
                    str = str.replacingOccurrences(of: (String(dream_teammateName.suffix(5))), with: "") as NSString
                    //: str = str.replacingOccurrences(of: "</img>", with: "") as NSString
                    str = str.replacingOccurrences(of: (String(k_resolutionPath)), with: "") as NSString
                    //: imageUrl = str as String
                    imageUrl = str as String
                }
            }

            //: if (imageUrl.count > 0 || imageData.count > 0) {
            if imageUrl.count > 0 || imageData.count > 0 {
                //: content = "[Photo]".localized
                content = (String(dream_speakPowData.prefix(5)) + String(k_environmentBeText.suffix(2))).localized
                //: } else {
            } else {
                //: content = content.deleteHtmlTips()
                content = content.north()
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 25 {
        } else if messageType == 25 { // 索要礼物
            //: if let wantGift  = msgInfo["wantGift"].dictionary {
            if let wantGift = msgInfo[(String(show_deadlineName) + String(noti_dropBodyTitle))].dictionary {
                //: let content = wantGift["content"]?.string ?? ""
                let content = wantGift[(mFunActName.replacingOccurrences(of: "thumb", with: "en"))]?.string ?? ""
                //: detailAttrString = NSMutableAttributedString(string: content)
                detailAttrString = NSMutableAttributedString(string: content)
            }

            //: } else {
        } else {
            // 图片

            //: let imageUri             = msgInfo["imageUri"].stringValue
            let imageUri = msgInfo[(String(user_operationText))].stringValue
            //: let imageData            = msgInfo["imageData"].stringValue
            let imageData = msgInfo[(String(k_heValue))].stringValue
            //: let intimatePhotoDict    = msgInfo["intimatePhoto"].dictionaryValue // 私密照片
            let intimatePhotoDict = msgInfo[(mNoseStr.replacingOccurrences(of: "intro", with: "i") + main_physicalText.replacingOccurrences(of: "hour", with: "m") + "Photo")].dictionaryValue // 私密照片
            //: if (imageUri.count > 0 || imageData.count > 0 || intimatePhotoDict.keys.count > 0) {
            if imageUri.count > 0 || imageData.count > 0 || intimatePhotoDict.keys.count > 0 {
                //: content = "[Photo]".localized
                content = (String(dream_speakPowData.prefix(5)) + String(k_environmentBeText.suffix(2))).localized
            }
            // 语音消息
            //: if SayEditChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
            if SayEditChatManager.shrink(msginfo: msgInfo.dictionaryValue) {
                //: content = "[Audio]".localized
                content = (String(dream_hintStr.suffix(5)) + String(m_movieValue.prefix(2))).localized
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)
        }

        //: if (!change) {
        if !change {
            //: if detailAttrString?.string == "[Audio]".localized {
            if detailAttrString?.string == (String(dream_hintStr.suffix(5)) + String(m_movieValue.prefix(2))).localized {
                //: let sendUid = model.sender
                let sendUid = model.sender
                //: let mid = AppManager.share.loginUserMode.userID
                let mid = AppManager.share.loginUserMode.userID
                //: if sendUid == mid {
                if sendUid == mid { // 我发送的
                    //: } else {
                } else { // 别人发的
                    //: var downurl = ""
                    var downurl = ""
                    //: if (voice_msgInfo?["audioData"].stringValue.isEmptyString) != nil {
                    if (voice_msgInfo?[(mainPopularPath.replacingOccurrences(of: "maximum", with: "d") + String(mRailroadTitle.prefix(4)))].stringValue.isEmptyString) != nil {
                        //: downurl = voice_msgInfo!["audioData"].stringValue
                        downurl = voice_msgInfo![(mainPopularPath.replacingOccurrences(of: "maximum", with: "d") + String(mRailroadTitle.prefix(4)))].stringValue
                        //: } else if (voice_msgInfo?["audioUri"].stringValue.isEmptyString) != nil {
                    } else if (voice_msgInfo?[(String(m_userId))].stringValue.isEmptyString) != nil {
                        //: downurl = voice_msgInfo!["audioUri"].stringValue
                        downurl = voice_msgInfo![(String(m_userId))].stringValue
                    }
                }
                //: let unreadColor = UIColor.appValueDetailColor()
                let unreadColor = UIColor.appLayer()
                //: detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
                detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))

                //: } else {
            } else {
                //: let unreadColor = UIColor.appValueDetailColor()
                let unreadColor = UIColor.appLayer()
                //: detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
                detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            }
        }
        //: if speColor.keys.count > 0 {
        if speColor.keys.count > 0 {
            //: for (key, value) in speColor {
            for (key, value) in speColor {
                //: detailAttrString?.addAttribute(.foregroundColor, value: value, range: key.rangeValue)
                detailAttrString?.addAttribute(.foregroundColor, value: value, range: key.rangeValue)
            }
        }
        //: detailAttrString?.addAttribute(.font, value: UIFont.pingfangFont(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
        detailAttrString?.addAttribute(.font, value: UIFont.concern(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))

        //: return detailAttrString
        return detailAttrString
    }

    /// 处理草稿消息
    //: static func getDraftString(draft: inout String) -> NSMutableAttributedString {
    static func errorAnnouncement(draft: inout String) -> NSMutableAttributedString {
        //: if draft.contains("draftContent") {
        if draft.contains((String(notiRecommendationWellRealizeStr))) {
            //: let dict = NSDictionary.dictionary(withJsonString: draft)
            let dict = NSDictionary.mouthManage(withJsonString: draft)
            //: draft = "\(String(describing: dict?["draftContent"]))"
            draft = "\(String(describing: dict?[(String(notiRecommendationWellRealizeStr))]))"
        }
        //: let draftString = NSMutableAttributedString.init(string: "[draft]%@".localizedArguments(draft))
        let draftString = NSMutableAttributedString(string: (String(main_analysisValue)).launchRid(draft))
        //: draftString.addAttribute(.foregroundColor, value: UIColor.msgTipsColor(), range: NSRange(location: 0, length: 7))
        draftString.addAttribute(.foregroundColor, value: UIColor.animalOf(), range: NSRange(location: 0, length: 7))
        //: return draftString
        return draftString
    }
}

// MARK: - Private Event

//: extension TalkingChatListTableCell {
extension SceneryView {
    /// 更新昵称、头像
    /// - Parameters:
    ///   - userInfo: 数据库信息
    ///   - model: tx数据
    ///   - isOther: 是否其他消息
    //: private func updateUserInfo(userInfo: RelatedTransformable?, model: TalkingConversationModel, isOther: Bool) {
    private func distinctness(userInfo: RelatedTransformable?, model: SayPreviouslyReactiveCompatible, isOther: Bool) {
        // 在线状态
        //: onlineStatus.isHidden = (model.listOnlineStatus == 0)
        onlineStatus.isHidden = (model.listOnlineStatus == 0)
        //: newImgV.isHidden = !model.isNewUser
        newImgV.isHidden = !model.isNewUser
        // 是否存在用户信息
        //: if userInfo == nil || userInfo?.uid == ImmediatelyThen.getXiaoMiID() || userInfo?.uid == ImmediatelyThen.getCustomerServiceID() {
        if userInfo == nil || userInfo?.uid == ImmediatelyThen.polytetrafluoroethylene() || userInfo?.uid == ImmediatelyThen.femaleId() {
            //: nickNameLabel.textColor = .appTitleColor()
            nickNameLabel.textColor = .telecom()
            //: nickNameLabel.text = model.showName
            nickNameLabel.text = model.showName
            //: nickNameLabel.snp.updateConstraints { make in
            nickNameLabel.snp.updateConstraints { make in
                //: make.trailing.lessThanOrEqualToSuperview().offset(-40)
                make.trailing.lessThanOrEqualToSuperview().offset(-40)
            }
            //: if model.chatType == .group || model.chatType == .moreMsg {
            if model.chatType == .group || model.chatType == .moreMsg {
                //: headImgView.image = UIImage.BundleImageNamed(name: model.faceUrl)
                headImgView.image = UIImage.verbalDescription(name: model.faceUrl)
                //: } else {
            } else {
                //: headImgView.setUrlImage(urlStr: model.faceUrl, placeImg: UIImage.placeSquareBigImgBanner())
                headImgView.beyondFore(urlStr: model.faceUrl, placeImg: UIImage.rootPrimary())
            }
            //: iconBorder.isHidden = true
            iconBorder.isHidden = true
            //: loungeImgV.isHidden = true
            loungeImgV.isHidden = true
            //: authImgV.isHidden = true
            authImgV.isHidden = true
            //: officialImgV.isHidden = true
            officialImgV.isHidden = true
            //: imtimateBtn.isHidden = true
            imtimateBtn.isHidden = true
            //: return
            return
        }
        //: guard let userInfo = userInfo else {
        guard let userInfo = userInfo else {
            //: return
            return
        }
        // 昵称
        //: nickNameLabel.textColor = userInfo.loungePlus ? .userVipColor():.appTitleColor()
        nickNameLabel.textColor = userInfo.loungePlus ? .atSituationRaw() : .telecom()
        //: nickNameLabel.text = userInfo.nickname.count > 0 ? userInfo.nickname:model.userID
        nickNameLabel.text = userInfo.nickname.count > 0 ? userInfo.nickname : model.userID
        // 头像
        //: headImgView.setUrlImage(urlStr: userInfo.headPic, placeImg: UIImage.placeHolderImage(sex: userInfo.sex))
        headImgView.beyondFore(urlStr: userInfo.headPic, placeImg: UIImage.salt(sex: userInfo.sex))
        // VIP头像框
        //: if isOther == false && !userInfo.headPicFrame.isEmptyString {
        if isOther == false, !userInfo.headPicFrame.isEmptyString {
            //: iconBorder.isHidden = false
            iconBorder.isHidden = false
            //: iconBorder.setHeadFrameUrlImage(urlStr: userInfo.headPicFrame)
            iconBorder.setFromImaginationImage(urlStr: userInfo.headPicFrame)
            //: } else {
        } else {
            //: iconBorder.isHidden = true
            iconBorder.isHidden = true
        }
        //: imtimateBtn.setTitle(model.totalIntimate > 99999 ? "99999+": "\(model.totalIntimate)", for: .normal)
        imtimateBtn.setTitle(model.totalIntimate > 99999 ? String(bytes: dream_underData.map{minimalFlow(grace: $0)}, encoding: .utf8)! : "\(model.totalIntimate)", for: .normal)
        //: imtimateBtn.isHidden = model.totalIntimate <= 0
        imtimateBtn.isHidden = model.totalIntimate <= 0
        //: if model.totalIntimate > 0 {
        if model.totalIntimate > 0 {
            //: let size = imtimateBtn.titleLabel?.text?.boundingRect(with: CGSize(width: CGFloat(MAXFLOAT), height: 0.0), options: .usesLineFragmentOrigin, attributes: [NSAttributedString.Key.font: imtimateBtn.titleLabel?.font ?? .pingfangFont(type: .Medium, fontSize: 12)], context: nil)
            let size = imtimateBtn.titleLabel?.text?.boundingRect(with: CGSize(width: CGFloat(MAXFLOAT), height: 0.0), options: .usesLineFragmentOrigin, attributes: [NSAttributedString.Key.font: imtimateBtn.titleLabel?.font ?? .concern(type: .Medium, fontSize: 12)], context: nil)
            //: imtimateBtn.snp.updateConstraints { make in
            imtimateBtn.snp.updateConstraints { make in
                //: make.width.equalTo((size?.width ?? 50) + 20)
                make.width.equalTo((size?.width ?? 50) + 20)
            }
        }
        // 是否其他消息
        //: if isOther == false {
        if isOther == false {
            //: loungeImgV.isHidden = !userInfo.loungePlus
            loungeImgV.isHidden = !userInfo.loungePlus
            //: authImgV.isHidden = !userInfo.tpAuth
            authImgV.isHidden = !userInfo.tpAuth
            //: officialImgV.isHidden = !userInfo.isOfficial
            officialImgV.isHidden = !userInfo.isOfficial
            //: } else {
        } else {
            //: loungeImgV.isHidden = true
            loungeImgV.isHidden = true
            //: authImgV.isHidden = true
            authImgV.isHidden = true
            //: officialImgV.isHidden = true
            officialImgV.isHidden = true
        }

        //: nickNameLabel.snp.updateConstraints { make in
        nickNameLabel.snp.updateConstraints { make in
            //: make.trailing.lessThanOrEqualToSuperview().offset( isOther == true ? -40 : -130)
            make.trailing.lessThanOrEqualToSuperview().offset(isOther == true ? -40 : -130)
        }
        //: loungeImgV.snp.updateConstraints { make in
        loungeImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(nickNameLabel.snp.trailing).offset(loungeImgV.isHidden == true ? 0:4)
            make.leading.equalTo(nickNameLabel.snp.trailing).offset(loungeImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(loungeImgV.isHidden == true ? 0:20)
            make.width.equalTo(loungeImgV.isHidden == true ? 0 : 20)
        }
        //: authImgV.snp.updateConstraints { make in
        authImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(loungeImgV.snp.trailing).offset(authImgV.isHidden == true ? 0:4)
            make.leading.equalTo(loungeImgV.snp.trailing).offset(authImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(authImgV.isHidden == true ? 0:16)
            make.width.equalTo(authImgV.isHidden == true ? 0 : 16)
        }
        //: let text = "New".localized
        let text = (String(dream_concentrationMessage.suffix(3))).localized
        //: let rect = text.boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.pingfangMediumFont(fontSize: 11)], context: nil)
        let rect = text.boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.equitySize(fontSize: 11)], context: nil)
        //: newImgV.snp.updateConstraints { make in
        newImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(authImgV.snp.trailing).offset(newImgV.isHidden == true ? 0:4)
            make.leading.equalTo(authImgV.snp.trailing).offset(newImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(newImgV.isHidden == true ? 0:rect.width+10)
            make.width.equalTo(newImgV.isHidden == true ? 0 : rect.width + 10)
        }
        //: officialImgV.snp.remakeConstraints { make in
        officialImgV.snp.remakeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.height.equalTo(15)
            make.height.equalTo(15)
            //: make.leading.equalTo(newImgV.snp.trailing).offset(officialImgV.isHidden == true ? 0:4)
            make.leading.equalTo(newImgV.snp.trailing).offset(officialImgV.isHidden == true ? 0 : 4)
            //: if officialImgV.isHidden {
            if officialImgV.isHidden {
                //: make.width.equalTo(0)
                make.width.equalTo(0)
                //: } else {
            } else {
                //: make.width.greaterThanOrEqualTo(50)
                make.width.greaterThanOrEqualTo(50)
            }
        }
    }

    /// 更新未读数
    /// - Parameter unreadCount: 未读消息数
    //: private func updateUnreadNumber(unreadCount: Int) {
    private func nearGuide(unreadCount: Int) {
        //: guard unreadCount > 0 else {
        guard unreadCount > 0 else {
            //: self.badgeLab.isHidden = true
            self.badgeLab.isHidden = true
            //: return
            return
        }

        //: self.badgeLab.isHidden = false
        self.badgeLab.isHidden = false
        //: var unreadCountStr = String(unreadCount)
        var unreadCountStr = String(unreadCount)
        //: if unreadCount > 99 {
        if unreadCount > 99 {
            //: unreadCountStr = String(unreadCount) + "+"
            unreadCountStr = String(unreadCount) + "+"
            //: badgeLab.text = "99+"
            badgeLab.text = "99+"
            //: } else {
        } else {
            //: badgeLab.text = unreadCountStr
            badgeLab.text = unreadCountStr
        }
        //: var width = badgeLab.text!.size(withAttributes: [.font: self.badgeLab.font!]).width + 12
        var width = badgeLab.text!.size(withAttributes: [.font: self.badgeLab.font!]).width + 12
        //: if width < 20 { width = 20 }
        if width < 20 { width = 20 }
        //: badgeLab.snp.updateConstraints { make in
        badgeLab.snp.updateConstraints { make in
            //: make.width.greaterThanOrEqualTo(width)
            make.width.greaterThanOrEqualTo(width)
        }
    }

    /// 处理详情消息
    //: private func handleC2CMessage(model: TalkingConversationModel) {
    private func toTillModel(model: SayPreviouslyReactiveCompatible) {
        //: if model.userStatus == 5 {
        if model.userStatus == 5 {
            //: let str = "Account is restricted！".localized
            let str = (String(noti_sceneTitle)).localized
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.pingfangFont(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.concern(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.appValueDetailColor()
                NSAttributedString.Key.foregroundColor: UIColor.appLayer(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            //: return
            return
        }
        //: if model.listShowMessage != nil {
        if model.listShowMessage != nil {
            //: msgShowLabel.attributedText = TalkingChatListTableCell.handleShowC2CDetailTextOfMessage(model: model)
            msgShowLabel.attributedText = SceneryView.fieldAlongside(model: model)
            //: } else if model.chatType == .group {
        } else if model.chatType == .group { // 公共聊天室入口
            //: let str = "%@ Online".localizedArguments(model.num)
            let str = (String(main_womanTitle.prefix(9))).launchRid(model.num)
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.pingfangFont(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.concern(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.appValueDetailColor()
                NSAttributedString.Key.foregroundColor: UIColor.appLayer(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)

            //: } else if model.chatType == .moreMsg {
        } else if model.chatType == .moreMsg { // 更多消息入口，无数据时，占位文案
            //: let str = "Messages not been replied here".localized
            let str = (String(kAuthorUrl) + String(dreamSupplyKey.prefix(4)) + String(user_edgeFormat)).localized
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.pingfangFont(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.concern(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.appValueDetailColor()
                NSAttributedString.Key.foregroundColor: UIColor.appLayer(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)

            //: } else {
        } else { // 草稿箱
            //: var attrStr: NSMutableAttributedString?
            var attrStr: NSMutableAttributedString?
            //: if !model.draftText.isEmptyString {
            if !model.draftText.isEmptyString {
                //: attrStr = TalkingChatListTableCell.getDraftString(draft: &model.draftText)
                attrStr = SceneryView.errorAnnouncement(draft: &model.draftText)
            }
            //: attrStr?.addAttribute(.font, value: UIFont.pingfangFont(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: attrStr?.length ?? 0))
            attrStr?.addAttribute(.font, value: UIFont.concern(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: attrStr?.length ?? 0))
            //: msgShowLabel.attributedText = attrStr
            msgShowLabel.attributedText = attrStr
        }
    }
}

// MARK: - UI

//: extension TalkingChatListTableCell {
extension SceneryView {
    //: private func createCellUI() {
    private func astatineFlag() {
        //: contentView.addSubview(headImgView)
        contentView.addSubview(headImgView)
        //: contentView.addSubview(iconBorder)
        contentView.addSubview(iconBorder)
        //: contentView.addSubview(onlineStatus)
        contentView.addSubview(onlineStatus)
        //: contentView.addSubview(badgeLab)
        contentView.addSubview(badgeLab)
        //: contentView.addSubview(nickNameLabel)
        contentView.addSubview(nickNameLabel)
        //: contentView.addSubview(loungeImgV)
        contentView.addSubview(loungeImgV)
        //: contentView.addSubview(authImgV)
        contentView.addSubview(authImgV)
        //: contentView.addSubview(newImgV)
        contentView.addSubview(newImgV)
        //: contentView.addSubview(officialImgV)
        contentView.addSubview(officialImgV)
        //: contentView.addSubview(msgShowLabel)
        contentView.addSubview(msgShowLabel)
        //: contentView.addSubview(timeLab)
        contentView.addSubview(timeLab)
        //: contentView.addSubview(imtimateBtn)
        contentView.addSubview(imtimateBtn)
    }

    //: private func createCellUIConstraints() {
    private func estimated() {
        //: headImgView.snp.makeConstraints { make in
        headImgView.snp.makeConstraints { make in
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.leading.equalTo(15)
            make.leading.equalTo(15)
            //: make.width.height.equalTo(60)
            make.width.height.equalTo(60)
        }
        //: iconBorder.snp.makeConstraints { make in
        iconBorder.snp.makeConstraints { make in
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.height.equalTo(69.5)
            make.width.height.equalTo(69.5)
        }
        //: onlineStatus.snp.makeConstraints { make in
        onlineStatus.snp.makeConstraints { make in
            //: make.trailing.equalTo(headImgView.snp.trailing).offset(-3)
            make.trailing.equalTo(headImgView.snp.trailing).offset(-3)
            //: make.bottom.equalTo(headImgView.snp.bottom).offset(-3)
            make.bottom.equalTo(headImgView.snp.bottom).offset(-3)
            //: make.width.height.equalTo(10)
            make.width.height.equalTo(10)
        }
        //: badgeLab.snp.makeConstraints { make in
        badgeLab.snp.makeConstraints { make in
            //: make.top.equalTo(self.headImgView.snp.top).offset(-5)
            make.top.equalTo(self.headImgView.snp.top).offset(-5)
            //: make.trailing.equalTo(self.headImgView.snp.trailing).offset(6)
            make.trailing.equalTo(self.headImgView.snp.trailing).offset(6)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
            //: make.width.greaterThanOrEqualTo(20)
            make.width.greaterThanOrEqualTo(20)
        }
        //: nickNameLabel.snp.makeConstraints { make in
        nickNameLabel.snp.makeConstraints { make in
            //: make.top.equalTo(headImgView).offset(6)
            make.top.equalTo(headImgView).offset(6)
            //: make.leading.equalTo(headImgView.snp.trailing).offset(10)
            make.leading.equalTo(headImgView.snp.trailing).offset(10)
            //: make.trailing.lessThanOrEqualToSuperview().offset(-40)
            make.trailing.lessThanOrEqualToSuperview().offset(-40)
            //: make.height.equalTo(21)
            make.height.equalTo(21)
        }
        //: loungeImgV.snp.makeConstraints { make in
        loungeImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(nickNameLabel.snp.trailing).offset(4)
            make.leading.equalTo(nickNameLabel.snp.trailing).offset(4)
            //: make.height.width.equalTo(20)
            make.height.width.equalTo(20)
        }
        //: authImgV.snp.makeConstraints { make in
        authImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(loungeImgV.snp.trailing).offset(4)
            make.leading.equalTo(loungeImgV.snp.trailing).offset(4)
            //: make.width.equalTo(16)
            make.width.equalTo(16)
            //: make.height.equalTo(16)
            make.height.equalTo(16)
        }
        //: newImgV.snp.makeConstraints { make in
        newImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(authImgV.snp.trailing).offset(4)
            make.leading.equalTo(authImgV.snp.trailing).offset(4)
            //: make.width.equalTo(26)
            make.width.equalTo(26)
            //: if LanguageManager.shared.direction == .rightToLeft {
            if SolidUntilLanguageManager.shared.direction == .rightToLeft {
                //: make.height.equalTo(16)
                make.height.equalTo(16)
                //: } else {
            } else {
                //: make.height.equalTo(14)
                make.height.equalTo(14)
            }
        }
        //: officialImgV.snp.makeConstraints { make in
        officialImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(newImgV.snp.trailing).offset(4)
            make.leading.equalTo(newImgV.snp.trailing).offset(4)
            //: make.width.greaterThanOrEqualTo(50)
            make.width.greaterThanOrEqualTo(50)
            //: make.height.equalTo(16)
            make.height.equalTo(16)
        }
        //: timeLab.snp.makeConstraints { make in
        timeLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.trailing.equalToSuperview().offset(-15)
            make.trailing.equalToSuperview().offset(-15)
        }
        //: imtimateBtn.snp.makeConstraints { make in
        imtimateBtn.snp.makeConstraints { make in
            //: make.centerY.equalTo(msgShowLabel)
            make.centerY.equalTo(msgShowLabel)
            //: make.width .equalTo(50)
            make.width.equalTo(50)
            //: make.trailing.equalToSuperview().offset(-15)
            make.trailing.equalToSuperview().offset(-15)
        }
        //: msgShowLabel.snp.makeConstraints { make in
        msgShowLabel.snp.makeConstraints { make in
            //: make.bottom.equalTo(headImgView.snp.bottom).offset(-6)
            make.bottom.equalTo(headImgView.snp.bottom).offset(-6)
            //: make.leading.equalTo(nickNameLabel)
            make.leading.equalTo(nickNameLabel)
            //: make.trailing.equalTo(imtimateBtn.snp.leading).offset(-10)
            make.trailing.equalTo(imtimateBtn.snp.leading).offset(-10)
        }
    }
}
