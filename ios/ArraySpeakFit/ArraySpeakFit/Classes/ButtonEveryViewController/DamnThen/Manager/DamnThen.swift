
//: Declare String Begin

/*: " customElem.data is error" :*/
fileprivate let noti_fatalName:[Character] = [" ","c","u","s","t","o","m","E","l","e","m",".","d","a","t","a"," ","i","s"," ","e","r"]
fileprivate let showRecognizeStr:[Character] = ["r","o","r"]

/*: "extra" :*/
fileprivate let user_fairTitle:String = "ethantra"

/*: "MF:PartyChatSysMsg" :*/
fileprivate let showCalendarId:[Character] = ["M","F",":","P","a","r","t","y","C","h","a","t"]
fileprivate let dream_primaryName:[Character] = ["S","y","s","M","s","g"]

/*: "opType" :*/
fileprivate let m_shrinkValue:String = "opTypeforth stick chase"

/*: "roomLogout" :*/
fileprivate let kRevenueIdent:String = "rfillm"
fileprivate let kFollowingData:String = "ban genLogout"

/*: "MF:PartyChatWelMsg" :*/
fileprivate let mainFailureKey:String = "MF:Parviolation us inner"
fileprivate let show_restrictionPath:String = "WelMtar heat"
fileprivate let user_acquireId:String = "SG"

/*: "startLive" :*/
fileprivate let user_dependenceValue:String = "shandar"

/*: "floatingScreen" :*/
fileprivate let dream_toneKey:[UInt8] = [0x78,0x72,0x71,0x7f,0x6a,0x77,0x70,0x79,0x4d,0x7d,0x6c,0x7b,0x7b,0x70]

private func himMiss(forehead num: UInt8) -> UInt8 {
    return num ^ 30
}

/*: "mute" :*/
fileprivate let dreamCreatureFormat:String = "mmakeupe"

/*: "uid" :*/
fileprivate let dreamCellValue:String = "uientity"

/*: "expireAt" :*/
fileprivate let show_borderContent:[Character] = ["e","x","p","i","r","e","A","t"]

/*: "unmute" :*/
fileprivate let user_latText:[Character] = ["u"]
fileprivate let main_relatedValue:String = "nunlesste"

/*: "MF:LiveChatGiftMsg" :*/
fileprivate let noti_turnName:String = "MF:Liexample called busy processing"
fileprivate let dream_progressivePocketContent:[Character] = ["a","t"]
fileprivate let showFocusStartFormat:[Character] = ["G","i","f","t","M","s","g"]

/*: "MF:PartyChatGiftMsg" :*/
fileprivate let dream_joinSchemeFormat:[Character] = ["M","F",":","P","a","r","t"]
fileprivate let mainSodiumMessage:String = "yChathrough distribution"
fileprivate let dream_policyRoundingText:String = "manager behaviortGiftMsg"

/*: "gift" :*/
fileprivate let appExplainStr:String = "gperspectiveft"

/*: "imgPreview" :*/
fileprivate let m_interruptId:[Character] = ["i","m","g","P","r","e","v"]
fileprivate let dreamLiberalIdent:String = "linkw"

/*: "name" :*/
fileprivate let app_designName:[Character] = ["n","a","m","e"]

/*: "num" :*/
fileprivate let noti_mayObserveName:[Character] = ["n","u","m"]

/*: "mfBean" :*/
fileprivate let m_effectiveName:[UInt8] = [0x1d,0x16,0xf2,0x15,0x11,0x1e]

fileprivate func keyScientific(resent num: UInt8) -> UInt8 {
    let value = Int(num) - 176
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "MF:LiveChatPrizeMsg" :*/
fileprivate let mBoyfriendValue:String = "MF:Lpoke tail target"
fileprivate let userCamMessage:[Character] = ["t","P"]
fileprivate let main_automaticallyKey:[Character] = ["r","i","z","e","M","s","g"]

/*: "<at>@[\\S\\s]+?</at>" :*/
fileprivate let noti_currencyKey:String = "access score total mine strip<at>@"
fileprivate let kStillExplainIdent:String = "]+?</at>still square very label ya"

/*: "加入弹幕房间" :*/
fileprivate let user_linkSampleText:String = "加\u{5165}弹幕房间"

/*: "Failed to enter the chat room. Please try again later" :*/
fileprivate let main_extentSortFormat:[UInt8] = [0x80,0x9b,0xa3,0xa6,0x9f,0x9e,0x5a,0xae,0xa9,0x5a,0x9f,0xa8,0xae,0x9f,0xac,0x5a,0xae,0xa2,0x9f,0x5a,0x9d,0xa2,0x9b,0xae,0x5a,0xac,0xa9,0xa9,0xa7,0x68,0x5a,0x8a,0xa6,0x9f,0x9b,0xad,0x9f,0x5a,0xae,0xac,0xb3,0x5a,0x9b,0xa1,0x9b,0xa3,0xa8,0x5a,0xa6,0x9b,0xae,0x9f,0xac]

fileprivate func signalAll(filter num: UInt8) -> UInt8 {
    let value = Int(num) + 198
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "live/sendMsg" :*/
fileprivate let userPlatformFormat:String = "live/list publicly want method hour"
fileprivate let showMiniPhotoData:String = "exclusive papersendMsg"

/*: "groupId" :*/
fileprivate let showImageTitle:String = "attach extended trap speculate margingroupId"

/*: "message" :*/
fileprivate let dreamRouteKey:String = "messnete"

/*: "toUid" :*/
fileprivate let show_pocketIdent:[Character] = ["t","o","U","i","d"]

/*: "party/sendMsg" :*/
fileprivate let main_darkId:String = "damn broadcast ask can paraparty/"
fileprivate let mRefrigeratorKey:[Character] = ["s","e"]
fileprivate let appGolfRejectRequireValue:String = "ndMsga something remove ping"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DamnThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/7/7.
//

//: import UIKit
import UIKit

//: protocol TalkingDanmuManagerDelegate: NSObject {
protocol LicenseManagerDelegate: NSObject {
    /// 弹幕消息
    //: func func__DanmuRecvNewModel(Msg: TalkingLiveRoomDanmuModel)
    func sameView(Msg: PhysicalModelType)
    /// 礼物动效
    //: func func__giftRecvNewModel(Msg: TalkingLiveRoomDanmuModel)
    func beforeSpan(Msg: PhysicalModelType)

    //: func func__actionUserNewModel(pushUid: String?)
    func derivation(pushUid: String?)

    //: func func__longTouchUserNewModel(nickName: String, atUid: String?)
    func thirdFamilyName(nickName: String, atUid: String?)
    // 用户进入房间
    //: func func__userLogin()
    func primary()
    // 用户退出房间
    //: func func__userLogout()
    func candid()
}

//: class TalkingDanmuManager: NSObject {
class DamnThen: NSObject {
    //: private var maxJoinRoom = 1
    private var maxJoinRoom = 1 // 重试加入弹幕房间次数
    //: private static var _instance: TalkingDanmuManager?
    private static var _instance: DamnThen? // singleton
    //: open weak var delegate: TalkingDanmuManagerDelegate?
    open weak var delegate: LicenseManagerDelegate?

    //: override private init() {}
    override private init() {}
    //: class func shared() -> TalkingDanmuManager {
    class func oldFee() -> DamnThen {
        //: guard _instance != nil else {
        guard _instance != nil else {
            //: _instance = TalkingDanmuManager()
            _instance = DamnThen()
            //: return _instance!
            return _instance!
        }
        //: return _instance!
        return _instance!
    }
}

// MARK: - 展示消息通知

//: extension TalkingDanmuManager {
extension DamnThen {
    /// 公告消息
    //: func showAnnouncementMsg(str: String) {
    func juggle(str: String) {
        //: var model = TalkingLiveRoomDanmuModel()
        var model = PhysicalModelType()
        //: var msgInfo = TalkingDanmuMsgInfo()
        var msgInfo = DamnMsgInfo()
        //: msgInfo.messageType = 1
        msgInfo.messageType = 1 /// (公告算是特殊的文本消息)
        //: msgInfo.content = str
        msgInfo.content = str
        //: model.msgInfo = msgInfo
        model.msgInfo = msgInfo
        //: let celldata = TalkingLiveRoomDammuCellData()
        let celldata = LandscapeCellData()
        //: model = celldata.showAnnouncementMsg(model: model)
        model = celldata.accommodate(model: model)
        //: delegate?.func__DanmuRecvNewModel(Msg: model)
        delegate?.sameView(Msg: model)
    }

    /// 处理接收到的弹幕消息，展示
    /// - Parameters:
    ///   - msg: V2TIMMessage
    //: func onRecvDanmuNewMsg(msg: V2TIMMessage) {
    func recv(msg: V2TIMMessage) {
        //: guard let extra = String(data: msg.customElem.data, encoding: .utf8) else {
        guard let extra = String(data: msg.customElem.data, encoding: .utf8) else {
            //: printLog(message: " customElem.data is error")
            printLog(message: (String(noti_fatalName) + String(showRecognizeStr)))
            //: return
            return
        }
        //: let jsonData: Data = extra.data(using: .utf8)!
        let jsonData: Data = extra.data(using: .utf8)!
        //: let dict = try? JSONSerialization.jsonObject(with: jsonData, options: .mutableContainers) as? NSDictionary
        let dict = try? JSONSerialization.jsonObject(with: jsonData, options: .mutableContainers) as? NSDictionary
        //: let extraDic = dict?["extra"] as? [String: Any]
        let extraDic = dict?[(user_fairTitle.replacingOccurrences(of: "than", with: "x"))] as? [String: Any]
        //: let ext = msg.customElem.extension
        let ext = msg.customElem.extension

        //: if ext == "MF:PartyChatSysMsg", extraDic?["opType"] as? String == "roomLogout" { // 语聊房退房操作
        if ext == (String(showCalendarId) + String(dream_primaryName)), extraDic?[(String(m_shrinkValue.prefix(6)))] as? String == (kRevenueIdent.replacingOccurrences(of: "fill", with: "oo") + String(kFollowingData.suffix(6))) { // 语聊房退房操作
            //: self.delegate?.func__userLogout()
            self.delegate?.candid()
            //: return
            return

                    //: } else if ext == "MF:PartyChatWelMsg" { // 语聊房进入欢迎消息
        } else if ext == (String(mainFailureKey.prefix(6)) + "tyChat" + String(show_restrictionPath.prefix(4)) + user_acquireId.lowercased()) { // 语聊房进入欢迎消息
            //: self.delegate?.func__userLogin()
            self.delegate?.primary()

            //: } else if let opType = extraDic?["opType"], opType as? String == "startLive" {
        } else if let opType = extraDic?[(String(m_shrinkValue.prefix(6)))], opType as? String == (user_dependenceValue.replacingOccurrences(of: "hand", with: "t") + "tLive") {
            //: NotificationCenter.default.post(name: LIVE_RECIVE_STARTLIVE_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: mFilterFormat, object: nil)
            //: return
            return

                    //: } else if let opType = extraDic?["opType"], opType as? String == "floatingScreen" { // 飘屏
        } else if let opType = extraDic?[(String(m_shrinkValue.prefix(6)))], opType as? String == String(bytes: dream_toneKey.map{himMiss(forehead: $0)}, encoding: .utf8)! { // 飘屏
            //: if let dict = extraDic?["floatingScreen"] {
            if let dict = extraDic?[String(bytes: dream_toneKey.map{himMiss(forehead: $0)}, encoding: .utf8)!] {
                //: NotificationCenter.default.post(name: LIVE_RECIVE_FLOATSCREEN_NOTIFICATION, object: nil, userInfo: ["floatingScreen": dict])
                NotificationCenter.default.post(name: dream_randomName, object: nil, userInfo: [String(bytes: dream_toneKey.map{himMiss(forehead: $0)}, encoding: .utf8)!: dict])
            }
            //: return
            return

                    //: } else if let opType = extraDic?["opType"], opType as? String == "mute" { // 禁言
        } else if let opType = extraDic?[(String(m_shrinkValue.prefix(6)))], opType as? String == (dreamCreatureFormat.replacingOccurrences(of: "makeup", with: "ut")) { // 禁言
            //: if let uid = extraDic?["uid"] as? Int {
            if let uid = extraDic?[(dreamCellValue.replacingOccurrences(of: "entity", with: "d"))] as? Int {
                //: if AppManager.share.loginUserMode.userID == "\(uid)" {
                if AppManager.share.loginUserMode.userID == "\(uid)" {
                    //: if ext == "MF:PartyChatSysMsg" { // 语聊房
                    if ext == (String(showCalendarId) + String(dream_primaryName)) { // 语聊房
                        //: TalkingVoiceRoomManager.shared().partyModel.muteExpireAt = extraDic?["expireAt"] as? Int ?? 0
                        StuffLicenseReactiveCompatible.iconPoint().partyModel.muteExpireAt = extraDic?[(String(show_borderContent))] as? Int ?? 0
                        //: }else {
                    } else {
                        //: TalkingLiveManager.shared().liveRoomModel.muteExpireAt = extraDic?["expireAt"] as? Int ?? 0
                        GuideUnitReactiveCompatible.commonFragment().liveRoomModel.muteExpireAt = extraDic?[(String(show_borderContent))] as? Int ?? 0
                    }
                }
            }
            //: return
            return

                    //: } else if let opType = extraDic?["opType"], opType as? String == "unmute" { // 解禁
        } else if let opType = extraDic?[(String(m_shrinkValue.prefix(6)))], opType as? String == (String(user_latText) + main_relatedValue.replacingOccurrences(of: "unless", with: "mu")) { // 解禁
            //: if let uid = extraDic?["uid"] as? Int {
            if let uid = extraDic?[(dreamCellValue.replacingOccurrences(of: "entity", with: "d"))] as? Int {
                //: if AppManager.share.loginUserMode.userID == "\(uid)" {
                if AppManager.share.loginUserMode.userID == "\(uid)" {
                    //: if AppManager.share.loginUserMode.userID == "\(uid)" {
                    if AppManager.share.loginUserMode.userID == "\(uid)" {
                        //: if ext == "MF:PartyChatSysMsg" { // 语聊房
                        if ext == (String(showCalendarId) + String(dream_primaryName)) { // 语聊房
                            //: TalkingVoiceRoomManager.shared().partyModel.muteExpireAt = 0
                            StuffLicenseReactiveCompatible.iconPoint().partyModel.muteExpireAt = 0
                            //: }else {
                        } else {
                            //: TalkingLiveManager.shared().liveRoomModel.muteExpireAt = 0
                            GuideUnitReactiveCompatible.commonFragment().liveRoomModel.muteExpireAt = 0
                        }
                    }
                }
            }
            //: return
            return
        }

        //: if var model = TalkingLiveRoomDanmuModel.deserialize(from: extraDic as? Dictionary) {
        if var model = PhysicalModelType.deserialize(from: extraDic as? Dictionary) {
            //: model.MsgExtension = msg.customElem.extension
            model.MsgExtension = msg.customElem.extension

            //: if model.msgInfo?.messageType == 3, model.MsgExtension == "MF:LiveChatGiftMsg" || model.MsgExtension == "MF:PartyChatGiftMsg" {
            if model.msgInfo?.messageType == 3, model.MsgExtension == (String(noti_turnName.prefix(5)) + "veCh" + String(dream_progressivePocketContent) + String(showFocusStartFormat)) || model.MsgExtension == (String(dream_joinSchemeFormat) + String(mainSodiumMessage.prefix(4)) + String(dream_policyRoundingText.suffix(8))) {
                //: let dic: Dictionary? = extraDic?["gift"] as? [String: Any]
                let dic: Dictionary? = extraDic?[(appExplainStr.replacingOccurrences(of: "perspective", with: "i"))] as? [String: Any]
                //: if dic == nil {
                if dic == nil {
                    //: return
                    return
                }
                //: model.gift?.fromUid = model.user?.uid ?? 0
                model.gift?.fromUid = model.user?.uid ?? 0
                //: model.gift?.fromNickname = model.user?.nickname ?? ""
                model.gift?.fromNickname = model.user?.nickname ?? ""
                //: model.gift?.fromHeadPic = model.user?.headPic ?? ""
                model.gift?.fromHeadPic = model.user?.headPic ?? ""
                //: model.gift?.pid = Int(dic?["id"] as! String)
                model.gift?.pid = Int(dic?["id"] as! String)
                //: model.gift?.giftPic = dic?["imgPreview"] as? String ?? ""
                model.gift?.giftPic = dic?[(String(m_interruptId) + dreamLiberalIdent.replacingOccurrences(of: "link", with: "ie"))] as? String ?? ""
                //: model.gift?.pname = dic?["name"] as! String
                model.gift?.pname = dic?[(String(app_designName))] as! String
                //: if model.gift?.animationTimes ?? 0 <= 0 {
                if model.gift?.animationTimes ?? 0 <= 0 {
                    //: model.gift?.animationTimes = dic?["num"] as? Int ?? 0
                    model.gift?.animationTimes = dic?[(String(noti_mayObserveName))] as? Int ?? 0
                }

                //: if TalkingLiveManager.shared().liveRoomModel.roomId.count > 0 {
                if GuideUnitReactiveCompatible.commonFragment().liveRoomModel.roomId.count > 0 { // 直播间弹幕礼物(包含男性用户)
                    //: if TalkingLiveManager.shared().config.isSmallMode {
                    if GuideUnitReactiveCompatible.commonFragment().config.isSmallMode {
                        //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveLiveRoomGiftMsg(giftMessageDic: dict! as NSDictionary)
                        MatterThen.shared.translationMessage(giftMessageDic: dict! as NSDictionary)
                        //: } else {
                    } else {
                        //: delegate?.func__giftRecvNewModel(Msg: model)
                        delegate?.beforeSpan(Msg: model)
                    }

                    //: } else if TalkingVoiceRoomManager.shared().isParty && model.gift?.isDisplay == true {
                } else if StuffLicenseReactiveCompatible.iconPoint().isParty, model.gift?.isDisplay == true { // 语聊房弹幕礼物
                    //: if TalkingVoiceRoomManager.shared().isSmallMode {
                    if StuffLicenseReactiveCompatible.iconPoint().isSmallMode {
                        //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveLiveRoomGiftMsg(giftMessageDic: dict! as NSDictionary)
                        MatterThen.shared.translationMessage(giftMessageDic: dict! as NSDictionary)
                        //: } else {
                    } else {
                        //: delegate?.func__giftRecvNewModel(Msg: model)
                        delegate?.beforeSpan(Msg: model)
                    }
                }

                //: let mfBean = model.msgInfo?.mfBean
                let mfBean = model.msgInfo?.mfBean
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean ?? 0.0])
                NotificationCenter.default.post(name: appWarningUrl, object: nil, userInfo: [String(bytes: m_effectiveName.map{keyScientific(resent: $0)}, encoding: .utf8)!: mfBean ?? 0.0])

                //: if model.gift?.giftPic.count ?? 0 > 0 {
                if model.gift?.giftPic.count ?? 0 > 0 {
                    //: if let newStr = model.gift?.giftPic.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain) {
                    if let newStr = model.gift?.giftPic.replacingOccurrences(of: mainLastName.doGender(), with: k_filterName) {
                        //: URLSession.shared.dataTask(with: URL(string: newStr)!) { data, _, error in
                        URLSession.shared.dataTask(with: URL(string: newStr)!) { data, _, error in
                            //: DispatchQueue.main.async {
                            DispatchQueue.main.async {
                                //: guard let data = data, error == nil, let image = UIImage(data: data) else {
                                guard let data = data, error == nil, let image = UIImage(data: data) else {
                                    //: self.setLiveRoomDammuCellData(danmuModel: model)
                                    self.danmuAcross(danmuModel: model)
                                    //: return
                                    return
                                }
                                //: model.gift?.giftImg = image
                                model.gift?.giftImg = image
                                //: self.setLiveRoomDammuCellData(danmuModel: model)
                                self.danmuAcross(danmuModel: model)
                            }
                            //: }.resume()
                        }.resume()
                    }
                }
                //: return
                return
            }
            //: if AppManager.share.appStatus == AppSkinStatus.special.rawValue, model.MsgExtension == "MF:LiveChatPrizeMsg" {
            if AppManager.share.appStatus == WrinkleSendable.special.rawValue, model.MsgExtension == (String(mBoyfriendValue.prefix(4)) + "iveCha" + String(userCamMessage) + String(main_automaticallyKey)) {
                //: return
                return
            }

            //: setLiveRoomDammuCellData(danmuModel: model)
            danmuAcross(danmuModel: model)
        }
    }

    //: private func setLiveRoomDammuCellData(danmuModel: TalkingLiveRoomDanmuModel) {
    private func danmuAcross(danmuModel: PhysicalModelType) {
        //: var model = danmuModel
        var model = danmuModel
        //: let celldata = TalkingLiveRoomDammuCellData()
        let celldata = LandscapeCellData()
        //: model = celldata.caculateMsgHeight(model: model)
        model = celldata.commentMagnitude(model: model)
        //: celldata.actionUserBlock = { [weak self] atUid in
        celldata.actionUserBlock = { [weak self] atUid in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.delegate?.func__actionUserNewModel(pushUid: atUid)
            self.delegate?.derivation(pushUid: atUid)
        }
        //: celldata.longUserBlock = { [weak self] atUid in
        celldata.longUserBlock = { [weak self] atUid in
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: var uid = String(model.user?.uid ?? 0)
            var uid = String(model.user?.uid ?? 0)
            //: var name = model.user?.nickname ?? ""
            var name = model.user?.nickname ?? ""
            //: if atUid != nil, model.msgInfo?.jumps.count ?? 0 > 0, atUid != AppManager.share.loginUserMode.userID, atUid != String(model.user?.uid ?? 0) {
            if atUid != nil, model.msgInfo?.jumps.count ?? 0 > 0, atUid != AppManager.share.loginUserMode.userID, atUid != String(model.user?.uid ?? 0) {
                //: uid = atUid!
                uid = atUid!
                //: let text = model.msgInfo?.content ?? ""
                let text = model.msgInfo?.content ?? ""
                //: let regex = try! NSRegularExpression(pattern: "<at>@[\\S\\s]+?</at>", options: [])
                let regex = try! NSRegularExpression(pattern: (String(noti_currencyKey.suffix(5)) + "[\\S\\s" + String(kStillExplainIdent.prefix(8))), options: [])
                //: let result = regex.matches(in: text, options: [], range: NSRange(location: 0, length: text.utf16.count))
                let result = regex.matches(in: text, options: [], range: NSRange(location: 0, length: text.utf16.count))
                //: let matchRange = result.first?.range
                let matchRange = result.first?.range
                //: if matchRange?.length ?? 0 > 10 {
                if matchRange?.length ?? 0 > 10 {
                    //: let subStr = String.substringByUnicodeIndex(str: text, from: (matchRange?.location ?? 0) + 5, to: (matchRange?.length ?? 0) - 5)
                    let subStr = String.fetch(str: text, from: (matchRange?.location ?? 0) + 5, to: (matchRange?.length ?? 0) - 5)
                    //: name = subStr
                    name = subStr
                }
            }

            //: self.delegate?.func__longTouchUserNewModel(nickName: name, atUid: uid)
            self.delegate?.thirdFamilyName(nickName: name, atUid: uid)
        }

        //: delegate?.func__DanmuRecvNewModel(Msg: model)
        delegate?.sameView(Msg: model)
    }
}

//: extension TalkingDanmuManager {
extension DamnThen {
    /// 释放单利
    //: class func danmu_releaseAllResource() {
    class func capacityBy() {
        //: if TalkingDanmuManager._instance != nil {
        if DamnThen._instance != nil {
            //: TalkingDanmuManager._instance = nil
            DamnThen._instance = nil
        }
    }

    /// 加入直播弹幕
    //: class func joinLiveRoomDanmu(groupId: String, announcementMsg: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
    class func federalize(groupId: String, announcementMsg: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
        V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
            //: printLog(message: "加入弹幕房间")
            printLog(message: (user_linkSampleText))
            //: if announcementMsg.count > 0 {
            if announcementMsg.count > 0 {
                //: TalkingDanmuManager.shared().showAnnouncementMsg(str: announcementMsg)
                DamnThen.oldFee().juggle(str: announcementMsg)
            }
            //: completion?(true)
            completion?(true)

            //: } fail: { _, _ in
        } fail: { _, _ in
            //: if TalkingDanmuManager.shared().maxJoinRoom == 1 {
            if DamnThen.oldFee().maxJoinRoom == 1 {
                //: TalkingDanmuManager.shared().maxJoinRoom = 0
                DamnThen.oldFee().maxJoinRoom = 0
                //: joinLiveRoomDanmu(groupId: groupId, announcementMsg: "")
                federalize(groupId: groupId, announcementMsg: "")
                //: } else {
            } else {
                //: TalkingDanmuManager.shared().func__showStatusBarErrorMsg(showMsg: "Failed to enter the chat room. Please try again later".localized)
                DamnThen.oldFee().changeManage(showMsg: String(bytes: main_extentSortFormat.map{signalAll(filter: $0)}, encoding: .utf8)!.localized)
            }
            //: completion?(false)
            completion?(false)
        }
    }

    /// 退出群聊弹幕
    //: class func qiutGroupDanmu(groupId: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
    class func good(groupId: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: V2TIMManager.sharedInstance().quitGroup(groupId, succ: {
        V2TIMManager.sharedInstance().quitGroup(groupId, succ: {
            //: completion?(true)
            completion?(true)
            //: }, fail: { _, _ in
        }, fail: { _, _ in
            //: completion?(false)
            completion?(false)
            //: })
        })
    }
}

// MARK: - 发送接口

//: extension TalkingDanmuManager {
extension DamnThen {
    /// 发送文本弹幕消息
    /// - Parameters:
    ///   - groupId: 弹幕群组id
    ///   - message: 消息内容
    ///   - toUid:   @用户的uid
    ///   - completion: 回调
    //: class func uploadToTextMsg(groupId: String, message: String, toUid: String? = "", completion: FinishBlock? = nil) {
    class func sharpCompletion(groupId: String, message: String, toUid: String? = "", completion: FinishBlock? = nil) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "live/sendMsg"
        reqModel.requestPath = (String(userPlatformFormat.prefix(5)) + String(showMiniPhotoData.suffix(7)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var dict = [String: Any]()
        var dict = [String: Any]()
        //: dict["groupId"] = groupId
        dict[(String(showImageTitle.suffix(7)))] = groupId
        //: dict["message"] = message
        dict[(dreamRouteKey.replacingOccurrences(of: "net", with: "ag"))] = message
        //: if toUid?.count ?? 0 > 0 {
        if toUid?.count ?? 0 > 0 {
            //: dict["toUid"] = toUid
            dict[(String(show_pocketIdent))] = toUid
        }
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion?(succeed, result, errorModel)
            completion?(succeed, result, errorModel)
        }
    }

    /// 语聊房发送文本弹幕消息
    /// - Parameters:
    ///   - groupId: 弹幕群组id
    ///   - message: 消息内容
    ///   - toUid:   @用户的uid
    ///   - completion: 回调
    //: class func voiceRoomSendTextMsg(groupId: String, message: String, toUid: String? = "", completion: FinishBlock? = nil) {
    class func divertLaunch(groupId: String, message: String, toUid: String? = "", completion: FinishBlock? = nil) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "party/sendMsg"
        reqModel.requestPath = (String(main_darkId.suffix(6)) + String(mRefrigeratorKey) + String(appGolfRejectRequireValue.prefix(5)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var dict = [String: Any]()
        var dict = [String: Any]()
        //: dict["groupId"] = groupId
        dict[(String(showImageTitle.suffix(7)))] = groupId
        //: dict["message"] = message
        dict[(dreamRouteKey.replacingOccurrences(of: "net", with: "ag"))] = message
        //: if toUid?.count ?? 0 > 0 {
        if toUid?.count ?? 0 > 0 {
            //: dict["toUid"] = toUid
            dict[(String(show_pocketIdent))] = toUid
        }
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion?(succeed, result, errorModel)
            completion?(succeed, result, errorModel)
        }
    }
}
