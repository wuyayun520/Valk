
//: Declare String Begin

/*: "cmd" :*/
fileprivate let mPhysicalMsg:[UInt8] = [0x11,0x1b,0x12]

fileprivate func handleLabel(table num: UInt8) -> UInt8 {
    let value = Int(num) + 82
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let k_processName:[Character] = ["c","a","n"]
fileprivate let userCounterestedValue:String = "celCalltoo valid collection"

/*: "data" :*/
fileprivate let mainBoundPath:[UInt8] = [0x81,0x84,0x91,0x84]

private func thinUntil(ping num: UInt8) -> UInt8 {
    return num ^ 229
}

/*: "logId" :*/
fileprivate let appProsecutorName:[Character] = ["l","o","g","I","d"]

/*: "#322870" :*/
fileprivate let kMortalityEveryMsg:String = "#32287air"

/*: "Tap to start video chatting" :*/
fileprivate let app_waterData:[Character] = ["T","a","p"," ","t","o"," ","s","t","a","r"]
fileprivate let k_sceneIdent:[Character] = ["t"," ","v","i","d","e","o"," ","c","h","a","t","t","i","n","g"]

/*: "icon_close_nor" :*/
fileprivate let kShirtValue:String = "resign talkicon_clo"
fileprivate let notiExplainDonorId:String = "effective familyse_nor"

/*: "icon_views_number" :*/
fileprivate let main_usMsg:String = "iwhich"
fileprivate let mainConcentrationShakeId:String = "on_viselection abstract opposed"
fileprivate let app_componentFormat:String = "umlostr"

/*: "%@ chatting" :*/
fileprivate let notiSessionData:String = "%@ chattmanual char"
fileprivate let dreamSingleStr:String = "indraw"

/*: "icon_lounge" :*/
fileprivate let app_bottomIdent:[Character] = ["i","c","o","n","_","l","o","u","n","g","e"]

/*: "icon_rank_coin" :*/
fileprivate let dreamVoiceKey:String = "proposal hung maximumicon_r"

/*: "vip_btn_top" :*/
fileprivate let showEqualPath:[Character] = ["v","i","p","_","b","t"]
fileprivate let user_policeId:[Character] = ["n","_","t","o","p"]

/*: "btn_nearby_settings_nor" :*/
fileprivate let user_veryMsg:String = "btn_automatically customer"
fileprivate let show_challengeValue:String = "straight strategy string completeby_set"
fileprivate let appPatientValue:[Character] = ["s","_","n","o","r"]

/*: "%@ Coins/min" :*/
fileprivate let mNeighborPath:String = "honey busy abs ball loo%@ Coi"

/*: "get json error" :*/
fileprivate let showSignatureValue:[Character] = ["g","e","t"," ","j","s","o","n"," ","e","r","r","o","r"]

/*: "matchId" :*/
fileprivate let show_soulIllegalName:[Character] = ["m","a","t","c","h","I","d"]

/*: "errno" :*/
fileprivate let mainHelpStr:[UInt8] = [0x50,0x5d,0x5d,0x59,0x5a]

fileprivate func popIn(possible num: UInt8) -> UInt8 {
    let value = Int(num) - 235
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "VIPFee" :*/
fileprivate let mAttitudeTitle:String = "wrap scheme destroy formatVIPFee"

/*: "Matching now" :*/
fileprivate let user_fatalStr:[Character] = ["M","a","t","c","h"]
fileprivate let dream_upUrl:String = "ing nowerror elsewhere"

/*: "userTouch" :*/
fileprivate let m_adjustValue:[Character] = ["u","s","e","r","T","o","u","c"]
fileprivate let appReamConnectId:[Character] = ["h"]

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let show_appMessage:[Character] = ["m","f","/","f","r","e","e","C","a","l","l","/","g","e","t","V","i"]
fileprivate let app_sheMessage:[Character] = ["d","e","o","I","n","f","o"]

/*: "fromType" :*/
fileprivate let noti_jackId:[UInt8] = [0x33,0x27,0x3a,0x38,0x1,0x2c,0x25,0x30]

private func suspendCos(stretch num: UInt8) -> UInt8 {
    return num ^ 85
}

/*: "You've been barred from receiving calls" :*/
fileprivate let main_agentFormat:[UInt8] = [0x97,0xa1,0xbb,0xe9,0xb8,0xab,0xee,0xac,0xab,0xab,0xa0,0xee,0xac,0xaf,0xbc,0xbc,0xab,0xaa,0xee,0xa8,0xbc,0xa1,0xa3,0xee,0xbc,0xab,0xad,0xab,0xa7,0xb8,0xa7,0xa0,0xa9,0xee,0xad,0xaf,0xa2,0xa2,0xbd]

private func withWarning(simply num: UInt8) -> UInt8 {
    return num ^ 206
}

/*: "source" :*/
fileprivate let dream_creationValue:[UInt8] = [0x29,0x35,0x2f,0x28,0x39,0x3f]

/*: "type" :*/
fileprivate let mainFlatValue:[UInt8] = [0x64,0x69,0x60,0x75]

/*: "uid" :*/
fileprivate let app_tarNaturalSodiumIdent:[UInt8] = [0x64,0x69,0x75]

/*: "fromFreeCall" :*/
fileprivate let mConstraintText:String = "should tun following anyfromF"
fileprivate let m_exclusiveIdent:String = "mention"

/*: "requestCall" :*/
fileprivate let kPadFormat:[Character] = ["r","e","q","u","e","s","t","C","a"]
fileprivate let dreamItUrl:[Character] = ["l","l"]

/*: "onVideoMatch" :*/
fileprivate let user_poundId:String = "onVideman estimated"
fileprivate let dreamBlockMsg:String = "oMatchlocal inmate railroad reminder quick"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let mHostMessage:[UInt8] = [0xd7,0xbd,0xf1,0xf0,0xeb,0xe9,0xdd,0xf0,0xe5,0xdf,0x9c,0xf2,0xe5,0xe0,0xe1,0xeb,0xd9,0x9c,0xce,0xe1,0xdf,0xe1,0xe5,0xf2,0xe1,0x9c,0xf2,0xe5,0xe0,0xe1,0xeb,0x9c,0xdf,0xdd,0xe8,0xe8,0x9c,0xe5,0xea,0xdf,0xeb,0xe9,0xe5,0xea,0xe3]

fileprivate func thirdHatMobile(male num: UInt8) -> UInt8 {
    let value = Int(num) - 124
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "refuseCall" :*/
fileprivate let show_bindMsg:[Character] = ["r","e","f","u","s","e","C"]
fileprivate let noti_arrowPicFormat:[Character] = ["a","l","l"]

/*: "onCancelCall" :*/
fileprivate let noti_pingText:String = "collect state catch futureonCan"
fileprivate let notiReadCurrentData:String = "celCalljudge since plot prompt"

/*: "retryAfter" :*/
fileprivate let m_narrowFeedbackIdent:[Character] = ["r","e","t","r","y","A","f","t","e","r"]

/*: "onRequestCall" :*/
fileprivate let app_startKey:[Character] = ["o","n","R","e","q","u","e","s"]
fileprivate let m_authorMessage:[Character] = ["t","C","a","l","l"]

/*: "failType" :*/
fileprivate let app_amazingFormat:String = "failTypeplant inside"

/*: "callWaitingTime" :*/
fileprivate let notiLogUrl:String = "cacigarette"
fileprivate let m_separateStr:String = "track element history calculate popgTime"

/*: "toUid" :*/
fileprivate let app_scanMsg:String = "positive protection agency spend fartoUid"

/*: "Free 1 min" :*/
fileprivate let dreamReadText:String = "Free adjustment read minimum"
fileprivate let dream_kitAMessage:String = "save conversion home them1 min"

/*: "%@/min" :*/
fileprivate let appGapKey:String = "%@/micase headline quit"
fileprivate let user_quoteIdent:[Character] = ["n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PictureViewController.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class PictureViewController: FictionalCharacterRecognizerDelegate {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.blackMusical()
            }
        }
        //: self.resetCamera()
        self.outside()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        photo()
        //: setupSubViewsConstraint()
        partyLivery()
        //: bindInteraction()
        windowNearby()
        //: videoManager.startPreview(isMatch: true)
        videoManager.readyAndWaitingGold(isMatch: true)
        //: setPlayer(isRandon: false)
        reconstruct(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            getInWithTelevisionOwner()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            StackSocketManager.shared.someoneDisable(info: [String(bytes: mPhysicalMsg.map{handleLabel(table: $0)}, encoding: .utf8)!: (String(k_processName) + String(userCounterestedValue.prefix(7))), String(bytes: mainBoundPath.map{thinUntil(ping: $0)}, encoding: .utf8)!: [(String(appProsecutorName)): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        actDownwardlyTimer()
        //: if TalkingSocketManager.shared.isObject == false {
        if StackSocketManager.shared.isObject == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            StackSocketManager.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            StackSocketManager.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            StackSocketManager.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            StackSocketManager.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: BagChatManager = {
        //: let m = TalkingVideoChatManager()
        let m = BagChatManager()
        //: let info = TalkingVideoChatModel()
        let info = OthersChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .telecom()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(getInWithTelevisionOwner), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: kGiftIdent + 1, height: user_allMessage + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (kMortalityEveryMsg.replacingOccurrences(of: "air", with: "0")))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(app_waterData) + String(k_sceneIdent)).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.verbalDescription(name: (String(kShirtValue.suffix(8)) + String(notiExplainDonorId.suffix(6)))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(pastVia), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: GoddamnThen = {
        //: let btn = TalkingButton()
        let btn = GoddamnThen()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.verbalDescription(name: (main_usMsg.replacingOccurrences(of: "which", with: "c") + String(mainConcentrationShakeId.prefix(5)) + "ews_n" + app_componentFormat.replacingOccurrences(of: "lost", with: "be"))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.verbalDescription(name: (main_usMsg.replacingOccurrences(of: "which", with: "c") + String(mainConcentrationShakeId.prefix(5)) + "ews_n" + app_componentFormat.replacingOccurrences(of: "lost", with: "be"))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.concern(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(notiSessionData.prefix(8)) + dreamSingleStr.replacingOccurrences(of: "draw", with: "g")).launchRid(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.verbalDescription(name: (String(app_bottomIdent)))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.verbalDescription(name: (String(dreamVoiceKey.suffix(6)) + "ank_coin"))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.concern(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.verbalDescription(name: (String(showEqualPath) + String(user_policeId)))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: GoddamnThen = {
        //: let btn = TalkingButton()
        let btn = GoddamnThen()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(expensiveness), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.verbalDescription(name: (String(user_veryMsg.prefix(4)) + "near" + String(show_challengeValue.suffix(6)) + "ting" + String(appPatientValue))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.verbalDescription(name: (String(dreamVoiceKey.suffix(6)) + "ank_coin"))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .atSituationRaw()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(mNeighborPath.suffix(6)) + "ns/min").launchRid(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.concern(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: SeeWorldViewThen = //: return SeeWorldViewThen(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension PictureViewController {
    //: private func setPlayer(isRandon: Bool) {
    private func reconstruct(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = EffectTool.default.variationType(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(showSignatureValue)))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func slowSatisfy(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(show_soulIllegalName)): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: mPhysicalMsg.map{handleLabel(table: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: mainHelpStr.map{popIn(possible: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        SavingGraceReactiveCompatible.dataConverterCompletion(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.aboveAcross()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(show_soulIllegalName))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func doingicial() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        SavingGraceReactiveCompatible.refer { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(mAttitudeTitle.suffix(6)))].intValue
            //: self.refreshVideoCallPrice()
            self.blackMusical()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension PictureViewController {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func expensiveness() {
        //: PipSqueakReactiveCompatible.share.func__pushToSubscribeAlert()
        PipSqueakReactiveCompatible.share.pushBack()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func pastVia() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        behindFemale()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        noti_thirdUrl.toSpeaker(eventID: k_depthPath)

        //: if AppManager.share.appConfigMode.displayMatchV4PopUp, AppManager.share.loginUserMode.freeCallTimes > 0,
        if AppManager.share.appConfigMode.displayMatchV4PopUp, AppManager.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.visualLink()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        decideBroad()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func getInWithTelevisionOwner() {
        //: guard TalkingSocketManager.shared.isObject == false else {
        guard StackSocketManager.shared.isObject == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.changeManage(showMsg: m_restoreInputKey)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        existScore()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(user_fatalStr) + String(dream_upUrl.prefix(7))).localized
        //: setPlayer(isRandon: true)
        reconstruct(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if AppManager.share.loginUserMode.randomVideoType == 2 {
        if AppManager.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            noti_thirdUrl.toSpeaker(eventID: showVersionPath)
            //: req_getVideoInfo()
            man()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            noti_thirdUrl.toSpeaker(eventID: noti_lessData)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            StackSocketManager.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            StackSocketManager.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            StackSocketManager.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            StackSocketManager.shared.errorDelegate = self
            //: restart_randomVideo()
            fireRestart()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func fireRestart(cmd: String = (String(m_adjustValue) + String(appReamConnectId)), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        actDownwardlyTimer()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        slowSatisfy(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func aboveAcross() {
        // 停止定时器
        //: destroyWaitingTimer()
        actDownwardlyTimer()
        //: showCloseBtn()
        behindFemale()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(app_waterData) + String(k_sceneIdent)).localized
        //: setPlayer(isRandon: false)
        reconstruct(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isObject == false {
        if StackSocketManager.shared.isObject == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            StackSocketManager.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            StackSocketManager.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            StackSocketManager.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            StackSocketManager.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        blackMusical()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func actDownwardlyTimer() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func man() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(show_appMessage) + String(app_sheMessage))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: noti_jackId.map{suspendCos(stretch: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.aboveAcross()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: AppManager.share.loginUserMode.randomVideoType = 1
                    AppManager.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.getInWithTelevisionOwner()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.changeManage(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = SmallModelType.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = ExecPlayerManager()
                //: player.setMute(bEnable: true)
                player.size(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.satisfyPer(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                WrongUnitThen.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                SatelliteThen.keepDependMicrophone { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.putRemark()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func putRemark() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        noti_thirdUrl.toSpeaker(eventID: show_tabKey)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard WrongUnitThen.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.changeManage(showMsg: String(bytes: main_agentFormat.map{withWarning(simply: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            WrongUnitThen.shared.videoCompletion(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.aboveAcross()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = GuideViewController()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.decideBroad()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.aboveAcross()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func existScore() {
        //: if AppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if AppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(AppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(AppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func behindFemale() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func outside() {
        //: guard TalkingSocketManager.shared.isObject == false else {
        guard StackSocketManager.shared.isObject == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.readyAndWaitingGold()
        }
    }
}

// MARK: - SocketObjectProtocol

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension PictureViewController: SocketObjectProtocol {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func kingdom(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = EditParentModelType.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(show_soulIllegalName)): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: dream_creationValue.map{$0^90}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: mainFlatValue.map{$0^16}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: app_tarNaturalSodiumIdent.reversed(), encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = PerformanceMagnitudeLiteral.randow
        //: if AppManager.share.loginUserMode.freeCallTimes > 0 && AppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if AppManager.share.loginUserMode.freeCallTimes > 0 && AppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(mConstraintText.suffix(5)) + "reeCal" + m_exclusiveIdent.replacingOccurrences(of: "mention", with: "l")))
            //: TalkingSocketManager.shared.isFreeCall = true
            StackSocketManager.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: mPhysicalMsg.map{handleLabel(table: $0)}, encoding: .utf8)!: (String(kPadFormat) + String(dreamItUrl)), String(bytes: mainBoundPath.map{thinUntil(ping: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        StackSocketManager.shared.someoneDisable(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            StackSocketManager.shared.someoneDisable(info: [String(bytes: mPhysicalMsg.map{handleLabel(table: $0)}, encoding: .utf8)!: (String(k_processName) + String(userCounterestedValue.prefix(7))), String(bytes: mainBoundPath.map{thinUntil(ping: $0)}, encoding: .utf8)!: [(String(appProsecutorName)): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.fireRestart(cmd: (String(user_poundId.prefix(6)) + String(dreamBlockMsg.prefix(6))))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func subjectMatter(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(appProsecutorName))].intValue
    }
}

// MARK: - StuffCallerDelegate【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension PictureViewController: StuffCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func whiteData(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func bringContext(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func froth(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func putData(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            ComparableBaseDelegate.stagePair(body: String(bytes: mHostMessage.map{thirdHatMobile(male: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = EditParentModelType.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            wedScene(toUid: String(uid))
        }
        //: stop_randomVideo()
        aboveAcross()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.letLooseResourceProgramEveryLast()
        //: AppManager.share.start1v1TalkCall(info: data, situation: .Calling)
        AppManager.share.outerSpace(info: data, situation: .Calling)
    }
}

// MARK: - CyclePartyDelegate【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension PictureViewController: CyclePartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func whiteReact(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        fireRestart(cmd: (String(show_bindMsg) + String(noti_arrowPicFormat)))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func codex(data _: [String: Any]) {}
}

// MARK: - PresenceErrorDelegate

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension PictureViewController: PresenceErrorDelegate {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func bring(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(noti_pingText.suffix(5)) + String(notiReadCurrentData.prefix(7))) { // 取消通话异常
            //: stop_randomVideo()
            aboveAcross()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(user_poundId.prefix(6)) + String(dreamBlockMsg.prefix(6))), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            aboveAcross()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(show_soulIllegalName))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.changeManage(showMsg: msg)
            //: uploadFailTime(type: 2)
            magnitudeense(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(user_poundId.prefix(6)) + String(dreamBlockMsg.prefix(6))), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(m_narrowFeedbackIdent))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(show_soulIllegalName))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.fireRestart(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(app_startKey) + String(m_authorMessage)), errorNo == WithdrawMeasurable.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            aboveAcross()
            //: guard AppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard AppManager.share.appStatus == WrinkleSendable.normal.rawValue else { return }
            //: PipSqueakReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            PipSqueakReactiveCompatible.share.halfHardware(clickEvent: mHundredTitle, sufficient: false)
            //: uploadFailTime(type: 1)
            magnitudeense(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        fireRestart(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension PictureViewController {
    //: func uploadFailTime(type: Int) {
    func magnitudeense(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(app_amazingFormat.prefix(8)))] = type
        //: parm["callWaitingTime"] = time
        parm[(notiLogUrl.replacingOccurrences(of: "cigarette", with: "ll") + "Waitin" + String(m_separateStr.suffix(5)))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        noti_thirdUrl.libraryDataWeight(eventID: notiUserValue, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func wedScene(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(notiLogUrl.replacingOccurrences(of: "cigarette", with: "ll") + "Waitin" + String(m_separateStr.suffix(5)))] = time
        //: parm["toUid"] = toUid
        parm[(String(app_scanMsg.suffix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        noti_thirdUrl.libraryDataWeight(eventID: app_gameContent, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension PictureViewController {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func blackMusical() {
        //: guard AppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard AppManager.share.appStatus == WrinkleSendable.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !AppManager.share.loginUserMode.loungePlus
        vipIcon.isHidden = !AppManager.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = PerformanceMagnitudeLiteral.randow
        //: if AppManager.share.loginUserMode.freeCallTimes > 0 && AppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if AppManager.share.loginUserMode.freeCallTimes > 0, AppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if AppManager.share.appUserConfigMode.callBtnStyle == 1 {
            if AppManager.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(dreamReadText.prefix(5)) + String(dream_kitAMessage.suffix(5))).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = AppManager.share.loginUserMode.loungePlus ? vipFee:fee
            let price = AppManager.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(appGapKey.prefix(5)) + String(user_quoteIdent)).launchRid(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(appGapKey.prefix(5)) + String(user_quoteIdent)).launchRid(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.equitySize(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = AppManager.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = AppManager.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = AppManager.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = AppManager.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !AppManager.share.loginUserMode.loungePlus
            priceStackView.isHidden = !AppManager.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func photo() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func partyLivery() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(dreamNoSampleValue + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func windowNearby() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(blackMusical),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: appMatchVersionMessage,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(doingicial),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: showExploreStr,
                                               //: object: nil)
                                               object: nil)
    }
}
