
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dream_blackJawValue:[UInt8] = [0xf,0x8,0xf,0x12,0x4e,0x5,0x9,0x2,0x3,0x14,0x5c,0x4f,0x46,0xe,0x7,0x15,0x46,0x8,0x9,0x12,0x46,0x4,0x3,0x3,0x8,0x46,0xf,0xb,0x16,0xa,0x3,0xb,0x3,0x8,0x12,0x3,0x2]

private func mentionBroadcast(steep num: UInt8) -> UInt8 {
    return num ^ 102
}

/*: "icon_cover_toast" :*/
fileprivate let m_materialValue:[Character] = ["i","c","o","n","_","c","o","v","e","r","_","t","o","a","s","t"]

/*: "Free video time is over, you can continue to call her if you are interested" :*/
fileprivate let user_physicalIdent:[UInt8] = [0x8f,0xbb,0xac,0xac,0xe9,0xbf,0xa0,0xad,0xac,0xa6,0xe9,0xbd,0xa0,0xa4,0xac,0xe9,0xa0,0xba,0xe9,0xa6,0xbf,0xac,0xbb,0xe5,0xe9,0xb0,0xa6,0xbc,0xe9,0xaa,0xa8,0xa7,0xe9,0xaa,0xa6,0xa7,0xbd,0xa0,0xa7,0xbc,0xac,0xe9,0xbd,0xa6,0xe9,0xaa,0xa8,0xa5,0xa5,0xe9,0xa1,0xac,0xbb,0xe9,0xa0,0xaf,0xe9,0xb0,0xa6,0xbc,0xe9,0xa8,0xbb,0xac,0xe9,0xa0,0xa7,0xbd,0xac,0xbb,0xac,0xba,0xbd,0xac,0xad]

/*: "#34C759" :*/
fileprivate let appPlantFirmText:[Character] = ["#","3","4","C","7","5","9"]

/*: "icon_match_stopcall_pop" :*/
fileprivate let dream_upperTitle:String = "ICON"
fileprivate let main_directorIdent:String = "soon fusion activity radicalh_stop"
fileprivate let dreamPickName:String = "_popprocedure stand category exec unless"

/*: "icon_lounge_big" :*/
fileprivate let mainParticleTitle:String = "another railroad successfulicon_"
fileprivate let notiDistantKey:String = "warning safety beforee_big"

/*: "#AB57F6" :*/
fileprivate let noti_learnApplyName:String = "#AB57Fspeak pie sharp direct choice"
fileprivate let showBasicIdent:String = "6"

/*: "#FC57B7" :*/
fileprivate let m_plainMessage:[Character] = ["#","F","C","5","7","B","7"]

/*: "#FE9074" :*/
fileprivate let dreamPoliticPath:String = "#FE9074towards smooth"

/*: "Maybe next time" :*/
fileprivate let noti_leadingMsg:String = "topic sheMaybe "
fileprivate let appLiberalBarnPath:String = "nexbirth"

/*: "type" :*/
fileprivate let k_originValue:[UInt8] = [0xe2,0xef,0xe6,0xf3]

/*: "toUid" :*/
fileprivate let app_attractiveFormat:String = "toUiddestroy today"

/*: "uid" :*/
fileprivate let dream_birthdayUrl:[UInt8] = [0x92,0x8e,0x83]

private func moveFit(kick num: UInt8) -> UInt8 {
    return num ^ 231
}

/*: "fromFreeCall" :*/
fileprivate let mainCapacityUrl:String = "or opportunity finite towards fromfromFr"
fileprivate let show_womanKey:String = "cancel"

/*: "cmd" :*/
fileprivate let dream_soMessage:[UInt8] = [0xb4,0xbe,0xb5]

fileprivate func dismissApprove(answer num: UInt8) -> UInt8 {
    let value = Int(num) - 81
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "requestCall" :*/
fileprivate let mainFellowStr:String = "rselectue"
fileprivate let userSearchionMsg:String = "voice"

/*: "data" :*/
fileprivate let userWithoutDrawingGoodMessage:[UInt8] = [0x57,0x54,0x67,0x54]

fileprivate func consentRed(dramatically num: UInt8) -> UInt8 {
    let value = Int(num) - 243
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "originalFee" :*/
fileprivate let userAliveProcessFastId:[Character] = ["o"]
fileprivate let userInputValue:String = "RIGI"

/*: "VIPFee" :*/
fileprivate let showCycleStr:String = "broad highlightVIPFe"
fileprivate let userWeightIdent:[Character] = ["e"]

/*: "freeCallTimes" :*/
fileprivate let main_injuryValue:[Character] = ["f","r","e","e","C"]
fileprivate let app_infoMsg:String = "whole mm vid administratorallTimes"

/*: "onRequestCall" :*/
fileprivate let user_donorSignaturePath:[Character] = ["o","n","R","e","q","u","e","s"]
fileprivate let main_powerUrl:[Character] = ["t","C","a","l","l"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ParentObjectProtocol.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/6/15.
//

//: import UIKit
import UIKit

//: class TalkingFreeStopCallPopUpView: UIView {
class ParentObjectProtocol: UIView {
    //: var popView: TalkingPopView?
    var popView: PanoramicViewBucketRecognizerDelegate?
    //: var data = [String: Any]()
    var data = [String: Any]()

    //: override init(frame: CGRect) {
    override init(frame: CGRect) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        hideSubviews()
        //: setupSubViewsConstraint()
        endemicCapability()
    }

    //: @available(*, unavailable)
    @available(*, unavailable)
    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dream_blackJawValue.map{mentionBroadcast(steep: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: TalkingSocketManager.shared.errorDelegate = nil
        StackSocketManager.shared.errorDelegate = nil
    }

    // 懒加载初始化

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let view = UIView()
        let view = UIView()
        //: view.backgroundColor = UIColor.white
        view.backgroundColor = UIColor.white
        //: view.layer.cornerRadius = 16
        view.layer.cornerRadius = 16
        //: return view
        return view
        //: }()
    }()

    //: lazy var topIcon: UIImageView = {
    lazy var topIcon: UIImageView = {
        //: let iamg = UIImageView()
        let iamg = UIImageView()
        //: iamg.image = UIImage.BundleImageNamed(name: "icon_cover_toast")
        iamg.image = UIImage.verbalDescription(name: (String(m_materialValue)))
        //: iamg.contentMode = .scaleAspectFill
        iamg.contentMode = .scaleAspectFill
        //: return iamg
        return iamg
        //: }()
    }()

    //: lazy var inputTView: UITextView = {
    lazy var inputTView: UITextView = {
        //: let textView = UITextView()
        let textView = UITextView()
        //: textView.font = UIFont.pingfangFont(type: .Regular, fontSize: 16)
        textView.font = UIFont.concern(type: .Regular, fontSize: 16)
        //: textView.textColor = UIColor.appTitleColor()
        textView.textColor = UIColor.telecom()
        //: textView.textContainer.maximumNumberOfLines = 0
        textView.textContainer.maximumNumberOfLines = 0
        //: textView.textAlignment = .center
        textView.textAlignment = .center
        //: textView.backgroundColor = .clear
        textView.backgroundColor = .clear
        //: textView.isEditable = false
        textView.isEditable = false
        //: textView.text = "Free video time is over, you can continue to call her if you are interested".localized
        textView.text = String(bytes: user_physicalIdent.map{$0^201}, encoding: .utf8)!.localized
        //: return textView
        return textView
        //: }()
    }()

    // 原价按钮
    //: private lazy var originalFeeBtn: TalkingButton = {
    private lazy var originalFeeBtn: GoddamnThen = {
        //: let btn = TalkingButton()
        let btn = GoddamnThen()
        //: btn.setBackgroundColor(color: UIColor(hex: "#34C759")!, forState: .normal)
        btn.inviteLicense(color: UIColor(hex: (String(appPlantFirmText)))!, forState: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_match_stopcall_pop"), for: .normal)
        btn.setImage(UIImage.verbalDescription(name: (dream_upperTitle.lowercased() + "_matc" + String(main_directorIdent.suffix(6)) + "call" + String(dreamPickName.prefix(4)))), for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 18)
        btn.titleLabel?.font = UIFont.concern(type: .Regular, fontSize: 18)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.spaceBetweenTitleAndImage = 6
        btn.spaceBetweenTitleAndImage = 6
        //: btn.layer.cornerRadius = 27
        btn.layer.cornerRadius = 27
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(originalFeeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(oversubscribedClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // VIP价格
    //: private lazy var vipFeeBtn: TalkingButton = {
    private lazy var vipFeeBtn: GoddamnThen = {
        //: let btn = TalkingButton()
        let btn = GoddamnThen()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_lounge_big"), for: .normal)
        btn.setImage(UIImage.verbalDescription(name: (String(mainParticleTitle.suffix(5)) + "loung" + String(notiDistantKey.suffix(5)))), for: .normal)
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.verticalAlignImageAndTitleWith(interval: 5, imageSize: CGSize(width: 28, height: 28), topOffset: 13)
        btn.albescent(interval: 5, imageSize: CGSize(width: 28, height: 28), topOffset: 13)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.concern(type: .Medium, fontSize: 18)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.spaceBetweenTitleAndImage = 6
        btn.spaceBetweenTitleAndImage = 6
        //: let colors = [UIColor(hex: "#AB57F6")!.cgColor, UIColor(hex: "#FC57B7")!.cgColor, UIColor(hex: "#FE9074")!.cgColor]
        let colors = [UIColor(hex: (String(noti_learnApplyName.prefix(6)) + showBasicIdent.capitalized))!.cgColor, UIColor(hex: (String(m_plainMessage)))!.cgColor, UIColor(hex: (String(dreamPoliticPath.prefix(7))))!.cgColor]
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: colors, size: CGSize(width: actualWidth(w: 257), height: actualWidth(w: 54))), for: .normal)
        btn.setBackgroundImage(UIImage.gravitySize(colors: colors, size: CGSize(width: actualWidth(w: 257), height: actualWidth(w: 54))), for: .normal)
        //: btn.layer.cornerRadius = 27
        btn.layer.cornerRadius = 27
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(fallFor), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appValueDetailColor(), .font: UIFont.pingfangFont(type: .Regular, fontSize: 13), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.appLayer(), .font: UIFont.concern(type: .Regular, fontSize: 13), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Maybe next time".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(noti_leadingMsg.suffix(6)) + appLiberalBarnPath.replacingOccurrences(of: "birth", with: "t") + " time").localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(closeBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(overPropertyClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Event

//: extension TalkingFreeStopCallPopUpView {
extension ParentObjectProtocol {
    /// 原价按钮拨打事件
    //: @objc func originalFeeButtonClick() {
    @objc func oversubscribedClick() {
        //: let type = self.data["type"] as? Int ?? -1
        let type = self.data[String(bytes: k_originValue.map{$0^150}, encoding: .utf8)!] as? Int ?? -1
        //: let toUid = self.data["toUid"] as? String ?? ""
        let toUid = self.data[(String(app_attractiveFormat.prefix(5)))] as? String ?? ""

        //: var data: [String: Any] = ["type": type,
        var data: [String: Any] = [String(bytes: k_originValue.map{$0^150}, encoding: .utf8)!: type,
                                   //: "uid": toUid]
                                   String(bytes: dream_birthdayUrl.map{moveFit(kick: $0)}, encoding: .utf8)!: toUid]
        //: if AppManager.share.loginUserMode.freeCallTimes > 0, AppManager.share.loginUserMode.sex == Gender.male.rawValue {
        if AppManager.share.loginUserMode.freeCallTimes > 0, AppManager.share.loginUserMode.sex == AlongsideDigitalTableConvertible.male.rawValue {
            //: data.updateValue(1, forKey: "fromFreeCall")
            data.updateValue(1, forKey: (String(mainCapacityUrl.suffix(6)) + "eeCal" + show_womanKey.replacingOccurrences(of: "cancel", with: "l")))
            //: TalkingSocketManager.shared.isFreeCall = true
            StackSocketManager.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": data]
        let info: [String: Any] = [String(bytes: dream_soMessage.map{dismissApprove(answer: $0)}, encoding: .utf8)!: (mainFellowStr.replacingOccurrences(of: "select", with: "eq") + "stCa" + userSearchionMsg.replacingOccurrences(of: "voice", with: "ll")), String(bytes: userWithoutDrawingGoodMessage.map{consentRed(dramatically: $0)}, encoding: .utf8)!: data]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        StackSocketManager.shared.someoneDisable(info: info)
        //: TalkingSocketManager.shared.errorDelegate = self
        StackSocketManager.shared.errorDelegate = self
        //: dismiss()
        peel()
    }

    /// 关闭按钮点击事件
    //: @objc func closeBtnClick() {
    @objc func overPropertyClick() {
        //: dismiss()
        peel()
    }

    /// 展示
    //: func show() {
    func capability() {
        //: popView = TalkingPopView(frame: UIScreen.main.bounds)
        popView = PanoramicViewBucketRecognizerDelegate(frame: UIScreen.main.bounds)
        //: frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        frame = CGRect(x: 0, y: 0, width: kGiftIdent, height: user_allMessage)
        //: popView?.initWithView(view: self)
        popView?.innerView(view: self)
        //: popView?.showInView(view: ImmediatelyThen.getWindow())
        popView?.viewEffect(view: ImmediatelyThen.wow())
        //: popView?.isRemoveTapGes = true
        popView?.isRemoveTapGes = true
    }

    //: @objc func dismiss() {
    @objc func peel() {
        //: popView?.dismissView()
        popView?.err()
        //: popView = nil
        popView = nil
    }

    /// 更新数据
    //: func setShowData(data: [String: Any]) {
    func theData(data: [String: Any]) {
        //: self.data = data
        self.data = data
        //: let originalFee = data["originalFee"] as? Int ?? 0
        let originalFee = data[(String(userAliveProcessFastId) + userInputValue.lowercased() + "nalFee")] as? Int ?? 0
        //: let vipFee = data["VIPFee"] as? Int ?? 0
        let vipFee = data[(String(showCycleStr.suffix(5)) + String(userWeightIdent))] as? Int ?? 0
        //: let freeCallTimes = data["freeCallTimes"] as? Int
        let freeCallTimes = data[(String(main_injuryValue) + String(app_infoMsg.suffix(8)))] as? Int
        //: AppManager.share.loginUserMode.freeCallTimes = freeCallTimes ?? 0
        AppManager.share.loginUserMode.freeCallTimes = freeCallTimes ?? 0
        //: originalFeeBtn.setTitle(String.setFreeStopPriceStr(originalFee: originalFee), for: .normal)
        originalFeeBtn.setTitle(String.boyfriend(originalFee: originalFee), for: .normal)
        //: vipFeeBtn.setTitle(String.setFreeStopPriceStr(originalFee: originalFee, vipFee: vipFee), for: .normal)
        vipFeeBtn.setTitle(String.boyfriend(originalFee: originalFee, vipFee: vipFee), for: .normal)
        //: updateSubViewsConstraint()
        tot()
    }

    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func fallFor() {
        //: guard AppManager.share.loginUserMode.loungePlus else {
        guard AppManager.share.loginUserMode.loungePlus else {
            // 非会员 && 默认模式
            //: if AppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if AppManager.share.appStatus == WrinkleSendable.normal.rawValue {
                //: PipSqueakReactiveCompatible.share.func__pushToSubscribeAlert()
                PipSqueakReactiveCompatible.share.pushBack()
            }
            //: dismiss()
            peel()
            //: return
            return
        }

        //: originalFeeButtonClick()
        oversubscribedClick()
    }
}

// MARK: - PresenceErrorDelegate

//: extension TalkingFreeStopCallPopUpView: TalkingSocketManagerErrorDelegate {
extension ParentObjectProtocol: PresenceErrorDelegate {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func bring(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "onRequestCall" {
        if cmd == (String(user_donorSignaturePath) + String(main_powerUrl)) {
            //: func__showStatusBarErrorMsg(showMsg: msg)
            changeManage(showMsg: msg)
            //: if errorNo == VideoChatErrorCode.MoneyLack.rawValue {
            if errorNo == WithdrawMeasurable.MoneyLack.rawValue {
                //: guard AppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
                guard AppManager.share.appStatus == WrinkleSendable.normal.rawValue else { return }
                //: PipSqueakReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickFreepopupButton, sufficient: false)
                PipSqueakReactiveCompatible.share.halfHardware(clickEvent: m_closeMessage, sufficient: false)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingFreeStopCallPopUpView {
extension ParentObjectProtocol {
    /// 更新约束
    //: private func updateSubViewsConstraint() {
    private func tot() {
        //: if AppManager.share.loginUserMode.loungePlus {
        if AppManager.share.loginUserMode.loungePlus {
            //: originalFeeBtn.isHidden = true
            originalFeeBtn.isHidden = true
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.height.equalTo(actualWidth(w: 255))
                make.height.equalTo(actualWidth(w: 255))
            }
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func hideSubviews() {
        //: addSubview(contentView)
        addSubview(contentView)
        //: contentView.addSubview(topIcon)
        contentView.addSubview(topIcon)
        //: contentView.addSubview(closeBtn)
        contentView.addSubview(closeBtn)
        //: contentView.addSubview(inputTView)
        contentView.addSubview(inputTView)
        //: contentView.addSubview(originalFeeBtn)
        contentView.addSubview(originalFeeBtn)
        //: contentView.addSubview(vipFeeBtn)
        contentView.addSubview(vipFeeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func endemicCapability() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.width.equalTo(actualWidth(w: 297))
            make.width.equalTo(actualWidth(w: 297))
            //: make.height.equalTo(actualWidth(w: 329))
            make.height.equalTo(actualWidth(w: 329))
        }

        //: topIcon.snp.makeConstraints { make in
        topIcon.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(actualWidth(w: -43))
            make.top.equalTo(actualWidth(w: -43))
            //: make.width.height.equalTo(actualWidth(w: 101))
            make.width.height.equalTo(actualWidth(w: 101))
        }

        //: inputTView.snp.makeConstraints { make in
        inputTView.snp.makeConstraints { make in
            //: make.top.equalTo(topIcon.snp.bottom)
            make.top.equalTo(topIcon.snp.bottom)
            //: make.leading.equalTo(20)
            make.leading.equalTo(20)
            //: make.trailing.equalTo(-20)
            make.trailing.equalTo(-20)
            //: make.height.equalTo(actualWidth(w: 103))
            make.height.equalTo(actualWidth(w: 103))
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.bottom.equalTo(actualWidth(w: -20))
            make.bottom.equalTo(actualWidth(w: -20))
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }

        //: vipFeeBtn.snp.makeConstraints { make in
        vipFeeBtn.snp.makeConstraints { make in
            //: make.bottom.equalTo(closeBtn.snp.top).offset(actualWidth(w: -15))
            make.bottom.equalTo(closeBtn.snp.top).offset(actualWidth(w: -15))
            //: make.width.equalTo(actualWidth(w: 257))
            make.width.equalTo(actualWidth(w: 257))
            //: make.height.equalTo(actualWidth(w: 54))
            make.height.equalTo(actualWidth(w: 54))
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: originalFeeBtn.snp.makeConstraints { make in
        originalFeeBtn.snp.makeConstraints { make in
            //: make.centerX.width.height.equalTo(vipFeeBtn)
            make.centerX.width.height.equalTo(vipFeeBtn)
            //: make.bottom.equalTo(vipFeeBtn.snp.top).offset(actualWidth(w: -20))
            make.bottom.equalTo(vipFeeBtn.snp.top).offset(actualWidth(w: -20))
        }
    }
}
