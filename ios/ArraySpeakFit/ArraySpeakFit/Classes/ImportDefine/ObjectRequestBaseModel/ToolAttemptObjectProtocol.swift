
//: Declare String Begin

/*: ; build: :*/
fileprivate let showWellFormat:[Character] = [";"," ","b","u","i","l"]
fileprivate let userNightStr:String = "d:guide down rhythm"

/*: ; iOS  :*/
fileprivate let main_platStr:[Character] = [";"," ","i","O","S"," "]

/*: "audio" :*/
fileprivate let notiFoodWelcomeTarName:String = "audiroof"

/*: .wav" :*/
fileprivate let mainBlockMessage:String = ".wavraw portrait man forward"

/*: "audio/wav" :*/
fileprivate let user_supposedStr:String = "exploreudi"

/*: .jpg" :*/
fileprivate let dream_compoundTitle:String = ".jpgstatement protocol among procedure effective"

/*: "image/jpeg" :*/
fileprivate let notiArrivalIdent:[Character] = ["i","m","a","g"]
fileprivate let app_historyIdent:String = "e/jpeginfluential invest"

/*: "://app." :*/
fileprivate let mBeginTitle:String = "trip fun discussion vessel bad://app."

/*: "data" :*/
fileprivate let k_blindFormat:String = "DATA"

/*: ":null" :*/
fileprivate let main_monitorHalfFormat:String = ":nullwed scenario"

/*: "Request failed, Try again later" :*/
fileprivate let k_modifyMsg:[UInt8] = [0x31,0x6,0x12,0x16,0x6,0x10,0x17,0x43,0x5,0x2,0xa,0xf,0x6,0x7,0x4f,0x43,0x37,0x11,0x1a,0x43,0x2,0x4,0x2,0xa,0xd,0x43,0xf,0x2,0x17,0x6,0x11]

private func sunNor(inside num: UInt8) -> UInt8 {
    return num ^ 99
}

/*: "json error" :*/
fileprivate let main_oldFormat:String = "meantime foundationjson erro"
fileprivate let kLooOvalData:[Character] = ["r"]

/*: "platform=iphone&version= :*/
fileprivate let dreamEachClientFormat:String = "platextended"
fileprivate let appHusbandResolveFormat:String = "hatne"
fileprivate let mFullPublishExistUrl:String = "contrast canon="

/*: &packageId= :*/
fileprivate let main_incomeSpendHowTitle:String = "&psound depend every comprehensive"
fileprivate let m_untilData:String = "acapplicationg"

/*: &bundleId= :*/
fileprivate let dreamBecomeMsg:String = "&bundleIjust night"
fileprivate let mainTheId:String = "d=preparation publisher method acquire advertising"

/*: &lang= :*/
fileprivate let dreamPingPath:[Character] = ["&","l","a","n","g","="]

/*: "/route/slowindex" :*/
fileprivate let show_layIdent:String = "key today formal focus uniform/rou"
fileprivate let app_netPath:[Character] = ["l","o","w"]
fileprivate let appSightValue:String = "ingivex"

/*: "/route/index" :*/
fileprivate let show_detectIdent:String = "failure perform yellow detect/rou"
fileprivate let dream_civicReinIdent:String = "ndcorex"

/*: "?path= :*/
fileprivate let main_phoneMsg:[Character] = ["?","p","a","t","h","="]

/*: "&GJNonceStr= :*/
fileprivate let dream_informationName:String = "&GJNocur common format design fragment"
fileprivate let app_stillPath:[Character] = ["n","c","e","S","t","r","="]

/*: "&params= :*/
fileprivate let mainMusicalPrivacyMsg:[Character] = ["&"]
fileprivate let showSimultaneouslyAddKey:String = "params=format medal do network merge"

/*: "token" :*/
fileprivate let notiFleetUrl:[Character] = ["t","o","k","e","n"]

/*: "请求成功 :*/
fileprivate let show_groundText:String = "\u{8bf7}\u{6c42}成功"

/*: "AAAAAAAA" :*/
fileprivate let app_beforeDownUrl:[Character] = ["A","A","A","A","A","A","A","A"]

/*: "下载完成 -  :*/
fileprivate let dream_titleMsg:String = "下ping完成 "
fileprivate let show_normallyFormat:[Character] = ["-"," "]

/*: "/Documents/" :*/
fileprivate let notiActivityUrl:String = "dimension just comfortable bind/Doc"
fileprivate let user_reserveChopUrl:[Character] = ["s","/"]

/*: ".mp4" :*/
fileprivate let user_towardsFormat:String = "publication headline.mp4"

/*: "移动地址: :*/
fileprivate let notiPolicyStr:[Character] = ["移","动","地","址",":"]

/*: "后台任务下载回来" :*/
fileprivate let appVisibleFormat:[Character] = ["\u{540e}","\u{53f0}","任","务","下","载","回","来"]

/*: "下载成功 : :*/
fileprivate let notiAccompanyPath:[Character] = ["下","载","成","\u{529f}"," ",":"]

/*: "Net Error" :*/
fileprivate let mainRoughlyClickData:String = "Net Ercondition permit detect"
fileprivate let kHostValue:[Character] = ["r","o","r"]

/*: "下载失败 : :*/
fileprivate let main_resFormat:String = "下载\u{5931}败 :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ToolAttemptObjectProtocol.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let dreamVideoFormat = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let dream_sampleValue = "\(noti_collectionMessage)/\(mColorButtonName) (\(k_clickMessage)" + (String(showWellFormat) + String(userNightStr.prefix(2))) + "\(m_contentMessage)" + (String(main_platStr)) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: PeopleErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let user_topEnvironmentId = ToolAttemptObjectProtocol()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class ToolAttemptObjectProtocol: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func thumbPrompt(model: ExecLaborReactiveCompatible, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.smoothOutObject() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.lipThe(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.completionDeadline(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func lipThe(model: ExecLaborReactiveCompatible, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.slowUp(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.directResumeModel(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    explore(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, PeopleErrorResponse(errorCode: PeopleCustomReflectable.NetError.rawValue, errorMsg: mainSizeFormat))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        changeManage(showMsg: mainSizeFormat)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (notiFoodWelcomeTarName.replacingOccurrences(of: "roof", with: "o")) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getBlock())+\(kLimitTitle)" + (String(mainBlockMessage.prefix(4))), mimeType: (user_supposedStr.replacingOccurrences(of: "explore", with: "a") + "o/wav"))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(dream_compoundTitle.prefix(4))), mimeType: (String(notiArrivalIdent) + String(app_historyIdent.prefix(6))))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    explore(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, PeopleErrorResponse(errorCode: PeopleCustomReflectable.NetError.rawValue, errorMsg: mainSizeFormat))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        changeManage(showMsg: mainSizeFormat)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func completionDeadline(model: ExecLaborReactiveCompatible, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.slowUp(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.directResumeModel(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: app_underFormat ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = dreamVideoFormat }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    explore(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, PeopleErrorResponse(errorCode: PeopleCustomReflectable.NetError.rawValue, errorMsg: mainSizeFormat))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        changeManage(showMsg: mainSizeFormat)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = dreamVideoFormat }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    explore(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, PeopleErrorResponse(errorCode: PeopleCustomReflectable.NetError.rawValue, errorMsg: mainSizeFormat))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        changeManage(showMsg: mainSizeFormat)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func explore(model: ExecLaborReactiveCompatible, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            SweepAddrTool.share.errorCookies(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if app_underFormat, model.requestServer.contains((String(mBeginTitle.suffix(7)))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.totalry(key: SweepAddrTool.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (k_blindFormat.lowercased()) + "\"" + (String(main_monitorHalfFormat.prefix(5))), with: "" + "\"" + (k_blindFormat.lowercased()) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !app_underFormat {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(main_monitorHalfFormat.prefix(5))), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<BubbleSolidMeasurable>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == PeopleCustomReflectable.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, PeopleErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    changeManage(showMsg: responseModel.msg ?? String(bytes: k_modifyMsg.map{sunNor(inside: $0)}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case PeopleCustomReflectable.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: notiScaleNetAcceptIdent, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, PeopleErrorResponse(errorCode: PeopleCustomReflectable.NetError.rawValue, errorMsg: (String(main_oldFormat.suffix(9)) + String(kLooOvalData)).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func slowUp(model: ExecLaborReactiveCompatible) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (dreamEachClientFormat.replacingOccurrences(of: "extended", with: "f") + "orm=ip" + appHusbandResolveFormat.replacingOccurrences(of: "at", with: "o") + "&versi" + String(mFullPublishExistUrl.suffix(3))) + "\(userReplaceContent)" + (String(main_incomeSpendHowTitle.prefix(2)) + m_untilData.replacingOccurrences(of: "application", with: "ka") + "eId=") + "\(kLimitTitle)" + (String(dreamBecomeMsg.prefix(8)) + String(mainTheId.prefix(2))) + "\(k_clickMessage)" + (String(dreamPingPath)) + "\(SweepAddrTool.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if app_underFormat, serverUrl.contains((String(mBeginTitle.suffix(7)))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if SweepAddrTool.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(show_layIdent.suffix(4)) + "te/s" + String(app_netPath) + appSightValue.replacingOccurrences(of: "give", with: "de")))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(show_detectIdent.suffix(4)) + "te/i" + dream_civicReinIdent.replacingOccurrences(of: "core", with: "e"))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(main_phoneMsg)) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(dream_informationName.prefix(5)) + String(app_stillPath)) + "\(SweepAddrTool.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.bodyPolitic(key: SweepAddrTool.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(mainMusicalPrivacyMsg) + String(showSimultaneouslyAddKey.prefix(7))) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func directResumeModel(model: ExecLaborReactiveCompatible) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(dream_sampleValue)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (String(notiFleetUrl)), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func musical(model: ExecLaborReactiveCompatible) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.slowUp(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (show_groundText) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (String(app_beforeDownUrl))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (dream_titleMsg.replacingOccurrences(of: "ping", with: "载") + String(show_normallyFormat)) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(notiActivityUrl.suffix(4)) + "ument" + String(user_reserveChopUrl)) + NSDate.getBlock() + (String(user_towardsFormat.suffix(4)))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (String(notiPolicyStr)) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (String(appVisibleFormat)))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = ComparableBaseDelegate.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func noHighlight(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: mainLastName.doGender(), with: k_filterName)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(notiAccompanyPath)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, PeopleErrorResponse(errorCode: PeopleCustomReflectable.Normal.rawValue, errorMsg: (String(mainRoughlyClickData.prefix(6)) + String(kHostValue)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, PeopleErrorResponse(errorCode: PeopleCustomReflectable.NetError.rawValue, errorMsg: (String(mainRoughlyClickData.prefix(6)) + String(kHostValue)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (main_resFormat) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func anatomy(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: mainLastName.doGender(), with: k_filterName)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(notiAccompanyPath)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, PeopleErrorResponse(errorCode: PeopleCustomReflectable.Normal.rawValue, errorMsg: (String(mainRoughlyClickData.prefix(6)) + String(kHostValue)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, PeopleErrorResponse(errorCode: PeopleCustomReflectable.NetError.rawValue, errorMsg: (String(mainRoughlyClickData.prefix(6)) + String(kHostValue)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (main_resFormat) + "\(errMsg)")
            }
        }
    }
}
