
//: Declare String Begin

/*: "http://" :*/
fileprivate let mCharacterName:String = "immune entry auto flat rewardhttp:/"
fileprivate let appDownStr:[Character] = ["/"]

/*: "https://" :*/
fileprivate let show_yearId:String = "hhairhairps"
fileprivate let userExploreValue:String = ":arrivalarrival"

/*: "http:// :*/
fileprivate let showDeadlineFleshIdent:[Character] = ["h","t","t","p",":"]
fileprivate let user_tripKey:String = "//"

/*: "serverSelctIndex =  :*/
fileprivate let show_capableTitle:[Character] = ["s","e","r","v","e","r"]
fileprivate let mOningTitle:[Character] = ["S","e","l","c","t","I","n"]
fileprivate let user_termsId:String = "dex = truly extent"

/*: "serverUrlStr =  :*/
fileprivate let kPlasticAddressPath:[Character] = ["s","e","r","v","e","r","U","r","l","S","t","r"]
fileprivate let notiExampleNextTitle:String = " = "

/*: "app. :*/
fileprivate let k_chopMessage:String = "retain file block publication toleranceapp."

/*: .com" :*/
fileprivate let dream_userSoundValue:String = ".compartner place state hide box"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let mainPairData:String = "user bag flagmf/c"
fileprivate let noti_bothPath:String = "builder time write earnom/s"
fileprivate let showIncreasingPath:[Character] = ["e","n","d","G","i","f","t"]

/*: "recharge/applePayNotify" :*/
fileprivate let kDismissSatisfyData:String = "weeklyech"
fileprivate let dreamIgnoreId:[Character] = ["l","e","P","a","y","N"]
fileprivate let dream_plasticName:String = "OTIFY"

/*: "user/appleLogin" :*/
fileprivate let mainPopularTenderTitle:[Character] = ["u","s","e","r","/","a","p"]
fileprivate let notiIndividualData:String = "pleLoginsimultaneously communicate business trip"

/*: "IM/getMyBlackList" :*/
fileprivate let mHolderSweepStr:[Character] = ["I","M","/","g","e","t","M","y","B","l","a","c"]
fileprivate let dreamDisabledIdent:[Character] = ["k","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let notiDonorContent:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","s","e","n"]
fileprivate let notiMatchValue:String = "con pack beautifuldMsg"

/*: "mf/business/sendGift" :*/
fileprivate let main_downUrl:[Character] = ["m","f","/","b","u","s","i","n","e","s"]
fileprivate let mPicId:String = "response just tar regular invites/send"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let show_whenUrl:String = "age hint termmf/cha"
fileprivate let app_valueKey:String = "reference play behavior manual dutym/"
fileprivate let user_usStr:[Character] = ["s","e","n","d","M","s","g"]

/*: "GJ21FB" :*/
fileprivate let m_cutIncomeData:[Character] = ["G","J","2","1","F"]
fileprivate let m_barnPath:String = "bed"

/*: "完成了检测,请求状态正常" :*/
fileprivate let app_boundMessage:String = "完\u{6210}了检测,\u{8bf7}"
fileprivate let mainRejectStr:[Character] = ["求","状","态","正","常"]

/*: "接口请求失败" :*/
fileprivate let mainBoltText:[Character] = ["\u{63a5}"]
fileprivate let mainTimingMessage:String = "口请求失败"

/*: "完成了检测,需要更换server url" :*/
fileprivate let appKingdomText:[Character] = ["完","成","了","检","\u{6d4b}",",","\u{9700}","要","\u{66f4}","换"]
fileprivate let user_elsewherePath:[Character] = ["s","e","r","v"]
fileprivate let mainBoyfriendValue:[Character] = ["e","r"," ","u","r","l"]

/*: "HTTPProxy" :*/
fileprivate let showSucceedFormat:String = "HtelltellPP"
fileprivate let user_meetQuantityerestId:String = "ROXY"

/*: "__SCOPED__" :*/
fileprivate let mMinimizeStr:[Character] = ["_","_","S","C","O","P","E","D","_","_"]

/*: "tap" :*/
fileprivate let show_activityIdent:String = "tpo"

/*: "tun" :*/
fileprivate let noti_broadcastReplySoonKey:String = "TUN"

/*: "ipsec" :*/
fileprivate let noti_mineIdent:String = "imusicalec"

/*: "ppp" :*/
fileprivate let showFunPath:String = "tiptiptip"

/*: "func__updateTalkingCookies =  :*/
fileprivate let appMentionStartUrl:[Character] = ["f","u","n","c","_","_","u","p","d","a","t","e","T","a","l","k","i","n","g","C","o","o"]
fileprivate let appReliefStr:[Character] = ["k","i","e","s"," ","="," "]

/*: "https:// :*/
fileprivate let noti_onceIdent:String = "hsituationsituationp"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let mainRequireName:[UInt8] = [0x6,0x15,0xe,0x3,0x3f,0x3f,0x4,0x5,0xc,0x5,0x14,0x5,0x33,0x1,0xd,0x5,0x23,0xf,0xf,0xb,0x9,0x5,0x13,0x37,0x9,0x14,0x8,0x35,0x12,0xc,0x40,0x5d,0x40]

/*: "UID" :*/
fileprivate let showGreenName:[Character] = ["U","I","D"]

/*: "PHPSESSID" :*/
fileprivate let user_layerValue:String = "PlabelPSE"

/*: "func__saveAllCookies =  :*/
fileprivate let noti_brigStr:String = "func__label motivation"
fileprivate let dream_findId:String = "both listen yourselfllCoo"

/*: "func__saveAllCookies  error" :*/
fileprivate let mainOrangeMsg:String = "choose addition pop album herefunc_"
fileprivate let mHandFaultWeddingName:String = "_saveperfect recover"
fileprivate let user_slipPath:String = "KIES"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SweepAddrTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum RowTitleTotalerpolation: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class SweepAddrTool: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = SweepAddrTool()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        upward()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(mCharacterName.suffix(6)) + String(appDownStr))) || newValue.hasPrefix((show_yearId.replacingOccurrences(of: "hair", with: "t") + userExploreValue.replacingOccurrences(of: "arrival", with: "/"))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if main_sinceName && !app_underFormat {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(showDeadlineFleshIdent) + user_tripKey.capitalized) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(showDeadlineFleshIdent) + user_tripKey.capitalized) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    errorCookies(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func upward() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = show_titleId.integer(forKey: kSucceedRouteTitle)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (String(show_capableTitle) + String(mOningTitle) + String(user_termsId.prefix(6))) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = nearHurry()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(mCharacterName.suffix(6)) + String(appDownStr))
        //: if Environment_Formal && !Encryption_Request {
        if main_sinceName && !app_underFormat {
            //: serverPrefix = "http://"
            serverPrefix = (String(mCharacterName.suffix(6)) + String(appDownStr))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (String(kPlasticAddressPath) + notiExampleNextTitle.capitalized) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func nearHurry() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func waitressMark(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(k_chopMessage.suffix(4))) + "\(k_filterName)" + (String(dream_userSoundValue.prefix(4))), (String(k_chopMessage.suffix(4))) + "\(k_filterName)" + (String(dream_userSoundValue.prefix(4)))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(mainPairData.suffix(4)) + "hatRo" + String(noti_bothPath.suffix(4)) + String(showIncreasingPath)),
         //: "recharge/applePayNotify",
         (kDismissSatisfyData.replacingOccurrences(of: "weekly", with: "r") + "arge/app" + String(dreamIgnoreId) + dream_plasticName.lowercased()),
         //: "user/appleLogin"] + slowPathArr1
         (String(mainPopularTenderTitle) + String(notiIndividualData.prefix(8)))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(mHolderSweepStr) + String(dreamDisabledIdent)),
         //: "mf/business/sendMsg",
         (String(notiDonorContent) + String(notiMatchValue.suffix(4))),
         //: "mf/business/sendGift",
         (String(main_downUrl) + String(mPicId.suffix(6)) + "Gift"),
         //: "mf/chatRoom/sendMsg"]
         (String(show_whenUrl.suffix(6)) + "tRoo" + String(app_valueKey.suffix(2)) + String(user_usStr))]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = ImmediatelyThen.getSystemLangCode()
        let lang = ImmediatelyThen.activate()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .increasing(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (String(m_cutIncomeData) + m_barnPath.replacingOccurrences(of: "bed", with: "B")) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension SweepAddrTool {
    //: func checkServerUrl() {
    func arcFeedback() {
        //: if AppManager.share.networkStatus != .Unavailable {
        if AppManager.share.networkStatus != .Unavailable {
            //: func_requestHost()
            overPlan()
            //: } else {
        } else {
            //: func_starCodeTime()
            windowNeed()
        }
    }

    //: func func_starCodeTime() {
    func windowNeed() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.overPlan()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func recapture() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func overPlan() {
        //: if AppManager.share.networkStatus == .Unavailable {
        if AppManager.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        recapture()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        DrawstringBagReactiveCompatible.logQuick { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (app_boundMessage + String(mainRejectStr)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (String(mainBoltText) + mainTimingMessage.capitalized))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.windowNeed()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (String(appKingdomText) + String(user_elsewherePath) + String(mainBoyfriendValue)))
                    //: self.func__changeHostUrl()
                    self.standard()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func standard() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        recapture()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        show_titleId.set(serverSelctIndex, forKey: kSucceedRouteTitle)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(mCharacterName.suffix(6)) + String(appDownStr))
        //: if Environment_Formal && !Encryption_Request {
        if main_sinceName, !app_underFormat {
            //: serverPrefix = "http://"
            serverPrefix = (String(mCharacterName.suffix(6)) + String(appDownStr))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension SweepAddrTool {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func switcheOne() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(showSucceedFormat.replacingOccurrences(of: "tell", with: "T") + user_meetQuantityerestId.lowercased())] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func towardAppVpn() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(mMinimizeStr))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((show_activityIdent.replacingOccurrences(of: "po", with: "ap"))) || keyStr.contains((noti_broadcastReplySoonKey.lowercased())) || keyStr.contains((noti_mineIdent.replacingOccurrences(of: "musical", with: "ps"))) || keyStr.contains((showFunPath.replacingOccurrences(of: "tip", with: "p"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension SweepAddrTool {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func errorCookies(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(appMentionStartUrl) + String(appReliefStr)) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = self.cookies()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = AppManager.share.appConfigMode.urlH5Domain
        var urlDomain: String = AppManager.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(mCharacterName.suffix(6)) + String(appDownStr)), with: (show_yearId.replacingOccurrences(of: "hair", with: "t") + userExploreValue.replacingOccurrences(of: "arrival", with: "/")))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((show_yearId.replacingOccurrences(of: "hair", with: "t") + userExploreValue.replacingOccurrences(of: "arrival", with: "/"))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (noti_onceIdent.replacingOccurrences(of: "situation", with: "t") + "s://") + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            discoid(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        systemSplitCookies()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func discoid(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: mainRequireName.map{$0^96}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func cookies() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(mCharacterName.suffix(6)) + String(appDownStr))
        //: if Environment_Formal && !Encryption_Request {
        if main_sinceName, !app_underFormat {
            //: serverPrefix = "https://"
            serverPrefix = (show_yearId.replacingOccurrences(of: "hair", with: "t") + userExploreValue.replacingOccurrences(of: "arrival", with: "/"))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(k_chopMessage.suffix(4))) + "\(k_filterName)" + (String(dream_userSoundValue.prefix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func liveryAboutNote(note _: Notification) {
        //: if AppManager.share.loginUid.isEmptyString {
        if AppManager.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            iii()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            headline()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func iii() {
        //: var htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(showGreenName)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(showGreenName))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (user_layerValue.replacingOccurrences(of: "label", with: "H") + "SSID") {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(user_layerValue.replacingOccurrences(of: "label", with: "H") + "SSID")] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(showGreenName)) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(String(showGreenName))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (user_layerValue.replacingOccurrences(of: "label", with: "H") + "SSID") {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(user_layerValue.replacingOccurrences(of: "label", with: "H") + "SSID")] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((String(showGreenName))), h5Cookie.keys.contains((String(showGreenName))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(String(showGreenName))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(String(showGreenName))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((user_layerValue.replacingOccurrences(of: "label", with: "H") + "SSID")), h5Cookie.keys.contains((user_layerValue.replacingOccurrences(of: "label", with: "H") + "SSID")) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(user_layerValue.replacingOccurrences(of: "label", with: "H") + "SSID")]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(user_layerValue.replacingOccurrences(of: "label", with: "H") + "SSID")]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func headline() {
        //: let htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain
        let htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == AppManager.share.loginUserMode.userID {
            if cookie.name == (String(showGreenName)), cookie.value == AppManager.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (user_layerValue.replacingOccurrences(of: "label", with: "H") + "SSID") {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(showGreenName)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(showGreenName))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (user_layerValue.replacingOccurrences(of: "label", with: "H") + "SSID") {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(user_layerValue.replacingOccurrences(of: "label", with: "H") + "SSID")] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((String(showGreenName))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(showGreenName))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((user_layerValue.replacingOccurrences(of: "label", with: "H") + "SSID")) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(user_layerValue.replacingOccurrences(of: "label", with: "H") + "SSID")] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func systemSplitCookies() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(noti_brigStr.prefix(6)) + "saveA" + String(dream_findId.suffix(5)) + "kies = ") + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            show_titleId.set(cookieData, forKey: mAskMsg)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(mainOrangeMsg.suffix(5)) + String(mHandFaultWeddingName.prefix(5)) + "AllCoo" + user_slipPath.lowercased() + "  error"))
        }
    }

    //: func func__setAppCookies() {
    func stammererGuide() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = show_titleId.object(forKey: mAskMsg)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func thenHalf() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        show_titleId.removeObject(forKey: mAskMsg)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
