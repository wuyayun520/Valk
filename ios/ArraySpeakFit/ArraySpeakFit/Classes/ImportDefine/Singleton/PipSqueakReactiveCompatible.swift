
//: Declare String Begin

/*: "fee" :*/
fileprivate let kPackageMessage:String = "exee"

/*: "VIPFee" :*/
fileprivate let user_bulletPowerData:String = "VIPFeemp priority"

/*: "chattingNum" :*/
fileprivate let mainJudgeTitle:String = "would"
fileprivate let k_mirrorHeatPath:[Character] = ["h","a","t","t","i","n","g","N","u","m"]

/*: "You're already in her live room" :*/
fileprivate let m_closedMsg:[UInt8] = [0xb4,0x82,0x98,0xca,0x9f,0x88,0xcd,0x8c,0x81,0x9f,0x88,0x8c,0x89,0x94,0xcd,0x84,0x83,0xcd,0x85,0x88,0x9f,0xcd,0x81,0x84,0x9b,0x88,0xcd,0x9f,0x82,0x82,0x80]

private func plainMessage(quickly num: UInt8) -> UInt8 {
    return num ^ 237
}

/*: "toUid" :*/
fileprivate let app_hundredValue:[UInt8] = [0xda,0xc1,0xfb,0xc7,0xca]

/*: "momentId" :*/
fileprivate let noti_greenVeryDirectionFormat:[UInt8] = [0xf8,0xfa,0xf8,0xf0,0xfb,0xe1,0xdc,0xf1]

private func topShe(author num: UInt8) -> UInt8 {
    return num ^ 149
}

/*: "extra" :*/
fileprivate let appPitchName:[UInt8] = [0xe8,0xfb,0xf7,0xf5,0xe4]

fileprivate func mindDetect(teammate num: UInt8) -> UInt8 {
    let value = Int(num) - 131
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let dreamBrigPerTitle:[UInt8] = [0x64,0x69,0x75]

/*: "onlineStatus" :*/
fileprivate let dream_manualLoopFormat:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x53,0x65,0x6e,0x69,0x6c,0x6e,0x6f]

/*: "isNewUser" :*/
fileprivate let appObstacleStr:[UInt8] = [0x19,0x3,0x3e,0x15,0x7,0x25,0x3,0x15,0x2]

/*: "userStatus" :*/
fileprivate let main_sunContent:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x53,0x72,0x65,0x73,0x75]

/*: "Account is restricted！" :*/
fileprivate let main_minuteWoodPhoneMessage:[Character] = ["A","c","c","o","u","n","t"," ","i","s"," ","r","e","s","t","r","i","c"]
fileprivate let appConsistValue:String = "ted！white quit anti beyond"

/*: "Failed to enter the chat room. Please try again later" :*/
fileprivate let mainCeaseName:[UInt8] = [0x83,0xa4,0xac,0xa9,0xa0,0xa1,0xe5,0xb1,0xaa,0xe5,0xa0,0xab,0xb1,0xa0,0xb7,0xe5,0xb1,0xad,0xa0,0xe5,0xa6,0xad,0xa4,0xb1,0xe5,0xb7,0xaa,0xaa,0xa8,0xeb,0xe5,0x95,0xa9,0xa0,0xa4,0xb6,0xa0,0xe5,0xb1,0xb7,0xbc,0xe5,0xa4,0xa2,0xa4,0xac,0xab,0xe5,0xa9,0xa4,0xb1,0xa0,0xb7]

private func spaceDraft(carrier num: UInt8) -> UInt8 {
    return num ^ 197
}

/*: "url" :*/
fileprivate let mainDoingicialMessage:[Character] = ["u","r","l"]

/*: "userDetail" :*/
fileprivate let mDetailCountInfrastructureId:String = "parent resourceuserD"
fileprivate let m_hairFormat:String = "eputail"

/*: "&type=7" :*/
fileprivate let userLifeFormat:String = "wood report in his effect&type=7"

/*: "amount" :*/
fileprivate let notiLoopName:[UInt8] = [0xd,0x1,0x3,0x19,0x2,0x18]

private func moreNews(lost num: UInt8) -> UInt8 {
    return num ^ 108
}

/*: "transparency" :*/
fileprivate let kBoldDiskUrl:[Character] = ["t","r","a","n","s","p","a"]
fileprivate let appRemoteRoleMsg:String = "resmoothy"

/*: "version= :*/
fileprivate let dreamScienceName:String = "vmodersion"
fileprivate let mSoldTitle:[Character] = ["="]

/*: &packageId= :*/
fileprivate let userCapPath:[Character] = ["&","p","a","c","k","a"]
fileprivate let showSkinUnlessData:String = "stack forth beauty sufficientgeId="

/*: &bundleId= :*/
fileprivate let mainPublishTitle:String = "safe director thread cover volume&bundle"
fileprivate let notiParentUrl:String = "Id=broad sir neat publicly design"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PipSqueakReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/31.
//

//: import UIKit
import UIKit

//: public class PipSqueakReactiveCompatible: NSObject {
public class PipSqueakReactiveCompatible: NSObject {
    //: @objc static public let share = PipSqueakReactiveCompatible()
    @objc public static let share = PipSqueakReactiveCompatible()
    //: private override init() {}
    override private init() {}
}

//: extension PipSqueakReactiveCompatible {
extension PipSqueakReactiveCompatible {
    // MARK: - 随机视频入口

    //: func func__pushToRandomVideoVC(isBeginRand: Bool) {
    func funcInsideEnableeByOne(isBeginRand: Bool) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickInRandomvideo)
        noti_thirdUrl.toSpeaker(eventID: userDisplayMessage)
        //: guard TalkingSocketManager.shared.isObject == false else {
        guard StackSocketManager.shared.isObject == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.changeManage(showMsg: m_restoreInputKey)
            //: return
            return
        }
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        SatelliteThen.keepDependMicrophone { isOpen in
            //: guard isOpen == true else { return }
            guard isOpen == true else { return }
            //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
            SavingGraceReactiveCompatible.refer { succeed, result, _ in
                //: guard succeed else { return }
                guard succeed else { return }
                //: let json = JSON(result ?? [String: Any]())
                let json = JSON(result ?? [String: Any]())
                //: let vc = TalkingRandomVideoViewController()
                let vc = PictureViewController()
                //: vc.fee = json["fee"].intValue
                vc.fee = json[(kPackageMessage.replacingOccurrences(of: "ex", with: "f"))].intValue
                //: vc.vipFee = json["VIPFee"].intValue
                vc.vipFee = json[(String(user_bulletPowerData.prefix(6)))].intValue
                //: vc.chattingNum = json["chattingNum"].intValue
                vc.chattingNum = json[(mainJudgeTitle.replacingOccurrences(of: "would", with: "c") + String(k_mirrorHeatPath))].intValue
                //: vc.isBeginRand = isBeginRand
                vc.isBeginRand = isBeginRand
                //: vc.isFirstRandow = isBeginRand
                vc.isFirstRandow = isBeginRand
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.decideBroad()?.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }

    // MARK: - 完善资料

    //: func func__pushToFillUserInfoVC() {
    func syllabus() {
        //: AppManager.share.userFillInfoMode = UserFillInfoModel.init()
        AppManager.share.userFillInfoMode = FaceInfoModel()
        //: let VC = TalkingLoginGenderVC()
        let VC = RowReactiveCompatible()
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = funcProcess()
        //: if currentVC != nil {
        if currentVC != nil {
            //: currentVC?.navigationController?.pushViewController(VC, animated: true)
            currentVC?.navigationController?.pushViewController(VC, animated: true)
        }
    }

    // MARK: - 绑定邮箱

    /// - Parameters:
    ///   - type: 类型
    ///   - isShowBack: 是否显示返回按钮
    //: func func__pushToLockUserEmailVC(isShowBack: Bool = true) {
    func materialBack(isShowBack: Bool = true) {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = funcProcess()
        //: if currentVC != nil {
        if currentVC != nil {
            //: if currentVC!.isKind(of: TalkingLoginBindEmailVC.self) ||
            if currentVC!.isKind(of: ParentReactiveCompatible.self) ||
                //: currentVC!.isKind(of: TalkingLoginMainViewController.self) {
                currentVC!.isKind(of: RowLicenseRecognizerDelegate.self)
            {
                //: return
                return
            }
            //: let vc = TalkingLoginBindEmailVC()
            let vc = ParentReactiveCompatible()
            //: vc.isBack = isShowBack
            vc.isBack = isShowBack
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
    }

    // MARK: - 跳转个人资料

    //: func func__pushToUserDetailVC(uid: String?) {
    func balanceConvertSeek(uid: String?) {
        //: guard let currentVC = self.currentViewController() else { return }
        guard let currentVC = self.decideBroad() else { return }
        //: if let liveVC = currentVC as? TalkingLiveBeautifyViewController, TalkingLiveManager.shared().isLive {
        if let liveVC = currentVC as? LandscapeViewController, GuideUnitReactiveCompatible.commonFragment().isLive {
            // 从主播页跳转个人资料时，切小屏模式
            //: liveVC.topView.miniButtonClick()
            liveVC.topView.labelSkirt()

            //: } else if currentVC.isKind(of: TalkingVoiceRoomViewController.self), TalkingVoiceRoomManager.shared().isParty {
        } else if currentVC.isKind(of: CreamThen.self), StuffLicenseReactiveCompatible.iconPoint().isParty {
            //: TalkingVoiceRoomManager.shared().voiceRoom_mini()
            StuffLicenseReactiveCompatible.iconPoint().attentionMini()

            //: } else if let profileVC = currentVC as? TalkingUserDetailViewController {
        } else if let profileVC = currentVC as? RegardViewDelegate {
            // 同一用户的资料页已经在顶部，只刷新
            //: if profileVC.uid == uid {
            if profileVC.uid == uid {
                //: profileVC.requestAllData()
                profileVC.keep()
                //: return
                return
            }
        }

        //: let vc = TalkingUserDetailViewController(tempUid: uid)
        let vc = RegardViewDelegate(tempUid: uid)
        //: getNavigationController()?.pushViewController(vc, animated: true)
        firstController()?.pushViewController(vc, animated: true)
    }

    // MARK: - 观众侧进入直播间

    /// 观众侧跳转到直播间
    /// - Parameters:
    ///   - uid: 主播id
    ///   - enterType: 入口
    //: func func_audiencePushToLiveRoomVC(uid: String, enterType: LiveEnterType) {
    func reOff(uid: String, enterType: ProgressEnterType) {
        //: guard TalkingPermissionTool.isLiveOrPartyActive() == false else { return }
        guard SatelliteThen.hike() == false else { return }

        // 同一直播间只能进一次
        //: guard String(TalkingLiveManager.shared().liveRoomModel.streamerInfo.uid) != uid else {
        guard String(GuideUnitReactiveCompatible.commonFragment().liveRoomModel.streamerInfo.uid) != uid else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You're already in her live room".localized)
            self.changeManage(showMsg: String(bytes: m_closedMsg.map{plainMessage(quickly: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        // 校验接口，通过后进入直播间
        //: TalkingAudienceManager().req_enterLiveRoom(streamerUid: uid) { succeed, result, errorModel in
        DemandAudienceManager().actHardWater(streamerUid: uid) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: guard let livemodel = TalkingLiveRoomModel.deserialize(from: result as? Dictionary) else { return }
            guard let livemodel = FleetMeasurable.deserialize(from: result as? Dictionary) else { return }
            // 退出之前的直播间
            //: let currentVC = self.func__getCurrentActivityVC()
            let currentVC = self.funcProcess()
            //: if let vcArr = currentVC?.navigationController?.viewControllers {
            if let vcArr = currentVC?.navigationController?.viewControllers {
                //: for index in 0..<(vcArr.count) {
                for index in 0 ..< (vcArr.count) {
                    //: let vc = vcArr[index]
                    let vc = vcArr[index]
                    //: if vc.isKind(of: TalkingLivePullStreamsViewController.self) == true {
                    if vc.isKind(of: NobodyObjectProtocol.self) == true {
                        //: (vc as! TalkingLivePullStreamsViewController).popCurrentViewController()
                        (vc as! NobodyObjectProtocol).utilize()
                    }
                }
            }

            // 进入新直播间
            //: TalkingLiveManager.shared().liveRoomModel = livemodel
            GuideUnitReactiveCompatible.commonFragment().liveRoomModel = livemodel
            //: let liveVc = TalkingLivePullStreamsViewController()
            let liveVc = NobodyObjectProtocol()
            //: liveVc.type = enterType
            liveVc.type = enterType
            //: currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            // 埋点
            //: switch enterType {
            switch enterType {
            //: case .match:
            case .match:
                //: uploadRecord.uploadRecordEvent(eventID: ClickMatchLiveWindowNoP, toUid: uid)
                noti_thirdUrl.toSpeaker(eventID: kPingMsg, toUid: uid)
            //: case .userDetail:
            case .userDetail:
                //: uploadRecord.uploadRecordEvent(eventID: ClickProfileLiveWindowNoP, toUid: uid)
                noti_thirdUrl.toSpeaker(eventID: m_dataId, toUid: uid)
            //: case .unknown: break
            case .unknown: break
            }
        }
    }

    /// 进入语聊房
    /// - Parameters:
    ///   - roomId: 房间Id（nil：创建房间；有值：进入房间）
    //: func func_pushToVoiceRoomVC(roomId: String? = nil) {
    func doingeVoice(roomId: String? = nil) {
        //: if TalkingLiveManager.shared().isLive == true {
        if GuideUnitReactiveCompatible.commonFragment().isLive == true { // 直播中
            //: func__showStatusBarErrorMsg(showMsg: kMessage_live_limit)
            changeManage(showMsg: user_systemUrl)
            //: return
            return
        }
        //: if roomId == TalkingVoiceRoomManager.shared().partyModel.roomId {
        if roomId == StuffLicenseReactiveCompatible.iconPoint().partyModel.roomId { // 返回语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_goback()
            StuffLicenseReactiveCompatible.iconPoint().vocalisation()
            //: return
            return
        }
        //: if TalkingVoiceRoomManager.shared().isParty == true {
        if StuffLicenseReactiveCompatible.iconPoint().isParty == true { // 切换语聊房
            //: if roomId != nil {
            if roomId != nil {
                //: TalkingVoiceRoomManager.shared().voiceRoom_switch(roomId: roomId!, beforeRoomId: TalkingVoiceRoomManager.shared().partyModel.roomId)
                StuffLicenseReactiveCompatible.iconPoint().libraryConclusionLip(roomId: roomId!, beforeRoomId: StuffLicenseReactiveCompatible.iconPoint().partyModel.roomId)
            }
            //: return
            return
        }
        //: TalkingVoiceRoomManager.shared().voiceRoom_checkAndTurnOn(roomId: roomId)
        StuffLicenseReactiveCompatible.iconPoint().versionTo(roomId: roomId)
    }

    // MARK: - 跳转聊天

    /// 跳转到消息列表页
    /// - Parameter isHalf: 是否半屏展示
    //: func func__pushToChatListVC(isHalfView: Bool = false) {
    func player(isHalfView: Bool = false) {
        //: let vc = TalkingChatListViewController(isHalfView: isHalfView)
        let vc = SayManagerDelegate(isHalfView: isHalfView)
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = funcProcess()
        //: if isHalfView == false {
        if isHalfView == false {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
            //: } else {
        } else {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
        }
    }

    /// 跳转到消息聊天页
    /// - Parameters:
    ///   - chatID: 对方Id
    ///   - isFrom: 私聊页入口
    /// - Returns: 消息聊天页

    //: func func__pushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, completion: ((_ vc: TalkingPrivateChatController) -> Void)? = nil ) {
    func vidScan(chatID: String, isFrom: JournalSayFromEnum = .Normal, completion: ((_ vc: FamilyFleetViewController) -> Void)? = nil) {
        //: guard chatID.count > 0 else { return }
        guard chatID.count > 0 else { return }

        //: if chatID != ImmediatelyThen.getXiaoMiID() {
        if chatID != ImmediatelyThen.polytetrafluoroethylene() { // 系统消息
            //: ProgressHUD.show()
            OrdinalProgressHUD.loo()
            //: let param = ["toUid": chatID, "momentId": "0", "extra": "1"]
            let param = [String(bytes: app_hundredValue.map{$0^174}, encoding: .utf8)!: chatID, String(bytes: noti_greenVeryDirectionFormat.map{topShe(author: $0)}, encoding: .utf8)!: "0", String(bytes: appPitchName.map{mindDetect(teammate: $0)}, encoding: .utf8)!: "1"]
            //: LifeReactiveCompatible.req_refreshUserChatInfo(param: param) { [weak self] succeed, result, errorModel in
            LifeReactiveCompatible.hubCompletion(param: param) { [weak self] succeed, result, _ in
                //: ProgressHUD.dismiss()
                OrdinalProgressHUD.vision()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: guard succeed else { return }
                guard succeed else { return }

                //: let userWrap = RelatedTransformable.func__transformedChatinfo(userDic: result as! Dictionary<String, Any>)
                let userWrap = RelatedTransformable.execute(userDic: result as! [String: Any])
                //: DBUserInfoManager.cache_updateMsgUserInfoAndPostNotif(with: userWrap)
                StateThen.sceneFlush(with: userWrap)
                // 更新消息列表用户在线状态
                //: let userInfo = ["uid": chatID,
                let userInfo = [String(bytes: dreamBrigPerTitle.reversed(), encoding: .utf8)!: chatID,
                                //: "onlineStatus": userWrap.onlineStatus,
                                String(bytes: dream_manualLoopFormat.reversed(), encoding: .utf8)!: userWrap.onlineStatus,
                                //: "isNewUser":userWrap.isNewUser,
                                String(bytes: appObstacleStr.map{$0^112}, encoding: .utf8)!: userWrap.isNewUser,
                                //: "userStatus": userWrap.userStatus]
                                String(bytes: main_sunContent.reversed(), encoding: .utf8)!: userWrap.userStatus]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: noti_tagData,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
                //: if userWrap.userStatus == 3 {
                if userWrap.userStatus == 3 { /// 被封号
                    //: self.func__showStatusBarErrorMsg(showMsg: "Account is restricted！".localized)
                    self.changeManage(showMsg: (String(main_minuteWoodPhoneMessage) + String(appConsistValue.prefix(4))).localized)
                    //: return
                    return
                }
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    let chatVC = self.bolt(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    //: completion?(chatVC)
                    completion?(chatVC)
                }
            }
            //: }else{
        } else {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: RelatedTransformable())
                let chatVC = self.bolt(chatID: chatID, isFrom: isFrom, userWrap: RelatedTransformable())
                //: completion?(chatVC)
                completion?(chatVC)
            }
        }
    }

    //: func func__canPushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: RelatedTransformable ) -> TalkingPrivateChatController {
    func bolt(chatID: String, isFrom: JournalSayFromEnum = .Normal, userWrap: RelatedTransformable) -> FamilyFleetViewController {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = funcProcess()
        //: if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
        if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index] .isKind(of: TalkingPrivateChatController.self) == true {
                if vcArr[index].isKind(of: FamilyFleetViewController.self) == true {
                    //: let vc = vcArr[index] as! TalkingPrivateChatController
                    let vc = vcArr[index] as! FamilyFleetViewController
                    //: if vc.targetId == chatID {
                    if vc.targetId == chatID {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return vc
                        return vc
                    }
                }
            }
        }

        //: let vc = TalkingPrivateChatController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        let vc = FamilyFleetViewController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        //: if isFrom == .LiveRoom {
        if isFrom == .LiveRoom {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
            //: } else {
        } else {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
        //: return vc
        return vc
    }

    // MARK: - 跳转群聊

    //: func func__pushToGroupChat(groupId: String) {
    func indigenousLanguage(groupId: String) {
        //: guard groupId.count > 0 else { return }
        guard groupId.count > 0 else { return }
        //: ProgressHUD.show()
        OrdinalProgressHUD.loo()
        //: LifeReactiveCompatible.req_checkChatRoom(roomId: groupId) { succeed, result, errorModel in
        LifeReactiveCompatible.superior(roomId: groupId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            OrdinalProgressHUD.vision()
            //: guard succeed == true else {
            guard succeed == true else {
                //: if errorModel?.errorCode == 106 {
                if errorModel?.errorCode == 106 {
                    //: } else {
                } else {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                    self.changeManage(showMsg: errorModel?.errorMsg ?? "")
                    //: if errorModel?.errorCode == 103 {
                    if errorModel?.errorCode == 103 { // 不在家族里
                    }
                }

                //: return
                return
            }

            //: guard let data = result as? [String: Any] else {
            guard let data = result as? [String: Any] else {
                //: return
                return
            }

            //: if ImmediatelyThen.isGroupChat(groupId) {
            if ImmediatelyThen.dark(groupId) {
                //: } else {
            } else { // 公共聊天室
                //: V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                    //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                    guard self.pureEdit(roomId: groupId) == false else { return }
                    //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                    let vc = JumpViewController(groupType: GroupType_Meeting, infoData: data)
                    //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                    self.funcProcess()?.navigationController?.pushViewController(vc, animated: true)

                    //: } fail: { code, desc in
                } fail: { code, _ in
                    //: if code == 10013 {
                    if code == 10013 { // 程序异常退出，未退出聊天室
                        //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                        guard self.pureEdit(roomId: groupId) == false else { return }
                        //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                        let vc = JumpViewController(groupType: GroupType_Meeting, infoData: data)
                        //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        self.funcProcess()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Failed to enter the chat room. Please try again later".localized)
                        self.changeManage(showMsg: String(bytes: mainCeaseName.map{spaceDraft(carrier: $0)}, encoding: .utf8)!.localized)
                    }
                }
            }
        }
    }

    /// 检测堆栈中是否有群聊控制器，有则跳转到群聊页
    /// - Parameter roomId: 房间Id
    /// - Returns: 检测结果（true: 有; false: 无）
    //: private func func__checkIsGroupChatVC(roomId: String) -> Bool {
    private func pureEdit(roomId: String) -> Bool {
        //: let currentVC = self.func__getCurrentActivityVC()
        let currentVC = self.funcProcess()
        //: if let vcArr = currentVC?.navigationController?.viewControllers {
        if let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index].isKind(of: TalkingGroupChatController.self) == true {
                if vcArr[index].isKind(of: JumpViewController.self) == true {
                    //: let vc = vcArr[index] as! TalkingGroupChatController
                    let vc = vcArr[index] as! JumpViewController
                    //: if vc.infoModel.roomId == roomId {
                    if vc.infoModel.roomId == roomId {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return true
                        return true
                    }
                }
            }
        }

        //: return false
        return false
    }

    // MARK: - 真人认证

    //: func func__pushUserVerifyController(toast: String?) {
    func utiliserBetweenHoweverPushEnableerFunc(toast _: String?) {
        //: let tpAuth = TPUserAuth(rawValue: AppManager.share.loginUserMode.isTPAuth)
        let tpAuth = FamilySendable(rawValue: AppManager.share.loginUserMode.isTPAuth)
        //: switch(tpAuth) {
        switch tpAuth {
        //: case .unknown, .isRefused:
        case .unknown, .isRefused:
            //: let vc = TalkingStatementEquityVC()
            let vc = AmongThen()
//            vc.toastStr = toast ?? ""
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = funcProcess()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: case .isOnGoing:
        case .isOnGoing:
            //: let vc = TalkingEditProfilesVC()
            let vc = DamnGuideAvatarDelegate()
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = funcProcess()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: default:
        default:
            //: break
            break
        }
    }

    // MARK: - 任意用户的动态列表

    //: func func__pushUserPostListWithUid(uid: String) {
    func gamut(uid _: String) {}

    // MARK: - 反馈feebook

    //: func func__pushFeedbackVC() {
    func funcDownNorth() {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = funcProcess()
        //: let vc = TalkingFeedbackVC.init()
        let vc = UnitComparableViewController()
        //: currentVC?.navigationController?.pushViewController(vc, animated: true)
        currentVC?.navigationController?.pushViewController(vc, animated: true)
    }

    // MARK: - 消息跳转

    //: func func__actionPushForMessage(jumpModel: TalkingMessageJumpModel) {
    func largeness(jumpModel: FamilyTransformable) {
        //: if jumpModel.jumpKey == "url" {
        if jumpModel.jumpKey == (String(mainDoingicialMessage)) {
            //: func__pushToWebVC(urlStr: jumpModel.url)
            pokeAt(urlStr: jumpModel.url)
            //: } else if jumpModel.jumpKey == "userDetail"{
        } else if jumpModel.jumpKey == (String(mDetailCountInfrastructureId.suffix(5)) + m_hairFormat.replacingOccurrences(of: "put", with: "t")) {}
    }

    // MARK: - 会员订阅网页

    //: @objc public func func__pushToSubscribePageWebVC() {
    @objc public func tee() {
        //: PipSqueakReactiveCompatible.share.func__pushToWebVC(webViewType: .SubscribePage)
        PipSqueakReactiveCompatible.share.circleResolution(webViewType: .SubscribePage)
    }

    // MARK: - 设置密码页面

    /// 跳转设置密码页面
    /// - Parameters:
    ///   - type: 手机号/邮箱
    ///   - content: 展示内容
    ///   - isNewPwd: 是否新设置密码（修改需要验证码）
    //: func func__pushToSetNewPasswordVC(type: LoginType, content: String, isNewPwd: Bool) {
    func setTag(type: BridgeTitleLiteral, content: String, isNewPwd: Bool) {
        //: let vc = TalkingSetNewPasswordVC()
        let vc = ExperienceRecognizerDelegate()
        //: vc.type = type
        vc.type = type
        //: vc.content = content
        vc.content = content
        //: vc.isNewPwd = isNewPwd
        vc.isNewPwd = isNewPwd
        //: func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
        funcProcess()?.navigationController?.pushViewController(vc, animated: true)
    }
}

// MARK: - 跳转H5

//: extension PipSqueakReactiveCompatible {
extension PipSqueakReactiveCompatible {
    // 跳转全屏H5入口
    //: func func__pushToWebVC(webViewType: WebUrlType) {
    func circleResolution(webViewType: CharacterSubqueryConvertible) {
        //: func__pushToWebVC(webViewType: webViewType, webConfig: TalkingWebConfig())
        parentClear(webViewType: webViewType, webConfig: OthersConfig())
    }

    /// 余额不足跳转充值/订阅页
    /// - Parameters:
    ///   - clickEvent: 埋点事件
    ///   - sufficient: 余额是否充足（默认true）
    //: func func__jumpToWebRecharge(clickEvent: String = "", sufficient: Bool = true, appendParams: String = "&type=7") {
    func halfHardware(clickEvent: String = "", sufficient: Bool = true, appendParams: String = (String(userLifeFormat.suffix(7)))) {
        //: let payWinType = AppManager.share.appUserConfigMode.payWinType
        let payWinType = AppManager.share.appUserConfigMode.payWinType
        // 样式：半屏充值页 || (已订阅 && 余额不足)
        //: if payWinType == 1 || (AppManager.share.loginUserMode.loungePlus && sufficient == false) {
        if payWinType == 1 || (AppManager.share.loginUserMode.loungePlus && sufficient == false) { // 半屏充值页
            //: func__pushToHalfWebVC(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
            damnBy(webViewType: .RechargeHalfPage, clickEvent: clickEvent)

            //: } else if payWinType == 2 {
        } else if payWinType == 2 { // 会员订阅弹窗
            //: func__pushToSubscribeAlert(appendParams: appendParams)
            pushBack(appendParams: appendParams)
        }
    }

    /// 跳转会员订阅弹窗
    /// - Parameter appendParams: url拼接参数（假视频流程拼接："&type=6；默认流程："&type=7"）
    //: func func__pushToSubscribeAlert(appendParams: String = "&type=7") {
    func pushBack(appendParams: String = (String(userLifeFormat.suffix(7)))) {
        //: var urlStr = TalkingWebManager.getFullUrl(urlType: .SubscribeAlert)
        var urlStr = AlongsideManager.mediumBoard(urlType: .SubscribeAlert)
        //: urlStr.append(appendParams)
        urlStr.append(appendParams)
        //: PipSqueakReactiveCompatible.share.func__pushToWebVC(urlStr: urlStr)
        PipSqueakReactiveCompatible.share.pokeAt(urlStr: urlStr)
        // 曝光用户数埋点
        //: uploadRecord.uploadRecordEvent(eventID: subscribtionRecordButton, parameterStr: ["amount": 0])
        noti_thirdUrl.libraryDataWeight(eventID: noti_lineSampleStr, parameterStr: [String(bytes: notiLoopName.map{moreNews(lost: $0)}, encoding: .utf8)!: 0])
    }

    /// 跳转半屏H5入口
    /// - Parameters:
    ///   - webViewType: 网页URL
    ///   - clickEvent: 埋点事件
    //: func func__pushToHalfWebVC(webViewType: WebUrlType, clickEvent: String = "") {
    func damnBy(webViewType: CharacterSubqueryConvertible, clickEvent: String = "") {
        //: var config = TalkingWebConfig.init()
        var config = OthersConfig()
        //: config.showLoadingView = false
        config.showLoadingView = false
        //: config.isHalfView = true
        config.isHalfView = true
        //: func__pushToWebVC(webViewType: webViewType, webConfig: config)
        parentClear(webViewType: webViewType, webConfig: config)
        // 充值埋点
        //: if webViewType == .RechargeHalfPage {
        if webViewType == .RechargeHalfPage {
            //: RECHARGE_CLICK_EVENT = clickEvent
            kOkName = clickEvent
            //: if clickEvent.count > 0 {
            if clickEvent.count > 0 {
                //: uploadRecord.uploadRecordEvent(eventID: clickEvent, parameterStr: ["amount": 0])
                noti_thirdUrl.libraryDataWeight(eventID: clickEvent, parameterStr: [String(bytes: notiLoopName.map{moreNews(lost: $0)}, encoding: .utf8)!: 0])
            }
        }
    }

    //: func func__pushToWebVC(webViewType: WebUrlType, webConfig: TalkingWebConfig) {
    func parentClear(webViewType: CharacterSubqueryConvertible, webConfig: OthersConfig) {
        //: let urlStr = TalkingWebManager.getFullUrl(urlType: webViewType)
        let urlStr = AlongsideManager.mediumBoard(urlType: webViewType)
        //: func__pushToWebView(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
        duringBuild(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
    }

    //: func func__pushToWebVC(urlStr: String?, webConfig: TalkingWebConfig? = nil) {
    func pokeAt(urlStr: String?, webConfig: OthersConfig? = nil) {
        //: let config = (webConfig == nil) ? TalkingWebConfig():webConfig
        let config = (webConfig == nil) ? OthersConfig() : webConfig
        //: func__pushToWebView(urlStr: urlStr, webViewType: nil, webConfig: config!)
        duringBuild(urlStr: urlStr, webViewType: nil, webConfig: config!)
    }

    //: func func__pushToWebView(urlStr: String?, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    func duringBuild(urlStr: String?, webViewType: CharacterSubqueryConvertible?, webConfig: OthersConfig) {
        //: guard urlStr != nil && urlStr != "" else { return }
        guard urlStr != nil, urlStr != "" else { return }
        //: var config = webConfig
        var config = webConfig

        // 有值且大于0，半屏按照比例展示
        //: if config.widthHeight ?? 0 > 0 {
        if config.widthHeight ?? 0 > 0 {
            //: config.isHalfView = true
            config.isHalfView = true
        }
        // 解析url中带的参数
        //: let urlParams = JSON(urlStr!.urlParameters ?? [:])
        let urlParams = JSON(urlStr!.urlParameters ?? [:])
        //: if urlParams["transparency"].stringValue == "1" { // 透明背景
        if urlParams[(String(kBoldDiskUrl) + appRemoteRoleMsg.replacingOccurrences(of: "smooth", with: "nc"))].stringValue == "1" { // 透明背景
            //: config.clearBgColor = true
            config.clearBgColor = true
        }

        // url拼接参数
        //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "`%^{}\"[]|\\<>").inverted
        let allowCharSet = CharacterSet(charactersIn: "`%^{}" + "\"" + "[]|\\<>").inverted
        //: var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        //: let otherParams = "version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)"
        let otherParams = (dreamScienceName.replacingOccurrences(of: "mode", with: "e") + String(mSoldTitle)) + "\(userReplaceContent)" + (String(userCapPath) + String(showSkinUnlessData.suffix(5))) + "\(kLimitTitle)" + (String(mainPublishTitle.suffix(7)) + String(notiParentUrl.prefix(3))) + "\(k_clickMessage)"
        //: if urlStr.contains("?") {
        if urlStr.contains("?") {
            //: urlStr = "\(urlStr)&\(otherParams)"
            urlStr = "\(urlStr)&\(otherParams)"
            //: } else {
        } else {
            //: urlStr = "\(urlStr)?\(otherParams)"
            urlStr = "\(urlStr)?\(otherParams)"
        }

        //: let vc = TalkingWebViewController.init(urlString: urlStr, webViewType: webViewType, webConfig: config)
        let vc = SmallMessageHandler(urlString: urlStr, webViewType: webViewType, webConfig: config)

        // 展示网页
        //: if let currentVC = func__getCurrentActivityVC() {
        if let currentVC = funcProcess() {
            // 如果当前视图是模态上来的，跳转到下级时只能使用模态效果跳转
            //: var currentVCIsPresent = false
            var currentVCIsPresent = false
            //: if currentVC.isKind(of: TalkingWebViewController.self) &&
            if currentVC.isKind(of: SmallMessageHandler.self) &&
                //: (currentVC as! TalkingWebViewController).isModal == true {
                (currentVC as! SmallMessageHandler).isModal == true
            {
                //: currentVCIsPresent = true
                currentVCIsPresent = true
            }

            //: if config.isHalfView {
            if config.isHalfView { // 半屏使用present
                //: if config.widthHeight == nil {
                if config.widthHeight == nil {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight*2/3)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: kGiftIdent, height: user_allMessage * 2 / 3)
                    //: vc.tranConfig.configCorner(corners: [.topLeft, .topRight], radius: 18)
                    vc.tranConfig.revenueRender(corners: [.topLeft, .topRight], radius: 18)
                    //: } else {
                } else {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenWidth/config.widthHeight!)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: kGiftIdent, height: kGiftIdent / config.widthHeight!)
                }
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else if config.clearBgColor || currentVCIsPresent {
            } else if config.clearBgColor || currentVCIsPresent { // 透明背景只能用present
                //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
                vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: kGiftIdent, height: user_allMessage)
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else {
            } else {
                //: vc.isModal = false
                vc.isModal = false
                //: currentVC.navigationController?.pushViewController(vc, animated: true)
                currentVC.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }
}

// MARK: - 获取当前视图、导航

//: extension PipSqueakReactiveCompatible {
public extension PipSqueakReactiveCompatible {
    //: @objc public func func__getCurrentActivityVC() -> UIViewController? {
    @objc func funcProcess() -> UIViewController? {
        //: return currentViewController()
        return decideBroad()
    }
}

//: struct TalkingMessageJumpModel: HandyJSON {
struct FamilyTransformable: HandyJSON {
    //: var jumpKey: String?
    var jumpKey: String? // 评论的id
    //: var jumpUid: String?
    var jumpUid: String?
    //: var url: String?
    var url: String?
}
