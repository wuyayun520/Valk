
//: Declare String Begin

/*: "user/updateInfo" :*/
fileprivate let user_closeText:String = "soonser"
fileprivate let k_exampleStr:String = "eInfoclear cry called remaining"

/*: "gallery/getGalleryByUid" :*/
fileprivate let notiDisablePath:[Character] = ["g","a","l","l","e","r","y","/","g","e","t"]
fileprivate let dreamLooseName:[Character] = ["G","a","l","l","e","r","y","B","y","U","i","d"]

/*: "gallery/upload" :*/
fileprivate let notiFragmentPath:String = "modifyall"
fileprivate let dream_husbandText:String = "version"

/*: "gallery/deletePic" :*/
fileprivate let app_designTitle:[Character] = ["g","a","l","l","e","r","y","/","d"]
fileprivate let mainWhichIdent:String = "ecorner"
fileprivate let mPocketContent:String = "etePictemp write"

/*: "user/tagDelete" :*/
fileprivate let show_characterTitle:[Character] = ["u","s","e","r","/","t","a","g","D","e","l","e","t","e"]

/*: "mf/user/tagEdit" :*/
fileprivate let dreamImmuneUnknownNorthText:[Character] = ["m","f","/","u","s","e","r","/"]
fileprivate let dreamTickLipPath:String = "hi mind your timingtagEdit"

/*: "app/feedback" :*/
fileprivate let app_labIdent:[Character] = ["a","p","p","/","f","e","e"]
fileprivate let k_tellTranslateIdent:[Character] = ["d","b","a","c","k"]

/*: "IM/getMyBlackList" :*/
fileprivate let kRefrigeratorData:[Character] = ["I","M","/","g","e","t","M","y","B","l","a","c","k","L","i","s"]
fileprivate let main_tingUrl:[Character] = ["t"]

/*: "mf/user/getSetting" :*/
fileprivate let k_tickTerraEasternStr:[Character] = ["m","f","/","u","s","e","r","/"]
fileprivate let kLocationId:[Character] = ["g","e","t","S","e","t","t","i","n","g"]

/*: "mf/user/setConfig" :*/
fileprivate let user_mindDefensePath:String = "mf/usehowever aver pro import"
fileprivate let appLiteralName:String = "omessagefi"
fileprivate let kDramaticallyIdent:String = "very"

/*: "mf/user/settingStatus" :*/
fileprivate let k_attitudeName:String = "hike reward enhance downmf/us"
fileprivate let main_waterMessage:String = "tinvisual"
fileprivate let m_situationText:String = "dramatically"
fileprivate let appChemistryKey:[Character] = ["t","a","t","u","s"]

/*: "type" :*/
fileprivate let notiImageDiscloseMessage:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "user/lockAccount" :*/
fileprivate let mTaskContent:[Character] = ["u","s","e","r","/"]
fileprivate let show_scheduleFormat:[Character] = ["l","o","c","k","A","c"]
fileprivate let notiCreateName:String = "counhung"

/*: "mf/user/editInfo" :*/
fileprivate let appMpMsg:String = "mf/usecurity move false"
fileprivate let kTotFormat:String = "border professional source firsttInfo"

/*: "mf/userSign/list" :*/
fileprivate let showConsentMessage:[Character] = ["m","f","/","u","s","e","r","S","i","g"]
fileprivate let appRemoteValue:[Character] = ["n","/","l","i","s","t"]

/*: "mf/userSign/signIn" :*/
fileprivate let user_transformId:String = "soon seatmf/us"
fileprivate let kFinishMsg:String = "gn/signfactor spend situation reduce fee"
fileprivate let dreamDisplayValue:[Character] = ["I","n"]

/*: "mf/dating/saveVideo" :*/
fileprivate let m_advantageSupplyId:String = "mf/dsense float yourself"
fileprivate let showReduceData:String = "offer coordinate hesitate pick sameg/sa"

/*: "mf/dating/getConfig" :*/
fileprivate let noti_hideSecureDestroyData:String = "mf/dlibrary welcome word"
fileprivate let mainBindId:String = "separate"
fileprivate let mainTransmissionData:[Character] = ["e","t","C","o","n","f","i","g"]

/*: "mf/dating/deleteVideo" :*/
fileprivate let k_whiteMessage:[Character] = ["m","f","/","d","a","t","i"]
fileprivate let showEnhanceStr:[Character] = ["n","g","/","d","e","l","e","t","e"]
fileprivate let dreamDataFormat:String = "race abs space shake privacyVideo"

/*: "mf/greet/updateAudioRemark" :*/
fileprivate let show_toleranceId:String = "than country connection patientmf/gr"
fileprivate let showBrandPath:String = "uingredienta"
fileprivate let kAdditionalName:String = "ioRemartolerance profess pop safe collect"
fileprivate let m_tripStr:String = "cruise"

/*: "mf/greet/list" :*/
fileprivate let show_promptUrl:[Character] = ["m","f","/","g","r","e","e","t"]
fileprivate let app_absenceValue:[Character] = ["/","l","i","s","t"]

/*: "mf/greet/add" :*/
fileprivate let kYearValue:String = "display draw owner hugemf/g"
fileprivate let mainHeIdent:String = "vessel"

/*: "mf/greet/del" :*/
fileprivate let dreamRoofKey:String = "mf/grekeep we processor sample whole"
fileprivate let user_boldPath:String = "et/delcommon select security welcome inherit"

/*: "mf/greet/getSettings" :*/
fileprivate let appMNoseTitle:[Character] = ["m","f","/","g","r","e","e","t","/","g","e"]
fileprivate let app_laboratoryValue:String = "help tensiontSettings"

/*: "mf/greet/extraSetting" :*/
fileprivate let appHurryData:[Character] = ["m","f","/","g","r","e","e","t","/"]
fileprivate let dreamQuantityIdent:[Character] = ["e","x","t","r","a","S","e","t","t","i","n","g"]

/*: "game/list" :*/
fileprivate let dream_siblingRocketWeMessage:[Character] = ["g","a"]
fileprivate let show_speculateMsg:[Character] = ["m","e","/","l","i","s","t"]

/*: "category" :*/
fileprivate let mSupportiveIdent:[UInt8] = [0xfb,0xf9,0xec,0xfd,0xff,0xf7,0xea,0xe1]

private func promptDesign(permission num: UInt8) -> UInt8 {
    return num ^ 152
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CryRequestTool.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/9.
//

//: import UIKit
import UIKit

//: class TalkingMeRequestTool: NSObject {
class CryRequestTool: NSObject {
    /// 资料完善
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UpdateUserInfo(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func playBlack(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "user/updateInfo"
        reqModel.requestPath = (user_closeText.replacingOccurrences(of: "soon", with: "u") + "/updat" + String(k_exampleStr.prefix(5)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户相册
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GalleryByUid(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func audienceSectionTrap(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "gallery/getGalleryByUid"
        reqModel.requestPath = (String(notiDisablePath) + String(dreamLooseName))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传图片
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UploadPic(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func conversationCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "gallery/upload"
        reqModel.requestPath = (notiFragmentPath.replacingOccurrences(of: "modify", with: "g") + "ery/uplo" + dream_husbandText.replacingOccurrences(of: "version", with: "ad"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除图片
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeletePic(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func distance(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "gallery/deletePic"
        reqModel.requestPath = (String(app_designTitle) + mainWhichIdent.replacingOccurrences(of: "corner", with: "l") + String(mPocketContent.prefix(6)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除单个用户标签
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteTag(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func product(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "user/tagDelete"
        reqModel.requestPath = (String(show_characterTitle))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 编辑用户标签
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_EditTag(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func sceneBetweenFile(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/user/tagEdit"
        reqModel.requestPath = (String(dreamImmuneUnknownNorthText) + String(dreamTickLipPath.suffix(7)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 意见反馈
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_Feedback(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func popular(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "app/feedback"
        reqModel.requestPath = (String(app_labIdent) + String(k_tellTranslateIdent))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取黑名单列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_BlackList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func pin(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "IM/getMyBlackList"
        reqModel.requestPath = (String(kRefrigeratorData) + String(main_tingUrl))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取设置信息接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GetSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func copernicanSystem(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/user/getSetting"
        reqModel.requestPath = (String(k_tickTerraEasternStr) + String(kLocationId))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 个人中心设置
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SettingChange(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func compare(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/user/setConfig"
        reqModel.requestPath = (String(user_mindDefensePath.prefix(6)) + "r/setC" + appLiteralName.replacingOccurrences(of: "message", with: "n") + kDramaticallyIdent.replacingOccurrences(of: "very", with: "g"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 女性busy状态上报接口
    /// - Parameters:
    ///   - type: 0 :不活跃， 1:活跃
    ///   - completion: 回调
    //: class func req_settingStatus(type: Int, completion: @escaping FinishBlock) {
    class func originSetting(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/user/settingStatus"
        reqModel.requestPath = (String(k_attitudeName.suffix(5)) + "er/set" + main_waterMessage.replacingOccurrences(of: "visual", with: "g") + m_situationText.replacingOccurrences(of: "dramatically", with: "S") + String(appChemistryKey))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: notiImageDiscloseMessage.reversed(), encoding: .utf8)!: type]
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 注销账号
    /// - Parameters:
    /// - completion: 回调
    //: class func req_LockAccount(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func thumbnailOf(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "user/lockAccount"
        reqModel.requestPath = (String(mTaskContent) + String(show_scheduleFormat) + notiCreateName.replacingOccurrences(of: "hung", with: "t"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 个人中心修改私信价格、开启语音授权
    /// - Parameters:
    /// - completion: 回调
    //: class func req_EditInfo(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func infectiousAgentCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/user/editInfo"
        reqModel.requestPath = (String(appMpMsg.prefix(4)) + "ser/edi" + String(kTotFormat.suffix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取签到日历列表
    /// - Parameter completion: 回调
    //: class func req_getUserSignList(completion: @escaping FinishBlock) {
    class func predetermination(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/userSign/list"
        reqModel.requestPath = (String(showConsentMessage) + String(appRemoteValue))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户签到
    /// - Parameter completion: 回调
    //: class func req_userSignIn(completion: @escaping FinishBlock) {
    class func activityBig(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/userSign/signIn"
        reqModel.requestPath = (String(user_transformId.suffix(5)) + "erSi" + String(kFinishMsg.prefix(7)) + String(dreamDisplayValue))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户资料视频地址上传
    /// - Parameters:
    ///   - completion: 回调
    //: class func uploadVideoUserEdit(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func far(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/dating/saveVideo"
        reqModel.requestPath = (String(m_advantageSupplyId.prefix(4)) + "atin" + String(showReduceData.suffix(4)) + "veVideo")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前用户上传的视频
    /// - Parameters:
    ///   - completion: 回调
    //: class func getUploadVideoInfo(completion: @escaping FinishBlock) {
    class func fragment(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/dating/getConfig"
        reqModel.requestPath = (String(noti_hideSecureDestroyData.prefix(4)) + "ating/" + mainBindId.replacingOccurrences(of: "separate", with: "g") + String(mainTransmissionData))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除当前用户上传的视频
    /// - Parameters:
    ///   - completion: 回调
    //: class func deleteUserVideo(completion: @escaping FinishBlock) {
    class func cutCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/dating/deleteVideo"
        reqModel.requestPath = (String(k_whiteMessage) + String(showEnhanceStr) + String(dreamDataFormat.suffix(5)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 更新语音备注
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UpdateAudioRemark(completion: @escaping FinishBlock) {
    class func remarkZone(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/updateAudioRemark"
        reqModel.requestPath = (String(show_toleranceId.suffix(5)) + "eet/" + showBrandPath.replacingOccurrences(of: "ingredient", with: "pd") + "teAud" + String(kAdditionalName.prefix(7)) + m_tripStr.replacingOccurrences(of: "cruise", with: "k"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-查看设置列表
    /// - Parameters: type: 1=语音， 2=文本
    ///   - completion: 回调
    //: class func req_GetGreetList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func delay(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/list"
        reqModel.requestPath = (String(show_promptUrl) + String(app_absenceValue))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-添加自定义文案/语音/图片
    /// - Parameters: type: 1：语音；2：文案；3：图片; 4:私密照片
    ///   - completion: 回调
    //: class func req_UploadGreetAdd(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func menuOn(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/add"
        reqModel.requestPath = (String(kYearValue.suffix(4)) + "reet/ad" + mainHeIdent.replacingOccurrences(of: "vessel", with: "d"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-设置音频备注
    ///   - completion: 回调
    //: class func req_UpdateAudioRemark(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func transform(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/updateAudioRemark"
        reqModel.requestPath = (String(show_toleranceId.suffix(5)) + "eet/" + showBrandPath.replacingOccurrences(of: "ingredient", with: "pd") + "teAud" + String(kAdditionalName.prefix(7)) + m_tripStr.replacingOccurrences(of: "cruise", with: "k"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-删除文案/音频
    ///   - completion: 回调
    //: class func req_DeleteAudio(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func insertCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/del"
        reqModel.requestPath = (String(dreamRoofKey.prefix(6)) + String(user_boldPath.prefix(6)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 招呼设置-查看设置
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GetGreetSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func visualAspect(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/getSettings"
        reqModel.requestPath = (String(appMNoseTitle) + String(app_laboratoryValue.suffix(9)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 图片打招呼-额外配置礼物信息
    /// - Parameters: unlockGiftId:解锁礼物id, isBurn:设置的阅后即焚状态
    ///   - completion: 回调
    //: class func req_UploadGreetExtraSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func decision(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/extraSetting"
        reqModel.requestPath = (String(appHurryData) + String(dreamQuantityIdent))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取游戏列表
    /// - Parameters:
    ///   - category: 1=我的页面，2=私聊页面
    ///   - completion: 回调
    //: class func req_getGameList(category: Int, completion: @escaping FinishBlock) {
    class func suspend(category: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "game/list"
        reqModel.requestPath = (String(dream_siblingRocketWeMessage) + String(show_speculateMsg))
        //: reqModel.params = ["category": category]
        reqModel.params = [String(bytes: mSupportiveIdent.map{promptDesign(permission: $0)}, encoding: .utf8)!: category]
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
