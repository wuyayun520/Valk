
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let notiDevicePath:[UInt8] = [0x37,0x30,0x37,0x2a,0x76,0x3d,0x31,0x3a,0x3b,0x2c,0x64,0x77,0x7e,0x36,0x3f,0x2d,0x7e,0x30,0x31,0x2a,0x7e,0x3c,0x3b,0x3b,0x30,0x7e,0x37,0x33,0x2e,0x32,0x3b,0x33,0x3b,0x30,0x2a,0x3b,0x3a]

/*: "Cancel" :*/
fileprivate let dream_angleContent:String = "Cancelsir narrow yes mid person"

/*: "Submit" :*/
fileprivate let noti_givingVesselTitle:String = "Submitvariety library adjustment correct three"

/*: "Please tell us your problems                   " :*/
fileprivate let showMasterUrl:[UInt8] = [0x7a,0x46,0x4f,0x4b,0x59,0x4f,0xa,0x5e,0x4f,0x46,0x46,0xa,0x5f,0x59,0xa,0x53,0x45,0x5f,0x58,0xa,0x5a,0x58,0x45,0x48,0x46,0x4f,0x47,0x59,0xa,0xa,0xa,0xa,0xa,0xa,0xa,0xa,0xa,0xa,0xa,0xa,0xa,0xa,0xa,0xa,0xa,0xa,0xa]

/*: "You need to choose at least one." :*/
fileprivate let show_scienceDarkMsg:[UInt8] = [0xbb,0x8d,0x97,0xc2,0x8c,0x87,0x87,0x86,0xc2,0x96,0x8d,0xc2,0x81,0x8a,0x8d,0x8d,0x91,0x87,0xc2,0x83,0x96,0xc2,0x8e,0x87,0x83,0x91,0x96,0xc2,0x8d,0x8c,0x87,0xcc]

private func flushCute(holiday num: UInt8) -> UInt8 {
    return num ^ 226
}

/*: "Please tell us your problems." :*/
fileprivate let app_writtenReportMessage:String = "Pleadetail scene"
fileprivate let noti_manageName:String = "opening you progressive bulletl us y"
fileprivate let main_conversationMsg:String = "prodown"

/*: "app/userReport" :*/
fileprivate let mainLooseMessage:[Character] = ["a","p","p","/","u","s","e","r","R"]
fileprivate let showPlantTitle:[Character] = ["e","p","o","r","t"]

/*: "reason" :*/
fileprivate let app_edgeData:[Character] = ["r","e","a","s","o","n"]

/*: "desc" :*/
fileprivate let notiChildText:[UInt8] = [0x5,0x4,0x12,0x2]

/*: "mf/moment/report" :*/
fileprivate let app_terraMsg:[Character] = ["m","f","/","m","o","m","e","n","t"]
fileprivate let user_boldReferData:String = "running scale care plus/report"

/*: "momentId" :*/
fileprivate let dreamConversationMsg:[UInt8] = [0x78,0x7a,0x78,0x70,0x79,0x7f,0x54,0x6f]

fileprivate func atomicCount85(challenge num: UInt8) -> UInt8 {
    let value = Int(num) - 11
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/momentReply/report" :*/
fileprivate let user_layIdent:String = "mf/mocease it"
fileprivate let mainSequenceText:String = "ly/reportdue ingredient beam"

/*: "app/videoReport" :*/
fileprivate let kCollectTitle:String = "enough towards simultaneously ion situationapp/vi"
fileprivate let noti_ampIdent:String = "deoResolid expression supply orange"

/*: "logId" :*/
fileprivate let noti_voiceText:[UInt8] = [0x5c,0x5f,0x57,0x39,0x54]

fileprivate func axialSkeletonYes(have num: UInt8) -> UInt8 {
    let value = Int(num) - 240
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Submitted successfully" :*/
fileprivate let user_quoteFormat:[Character] = ["S","u","b","m","i","t","t","e","d"," ","s","u","c","c"]
fileprivate let kEquallyTitle:String = "ecityf"

/*: "Nudity" :*/
fileprivate let userAppName:String = "tar mNudity"

/*: "Violence" :*/
fileprivate let m_simultaneouslyMessage:[Character] = ["V","i","o","l","e","n"]
fileprivate let kStorageFormat:[Character] = ["c","e"]

/*: "Harassment" :*/
fileprivate let dreamFailActTitle:[Character] = ["H","a","r","a","s"]
fileprivate let m_eachId:String = "SMENT"

/*: "Suicide or Self-injury" :*/
fileprivate let k_dataHairPath:String = "Suiciowner separate protection style"
fileprivate let notiVeryTitle:[Character] = ["r"," ","S"]
fileprivate let appDivisionId:String = "elf-ime global bot ground resolution"

/*: "False Information" :*/
fileprivate let show_clickMessage:String = "Falseease hurry margin"
fileprivate let user_historyMessage:String = "tioprosecutor"

/*: "Spam" :*/
fileprivate let kToleranceTitle:[Character] = ["S","p","a","m"]

/*: "Hate Speech" :*/
fileprivate let kAllowUrl:String = "Hate Slay former local"
fileprivate let show_guidanceDecideText:String = "PEECH"

/*: "Terrorism" :*/
fileprivate let dream_cuteToId:[Character] = ["T","e","r"]
fileprivate let k_unableKey:String = "rorisoriginal"

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let dreamPublisherIdent:[UInt8] = [0x11,0x1f,0x2a,0x23,0xde,0x2d,0x24,0xde,0x7,0x2a,0x2a,0x23,0x25,0x1f,0x2a,0xde,0x2d,0x30,0xde,0x10,0x23,0x25,0x33,0x2a,0x1f,0x32,0x23,0x22,0xde,0x5,0x2d,0x2d,0x22,0x31]

fileprivate func honeyAgency(you num: UInt8) -> UInt8 {
    let value = Int(num) + 66
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Sending disgusting messages" :*/
fileprivate let notiResourceSirContent:[Character] = ["S","e","n","d","i","n","g"," ","d","i","s","g","u","s","t","i","n","g"," ","m","e","s"]
fileprivate let mMiniUrl:String = "charmges"

/*: "Using photos of nudity or drugs" :*/
fileprivate let dreamStreamMessage:[UInt8] = [0x73,0x67,0x75,0x72,0x64,0x20,0x72,0x6f,0x20,0x79,0x74,0x69,0x64,0x75,0x6e,0x20,0x66,0x6f,0x20,0x73,0x6f,0x74,0x6f,0x68,0x70,0x20,0x67,0x6e,0x69,0x73,0x55]

/*: "Harassing or sending ads" :*/
fileprivate let noti_partnerPath:String = "post soundHaras"
fileprivate let mWeeklyValue:String = "r sepush simultaneously"
fileprivate let mScanCalledTitle:String = "skull"

/*: "Defraud or using fake info" :*/
fileprivate let showPeelMakerUrl:[Character] = ["D","e","f","r","a","u","d"," ","o"]
fileprivate let main_originalData:String = "confirm note squarer usin"
fileprivate let main_minimizeKey:String = "component local clothes add ting fake"

/*: "Others" :*/
fileprivate let showGenNagStr:String = "Othersassistant enter sum moon barn"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TransportThen.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import Then
import Then
//: import UIKit
import UIKit

//: enum TalkingReportAlertType: CaseIterable {
enum LeadAstrayAlertType: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class TransportThen: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: LeadAstrayAlertType?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [BounceReportModel] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = BounceReportModel()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: LeadAstrayAlertType?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        ask()
        //: setupSubViewsConstraint()
        filter()
        //: setupReasons()
        motionAccess()
        //: bindInteraction()
        enableer()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: notiDevicePath.map{$0^94}, encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(dream_angleContent.prefix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.storageEngine(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.concern(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.storageEngine().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(fence), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(noti_givingVesselTitle.prefix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.concern(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.gravitySize(colors: UIColor.decelerate(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(pile), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: kGiftIdent - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.optimize(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.appValueDetailColor(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = OrbiterReactiveCompatible(placeholder: String(bytes: showMasterUrl.map{$0^42}, encoding: .utf8)!.localized, placeholderColor: UIColor.appLayer(), frame: CGRect(x: 10, y: 10, width: kGiftIdent - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .concern(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.telecom()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .concern(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.optimize(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(TerraceReactiveCompatible.self, forCellReuseIdentifier: TerraceReactiveCompatible.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension TransportThen {
    //: @objc func func__TapGestureRecognizer() {
    @objc func adminRecognizer() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func asRemove(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = ImmediatelyThen.getWindow()
            let window = ImmediatelyThen.wow()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func targetSucceed(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func fence() {
        //: self.dismissReportView(succeed: false)
        self.targetSucceed(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func pile() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.changeManage(showMsg: String(bytes: show_scienceDarkMsg.map{flushCute(holiday: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.changeManage(showMsg: (String(app_writtenReportMessage.prefix(4)) + "se tel" + String(noti_manageName.suffix(6)) + "our " + main_conversationMsg.replacingOccurrences(of: "down", with: "b") + "lems.").localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(mainLooseMessage) + String(showPlantTitle))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (String(app_edgeData)): selectedModel.type, String(bytes: notiChildText.map{$0^97}, encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(app_terraMsg) + String(user_boldReferData.suffix(7)))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: dreamConversationMsg.map{atomicCount85(challenge: $0)}, encoding: .utf8)!: tempId, (String(app_edgeData)): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(user_layIdent.prefix(5)) + "mentRep" + String(mainSequenceText.prefix(9)))
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (String(app_edgeData)): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(kCollectTitle.suffix(6)) + String(noti_ampIdent.prefix(5)) + "port")
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: noti_voiceText.map{axialSkeletonYes(have: $0)}, encoding: .utf8)!: tempId, (String(app_edgeData)): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        FurtheranceReactiveCompatible.basic(reqPath: path, reqParams: param) { succeed, _, _ in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.licenseFee(showMsg: (String(user_quoteFormat) + kEquallyTitle.replacingOccurrences(of: "city", with: "ss") + "ully").localized)
                //: self.dismissReportView(succeed: true)
                self.targetSucceed(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension TransportThen: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: TerraceReactiveCompatible.className()) as! TerraceReactiveCompatible
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.model(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension TransportThen {
    /// 添加视图
    //: private func setupSubviews() {
    private func ask() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func filter() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func motionAccess() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(userAppName.suffix(6))).localized,
                           //: "Violence".localized,
                           (String(m_simultaneouslyMessage) + String(kStorageFormat)).localized,
                           //: "Harassment".localized,
                           (String(dreamFailActTitle) + m_eachId.lowercased()).localized,
                           //: "Suicide or Self-injury".localized,
                           (String(k_dataHairPath.prefix(5)) + "de o" + String(notiVeryTitle) + String(appDivisionId.prefix(5)) + "njury").localized,
                           //: "False Information".localized,
                           (String(show_clickMessage.prefix(5)) + " Informa" + user_historyMessage.replacingOccurrences(of: "prosecutor", with: "n")).localized,
                           //: "Spam".localized,
                           (String(kToleranceTitle)).localized,
                           //: "Hate Speech".localized,
                           (String(kAllowUrl.prefix(6)) + show_guidanceDecideText.lowercased()).localized,
                           //: "Terrorism".localized,
                           (String(dream_cuteToId) + k_unableKey.replacingOccurrences(of: "original", with: "m")).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: dreamPublisherIdent.map{honeyAgency(you: $0)}, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(notiResourceSirContent) + mMiniUrl.replacingOccurrences(of: "charm", with: "sa")).localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: dreamStreamMessage.reversed(), encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(noti_partnerPath.suffix(5)) + "sing o" + String(mWeeklyValue.prefix(4)) + "nding a" + mScanCalledTitle.replacingOccurrences(of: "skull", with: "ds")).localized,
                           //: "Defraud or using fake info".localized,
                           (String(showPeelMakerUrl) + String(main_originalData.suffix(6)) + String(main_minimizeKey.suffix(6)) + " info").localized,
                           //: "Others".localized]
                           (String(showGenNagStr.prefix(6))).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = AppManager.share.appConfigMode.videoReport["reason"] {
            if let reason = AppManager.share.appConfigMode.videoReport[(String(app_edgeData))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = BounceReportModel()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func enableer() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
