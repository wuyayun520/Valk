
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let mEnableContent:[UInt8] = [0x57,0x50,0x57,0x4a,0x16,0x5d,0x51,0x5a,0x5b,0x4c,0x4,0x17,0x1e,0x56,0x5f,0x4d,0x1e,0x50,0x51,0x4a,0x1e,0x5c,0x5b,0x5b,0x50,0x1e,0x57,0x53,0x4e,0x52,0x5b,0x53,0x5b,0x50,0x4a,0x5b,0x5a]

private func mushroomScale(able num: UInt8) -> UInt8 {
    return num ^ 62
}

/*: "There's no posts yet." :*/
fileprivate let main_contrastStr:String = "Thermark girl single holder"
fileprivate let userMonitorName:String = "method restore button livery findno post"
fileprivate let userScaleTitle:String = "s yet.duty slip icon average"

/*: "uid" :*/
fileprivate let k_customerMessage:String = "destinationid"

/*: "page" :*/
fileprivate let noti_poseName:String = "pagtelevision"

/*: "list" :*/
fileprivate let appSmoothText:String = "liscalled"

/*: "nickname" :*/
fileprivate let dreamFragmentId:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "age" :*/
fileprivate let noti_faultStr:[Character] = ["a","g","e"]

/*: "sex" :*/
fileprivate let k_hangMessage:[Character] = ["s","e","x"]

/*: "isTPAuth" :*/
fileprivate let showTitleureKey:String = "isTPrhythm break"

/*: "headPic" :*/
fileprivate let notiImageTodayUrl:String = "retain floor compoundheadPic"

/*: "pinCount" :*/
fileprivate let app_makerHatSaveMsg:[Character] = ["p","i","n","C","o","u","n","t"]

/*: "model" :*/
fileprivate let show_dismissContent:[Character] = ["m","o","d","e","l"]

/*: "Unpin from profile" :*/
fileprivate let mStudyText:String = "extended plasticUnpin"
fileprivate let dream_hiddenValue:String = "m prorobot beam above explain"
fileprivate let userSucceedMessage:String = "alongsideile"

/*: "Delete Post" :*/
fileprivate let kProcedureMessage:String = "Delethe explain scream"
fileprivate let noti_supposedPolicePath:[Character] = ["e"," ","P","o","s","t"]

/*: "Pin to profile" :*/
fileprivate let app_aspectFormat:[Character] = ["P","i","n"," ","t","o"," "]
fileprivate let dreamIncidentStr:[Character] = ["p","r","o","f","i","l","e"]

/*: "momentId" :*/
fileprivate let mainPartName:[Character] = ["m","o","m","e","n","t","I","d"]

/*: "status" :*/
fileprivate let mConfirmMsg:[Character] = ["s","t","a","t","u","s"]

/*: "Your post has been pinned" :*/
fileprivate let userMainName:String = "Your pmoon net"
fileprivate let k_safetyScanUrl:[Character] = ["o"]
fileprivate let notiNearbyTitle:String = "and way coatst h"
fileprivate let kHerValue:String = "condition transactionen pinned"

/*: "Your post has been Unpinned" :*/
fileprivate let userInformSuiteFormat:[Character] = ["Y","o","u","r"," ","p","o","s","t"," ","h","a","s"," ","b","e","e","n"," ","U","n","p","i","n","n"]
fileprivate let show_ovalUrl:String = "yourd"

/*: "Posts" :*/
fileprivate let appWhiteIdent:[Character] = ["P","o","s","t","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CoatButtonViewController.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/22.
//

//: import UIKit
import UIKit

// MARK: - 属性声明 & 生命周期方法

//: class TalkingUserMomentVC: TalkingBaseViewController {
class CoatButtonViewController: FictionalCharacterRecognizerDelegate {
    //: var uid = ""
    var uid = ""
    //: var pageIndex = 0
    var pageIndex = 0
    //: var disposeBag = DisposeBag()
    var disposeBag = DisposeBag()
    //: var dataSourceArr: [TalkingMomentModel] = []
    var dataSourceArr: [ComprehensiveAlterTransformable] = []

    //: init(uid: String) {
    init(uid: String) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: self.uid = uid
        self.uid = uid
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: mEnableContent.map{mushroomScale(able: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: setupSubviews()
        statementActivity()
        //: setupSubViewsConstraint()
        library()
        //: bindInteraction()
        submitDetectApplication()
        //: reqData()
        big()
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: lazy var mainTableView: UITableView = {
    lazy var mainTableView: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kGiftIdent, height: user_allMessage), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: table.contentInsetAdjustmentBehavior = .never
            table.contentInsetAdjustmentBehavior = .never
            //: } else {
        } else {
            //: self.automaticallyAdjustsScrollViewInsets = false
            self.automaticallyAdjustsScrollViewInsets = false
        }
        //: table.showsVerticalScrollIndicator = true
        table.showsVerticalScrollIndicator = true
        //: table.estimatedRowHeight = 0
        table.estimatedRowHeight = 0
        //: table.estimatedSectionFooterHeight = 0
        table.estimatedSectionFooterHeight = 0
        //: table.estimatedSectionHeaderHeight = 0
        table.estimatedSectionHeaderHeight = 0
        //: table.separatorStyle = .none
        table.separatorStyle = .none
        //: table.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        table.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: var style = EmptyStyle()
        var style = PresenceEmptyStyle()
        //: style.TipsTitle = "There's no posts yet."
        style.TipsTitle = (String(main_contrastStr.prefix(4)) + "e\'s " + String(userMonitorName.suffix(7)) + String(userScaleTitle.prefix(6)))
        //: let view = EmptyView.init(frame: self.view.frame, style: style)
        let view = EmptyView(frame: self.view.frame, style: style)
        //: return view
        return view
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingUserMomentVC {
extension CoatButtonViewController {
    //: func reqData() {
    func big() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["uid"] = uid
        dict[(k_customerMessage.replacingOccurrences(of: "destination", with: "u"))] = uid
        //: dict["page"] = pageIndex
        dict[(noti_poseName.replacingOccurrences(of: "television", with: "e"))] = pageIndex
        //: TalkingMomentRequestTool.req_UserMomentList(params: dict) { succeed, result, errorModel in
        BubbleReactiveCompatible.close(params: dict) { succeed, result, errorModel in

            //: self.mainTableView.endRefresh()
            self.mainTableView.dart()
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: if self.pageIndex > 0 {
                    if self.pageIndex > 0 {
                        //: self.mainTableView.mj_footer?.endRefreshingWithNoMoreData()
                        self.mainTableView.mj_footer?.endRefreshingWithNoMoreData()
                    }
                    //: return
                    return
                }

                //: guard let array = dict["list"] as? Array<Any> else {
                guard let array = dict[(appSmoothText.replacingOccurrences(of: "called", with: "t"))] as? [Any] else {
                    //: self.mainTableView.mj_footer?.endRefreshingWithNoMoreData()
                    self.mainTableView.mj_footer?.endRefreshingWithNoMoreData()
                    //: return
                    return
                }
                //: var dataArr: [TalkingMomentModel] = []
                var dataArr: [ComprehensiveAlterTransformable] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if var model = JSONDeserializer<TalkingMomentModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if var model = JSONDeserializer<ComprehensiveAlterTransformable>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: model.nickname = dict["nickname"] as? String
                        model.nickname = dict[(String(dreamFragmentId))] as? String
                        //: model.age = dict["age"] as? Int
                        model.age = dict[(String(noti_faultStr))] as? Int
                        //: model.sex = dict["sex"] as? String
                        model.sex = dict[(String(k_hangMessage))] as? String
                        //: model.uid = dict["uid"] as? String
                        model.uid = dict[(k_customerMessage.replacingOccurrences(of: "destination", with: "u"))] as? String
                        //: model.isTPAuth = dict["isTPAuth"] as? Bool ?? false
                        model.isTPAuth = dict[(String(showTitleureKey.prefix(4)) + "Auth")] as? Bool ?? false
                        //: model.headPic = dict["headPic"] as? String
                        model.headPic = dict[(String(notiImageTodayUrl.suffix(7)))] as? String
                        //: model.pinCount = dict["pinCount"] as! Int
                        model.pinCount = dict[(String(app_makerHatSaveMsg))] as! Int
                        //: model.caculateItemHeight()
                        model.perPost()
                        //: if model.uid == AppManager.share.loginUserMode.userID {
                        if model.uid == AppManager.share.loginUserMode.userID {
                            //: model.isUserDetail = true
                            model.isUserDetail = true
                        }
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }

                //: if dataArr.count < 20 {
                if dataArr.count < 20 { // 不足一页，隐藏更多
                    //: self.mainTableView.mj_footer?.endRefreshingWithNoMoreData()
                    self.mainTableView.mj_footer?.endRefreshingWithNoMoreData()
                }

                //: if self.pageIndex == 0 {
                if self.pageIndex == 0 {
                    //: self.dataSourceArr = dataArr
                    self.dataSourceArr = dataArr
                    //: if dataArr.count>0 {
                    if dataArr.count > 0 {
                        //: self.emptyView.removeFromSuperview()
                        self.emptyView.removeFromSuperview()
                    }
                    //: } else {
                } else {
                    //: self.dataSourceArr.append(contentsOf: dataArr)
                    self.dataSourceArr.append(contentsOf: dataArr)
                }

                //: self.mainTableView.reloadData()
                self.mainTableView.reloadData()
                //: } else {
            } else {
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg )
                self.changeManage(showMsg: errorModel!.errorMsg)
            }
        }
    }
}

// MARK: - 事件方法

//: extension TalkingUserMomentVC {
extension CoatButtonViewController {
    //: func headerRefresh() {
    func menu() {
        //: pageIndex = 0
        pageIndex = 0
        //: mainTableView.mj_header?.beginRefreshing()
        mainTableView.mj_header?.beginRefreshing()
        //: reqData()
        big()
    }

    //: func footerRefresh() {
    func name() {
        //: pageIndex += 1
        pageIndex += 1
        //: reqData()
        big()
    }

    //: @objc func updateLikeNumber(notification: NSNotification) -> Void {
    @objc func successfullyView(notification: NSNotification) {
        //: let userinfo = notification.userInfo as![String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: var model: TalkingMomentModel = userinfo["model"] as! TalkingMomentModel
        var model: ComprehensiveAlterTransformable = userinfo[(String(show_dismissContent))] as! ComprehensiveAlterTransformable
        //: if model.uid == AppManager.share.loginUserMode.userID {
        if model.uid == AppManager.share.loginUserMode.userID {
            //: model.isUserDetail = true
            model.isUserDetail = true
        }
        //: for i in 0..<self.dataSourceArr.count {
        for i in 0 ..< self.dataSourceArr.count {
            //: let tmodel = self.dataSourceArr[i]
            let tmodel = self.dataSourceArr[i]
            //: if model.mid == tmodel.mid {
            if model.mid == tmodel.mid {
                //: self.dataSourceArr[i] = model
                self.dataSourceArr[i] = model
                //: self.mainTableView.reloadRows(at: [IndexPath.init(row: i, section: 0)], with: .none)
                self.mainTableView.reloadRows(at: [IndexPath(row: i, section: 0)], with: .none)
                //: return
                return
            }
        }
    }

    //: func deleteCell(model: TalkingMomentModel, index: Int) {
    func warningIndex(model: ComprehensiveAlterTransformable, index: Int) {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = MunuView(frame: self.view.frame)

        //: if model.pinCount<3 {
        if model.pinCount < 3 {
            //: if model.pinStatus {
            if model.pinStatus {
                //: vc.initwithList(cellTitleList: ["Unpin from profile".localized, "Delete Post".localized])
                vc.tricolour(cellTitleList: [(String(mStudyText.suffix(5)) + " fro" + String(dream_hiddenValue.prefix(5)) + userSucceedMessage.replacingOccurrences(of: "alongside", with: "f")).localized, (String(kProcedureMessage.prefix(5)) + String(noti_supposedPolicePath)).localized])
                //: } else {
            } else {
                //: vc.initwithList(cellTitleList: ["Pin to profile".localized, "Delete Post".localized])
                vc.tricolour(cellTitleList: [(String(app_aspectFormat) + String(dreamIncidentStr)).localized, (String(kProcedureMessage.prefix(5)) + String(noti_supposedPolicePath)).localized])
            }
            //: } else {
        } else {
            //: if model.pinStatus {
            if model.pinStatus {
                //: vc.initwithList(cellTitleList: ["Unpin from profile".localized, "Delete Post".localized])
                vc.tricolour(cellTitleList: [(String(mStudyText.suffix(5)) + " fro" + String(dream_hiddenValue.prefix(5)) + userSucceedMessage.replacingOccurrences(of: "alongside", with: "f")).localized, (String(kProcedureMessage.prefix(5)) + String(noti_supposedPolicePath)).localized])
                //: } else {
            } else {
                //: vc.initwithList(cellTitleList: ["Delete Post".localized])
                vc.tricolour(cellTitleList: [(String(kProcedureMessage.prefix(5)) + String(noti_supposedPolicePath)).localized])
            }
        }

        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, str in
            //: guard let self = self else {return}
            guard let self = self else { return }

            //: if str == "Unpin from profile".localized {
            if str == (String(mStudyText.suffix(5)) + " fro" + String(dream_hiddenValue.prefix(5)) + userSucceedMessage.replacingOccurrences(of: "alongside", with: "f")).localized {
                //: self.topMoment(isTop: 0, model: model)
                self.crown(isTop: 0, model: model)
                //: } else if str == "Pin to profile".localized {
            } else if str == (String(app_aspectFormat) + String(dreamIncidentStr)).localized {
                //: self.topMoment(isTop: 1, model: model)
                self.crown(isTop: 1, model: model)
                //: } else if str == "Delete Post".localized {
            } else if str == (String(kProcedureMessage.prefix(5)) + String(noti_supposedPolicePath)).localized {
                //: ProgressHUD.show()
                OrdinalProgressHUD.loo()
                //: var dict = Dictionary<String, Any>()
                var dict = [String: Any]()
                //: dict["id"] = model.mid
                dict["id"] = model.mid
                //: TalkingMomentRequestTool.req_DeleteMoment(params: dict) { succeed, result, errorModel in
                BubbleReactiveCompatible.own(params: dict) { succeed, _, errorModel in
                    //: ProgressHUD.dismiss()
                    OrdinalProgressHUD.vision()
                    //: if succeed {
                    if succeed {
                        //: if index<self.dataSourceArr.count {
                        if index < self.dataSourceArr.count {
                            //: self.disposeBag = DisposeBag()
                            self.disposeBag = DisposeBag()
                            //: self.mainTableView.mj_header?.beginRefreshing()
                            self.mainTableView.mj_header?.beginRefreshing()
                            //: } else if self.dataSourceArr.count == 1 {
                        } else if self.dataSourceArr.count == 1 {
                            //: self.dataSourceArr.removeAll()
                            self.dataSourceArr.removeAll()
                            //: self.disposeBag = DisposeBag()
                            self.disposeBag = DisposeBag()
                            //: self.view.addSubview(self.emptyView)
                            self.view.addSubview(self.emptyView)
                        }
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                        self.changeManage(showMsg: errorModel!.errorMsg)
                    }
                }
            }
        }
    }

    //: func topMoment(isTop: Int, model: TalkingMomentModel) {
    func crown(isTop: Int, model: ComprehensiveAlterTransformable) {
        //: ProgressHUD.show()
        OrdinalProgressHUD.loo()
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["momentId"] = model.mid
        dict[(String(mainPartName))] = model.mid
        //: dict["status"] = isTop
        dict[(String(mConfirmMsg))] = isTop
        //: TalkingMomentRequestTool.req_TopMoment(params: dict) { succeed, result, errorModel in
        BubbleReactiveCompatible.destination(params: dict) { succeed, _, errorModel in
            //: ProgressHUD.dismiss()
            OrdinalProgressHUD.vision()
            //: if succeed {
            if succeed {
                //: let str = isTop==1 ? "Your post has been pinned".localized : "Your post has been Unpinned".localized
                let str = isTop == 1 ? (String(userMainName.prefix(6)) + String(k_safetyScanUrl) + String(notiNearbyTitle.suffix(4)) + "as be" + String(kHerValue.suffix(9))).localized : (String(userInformSuiteFormat) + show_ovalUrl.replacingOccurrences(of: "your", with: "e")).localized
                //: self.func__showStatusBarSuccessMsg(showMsg: str)
                self.licenseFee(showMsg: str)
                //: self.disposeBag = DisposeBag()
                self.disposeBag = DisposeBag()
                //: self.mainTableView.mj_header?.beginRefreshing()
                self.mainTableView.mj_header?.beginRefreshing()
                //: } else {
            } else {
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                self.changeManage(showMsg: errorModel!.errorMsg)
            }
        }
    }
}

// MARK: - UITableViewDelegate,UITableViewdataSourceArr

//: extension TalkingUserMomentVC: UITableViewDelegate, UITableViewDataSource {
extension CoatButtonViewController: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return self.dataSourceArr.count
        return self.dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: let model = self.dataSourceArr[indexPath.row]
        let model = self.dataSourceArr[indexPath.row]
        //: return model.itemHeight ?? 56
        return model.itemHeight ?? 56
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = TalkingMomentItemCell(style: .default, reuseIdentifier: TalkingMomentItemCell.className(), isMyHost: true)
        let cell = EditViewCell(style: .default, reuseIdentifier: EditViewCell.className(), isMyHost: true)
        //: let model: TalkingMomentModel = self.dataSourceArr[indexPath.row]
        let model: ComprehensiveAlterTransformable = self.dataSourceArr[indexPath.row]
        //: cell.configCell(model: model)
        cell.hebdomadModel(model: model)
        //: cell.userInfoView.deleteBtn.rx.tap.subscribe(onNext: { [weak self] in
        cell.userInfoView.deleteBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.deleteCell(model: model, index: indexPath.row)
            self.warningIndex(model: model, index: indexPath.row)
            //: }).disposed(by: cell.cellDisposeBag )
        }).disposed(by: cell.cellDisposeBag)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, willDisplay cell: UITableViewCell, forRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, willDisplay _: UITableViewCell, forRowAt _: IndexPath) {}

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: let model = self.dataSourceArr[indexPath.row]
        let model = self.dataSourceArr[indexPath.row]
        //: let vc =  TalkingMomentDetailVC.init(model: model)
        let vc = SwiftDetailVc(model: model)
        //: vc.delegete = { [weak self] in
        vc.delegete = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if indexPath.row<self.dataSourceArr.count {
            if indexPath.row < self.dataSourceArr.count {
                //: self.dataSourceArr.remove(at: indexPath.row)
                self.dataSourceArr.remove(at: indexPath.row)
                //: self.mainTableView.reloadData()
                self.mainTableView.reloadData()
                //: self.disposeBag = DisposeBag()
                self.disposeBag = DisposeBag()
            }
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }
}

// MARK: - Layout

//: extension TalkingUserMomentVC {
extension CoatButtonViewController {
    // 添加视图
    //: private func setupSubviews() {
    private func statementActivity() {
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.colouringDramatically()
        //: self.title = "Posts".localized
        self.title = (String(appWhiteIdent)).localized
        //: self.view.addSubview(emptyView)
        self.view.addSubview(emptyView)

        //: self.view.addSubview(mainTableView)
        self.view.addSubview(mainTableView)
        //: mainTableView.register(TalkingMomentItemCell.self, forCellReuseIdentifier: TalkingMomentItemCell.className())
        mainTableView.register(EditViewCell.self, forCellReuseIdentifier: EditViewCell.className())
        //: mainTableView.dataSource = self
        mainTableView.dataSource = self
        //: mainTableView.delegate = self
        mainTableView.delegate = self
        //: self.view.bringSubviewToFront(emptyView)
        self.view.bringSubviewToFront(emptyView)
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func library() {
        //: mainTableView.snp.makeConstraints { make in
        mainTableView.snp.makeConstraints { make in
            //: make.top.equalTo(self.view).offset(12)
            make.top.equalTo(self.view).offset(12)
            //: make.bottom.equalTo(self.view.snp.bottom).offset(-12)
            make.bottom.equalTo(self.view.snp.bottom).offset(-12)
            //: make.leading.trailing.equalTo(self.view)
            make.leading.trailing.equalTo(self.view)
        }
    }

    // 添加事件
    //: private func bindInteraction() {
    private func submitDetectApplication() {
        //: mainTableView.addHeaderRefresh { [weak self] in
        mainTableView.complection { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.headerRefresh()
            self.menu()
        }
        //: mainTableView.addFooterRefresh { [weak self] in
        mainTableView.hesitateComplection { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.footerRefresh()
            self.name()
        }

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.mainTableView.mj_header?.beginRefreshing()
            self.mainTableView.mj_header?.beginRefreshing()
        }
        //: NotificationCenter.default.addObserver(self, selector: #selector(updateLikeNumber(notification:)), name: UPDATE_LIKE_NUMBER_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(successfullyView(notification:)), name: user_closeNearbyPopFormat, object: nil)
    }
}
