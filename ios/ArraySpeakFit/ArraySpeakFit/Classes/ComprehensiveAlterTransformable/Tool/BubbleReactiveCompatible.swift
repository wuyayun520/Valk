
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let notiBlankStr:String = "mf/momenbrow club app scientific pi"
fileprivate let noti_tailId:String = "t/listprotection eff"

/*: "mf/moment/moments" :*/
fileprivate let dreamSoftlyBuildValue:String = "mf/moloop enough demonstrate recording"
fileprivate let showPetData:[Character] = ["/","m","o"]
fileprivate let main_piMsg:String = "menimmunes"

/*: "mf/moment/like" :*/
fileprivate let noti_whyBirthdayKey:[Character] = ["m","f","/","m","o","m","e","n"]
fileprivate let kTopKey:[Character] = ["t","/","l","i","k","e"]

/*: "mf/crush/send" :*/
fileprivate let kBounceName:String = "mf/crushbarn incident object"
fileprivate let dreamLifePath:[Character] = ["/","s","e","n","d"]

/*: "mf/moment/view" :*/
fileprivate let notiReadyUrl:String = "correct success border preparationmf/mo"
fileprivate let noti_funTitle:String = "vieblue"

/*: "mf/momentReply/list" :*/
fileprivate let noti_areaZoneMsg:String = "mf/momcivic thin staff charm proposal"
fileprivate let dream_trustPath:[Character] = ["e","n"]
fileprivate let noti_ofEarnContent:String = "tReplylabel weak jack writing moment"

/*: "mf/momentReply/save" :*/
fileprivate let mainTeachingValue:[Character] = ["m","f"]
fileprivate let mainSecurityBasicText:String = "dew create projection calculate super/mome"
fileprivate let appAnswerId:String = "opportunity from ticket withoutply/save"

/*: "mf/moment/videoSign" :*/
fileprivate let dream_becomeKey:String = "button if busymf/mom"
fileprivate let mSightName:[Character] = ["e"]
fileprivate let dreamAtId:String = "oSignheat now count right"

/*: "dynamicPic.zip" :*/
fileprivate let noti_existingValue:String = "dynalongside"
fileprivate let main_interestAnimaMsg:[Character] = ["i","c","P","i","c",".","z","i","p"]

/*: "pic" :*/
fileprivate let dream_equallyValue:[UInt8] = [0x32,0x2b,0x21]

private func extraHi(feedback num: UInt8) -> UInt8 {
    return num ^ 66
}

/*: "content" :*/
fileprivate let appNameIdent:String = "CONTEN"
fileprivate let m_cutData:String = "sibling"

/*: "mf/moment/save" :*/
fileprivate let user_willBoltName:[Character] = ["m","f","/","m","o"]
fileprivate let kRoofPizzaKey:[Character] = ["m","e","n","t","/","s","a","v","e"]

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let mainArrivalKey:[UInt8] = [0x48,0x6d,0x7a,0x74,0x71,0x6b,0x70,0x38,0x4b,0x6d,0x7b,0x7b,0x7d,0x6b,0x6b,0x7e,0x6d,0x74,0x34,0x38,0x5f,0x77,0x38,0x6c,0x77,0x38,0x55,0x61,0x38,0x5c,0x61,0x76,0x79,0x75,0x71,0x7b,0x6b,0x38,0x6c,0x77,0x38,0x6e,0x71,0x7d,0x6f]

private func produceerWithin(kit num: UInt8) -> UInt8 {
    return num ^ 24
}

/*: "mf/moment/saveVideo" :*/
fileprivate let app_plusFormat:String = "mf/momgrace cos calendar cheat"
fileprivate let app_nowPath:[Character] = ["v"]
fileprivate let noti_pureValue:String = "eVideoquickly mp average expression natural"

/*: .png" :*/
fileprivate let appShrinkMovieId:String = ".pngicon additional hi rank mobile"

/*: "dynamicPic" :*/
fileprivate let m_blackIdent:String = "DYNA"

/*: "freeImage" :*/
fileprivate let showBeforePath:String = "coordinator ratefreeI"
fileprivate let k_shirtRowTitle:[Character] = ["m","a","g","e"]

/*: "mf/momentReply/replylist" :*/
fileprivate let user_obstacleMinimumFormat:String = "mf/momerequired convert"
fileprivate let user_golfPath:String = "pic break super vision limitedntRe"
fileprivate let mOkName:String = "repsecond"

/*: "mf/moment/delete" :*/
fileprivate let user_souFormat:String = "must imf/mo"
fileprivate let m_circleUrl:String = "deleratinge"

/*: "mf/momentReply/delete" :*/
fileprivate let showHapKey:String = "mf/momenrefrigerator sweet submit"
fileprivate let notiBulkName:String = "dominant simultaneously viatReply"

/*: "mf/moment/pin" :*/
fileprivate let notiChangeId:[Character] = ["m","f","/","m","o","m","e","n","t","/","p","i","n"]

/*: "mf/moment/remindNum" :*/
fileprivate let show_reserveValue:[Character] = ["m","f","/","m","o","m","e","n","t"]
fileprivate let show_poundData:[Character] = ["/"]
fileprivate let userHoweverStr:[Character] = ["r","e","m","i","n","d","N","u","m"]

/*: "mf/moment/remindList" :*/
fileprivate let dreamDonFormat:[Character] = ["m","f","/","m","o","m","e","n","t","/","r","e"]
fileprivate let k_performRealizeFormat:String = "dimension"
fileprivate let user_drillId:String = "shade hand eyeindList"

/*: "page" :*/
fileprivate let noti_redName:[UInt8] = [0x65,0x67,0x61,0x70]

/*: "cos/getCredential" :*/
fileprivate let mWhichKingData:String = "which still pure shirtcos/"
fileprivate let user_frameworkId:String = "repartnernt"
fileprivate let main_canKey:[Character] = ["i","a","l"]

/*: "type" :*/
fileprivate let appFindQuantityPath:[Character] = ["t","y","p","e"]

/*: "user/reportUserInfo" :*/
fileprivate let dreamGivenUrl:String = "user/extension struggle inside club dew"
fileprivate let app_cookieValue:String = "reportUtopic heel mine"
fileprivate let kMonthPath:[Character] = ["s","e","r","I","n","f","o"]

/*: "uid" :*/
fileprivate let main_oneKey:[Character] = ["u","i","d"]

/*: "deviceId" :*/
fileprivate let showWhoUrl:String = "op simply lap measure identitydeviceId"

/*: "deviceName" :*/
fileprivate let userOptionId:String = "fail resolution age coat latedeviceNa"
fileprivate let showBeforeAfterName:String = "beautye"

/*: "iphone" :*/
fileprivate let mConcernText:String = "onlyphone"

/*: "fileUrl" :*/
fileprivate let m_latRepresentStr:[Character] = ["f","i","l","e","U","r","l"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BubbleReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import Adjust
import Adjust
//: import UIKit
import UIKit

//: class TalkingMomentRequestTool: NSObject {
class BubbleReactiveCompatible: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func ta(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(notiBlankStr.prefix(8)) + String(noti_tailId.prefix(6)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func close(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(dreamSoftlyBuildValue.prefix(5)) + "ment" + String(showPetData) + main_piMsg.replacingOccurrences(of: "immune", with: "t"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func bot(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(noti_whyBirthdayKey) + String(kTopKey))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func director(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(kBounceName.prefix(8)) + String(dreamLifePath))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func declaration(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(notiReadyUrl.suffix(5)) + "ment/" + noti_funTitle.replacingOccurrences(of: "blue", with: "w"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func visible(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(noti_areaZoneMsg.prefix(6)) + String(dream_trustPath) + String(noti_ofEarnContent.prefix(6)) + "/list")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func params(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(mainTeachingValue) + String(mainSecurityBasicText.suffix(5)) + "ntRe" + String(appAnswerId.suffix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func enfold(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(dream_becomeKey.suffix(6)) + "ent/vid" + String(mSightName) + String(dreamAtId.prefix(5)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func findClick(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        trademark()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            visualisation(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((noti_existingValue.replacingOccurrences(of: "alongside", with: "am") + String(main_interestAnimaMsg)))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: dream_equallyValue.map{extraHi(feedback: $0)}, encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(appNameIdent.lowercased() + m_cutData.replacingOccurrences(of: "sibling", with: "t"))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = ExecLaborReactiveCompatible()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(user_willBoltName) + String(kRoofPizzaKey))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.licenseFee(showMsg: String(bytes: mainArrivalKey.map{produceerWithin(kit: $0)}, encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.trademark()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func valueRes(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(app_plusFormat.prefix(6)) + "ent/sa" + String(app_nowPath) + String(noti_pureValue.prefix(6)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func visualisation(index: Int, image: UIImage) {
        //: let data = image.headMargin()
        let data = image.headMargin()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(appShrinkMovieId.prefix(4)))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((m_blackIdent.lowercased() + "micPic"))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func trademark() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((String(showBeforePath.suffix(5)) + String(k_shirtRowTitle)))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func beyondDelay(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(user_obstacleMinimumFormat.prefix(7)) + String(user_golfPath.suffix(4)) + "ply/" + mOkName.replacingOccurrences(of: "second", with: "l") + "ylist")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func own(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(user_souFormat.suffix(5)) + "ment/" + m_circleUrl.replacingOccurrences(of: "rating", with: "t"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func process(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(showHapKey.prefix(8)) + String(notiBulkName.suffix(6)) + "/delete")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func destination(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(notiChangeId))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func talkBirthdayLast(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(show_reserveValue) + String(show_poundData) + String(userHoweverStr))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func month(page: Int, completion: @escaping (_ data: [RowModelType]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(dreamDonFormat) + k_performRealizeFormat.replacingOccurrences(of: "dimension", with: "m") + String(user_drillId.suffix(7)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: noti_redName.reversed(), encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { _, result, _ in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [RowModelType] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<RowModelType>.deserialize(from: arr as? Array) as? [RowModelType] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func video(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(mWhichKingData.suffix(4)) + "getC" + user_frameworkId.replacingOccurrences(of: "partner", with: "de") + String(main_canKey))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(String(appFindQuantityPath)): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func cryForOwner(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExecLaborReactiveCompatible()
        //: reqModel.requestServer = AppManager.share.appConfigMode.reportDomain
        reqModel.requestServer = AppManager.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (String(dreamGivenUrl.prefix(5)) + String(app_cookieValue.prefix(7)) + String(kMonthPath))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = AppManager.share.loginUserMode.userID
        params[(String(main_oneKey))] = AppManager.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(String(showWhoUrl.suffix(8)))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(String(userOptionId.suffix(8)) + showBeforeAfterName.replacingOccurrences(of: "beauty", with: "m"))] = (mConcernText.replacingOccurrences(of: "only", with: "i"))
        //: params["type"] = 1
        params[(String(appFindQuantityPath))] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(m_latRepresentStr))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_topEnvironmentId.thumbPrompt(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
