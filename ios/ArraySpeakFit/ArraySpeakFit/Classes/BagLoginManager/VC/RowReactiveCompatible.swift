
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let user_talkData:[UInt8] = [0xa9,0xae,0xa9,0xb4,0xe8,0xa3,0xaf,0xa4,0xa5,0xb2,0xfa,0xe9,0xe0,0xa8,0xa1,0xb3,0xe0,0xae,0xaf,0xb4,0xe0,0xa2,0xa5,0xa5,0xae,0xe0,0xa9,0xad,0xb0,0xac,0xa5,0xad,0xa5,0xae,0xb4,0xa5,0xa4]

private func peaceSwitchicer(unable num: UInt8) -> UInt8 {
    return num ^ 192
}

/*: "Personal information" :*/
fileprivate let show_wowUrl:String = "ok boardPers"
fileprivate let userAgainstGoalAdjustTitle:String = "ad trip infor"
fileprivate let dream_bagName:String = "minvitationion"

/*: "AlongsideDigitalTableConvertible" :*/
fileprivate let app_patientMessage:String = "poke extreme anti giveGender"

/*: "Once gender is set, it Cannot be changed." :*/
fileprivate let mLaunchStr:[UInt8] = [0x2e,0x64,0x65,0x67,0x6e,0x61,0x68,0x63,0x20,0x65,0x62,0x20,0x74,0x6f,0x6e,0x6e,0x61,0x43,0x20,0x74,0x69,0x20,0x2c,0x74,0x65,0x73,0x20,0x73,0x69,0x20,0x72,0x65,0x64,0x6e,0x65,0x67,0x20,0x65,0x63,0x6e,0x4f]

/*: "#2ABBFF" :*/
fileprivate let m_spanStr:String = "#2ABBFFdiscussion optimistic just device"

/*: "btn_male_nv_nor" :*/
fileprivate let appColumnSourceKey:[Character] = ["b","t","n","_","m","a"]
fileprivate let showAlongStr:String = "le_appear activity carrier hello"
fileprivate let noti_yesMsg:String = "nv_norok greet relief"

/*: "btn_male_nv_sel" :*/
fileprivate let showSlowMonthFormat:String = "btn_window sun operate soil flow"
fileprivate let main_shadowTitle:String = "advertising"

/*: "Male" :*/
fileprivate let main_remainingIncomeMicPath:String = "table someone chanceMale"

/*: "#FF5372" :*/
fileprivate let dream_reasonIdent:[Character] = ["#","F","F","5","3","7","2"]

/*: "btn_female_nv_nor" :*/
fileprivate let notiBoardHalfData:String = "used secretbtn_f"
fileprivate let k_popName:String = "slow absence worlde_nv"
fileprivate let user_holidaySkirtMsg:String = "head language_nor"

/*: "btn_female_nv_sel" :*/
fileprivate let notiFeedbackTitle:[Character] = ["b","t","n","_","f","e","m"]
fileprivate let dream_hardwareIdent:String = "ale_ndate arrival"

/*: "Female" :*/
fileprivate let showFadeName:String = "opening elementFemale"

/*: "Next" :*/
fileprivate let dreamConValue:[Character] = ["N","e","x","t"]

/*: "sex" :*/
fileprivate let user_heliName:[UInt8] = [0x90,0x86,0x9b]

private func miniCorrect(upper num: UInt8) -> UInt8 {
    return num ^ 227
}

/*: "RegisterSuccess" :*/
fileprivate let kKindValue:[Character] = ["R","e","g","i","s","t","e","r"]
fileprivate let kFloorFormat:String = "exist browSuccess"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  RowReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/30.
//

//: import UIKit
import UIKit

//: class TalkingLoginGenderVC: TalkingBaseViewController {
class RowReactiveCompatible: FictionalCharacterRecognizerDelegate {
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: user_talkData.map{peaceSwitchicer(unable: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: popGesture(isOpen: false)
        externalise(isOpen: false)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Personal information".localized
        self.title = (String(show_wowUrl.suffix(4)) + "onal" + String(userAgainstGoalAdjustTitle.suffix(6)) + dream_bagName.replacingOccurrences(of: "invitation", with: "at")).localized
        //: AppManager.share.userFillInfoMode.sex = "1"
        AppManager.share.userFillInfoMode.sex = "1"
        //: self.setupSubviews()
        self.shouldSubviews()
        //: self.setupSubViewsConstraint()
        self.sumerchange()
        //: self.bindInteraction()
        self.lastSumeraction()
        //: func__checkNextBtnState()
        me()
    }

    //: deinit {
    deinit {}

    //: private lazy var logoTitleLabel: UILabel = {
    private lazy var logoTitleLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 17)
        label.font = .concern(type: .Semibold, fontSize: 17)
        //: label.textColor = .appTitleColor()
        label.textColor = .telecom()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.text = "Gender".localized
        label.text = (String(app_patientMessage.suffix(6))).localized
        //: return label
        return label
        //: }()
    }()

    //: private lazy var genderTipLabel: UILabel = {
    private lazy var genderTipLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 15)
        label.font = .concern(type: .Regular, fontSize: 15)
        //: label.textColor = .appValueColor()
        label.textColor = .componentHighlight()
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: label.text = "Once gender is set, it Cannot be changed.".localized
        label.text = String(bytes: mLaunchStr.reversed(), encoding: .utf8)!.localized
        //: return label
        return label
        //: }()
    }()

    //: private lazy var maleButton: TalkingButton = {
    private lazy var maleButton: GoddamnThen = {
        //: let btn = TalkingButton(type: .custom)
        let btn = GoddamnThen(type: .custom)
        //: btn.isSelected = false
        btn.isSelected = false
        //: btn.spaceBetweenTitleAndImage = 20
        btn.spaceBetweenTitleAndImage = 20
        //: btn.setBackgroundColor(color: UIColor.init(red: 244/255.0, green: 244/255.0, blue: 244/255.0, alpha: 1.0), forState: .normal)
        btn.inviteLicense(color: UIColor(red: 244 / 255.0, green: 244 / 255.0, blue: 244 / 255.0, alpha: 1.0), forState: .normal)
        //: btn.setBackgroundColor(color: UIColor.init(hex: "#2ABBFF")!, forState: .selected)
        btn.inviteLicense(color: UIColor(hex: (String(m_spanStr.prefix(7))))!, forState: .selected)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_male_nv_nor"), for: .normal)
        btn.setImage(UIImage.verbalDescription(name: (String(appColumnSourceKey) + String(showAlongStr.prefix(3)) + String(noti_yesMsg.prefix(6)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_male_nv_sel"), for: .selected)
        btn.setImage(UIImage.verbalDescription(name: (String(showSlowMonthFormat.prefix(4)) + "male_nv_s" + main_shadowTitle.replacingOccurrences(of: "advertising", with: "el"))), for: .selected)
        //: btn.setTitle("Male".localized, for: .normal)
        btn.setTitle((String(main_remainingIncomeMicPath.suffix(4))).localized, for: .normal)
        //: btn.setTitle("Male".localized, for: .selected)
        btn.setTitle((String(main_remainingIncomeMicPath.suffix(4))).localized, for: .selected)
        //: btn.setTitleColor(.black, for: .normal)
        btn.setTitleColor(.black, for: .normal)
        //: btn.setTitleColor(.white, for: .selected)
        btn.setTitleColor(.white, for: .selected)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 17)
        btn.titleLabel?.font = UIFont.duringWindowExtreme(fontSize: 17)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var femaleButton: TalkingButton = {
    private lazy var femaleButton: GoddamnThen = {
        //: let btn = TalkingButton(type: .custom)
        let btn = GoddamnThen(type: .custom)
        //: btn.isSelected = false
        btn.isSelected = false
        //: btn.spaceBetweenTitleAndImage = 20
        btn.spaceBetweenTitleAndImage = 20
        //: btn.setBackgroundColor(color: UIColor.init(red: 244/255.0, green: 244/255.0, blue: 244/255.0, alpha: 1.0), forState: .normal)
        btn.inviteLicense(color: UIColor(red: 244 / 255.0, green: 244 / 255.0, blue: 244 / 255.0, alpha: 1.0), forState: .normal)
        //: btn.setBackgroundColor(color: UIColor.init(hex: "#FF5372")!, forState: .selected)
        btn.inviteLicense(color: UIColor(hex: (String(dream_reasonIdent)))!, forState: .selected)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_female_nv_nor"), for: .normal)
        btn.setImage(UIImage.verbalDescription(name: (String(notiBoardHalfData.suffix(5)) + "emal" + String(k_popName.suffix(4)) + String(user_holidaySkirtMsg.suffix(4)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_female_nv_sel"), for: .selected)
        btn.setImage(UIImage.verbalDescription(name: (String(notiFeedbackTitle) + String(dream_hardwareIdent.prefix(5)) + "v_sel")), for: .selected)
        //: btn.setTitle("Female".localized, for: .normal)
        btn.setTitle((String(showFadeName.suffix(6))).localized, for: .normal)
        //: btn.setTitle("Female".localized, for: .selected)
        btn.setTitle((String(showFadeName.suffix(6))).localized, for: .selected)
        //: btn.setTitleColor(.black, for: .normal)
        btn.setTitleColor(.black, for: .normal)
        //: btn.setTitleColor(.white, for: .selected)
        btn.setTitleColor(.white, for: .selected)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 17)
        btn.titleLabel?.font = UIFont.duringWindowExtreme(fontSize: 17)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var commitButton: UIButton = {
    private lazy var commitButton: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        btn.titleLabel?.font = UIFont.concern(type: .Medium, fontSize: 17)
        //: btn.setTitle("Next".localized, for: .normal)
        btn.setTitle((String(dreamConValue)).localized, for: .normal)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: ScreenWidth-60, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.gravitySize(colors: UIColor.decelerate(), size: CGSize(width: kGiftIdent - 60, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Public Event

//: extension TalkingLoginGenderVC {
extension RowReactiveCompatible {
    //: private func func__checkNextBtnState() {
    private func me() {
        //: if AppManager.share.userFillInfoMode.sex == "1" {
        if AppManager.share.userFillInfoMode.sex == "1" {
            //: self.commitButton.isEnabled = true
            self.commitButton.isEnabled = true
            //: self.maleButton.isSelected = true
            self.maleButton.isSelected = true
            //: self.femaleButton.isSelected = false
            self.femaleButton.isSelected = false
            //: } else if AppManager.share.userFillInfoMode.sex == "2"{
        } else if AppManager.share.userFillInfoMode.sex == "2" {
            //: self.commitButton.isEnabled = true
            self.commitButton.isEnabled = true
            //: self.maleButton.isSelected = false
            self.maleButton.isSelected = false
            //: self.femaleButton.isSelected = true
            self.femaleButton.isSelected = true
            //: } else {
        } else {
            //: self.commitButton.isEnabled = false
            self.commitButton.isEnabled = false
            //: self.maleButton.isSelected = false
            self.maleButton.isSelected = false
            //: self.femaleButton.isSelected = false
            self.femaleButton.isSelected = false
        }
    }

    //: private func func__pushToNextProgress() {
    private func language() {
        //        // 1.3 判断用户设备SIM卡归属地是否来自被剔除的国家或地区
        //        // 1.4 判断用户设备是否开启VPN
//        if AppManager.share.appConfigMode.vpnSimCardCheck {
//
//            guard  !SweepAddrTool.isUsedProxy() && !SweepAddrTool.isUsedVPN() else {
//                self.func__showStatusBarErrorMsg(showMsg: "Network error. Check it and try again.".localized)
//                self.navigationController?.popToRootViewController(animated: true)
//                NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
//                DrawstringBagReactiveCompatible.func__reportDeviceID()
//                return
//            }
//        }
        /// 清除后面已填写过资料
//        let sex = AppManager.share.userFillInfoMode.sex
//        AppManager.share.userFillInfoMode = FaceInfoModel.init()
//        AppManager.share.userFillInfoMode.sex = sex
//        let VC = EditorJumpRecognizerDelegate()
//        self.navigationController?.pushViewController(VC, animated: true)

        //: view.endEditing(true)
        view.endEditing(true)
        //: ProgressHUD.show()
        OrdinalProgressHUD.loo()
        //: TalkingLoginRequestTool.req_updateUserInfo(params: ["sex":AppManager.share.userFillInfoMode.sex]) { succeed, result, errorModel in
        AlterThen.scheduleNetwork(params: [String(bytes: user_heliName.map{miniCorrect(upper: $0)}, encoding: .utf8)!: AppManager.share.userFillInfoMode.sex]) { succeed, result, _ in
            //: ProgressHUD.dismiss()
            OrdinalProgressHUD.vision()
            //: if succeed {
            if succeed {
                // 发送通知
                //: NotificationCenter.default.post(name: DID_LOGIN_SUCCESS_NOTIFICATION, object: nil, userInfo: result as! Dictionary<String, Any>)
                NotificationCenter.default.post(name: appEventTitle, object: nil, userInfo: result as! [String: Any])
                // 注册成功埋点
                //: TalkingAdjustManager.share.addEvent(key: "RegisterSuccess")
                SolidThen.share.cheatStar(key: (String(kKindValue) + String(kFloorFormat.suffix(7))))
                //: FirebaseAnalyticsManager.share.logEvent(name: "RegisterSuccess")
                ValueReactiveCompatible.share.simultaneouslyModify(name: (String(kKindValue) + String(kFloorFormat.suffix(7))))
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingLoginGenderVC {
extension RowReactiveCompatible {
    // 添加视图
    //: private func setupSubviews() {
    private func shouldSubviews() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: view.addSubview(logoTitleLabel)
        view.addSubview(logoTitleLabel)
        //: view.addSubview(genderTipLabel)
        view.addSubview(genderTipLabel)
        //: view.addSubview(maleButton)
        view.addSubview(maleButton)
        //: view.addSubview(femaleButton)
        view.addSubview(femaleButton)
        //: view.addSubview(commitButton)
        view.addSubview(commitButton)
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func sumerchange() {
        //: logoTitleLabel.snp.makeConstraints { make in
        logoTitleLabel.snp.makeConstraints { make in
            //: make.top.equalTo(34)
            make.top.equalTo(34)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
        }
        //: genderTipLabel.snp.makeConstraints { make in
        genderTipLabel.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.width.equalTo(ScreenWidth-60)
            make.width.equalTo(kGiftIdent - 60)
            //: make.top.equalTo(logoTitleLabel.snp.bottom).offset(6)
            make.top.equalTo(logoTitleLabel.snp.bottom).offset(6)
        }
        //: maleButton.snp.makeConstraints { make in
        maleButton.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.width.equalTo(actualWidth(w: 140))
            make.width.equalTo(actualWidth(w: 140))
            //: make.height.equalTo(actualWidth(w: 50))
            make.height.equalTo(actualWidth(w: 50))
            //: make.top.equalTo(genderTipLabel.snp.bottom).offset(20)
            make.top.equalTo(genderTipLabel.snp.bottom).offset(20)
        }
        //: femaleButton.snp.makeConstraints { make in
        femaleButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.width.equalTo(actualWidth(w: 140))
            make.width.equalTo(actualWidth(w: 140))
            //: make.height.equalTo(actualWidth(w: 50))
            make.height.equalTo(actualWidth(w: 50))
            //: make.top.equalTo(genderTipLabel.snp.bottom).offset(20)
            make.top.equalTo(genderTipLabel.snp.bottom).offset(20)
        }
        //: commitButton.snp.makeConstraints { make in
        commitButton.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.width.equalTo(ScreenWidth-60)
            make.width.equalTo(kGiftIdent - 60)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
            //: make.top.equalTo(maleButton.snp.bottom).offset(40)
            make.top.equalTo(maleButton.snp.bottom).offset(40)
        }
    }

    // 添加事件
    //: private func bindInteraction() {
    private func lastSumeraction() {
        //: commitButton.rx.tap
        commitButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func__pushToNextProgress()
                self.language()

                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: femaleButton.rx.tap
        femaleButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: AppManager.share.userFillInfoMode.sex = "2"
                AppManager.share.userFillInfoMode.sex = "2"
                //: self.func__checkNextBtnState()
                self.me()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: maleButton.rx.tap
        maleButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: AppManager.share.userFillInfoMode.sex = "1"
                AppManager.share.userFillInfoMode.sex = "1"
                //: self.func__checkNextBtnState()
                self.me()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
